# System State

Last updated: 2026-08-21 (Ignition sidebar tested LIVE: the flapping was multi-frame status clobbering, not reconnects; I retracted a fix-verification measured on a harness that never fed connection lifecycle; advice now expires with its decision)

> **State-file discipline.** This file is the fast-scan canonical state of the system. It MUST stay readable in one screen.
> - Each row: terse. Detail belongs in linked files (project files, session files, SYSTEM_MODEL.md).
> - **Recent Sessions: last 5 only.** Older sessions go to `.claude/workstream/sessions/` (canonical CWOS form) or `system/state-history.md` (raw archive for sessions never captured as session files).
> - If a section grows past its row count, **prune and archive** — do not let it accumulate. The cumulative session changelog that used to live at the top was archived to `system/state-history.md` on 2026-05-10 because it had ballooned to ~40K tokens.
> - **Append discipline rule (binding):** at `/session-end`, the protocol must enforce this shape. Adding a new Recent Sessions row pushes the oldest to archive.

---

## Vital Signs

| Area | Status | Check | Detail |
|------|--------|-------|--------|
| 2026-08-07 | **Overnight autonomous run + commit-queue cleanup (ses-20260807-0535)** | 5 queue items DONE on branch night/overnight-20260807 (WS-428/429/430/437/438 — commits b4c38dc1..3bc43abd): opportunitiesPerHand built with max-decisions refusal test; **edgeBB measured NOT rake-invariant (-0.184 bb/doubling, ~18x tolerance)**; beliefState measured (q8 = full capture affordable, 2.6-16.6 GB); atom-store disk preflight + TRUNCATED recovery; 15-number figure relabeled + Result Card + currency lint. WS-431 skipped (founder decision flag). 8 protocol runs stamped with declared multi-agent engines (self-compliance 0.82↑, data-provenance, domain-correctness delta+theory_completeness, design first-in-108d, methodology first-ever, SoR, launch **NOT READY**); **56 findings filed FIND-055..110** (5 critical: deploy never runs tests + failing EV assertion live; delete-account purges nothing; backup = 3 of ~25 stores; main frozen red 82 commits). theory-gaps.draft.md rewritten. Lost founder P0s re-filed WS-439/440/441. Record corrections: FM-STATUS/DENSITY bugs were fixed in April (docs+memory said shipping 99 days); WS-371 fix already landed (stale P0); "environmental" test failures are kit regressions (FIND-108). DAYTIME: 13 orphaned branches processed — 10 merged into integrate/orphans-20260807 (~105 commits recovered: WS-315 rotate-gate fix, seat-stack ledger, bankroll variance, layout doctrine), 9 deleted, 3 tracked in WS-442 for domain merges; 20 collided decisions re-recorded DEC-048..067; main merged into ws-292 (fully contained). Full handoff: .claude/workstream/evidence/overnight-handoff-2026-08-07.md |
| Tests | **AMBER** | `bash scripts/smart-test-runner.sh` | 2026-08-12 quiet-machine gate: **645 of 650 files green**. The 5 red: `sessionRecoveryLiveness.test.js` (10 tests — a REAL pre-existing kit defect, fails identically at commit d3cf5993 in a clean worktree; matches live behavior: recovery closes dead sessions but leaves queue claims; friction captured ×2) + 4 files that are load-timeout flakes passing in isolation (76/76). Two CPU-bound suites got explicit timeout budgets this session (`computeDepth2Plan`, `logicalBailStratumGuarantee`) after worktree-verified as pre-existing. Engine+advisor suites 84/84 green. |
| Build | GREEN | `npm run build` | 2026-08-12: clean. |
| Git | GREEN | `git status --short` | 2026-08-12: session committed in 8 (`a771d86e`..close); tree clean except hook-churned workstream state. |
| Dependencies | NEEDS CHECK | `npm audit` | Last verified: never since CWOS adoption (2026-05-01) |
| Target device | **Galaxy S22 — DPR provisional** | open `/device-probe.html` on the phone | WS-334 founder ruling; 2340×1080 physical, CSS viewport ≈1170×540 assumed at DPR 2 (never measured on-device). Touch floor enforced at this profile by `tests/playwright/touch-floor.spec.js` (WS-441); DPR correction is one line in `tests/playwright/deviceProfiles.mjs`. |

## Project Phase

| Field | Value |
|-------|-------|
| Current phase | pre-launch |
| Phase changed at | 2026-04-30 (foundation → pre-launch) |
| Refactor Sprint | **CLOSED 2026-05-14** (opened 2026-05-10; all 7 items done across SPR-077..080) |
| Active program | **Master Plan 2026-04-30** resumed — `.claude/projects/master-plan-2026-04-30.md` |
| Recent close-outs | Refactor Sprint: SYSTEM_MODEL.md restore + state.md reshape + persistence migration registry + decisionSystems extraction (anchorLibrary migrated) + useLiveActionAdvisor split + queue triage + persistence/cache audit (12 failure modes + 18 cache surfaces + 4 ADRs + 5 INV-PERSIST + 6 new TDs) |

## Metrics

| Metric | Value | Notes |
|--------|-------|-------|
| Test count (app) | **14,113** | Vitest (594 files; 20 skipped). 2026-08-04. |
| Test count (extension) | **2,562** | ignition-poker-tracker (92 files). 2026-08-21 — +130 over the 2,432 baseline: table-identity/reconnect lifecycle, multi-tabling guard (same URL ≠ same table), silence-detector recency, capture-gap ledger, status-bar recency, connected-waiting re-arm, journal quota, storage warning, build stamp, heroSeatConfidence lifecycle, advice expiry. **Merged and pushed — origin/main at `b76deb47`.** |
| Architecture version | v123 | per CLAUDE.md |
| IndexedDB schema | **v27** | Additive-only invariant. **Migration registry** at `src/utils/persistence/migrationRegistry.js` (27 entries + 3 helpers); CI gate `scripts/check-idb-additive.sh`. 23 object stores. v27 = reviewTag field on hands (WS-190). |
| Reducers / Contexts / Hooks / Views / Engines | **13 / 20 / 62 / 19 / 4** | PrototypeFinderView deleted 2026-06-12 (WS-225) |
| Shared primitives | `heroState/` + **`holdingKnowledge/`** | Hero-side and villain-side. WS-292 (2026-08-01) added the latter: THE single derivation of "the range at this point". |
| Persistence modules | 29 | `src/utils/persistence/` |

Architecture detail: `.claude/context/SYSTEM_MODEL.md` (restored from backup 2026-05-10).

## Queue Summary

| Status | Count |
|--------|-------|
| Backlog | 125 |
| In Progress | 3 |
| Blocked | 10 |
| Done | 45 |
| Completed | 7 |
| Deferred | 44 |
| Decomposed | 1 |
| Dismissed | 1 |
| Review | 1 |
| **Total** | **196** |

> 2026-08-01: +1 item filed — **WS-321** (fit a PER-PLAYER range width from calibration; the second half of WS-292, carrying the measured per-player Δlog table as its evidence).

> 2026-07-31: +6 items filed — **WS-316** (IP check-back vs OOP check pooled into one 'check': 10.3% vs 34.3% strong), **WS-317** (situation-key keystone — DONE), **WS-318** (weakness matching compares `isAgg` while calling it the action; user-visible "in flop agg spots"), **WS-319** (EV-by-percentile curve), **WS-320** (study-ladder separability test). Done-count differs from the previous table because archived items are no longer indexed.

> 2026-06-10: engineering cap-breach (18 open vs cap 15) cleared by deferring 11 stale legacy items (HRP paused streams WS-068..073, Range Lab stretch phases WS-058..060, US-2 refit WS-062, event-driven WS-156) — all reversible with revival triggers in each YAML.

> **CWOS-ops pending — WS-212:** this repo is on **kit 3.5.0**; HomeBase is **3.8.0** and the `kit/scripts/` suite (`cwos-reconcile.js` et al.) was never installed (arrived in 3.6.0). Root cause of the recurring manual index-reconcile each session (friction fr-011…fr-014). Fix runs **from a HomeBase session** — `/adopt "<this repo>" --update`, then optional in-repo `/kit-upgrade`. See `WS-212` for the full runbook.

See `.claude/workstream/queue-index.yaml`. **Refactor Sprint anchor:** feature work paused; Items 1+2+3 DONE; Items 4 (decision-system extraction, NEXT), 5 (useLiveActionAdvisor split), 6 (queue triage) drive the next ~3–6 sessions.

## Program Health

| Program | Tier | Status | Last Run | Priority |
|---------|------|--------|----------|----------|
| domain-correctness (Poker Theory Integrity) | critical | ACTIVE | 2026-07-22 (sweep+delta+challenge; spec PROMOTED) | 1 |
| **data-provenance** (Source Accountability) | active | NEW | 2026-07-31 (verification sweep + registry/chain-map PROMOTED) | 1 |
| methodology-integrity | active | NEW | — | 1 |
| design (5-Gate UX Framework) | active | ACTIVE | 2026-04-21 | 2 |
| **engineering** (Refactor Sprint active) | watch | ACTIVE | 2026-05-10 | 3 |
| anti-hallucination | watch | NEW | — | 3 |
| change-management | active | NEW | — | 4 |
| launch | active | ACTIVE | 2026-07-22 (sweep: NOT READY, correct) | 4 |

5 of 7 programs have `status: NEW` — first audit runs pending. Refactor Sprint moved engineering from NEW → ACTIVE on 2026-05-10.

## Recent Sessions

> Last 5 only. Older sessions: `system/state-history.md` (raw, 2026-04-30 → 2026-05-10) and `.claude/workstream/sessions/` (2 canonical session files).

| Date | Primary Work | Outcome |
|------|-------------|---------|
| 2026-08-21 | **Ignition sidebar tested LIVE at a real table — the flapping was never reconnects, and I had reported a fix on evidence from a harness that could not measure it** | **THE HEADLINE CORRECTION: I told the founder the panel-flapping was fixed and cited a 13→9 session-fragmentation improvement. He tested on the correct build (stamp confirmed) and it still flapped. The measurement was taken on `backfillFromCapture.mjs`, which replayed only `kind:'msg'` and DROPPED `kind:'conn'` entirely — so TableManager was never told a socket opened or closed, and reconnect handling is driven ENTIRELY by those events. Wired the lifecycle in, re-ran controlled with only `table-manager.js` swapped: pre-change 10 sessions / 110 hands / 99 hero / 7 partials vs fixed 10 / 108 / 101 / 4. Sessions UNCHANGED. The 13→9 was an artifact. The real benefit is fewer junk `reconnectInterrupted` partials (7→4, one had ZERO actions) and +2 hero attributions — not less fragmentation.** **THE ACTUAL FLAPPING BUG:** `content/ignition-capture.js` is injected `all_frames:true`, so EVERY frame on the iframed Ignition client runs its own TableManager, and every non-game frame (lobby/chrome/ads) publishes `tables:{}` on the 30s interval. `service-worker.js` kept `lastPipelineStatus = msg.status` — one global, last-writer-wins. Game frame published the table → seatmap; lobby frame published emptiness → 5s grace → `clearForTableSwitch()` → "No active table detected" over a live hand; next game push → table back minus advice → "Analyzing…". **The cadence was the tell and I read past it: "shuffles in and out" is rhythmic, and ~30s matched the status interval exactly.** SW now AGGREGATES per capture port (it is the only component that sees all frames); ports release their contribution on disconnect and the panel is re-pushed immediately. **A BUG I INTRODUCED, CAUGHT BEFORE IT BIT:** the game WS URL carries NO table identifier — one URL is shared by up to 5 connIds across the four captures — so my earlier stitch, which migrated on URL match alone, would have had a second table's first message STEAL the first table's machine and merge two tables' hands. Only 0 overlapping same-URL pairs (founder not multi-tabling) kept it harmless. A reconnect now requires a preceding CLOSE; corruption is worse than fragmentation. **ADVICE OUTLIVED ITS DECISION** (founder, live): shown after he folded and across later streets. `lastGoodAdvice` was only ever REPLACED, never INVALIDATED. `classifyDecisionState` already returned OBSERVING/REFLECTION on a fold and invariant R3 already detected advice trailing the street — **both existed, neither was consulted by the function that draws the recommendation**, the same knowledge-present-but-unwired shape as `GET_JOURNAL_HANDS` shipping with 0 handlers. Now suppressed on fold ("Observing — folded", not "Analyzing…") and on street advance; 9 tests, 5 fail against the pre-fix builder. **LIVE EVIDENCE FOR THE RECONNECT FIX, first of the day:** one session accumulated **21 hands over ~25 min on a single hero seat with 3 `carried` hands and NO fragmentation** — `carried` is the mid-hand-reconnect signature (Ignition's snapshot omits hero's own `pcard`), so reconnects were absorbed rather than splitting the table. Consistent with the fix, though not proof — the close events were not directly observed. **QUEUE CLEANUP:** 14 orphan claims released (WS-492 fenced 6 days by a dead session while `cwos-session-recovery` printed "none abandoned"; oldest from May) — recovery builds its list from `session.claimed_items` while ownership lives in `claimed_by`; filed **WS-618** (kit fix belongs in HomeBase). 8.2MB `claim-corpus.jsonl` gitignored per artifact-location.md (112× the largest tracked file there; the 73KB *sample* stays). 3 contained worktrees removed; **`ws-445-ledger` deliberately KEPT** — it holds 7 dirty files of real uncommitted code. **Pushed to origin/main (`b76deb47`), node1 unblocked.** 92 files / 2562 extension tests green. **STILL OPEN: the founder reports slow panel load** — hypothesis is my per-port merge only contains frames that pushed since the SW woke, and MV3 suspends aggressively; unverified, needs his answer on whether it follows a refresh. |
| 2026-08-21 | **SPR-197 / WS-540 closed + WS-612 — the ladder's absolute edge resolved, and an orphaned session's work was recovered (commits 2fd6de59, 7b8290a2)** | Three runs on one instrument: n=85 nothing resolved, n=77,450 all four paired DELTAS resolved but no absolute edge, n=235,217 **four of five absolute edges resolve — r4-spr-gate +0.2465 bb [+0.1189, +0.3790]**, r0 resolves NEGATIVE (-0.1700, entry-only is worse than the field), r1 alone still straddles zero. Both runs gated **bit-identical** on replay; a new rung now costs ~4-12 min instead of a full corpus walk. **REFUTED MY OWN PREDICTION:** WS-612 said r2 needed ~853,000 decisions and would not resolve — it resolved at 235,217; I projected an effect size from a point estimate whose interval straddled zero (DEC-157). First OOM at 4,086 MB fixed by an 8 GB heap sized from measurement; structural O(eval-players) ceiling filed as WS-614. Filed WS-611/612/613/614/616/617. **Not citable yet: NO Result Card (WS-613), essShare 0.397 lowSupport, corpus is 2009 online 50NL — TRANSFERRED, not measured.** Orphan cleanup: ses-20260821-0422-a58ee318 died leaving 62 files uncommitted; landed after both live peers disclaimed them and its 44 tests passed (DEC-156). claim-corpus.jsonl (7.9 MB) left untracked as a re-derivable derivative, not deleted. `git-guard` blocked a tree-wide stage and was right to. **Open for founder:** close WS-612?; WS-540's completion_commit c74536df is an abandoned session's unrelated commit; claim-corpus.jsonl needs a home. |
| 2026-08-21 | **Ignition sidebar — the panel was lying about its own state in four independent ways; all four fixed on `worktree-sidebar-table-identity` (4 commits, NOT merged)** | Founder report: *"'No active table detected' when I'm in the hand, then it shuffles in and out of 'analyzing' or says it dropped the connection."* Root-caused to four defects, each verified by execution rather than by reading. **(1) Table identity was the WebSocket connId** — a monotonic per-socket counter (`capture-websocket-probe.js:85`), so every routine Ignition socket cycle read as a table switch at `side-panel.js:404` and ran `clearForTableSwitch()`, wiping advice, live context, seat stats and villain reads mid-hand. Identity is now the stable game-WS URL (`tableKey`), exposed via `getTableStates()`. **(2) The reconnect-stitch was DEAD on the real lifecycle** — it required the old table to still be in `tables`, which `handleConnectionClosed` had already deleted; the pre-existing tests passed only because none of them ever fired a close event. Probe against HEAD: mid-hand state destroyed, `heroSeat 5 → null`, `completedHandCount 7 → 0`, table map `['1'] → [] → ['2']`. A close now opens a 15s grace window (`RECONNECT_GRACE_MS`); reconnect migrates the machine intact — safe because Ignition resends `CO_TABLE_INFO` with dealer seat, street, hole cards and board (`spike-data/SPIKE_REPORT.md`), which `adaptTableInfo` already decomposes. Grace expiry reaps + emits the partial; `flushDisconnected()` on teardown so nothing dies with the page. **(3) The HUD shell gated on `hasTableHands`** = "a hand finished and was written to session storage" — false for the whole first hand at any table, so the panel rendered "No active table detected" **over a live hand**. Now gates on `currentActiveTableId`. Seat arc had the same defect (`buildSeatArcHTML` already handled null stats; the call site refused to invoke it). Invariant R1 was gated on `lastHandCount > 0` and structurally blind to the case that shipped — added R1b. **(4) WS-516: the silence alarm was INVERTED.** Early-returned on `gameWsMessageCount > 0`, never reset anywhere, so it went permanently blind at the first game message. Replaying the old guard: capture-ran-then-died → *no alarm at +5s/+15s/+60s/+300s, ever*; capture-never-started → alarms fire. Liveness is now recency, asked every tick; decision extracted to pure `shared/silence-detector.js` (a mirror test would have verified the mirror — which is exactly how this survived). **WS-517:** `buildStatusBar` returned LIVE off a cumulative hand count with no recency term — a dead capture was pixel-identical to a live one. Measured after: live `Tracking (id:1) · 190 hands` green vs dead `Tracking (id:1) STOPPED — no hand for 22 min · 105 hands` red. Both LIVE branches fixed; 30s escalation re-arms (was `handCount === 0`, could only fire before a session's first hand); boot-race null rule preserved + pinned. **WS-515:** `journalAppend` swallowed the quota throw and `enqueueHand` returned `success: true` for a hand with no durable copy. **Ticket's ~2,932 B/record estimate CORRECTED by measurement** — 500 records through the real HSM with realistic action-length spread: mean **4,965 B**, p95 **9,185 B** → ~2,112 hands (~651 with frame capture), so `MAX_JOURNAL = 5000` needs ~44MB and was never the binding limit. Fixed by `unlimitedStorage` (remove the ceiling, not budget under it), a dedicated quota-failure counter, `journalled:false` on the return, and a founder-visible `#storage-warning` strip in Z0 that warns *before* loss. **Corpus-integrity addition:** capture gaps now go to a durable ledger (`STORAGE_KEYS.CAPTURE_GAPS`) — a dismissed banner leaves no trace, and a session with an unrecorded hole is indistinguishable from a short one, so k/n over it is conditioned on an interval nobody chose. **Two self-corrections, both caught by checking not reasoning:** my first harness "verification" was a FALSE PASS — `verify-harness.mjs` reuses any server on :3333 and reported 58 clean screenshots against another worktree's build (tell: fixture count one short); guard added + `HARNESS_PORT`. And my first fix broke the harness with a `ReferenceError`, caught by the mandatory visual check. Also found: `harness.js` held a THIRD disagreeing copy of the shell gate, so the primary visual tool structurally could not display the defect it existed to catch. **Verified:** 90 files / 2,510 tests green (78 new), clean build with `unlimitedStorage` in the built manifest, harness on an isolated port (52 fixtures / 60 screenshots / no anomalies), shell exclusivity + no-undefined swept across all 51 scenarios in a live browser. **NOT verified:** the real-table session and the real-fill check — the harness does not run `side-panel.js`. **MERGE BLOCKED:** 5 of my files hold 443 lines of another session's uncommitted work (a local session sink, `GET_JOURNAL_HANDS`, `SINK_LAST_SEEN_AT`) reasoning about the SAME journal quota; `--ff-only` refused and nothing was forced. That session should know 5000 hands do not fit — it is ~2,100. |
| 2026-08-20 | **SPR-198 (hand-composed) — the Conduct Card got a held-out score and LOST to a four-cell legality table; sizing reached the card and the induction for the first time** | Eight items code-complete, NONE marked done: most acceptance criteria need the node1 emit run, which the founder held. **THE RESULT: an 18-rule card scores 0.9302 bits/decision and is BEATEN by the `legality-conditioned` baseline at 0.8651, paired interval [-0.1099, -0.0209] entirely below zero, coverage 100%** — one villain, 878 hands / 1156 decisions (903 preflop), online-2009 corpus, transferred not measured. The orphaned scorer had a real bug first: `[...new Set(ids)].sort()` on NUMERIC handId, so "walk-forward" was walk-forward over a lexicographic scramble. The card that lost had NO sizing rules, and **we cannot yet test whether sizing fixes it because scoreHeldOut still measures ACTION lift only — WS-606 is that one extension, with the falsifier pre-registered.** **WS-578** put sizing on the card (schema v3, `sizingBands.mjs`, S2 v2 / S3 v1 as permanent two arms per unmeasured-constants.md): `my_raise_to_bb` was populated 1676/1676 aggressive actions and reached ZERO rules. An agent overturned MY band spec with measurement — 121 of 191 raises in the `>=8` band were exactly 12bb, carrying 8,325,235 of 8,327,391 total sum-of-squares; **pre-registered -11% RMSE, actual -71.2%** (8.6786 -> 2.4981), and the preflop arm delta went +0.0014 (indistinguishable) -> **+6.18**. The 79bb jam edge is FOUND not fitted (largest gap in the distribution is 58->100, 42bb, vs 9bb next). **WS-552** made the induction see sizing — it was blind BY CONSTRUCTION (`SITUATION_FIELDS` excludes the ACTION group; purity/entropy tally `d.action` alone). Second pass over action-homogeneous strata, not a combined joint, because the joint double-counts (folds have no size). Load-bearing synthetic: **old induction 1 rule / 0 conditions, new one splits at pAdj 1.07e-35**; action-induction parity IDENTICAL vs HEAD over 1,400 decisions. **WS-537** shipped the change ledger + NET/GROSS on the Result Card: **GROSS/|NET| = 14.32 on the live hero-EV path — only 7% of per-branch movement survives to the headline**, identity drift 1.1e-15 over 18 branches; the publish guard rejected 13 existing fixtures including the committed RC-depth-ablation.json it was filed against. It also overrode SCORED-READOUT-SPEC §9.3, whose formula is not exact for the self-normalized WIS estimator — spec needs amending. **WS-599** gave cards a canonical pointer + a resolver that REFUSES (no `{fallback}`/`{allowStale}`/`{recompute}` defeats it); 7 legacy cards registered with recovered provenance but NONE current — `rulesetHash` is unrecoverable and recomputing it from positional rule ids was tested and rejected on measured collisions. Also WS-579 (nine-handed selection), WS-591 (4 control-byte tickets; 590 queue files now parse; write-path fix UNDONE), WS-573 (rehydration test; the fix it guards is still UNCOMMITTED at HEAD). **8 tickets filed, all P1:** WS-599..604, WS-606. Four of them — WS-578/601/602/604 — are ONE mechanism: a producer holds a machine-readable fact, renders it for humans, keeps only the rendering. **TWO CLOBBERS, both recovered and verified:** a subagent ran `git checkout --` and destroyed another session's uncommitted work; then MY OWN safety check (diff against `git HEAD`) reported LOST:0 while wiping 8 changeLedger fields — HEAD cannot see uncommitted work. Rule now: snapshot before the edit, diff against the snapshot. **The ID allocator also collided** — WS-605 was handed to two sessions and the second write won silently (re-filed as WS-606). Verification run TARGETED, not full-suite, because the suite is non-deterministic (the other session's WS-605 measured 12 files >70s running 4-wide against a 5s ceiling): 313/313 sprint suites, 677/677 standardOfRecord, additive gate OK, 590 queue files parse, 16/16 node --check. node1 still blocked and it is TWO steps: WS-574's gameTree files must land, THEN origin/main must advance — node1 runs from its own checkout. |
| 2026-08-20 | **SPR-197 / WS-540 — the ladder got ~165x faster and the ticket's own cost diagnosis was refuted twice (commits 534b6ee5, 17f1c421, pushed)** | A pre-registered falsifier fired at Phase 0 and killed the approved plan before any of it was built. WS-540 asserted the per-decision cost was the per-player corpus walk; **measured, the corpus walk is under 2%** and 99.2% was `sampleCombos`, recomputed once per rung per decision. Confirmed independently by `node --cpu-prof` (57.9% self time in `evaluate5`), which knows nothing about the new instrument. Persisting the decision set — the approved Phase 2 — would have bought **0.8%**. **Fix 1** (534b6ee5): the combo sample is pure in (range, board, k), all decision properties, so it is built once per decision and handed to every arm via an optional param — 474 arm calls now drive 102 samples, 69.3s→19.3s (3.65x). **Fix 2** (17f1c421): `comboStrengthPercentile` enumerates ~1,176 opponent combos per hero combo and those inner scores depend only on the BOARD — the repo already had `computeBoardPercentileTable` (handEvaluator.js:371) and `handStrength.mjs:355` was already using it, so this was a call-site change with **handEvaluator.js unmodified**; its self-asserted equivalence was CHECKED first (9,122 combos / 8 boards, zero mismatches) rather than trusted. 141.6s→2.3s (61.6x). Combined **4.94 → 0.03 s/decision (~165x)**, output BIT-IDENTICAL at every step by full JSON diff. **Two of my own errors caught by checking, not reasoning:** the first mechanism guess (`comboClass`) was wrong — it is cheap string math; and the instrument recorded INCLUSIVE time, double-counting nested phases, the tell being a NEGATIVE 'unaccounted wall'. Per-phase cost is now measured (accept criterion #3 — there had been NO timing instrumentation in scripts/backtest at all) and persisted into the run JSON so the falsifier stays checkable. Both refuted predictions kept in run-rule-ladder.mjs's header: the 2026-08-17 corpus-walk claim is TRUE today (99.6%) but was false when made — it became true only once the dominant term was removed. **WS-540 NOT done, claim released, 2 of 6 criteria open**; the decoupling is re-scoped onto the combo sample and every rung is still UNRESOLVED at n=93, but ~26,000 decisions is now ~13 min (was ~36 h). **Concurrency was the dominant hazard**: 6 live sessions on one tree/index — `.current-session` pointed at a DIFFERENT session, the index held 12 files staged by another, and a text-mode YAML edit silently rewrote CRLF→LF; all three caught, none committed, 5 friction captures filed. 43 commits still unmerged to origin/main. |
<!-- 2026-08-19 Conduct Card instrument (six live defects; card made EVALUABLE and scored held-out for the first time; heroReach; blind-primitive pilot; WS-576..583 queued) row pushed to archive 2026-08-21 (live-table sidebar session). Canonical record in .claude/workstream/sessions/ + sprint-index.yaml. -->
<!-- 2026-08-15 row pushed to archive 2026-08-21 (SPR-197 session). | 2026-08-15 | **SPR-185 / WS-441 — 44px touch floor: measured, mitigated, enforced (commit 5c* feat(design))** | Delta protocol cleared first (2 fresh-context reviewers over 282f209e..HEAD: zero domain defects; 4 hygiene fixes committed separately). WS-441: doctrine + geometry contract + device evidence + devshot recovered from orphan merge 9de247d1 (docs/tooling only; src half superseded by WS-440); device record corrected everywhere (target = **Galaxy S22**, 1600×720 is the canvas; the "S22 clamps scale to 1.0" premise in 3 YAMLs was FALSE — real s≈0.695 at provisional DPR 2, floor = 64 declared px; `/device-probe.html` measures the true DPR, one-line correction in `deviceProfiles.mjs`). Interim raise (founder-approved): tabs/orbit/custom-input/GO/replay-transport → 64px both axes; **devshot caught the raise clipping Next Hand below the fold — the spec had passed it** (boundingBox measures off-screen), so the geometry contract's scroll structure landed in CommandStrip (tabs pin, ControlZone pins, middle scrolls) + pinned-anchor visibility assertions. `touch-floor.spec.js` blocking in ci.yml; falsified deliberately (40px tab → red both profiles). **WS-489 filed** = the real fix (controls exit the canvas; re-file of collision-lost WS-322). Full suite 15,320 green / 1 pre-existing environmental red (compactTier worktree sweep, friction captured); Playwright 8/8; sessionRecoveryLiveness now GREEN (kit backport fixed it — state row above is stale). NOTE: concurrent session's in-flight FIND-137 work rakes the fold branch postflop, revising WS-451's "fold branch never taxed" — not adjudicated here. | -->
<!-- 2026-08-14 SPR-182/WS-442 (three orphan branches domain-merged; ticket framing overturned 3x; ENGINE_VERSION v125; WS-477/478/479 filed) row pushed to archive 2026-08-21 (ignition capture-truthfulness session). Canonical record in .claude/workstream/sessions/ + sprint-index.yaml. -->
<!-- 2026-08-04 scroll-guard row pushed to archive 2026-08-14 (SPR-182 session). Raw row in system/state-history.md. 5 view fixes + INV-VIEW-SCROLL guard. -->
<!-- 2026-08-01 SPR-167/WS-292 holding-knowledge row pushed to archive 2026-08-13 (WS-440 orientation session). Raw row in system/state-history.md; canonical record in .claude/workstream/sessions/ + sprint-index.yaml. -->
<!-- 2026-07-25 row pushed to archive 2026-08-06 (WS-424 failed-session close-out). Canonical record in .claude/workstream/sessions/ + sprint-index.yaml. Gate unblock + WS-262 close-out + SPR-149. -->
<!-- 2026-07-22 row pushed to archive 2026-08-06 (WS-424 failed-session close-out). Canonical record in .claude/workstream/sessions/ + sprint-index.yaml. Gate unblock (4 protocol runs) + SPR-146 domain-spec promotion. -->
<!-- 2026-07-22 SPR-145 (WS-236 HUD villain-stat attribution + stats-engine parity seam, FIND-024/DP-008) row pushed to archive 2026-08-04 (view-scroll bug-class session). Canonical record in sprint-index.yaml + SPR-145 sprint file. -->
<!-- 2026-06-20 SPR-137/138/139 (domain-correctness first-principles cluster; DEC-022/023/024) row pushed to archive 2026-08-01 (SPR-167 close-out). Canonical record in sprint-index.yaml + SPR-137/138/139 sprint files. -->
<!-- 2026-06-13 SPR-126 (WS-221 global anchor-library calibration reset, red line #4b) row pushed to archive 2026-08-01 (SPR-167 close-out). Canonical record in sprint-index.yaml + SPR-126 sprint file. -->
<!-- 2026-06-12 SPR-123 (resumed orphaned approved sprint; WS-227 verified shipped 513f284, WS-225 deletion committed e8e6c86) row pushed to archive 2026-07-25 (SPR-149 close-out). Canonical record in sprint-index.yaml + SPR-123 sprint file. Original row: -->
<!-- | 2026-06-12 | **SPR-123 — resumed orphaned approved sprint: WS-227 tournament-zone builders + WS-225 PrototypeFinderView deletion** | `/next` gate showed no active sprint and composed a NEW sprint with the same two items — but premise check found WS-227 ALREADY SHIPPED (commit `513f284`, prior session: buildTournamentBarHTML/buildTournamentDetailHTML in render-orchestrator.js, single-source TOURNEY_ZONE_COLORS/ICM_ZONES, 27 zone-tournament tests, comment-debt cleanup) and WS-225 sitting UNCOMMITTED in the working tree. Root cause: prior session approved SPR-123 (status:approved), executed item 1, half of item 2, ended mid-sprint — and the gate only resumes status:active sprints (**2nd bite of `[[resume-approved-sprints]]` / fr-012, new variant: approved AND partially executed**). Discarded the duplicate composition, resumed SPR-123. **WS-227 verified:** extension suite 2,348/2,348 (80 files); harness rebuilt + Playwright re-verified betweenHandsTournament + zx_x5e critical-urgency (zero undefined/null/NaN; prior session's own Playwright run confirmed at 21:33-35Z artifacts). **WS-225 finished + committed (`e8e6c86`):** view + mockPlayers deleted (−1,694 lines), SCREEN.PROTOTYPE_FINDER route/hash/orientation wiring + AdminSection sandbox entry removed (decision noted: empty Admin section hides entirely), 4 extracted-primitive doc headers updated, zero dangling refs, build clean (PWA 66→64 entries). Full suite 12,348/507 — single failure is the documented load-flake class (component project green isolated). Both items closed via `done`; queue Backlog 41 / Done 151. | -->
<!-- 2026-06-12 SPR-122 (shared IDB tx helpers WS-226, 19 modules migrated, −884 lines, commit 77d065e; durable-writes founder decision) row pushed to archive 2026-07-22 (SPR-146 close-out). Canonical record in sprint-index.yaml + SPR-122 sprint file. Original row: -->
<!-- | 2026-06-12 | **SPR-122 — shared IDB transaction helpers (WS-226) + refactor-candidate scan queued WS-225/227** | Founder-requested refactor scan (2 cto-agent reviewers: main app + extension) produced 3 queue items: WS-225 PrototypeFinderView deletion (P12/S), WS-226 persistence tx-helper dedup (P14/M), WS-227 extension tournament-zone pure-builder extraction (P13/M); second-tier findings (CommandStrip, AnchorObservationModal, diagnostics panel) deliberately NOT queued (opportunistic-only per scan verdict). `/next` anchored WS-226 (plan-first). **Founder decision (Medium): durable writes** — write helpers resolve on `tx.oncomplete` (after commit), not `request.onsuccess`; closes the resolve-then-rollback window (quota-at-commit class). **Shipped (commit `77d065e`):** 5 helpers in database.js (readTx/writeTx/updateTx/cursorTx/atomicTx; contract: raw-DOMException rejection — QuotaExceededError name-checks survive; helpers never log — modules' createPersistenceLogger try/catch owns swallow-vs-propagate) + all 19 persistence modules migrated behind unchanged public signatures (95/97 raw `db.transaction` sites collapsed; net −884 lines). Documented exceptions: batchUpdateSeatPlayers (bespoke abort-tracking) + deletePlayer (parallel cursor cascade) + migrations.js (versionchange, out of scope). NEW dbTransactions.test.js (19 tests: ConstraintError raw-error contract, updateTx rollback proof, atomicTx cross-store atomicity proof). Full suite 12,349/507 files green; build clean; only documented load flakes (all green isolated; AnchorObservationModal timeout added to the known list). SYSTEM_MODEL §1 persistence row updated (v27 + helper contract). | -->
<!-- 2026-06-11 SPR-121 (extension WS replay seam test WS-217 + HUD-drop fold bug fix; commit 000f4a5) row pushed to archive 2026-07-22 (SPR-145 close-out). Canonical record in sprint-index.yaml + SPR-121 sprint file. -->
<!-- 2026-06-11 SPR-120 (full-chain analytics seam test, WS-216; analysisPipeline.seam.test.js, 4 zero-mock tests; commit 361cc97) row pushed to archive 2026-06-20 (SPR-137/138/139 close-out). Canonical record in sprint-index.yaml + SPR-120 sprint file. -->
<!-- 2026-06-10 SPR-119 (anchor-owned predicate registry + two-validator inheritance, WS-218; DEC-020) row pushed to archive 2026-06-13 (SPR-126 close-out). Canonical record in sprint-index.yaml + SPR-119 sprint file. -->
<!-- (SPR-119 detail: ANCHOR_PREDICATE_KEYS anchor-owned registry; validator.js additive inheritance + validateAnchorFull; all 4 seeds to full v1.1; commit a2cf5c4.) -->
<!-- 2026-06-10 SPR-118 (range-profile rules re-gated on observed frequency, WS-223 + WS-215; "observed frequency everywhere" theory decision) row pushed to archive 2026-06-12 (SPR-123 close-out). Canonical record in sprint-index.yaml + SPR-118 sprint file. -->
<!-- 2026-06-10 SPR-117 (rangeEngine→exploitEngine seam-test family WS-214 + bucketQueryUtils contract fix; WS-223 finding; 3 LAZY placeholders deferred) row pushed to archive 2026-06-12 (SPR-122 close-out). Canonical record in sprint-index.yaml + SPR-117 sprint file. -->
<!-- 2026-06-10 SPR-116 (YAML reader inline-comment hardening WS-213 + stale HSP-v2 umbrella close-out, WS-150 closed) row pushed to archive 2026-06-11 (SPR-121 close-out). Canonical record in sprint-index.yaml + SPR-116 sprint file. -->
<!-- 2026-06-10 SPR-115 (EAL autonomy red-lines contract suite WS-025 + engineering cap-breach root fix, 11 stale items deferred) row pushed to archive 2026-06-11 (SPR-120 close-out). Canonical record in sprint-index.yaml + SPR-115 sprint file. -->
<!-- 2026-06-09 SPR-110 (PIO recognition scoring + confidence, WS-164; DEC-019) row pushed to archive 2026-06-10 (SPR-119 close-out). Canonical record in sprint-index.yaml + SPR-110 sprint file. -->
<!-- 2026-06-08 SPR-109 (SCF turn-barrel + RFI open-fold frequency rules, WS-146 6th claim) row pushed to archive 2026-06-10 (SPR-118 close-out). Canonical record in sprint-index.yaml + SPR-109 sprint file. -->
<!-- 2026-06-06 SPR-108 (SCF first multiway leak rule + frequency-of-aggression substrate, WS-146 5th claim) row pushed to archive 2026-06-10 (SPR-117 close-out). Canonical record in sprint-index.yaml + SPR-108 sprint file. -->
<!-- 2026-06-05 SPR-107 (mid-hand tag-for-review WS-190; IDB v27) row pushed to archive 2026-06-10 (SPR-116 close-out). Canonical record in sprint-index.yaml + SPR-107 sprint file. -->
<!-- 2026-06-04 SPR-106 (HSP-M1 full multiway model, WS-154; HSP v2 hard-dep closed) row pushed to archive 2026-06-10 (SPR-115 close-out). Canonical record in sprint-index.yaml + SPR-106 sprint file. -->
<!-- 2026-06-04 SPR-104 + SPR-105 (back-to-back HSP v2 close-out — WS-211 orchestrator turn/river precision + WS-155 HSP-X1 adjustment composition) row pushed to archive 2026-06-09 (SPR-110 close-out). Canonical record in sprint-index.yaml + SPR-104/SPR-105 sprint files. -->
<!-- 2026-05-30 SPR-103 (HSP v2 turn+river close-out, WS-152 + WS-153) row pushed to archive 2026-06-08 (SPR-109 close-out). -->
<!-- 2026-05-21 SPR-100 (Range Lab Phase 2 equity histogram + subrange filters, WS-057) row pushed to archive 2026-06-06 (SPR-108 close-out). -->
<!-- 2026-05-16 SPR-083 (Postflop EV-bucket partition display fix, WS-185) row pushed to archive 2026-06-05 (SPR-107 close-out). -->
<!-- 2026-05-15 SPR-082 (SLS Stream B1 Range Silhouette WS-041) row pushed to archive 2026-06-04 (SPR-106 close-out). -->
<!-- 2026-05-14 SPR-077 (seat-autoselector Phase 1 telemetry, WS-189) row pushed to archive 2026-05-31 (SPR-104 compose-and-queue). -->
<!-- 2026-05-13 SPR-076 CameraCaptureModal row pushed to archive 2026-05-30 (SPR-103 close-out). -->
<!-- 2026-05-13 SPR-075 row pushed to archive 2026-05-21 (SPR-100 close-out). -->
<!-- 2026-05-11 Refactor Sprint Item 3 row pushed to archive 2026-05-16 (SPR-083 close-out). Canonical record at .claude/workstream/sessions/session-2026-05-11-refactor-sprint-item-3.yaml. -->

## Session Mode Usage

| Mode | Last 30 Days | Last Used |
|------|-------------|-----------|
| Quick Fix | 0 | — |
| Standard | 1 | 2026-05-01 (audit) |
| Strategic | 2 | 2026-05-11 (refactor sprint Item 3) |

Updated automatically by session protocol.
