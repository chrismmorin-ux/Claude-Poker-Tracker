# Project Status

Last updated: 2026-04-15 by Claude (SR-6.17 COMPLETE — shell visibility redesign + all-zone DOM migration. #hud-content-v2 wrapper deleted; zones z1–z4 + zx nested inside #hud-content; zone-z0 promoted to body-top chrome wrapper. Suite 1771 → 1795, +24. SR-7 unblocked.)

---

## Active Sessions

- **SR-7 NEXT (cutover):** flip `sidebarRebuild` default to true, delete legacy code paths, write post-mortem. Unblocked by SR-6.17.

## Recently Completed (additions)

- **SR-6.17 COMPLETE — 2026-04-15:** Shell visibility redesign + all-zone DOM migration. **HTML:** `#hud-content-v2` wrapper deleted entirely; its 6 empty zone containers replaced by content-bearing zones. Zones `z1`/`z2`/`z3`/`zx`/`z4` now nest inside `#hud-content` (single shell, still `hasTableHands`-gated). Zone `z0` promoted to body-top-level chrome wrapper containing status-bar + pipeline-health + pid-summary (always visible, chrome never disappears). CSS `#hud-content-v2 { display:flex }` rule removed; all 6 zone min-heights preserved. **Content migration:** z1 ← seat-arc; z2 ← action-bar + context-strip + cards-strip + plan-panel; z3 ← tournament-bar + tournament-detail + street-progress + street-card; zx ← between-hands + app-launch-prompt; z4 ← more-analysis-btn/content + model-audit-btn/content. **side-panel.js:** `renderAll` shell toggle collapsed from 21-line if/else (hud-content + hudV2 + rebuildOn branch) to 10 lines — single `#hud-content` visibility gate driven by `hasTableHands`. `rebuildOn` sentinel + `hudV2` lookup deleted. **Tests:** `zone-shell.test.js` rewritten — drops v2 assertion, adds (a) z0-outside-#hud-content, (b) z1–z4/zx-inside-#hud-content, (c) per-zone content-ID placement pins (13 migrated IDs × 1 test each). Suite 1771 → 1795 (+24). Build clean (6 entry points). Legacy render pipeline unchanged — every renderer still writes into same element IDs; only DOM parentage shifted, and `getElementById` is parent-agnostic. Replay framework (`signatures.js`, `replayer.js`) + harness (`harness.js`) still reference `#hud-content` which persists as sole shell. **SR-7 unblocked.**

- **SR-6.12 COMPLETE — 2026-04-14:** Z2 PR shipped. Both blocking deltas (B1 + B2) from the SR-5 architecture audit resolved in this PR. **B2 (equity in headline):** `_buildStandardActionBar` restructured — equity `{N}% equity` + EV edge now inline on `ab-row1` (headline); `ab-row2` is SPR-only per §2.4 (shows `SPR: N.N` with R-4.2 `SPR: —` placeholder when unknown). Mixed-unit bug defense: `heroEquity ≤ 1` treated as fraction and ×100-scaled; else rendered as-is. **B1 (pot chip + street strip stale between hands):** between-hands pot chip + street pill suppressed in `buildUnifiedHeaderHTML` by an explicit `handLive` guard (`live.state !== 'COMPLETE' && !== 'IDLE'`); same guard added to `renderAll`'s street-progress branch so the dot-strip blanks between hands (previously rendered from `lastGoodAdvice.currentStreet` fallback, producing S4/02-a pot chip + S4/02-b all-filled-dots regression). **§2.10 consolidation:** new `computeAdviceStaleness(advice, ctx, now)` pure helper is the single source of truth — both the main render path and the 1 Hz `adviceAgeBadge` timer now call through it; duplicated `Date.now() - _receivedAt` + `advice.currentStreet !== ctx.currentStreet` arithmetic deleted from both call sites (render loop went from ~14 lines of inline stale computation to 2-line helper call + destructure). New `__tests__/z2-decision.test.js` (15 cases): B2 equity-in-headline + row-2 SPR-only + R-4.2 placeholders + fraction/integer mixed-unit defense; B1 pot+street blank on COMPLETE/IDLE + render on FLOP; §2.10 source-level pins (helper exists, render calls helper without inline arithmetic, 1 Hz timer calls helper, four reason branches present). Suite 1662 → 1677 (+15). Build clean (6 entry points). Z2 batch invariants 1–9 satisfied. **Next in order:** SR-6.13 (Z3 PR — adds 3.11 multiway seat selector, removes 3.10 orphan, wires Rule V cross-zone contract; M).

- **SR-6.11 COMPLETE — 2026-04-14:** Z1 PR shipped. Three Z1 features landed in one PR. **1.1 logarithmic ring:** new pure `quantizeSampleBand(handsCount)` in `render-orchestrator.js` bands `{empty, tiny (<20), small (<100), medium (<500), large (500+)}`. `buildSeatArcHTML` emits `.ring-band-{band}` class; inline CSS adds four outer box-shadow rules with escalating stroke widths so each band is visually distinct (R-5.1 separate channel from `.focused` gold glow + `.pinned` top-right pin glyph). **1.1 R-4.2 placeholder:** occupied-zero seats (sampleSize === 0 && !vacant && !folded) now render a `—` glyph overlay via new `.seat-sample-unknown` span — distinct from the tiny band so low-sample ≠ no-sample in glance. **1.11 Rule V selection ring:** new coordinator slot `rangeSelectedSeat` (in `_state`, snapshot, renderKey, cleared on `clearForTableSwitch` AND on new-hand boundary per R-2.4 batch-invariant 4); seat circle gains `.rule-v-selected` class; CSS dashed purple outline distinct from pinned pin glyph and focused gold glow. **Rule V pill-click contract:** villain-tab click handler in `side-panel.js` previously set `pinnedVillainSeat` (conflating scouting pin with range-grid selection) — now writes `rangeSelectedSeat` only, with explicit code comment documenting the R-1.3 contract. `computeFocusedVillain` gets a new top-priority branch `rangeSelectedSeat > pinnedVillainSeat > advice.villainSeat > pfAggressor > HU` so the range-grid UX still updates when the user clicks a tab. Click-again-to-deselect wired via `current === seat ? null : seat` toggle. New `__tests__/z1-table-read.test.js` (23 cases): band thresholds, arc band class + `—` placeholder, Rule V priority, coordinator lifecycle (default null, snapshot, renderKey flip, clearForTableSwitch, hand-boundary reset), source pin for villain-tab → rangeSelectedSeat (not pinnedVillainSeat), CSS pins for 4 band rules + `.rule-v-selected` + `.seat-sample-unknown`. Suite 1639 → 1662 (+23). Build clean. **Next in order:** SR-6.12 (Z2 PR, M — fixes B1 + B2 regressions).

- **SR-6.15 COMPLETE — 2026-04-14:** Zx PR shipped. Main new work: `betweenHands` FSM wiring from app code (registered in SR-6.5 but never dispatched). `side-panel.js` `handleLiveContextPush` now dispatches `liveContextArrived` with `betweenHandsOrIdle = state === 'IDLE' || state === 'COMPLETE'` on every live-context push — mid-hand states (DEALING/PREFLOP/FLOP/TURN/RIVER/SHOWDOWN) never mount the banner, which matches X.1 spec that X.4 owns disconnected-during-hand (not X.1). `handleAdvicePush` dispatches `adviceArrived` on accepted advice BEFORE the scheduleRender branch so both the promoted and buffered (`_pendingAdvice`) paths end the banner the moment hero has live advice. X.4c recovery-btn re-enable timer verified on `coordinator.scheduleTimer('recoveryBtn_reEnable', …)` (RT-60 compliance, pre-existing from SR-6.3 — pin regression in new test). New `__tests__/zx-overrides.test.js` (9 cases): 3 source-level wiring pins + 6 end-to-end FSM transitions through the coordinator (initial inactive, active on COMPLETE, stays inactive on PREFLOP, adviceArrived clears from active, active → inactive on next non-between-hands context, tableSwitch cancels). Suite 1630 → 1639 (+9). Build clean (6 entry points). **Deferred inside scope:** Observer-mode tier verification (X.6/X.7) and X.5 placement verification are both satisfied by pre-existing rendering behavior + corpus frames S10/S11; making those assertions explicit harness-level tests belongs to the Z2/Z3 PRs when those zones have containers. **Next in order:** SR-6.11 (Z1 PR, M).

- **SR-6.10 COMPLETE — 2026-04-14:** First zone PR shipped. Z0 0.2 and 0.7 both addressed. `render-coordinator.js` default `lastHandCount: 0 → null` (R-4.2 distinguishes boot-race unknown from known-zero; `> 0` / `!==` downstream checks all still pass with null). `side-panel.js` `updateStatusBar` writes `—` (U+2014) into `#hand-count` when `handCount == null`, numeric otherwise. `side-panel.html` wraps `#diag-show` in new `#diag-footer` div with `style="display:none"` default (flag defaults false, so non-debug users see nothing). New `renderDiagnosticsGate(snap)` called from renderAll: toggles `diagFooter.style.display` by `snap.settings?.debugDiagnostics`, and when the flag flips off mid-session with the panel open, closes it + clears the `diag_autorefresh` timer. `diag-show` click handler also guards with `coordinator.get('settings')?.debugDiagnostics !== true` early-return (belt-and-braces for stale DOM references). 0.7 "footer placement = bottom of sidebar" was pre-existing (button already at end of body). New `__tests__/z0-chrome.test.js` (8 cases): coordinator default null, HTML wrapper + default display:none, source-level assertions on updateStatusBar/renderDiagnosticsGate/click guard, renderAll wiring. Suite 1622 → 1630 (+8). `node build.mjs` clean (6 entry points). **Next in order:** SR-6.15 (Zx PR).

- **SR-6.7 COMPLETE — 2026-04-14:** R-7.3 1-street gap tolerance revoked. `handleAdvice` (line ~492) + `handleLiveContext` pending-promote mid-hand branch (line ~594) both tightened from `(adviceRank - liveRank) <= 1` to `adviceRank === liveRank`. Ahead-of-context advice now buffers in `_pendingAdvice` until live context catches up; lagging advice (live ahead of advice) is rejected outright. `side-panel.js` renderAll adds `isStreetMismatch = advice.currentStreet !== liveCtx.currentStreet`; OR'd into `isAdviceStale` AND threaded into `updateStaleAdviceBadge(isStale, ageMs, reason)` — when `reason === 'street-mismatch'` the badge text reads `"Stale — recomputing"` (otherwise the existing `"Stale Ns"` age badge). 1 Hz `adviceAgeBadge` timer path mirrors the same logic so the label refreshes without a full render. `lastGoodAdvice` is NOT blanked — stale tint + "recomputing" label surface the pending recomputation (S2 regression: advice panel never shows preflop advice during flop). **Tests:** new `SR-6.7 — exact-street match for advice acceptance` describe block in `render-coordinator.test.js` with 4 cases (S2: flop advice held while preflop; preflop advice lingers but stream mismatches once flop live; exact-match accepted; mid-hand reconnect preflop push rejects pending flop under revoked tolerance). Updated `message-integration.test.js` Scenario 11 legacy "gap=1 accepted" case → "held under revoked tolerance". Suite 1618 → 1622 (+4 net). `node build.mjs` clean. **Foundation band 11/11 done; zone PRs now the critical path.**

- **SR-6.6 COMPLETE — 2026-04-14:** Freshness records + R-4.3 partial-payload retention landed (narrowed scope — freshness sidecar rather than per-value envelope unwrap in every renderer, to stay M-sized; per-value unwrap deferred to zone PRs when each row picks its own placeholder + threshold). `_freshness` slot added to coordinator: `{ currentLiveContext: {timestamp, source}, currentLiveContextFields: {[key]: {timestamp, source}}, appSeatData: {[seat]: {timestamp, source}} }`. Snapshot exposes shallow-copied `freshness` slot; existing readers unchanged (R-4.1 metadata grain; `confidence` deferred until a renderer consumes it). **`handleLiveContext`** now field-merges mid-hand (`{...prior, ...ctx}`), retains prior fields absent from new payload (S1 fix — partial `push_live_context` with only `currentStreet` no longer nulls `pot`/`communityCards`); new-hand boundary (`PREFLOP`/`DEALING` transition) still does full replace to prevent stale board/pot retention. **New `mergeAppSeatData(seatMap, source)`** method: per-seat field-level merge, retains prior seats absent from push (S5 fix), bumps `appSeatDataVersion` so renderKey invalidates. `side-panel.js` `handleExploitsPush` now calls `mergeAppSeatData(seatData, 'push_exploits')` instead of `set('appSeatData', ...)` + manual version bump. `clearForTableSwitch` resets `currentLiveContext` freshness; `appSeatData` freshness retained to match `appSeatData` itself not being cleared on table switch (preserves existing doctrine). **Tests:** 9 new cases in `render-coordinator.test.js` SR-6.6 block — mid-hand partial retention (S1), new-hand boundary clears, explicit-null override, per-field freshness timestamps, per-seat merge retention (S5), version bump, freshness stamping, null/undefined payload guard, clearForTableSwitch freshness behavior. Suite 1609 → 1618 (+9). `node build.mjs` clean (6 entry points). **Foundation band 10/10 done; SR-6.7 unblocked; zone PRs SR-6.10/6.12/6.14 now the critical path.**

- **SR-6.5 COMPLETE — 2026-04-14:** FSM authoring landed. 5 declarative panel FSMs in new `side-panel/fsms/` dir (recovery-banner, seat-popover, deep-expander, between-hands, street-card) using a ~60-line `fsm.js` helper (`defineFsm({id, initial, states:{on:{event:handler}}})` — returns `{state, changed, extra?}`; unknown event/state is a silent no-op; string/object/null handler returns all supported). `render-coordinator.js` gained `panels` slot + `seatPopoverDetail` slot + `registerFsm`/`dispatch`/`getPanelState`/`dispatchTableSwitch`/`dispatchHandNew`/`onHandNew` API (dispatch on state-change calls `scheduleRender(IMMEDIATE, reason='fsm:<id>:<event>')`; seatPopover extra surfaced to `seatPopoverDetail` slot). Snapshot + renderKey include `panels` (sorted `id:state` pairs) + `seatPopoverDetail.seat` — every transition forces re-render. `clearForTableSwitch` fans `tableSwitch` event to every FSM before wiping state; `handleLiveContext` at PREFLOP/DEALING boundary fans `handNew` to every FSM + hooks. **9 DOM-mutation sites rewired in `side-panel.js`:** (1–3) onDisconnect/onContextDead/onVersionMismatch now set `connState.{cause,text}` + scheduleRender → new `renderConnectionStatus(snap)` writes the dot/text. (4–5) recovery banner `showRecoveryBanner`/`hideRecoveryBanner` imperative helpers deleted; new `renderRecoveryBanner(snap)` reads `panels.recoveryBanner` FSM state + `recoveryMessage` slot; button click dispatches `userReload`; 5s timer dispatches `reenableTimerFire`; all `push_recovery_*` / `push_silence_alert` message handlers also dispatch. (6) tournament detail click handler drops `detail/chevron.classList.add/remove('open')`; new idempotent sync at the same site toggles from `tournamentCollapsed`. (7) deep-expander click: dispatches `deepExpander:userToggle` + mirrors boolean; `renderDeepExpander` now has idempotent classList sync so auto-open-on-postflop paints correctly without touching DOM directly. (8) seat popover: extracted pure `buildSeatPopoverHtml(seat, appSeatData, cachedSeatStats)`; deleted `activePopoverSeat` module var + `showSeatPopover`/`hideSeatPopover` imperative fns; click handler dispatches `seatClick`/`outsideClick` (coords passed in payload); new `renderSeatPopover(snap)` owns innerHTML + classList + style writes. (9) stale-tint toggle left in renderAll path (already in render — flagged in audit as C7 coupling, not a rule violation). **`render-street-card.js`:** `setStreetCardTimerHost` now accepts optional `dispatch`; `dispatchFsm('streetChange'|'fadeTimerFire'|'heightReleaseFire')` surfaces the transition phase in the streetCard FSM as observable spec (timer-host still drives actual fade). **Tests:** new `fsm.test.js` (7 helper cases + 5 FSM-specific blocks, ~35 assertions) + new `coordinator-dispatch.test.js` (10 integration cases covering registerFsm idempotence, unknown-id no-op, render scheduling, seatPopoverDetail surfacing, renderKey flips, tableSwitch + handNew fan-out). Suite 1560 → 1609 (+49). `node build.mjs` clean (6 entry points). **Deferred:** timer-discipline lint extension to forbid `.classList.add/remove/toggle` in event handlers is non-trivial (requires scope analysis vs allowlisted render fns); punted to SR-6-followup. **betweenHands FSM** is registered but not yet dispatched from code — its wiring lands in SR-6.12 (Z2 PR) alongside the B1 S4/02-a/b fix. **Unblocks:** SR-6.7 (dep on SR-6.5 satisfied; still dep on SR-6.6), SR-6.11, SR-6.13, SR-6.15. Foundation band now 9/9 complete except SR-6.6.

- **SR-6.9 COMPLETE — 2026-04-14:** Motion budget landed. `side-panel.html` inline style edits: `.street-card.fade-in` animation 0.15s → 0.2s (R-6.1 floor); `.deep-body` max-height transition 0.35s → 0.3s (R-6.2 ceiling). `render-street-card.js` fade-out timer bumped 150 → 200 to track the CSS floor (no duration drift between classList toggle and content swap). New `@media (prefers-reduced-motion: reduce)` block appended to the inline `<style>` zeroes `animation-duration` + `transition-duration` (+ `animation-iteration-count: 1`, `scroll-behavior: auto`) globally via `*, *::before, *::after` with `!important` — honors R-6.3 without reaching into every keyframed/transition rule individually. New `side-panel/__tests__/motion-budget.test.js` pins (a) CSS duration values (0.2s fade-in, 0.3s deep-body), (b) media query block exists + zeroes both duration kinds, (c) render-street-card scheduleTimer call uses 200ms — prevents future JS/CSS drift. Suite 1555 → 1560 (+5). `node build.mjs` clean (6 entry points). SR-6.5 / SR-6.6 remain the NEXT candidates; zone PRs still gated on SR-6.5.

- **SR-6.8 COMPLETE — 2026-04-14:** Zone shell landed behind `sidebarRebuild` flag. `side-panel.html` gains `#hud-content-v2` with 6 empty zone containers (`.zone-z0`/`z1`/`z2`/`z3`/`z4`/`zx`) + per-zone CSS min-height contracts (72/204/150/300/24/0 px respectively) inside the single inline `<style>` block. `renderAll` in `side-panel.js` toggles between legacy `#hud-content` and the v2 shell inside the SR-6.1 gate sentinel (flag-off = legacy, flag-on = v2); the no-table branch hides both. New test file `side-panel/__tests__/zone-shell.test.js` enforces (a) all 6 zone IDs/classes present in HTML, (b) each `.zone-z{id}` declares a `min-height`, (c) CSS lint forbids `display:none` inside any `.zone-*` selector — enforces R-1.3 slot-reservation invariant (empty zones reserve space; they never collapse). Suite 1541 → 1555 (+14). `node build.mjs` clean (6 entry points). SR-6.9 (motion budget) unblocked; zone PRs SR-6.10/6.12/6.14 lose their SR-6.8 dependency; 6.11/6.13 still blocked on SR-6.5.

- **SR-6.4 COMPLETE — 2026-04-14:** `buildRenderKey` in `render-coordinator.js` now fingerprints all 4 target fields: `deepExpanderOpen`, `planPanelOpen`, `tournamentCollapsed` (each 1 bit), plus `lastGoodExploits` as a presence bit + `appConnected` bit (the push-count covers increments; these two bits cover clears on table switch and appConnected transitions carried on the exploits object). Five regression tests added to the `buildRenderKey: state-drift invalidation coverage` block in `render-coordinator.test.js` — one per UI toggle, one for exploits-clear, one for appConnected flip. Suite 1536 → 1541 passing. R-5.4 gate satisfied. Unblocks SR-6.6 (freshness records) and SR-6.8 (zone containers) — both listed SR-6.4 as a dependency. SR-6.5 (FSM authoring) still gated only by SR-6.1 and SR-6.4, both now complete.

- **SR-6.3 COMPLETE — 2026-04-14:** RT-60 timer contract now exhaustive. Owner chose the long-term-fix reading over the handoff's 5-site list: every bare `setTimeout`/`setInterval` call in `side-panel.js` + `render-street-card.js` is gone. Coordinator gains `scheduleTimer(key, cb, ms, kind)` + `hasTimer(key)` + `activeTimerCount()` + `_setInterval` dep; 13 call sites migrated (recovery-btn re-enable, tableGrace, handStats retry, staleContext, tourneyTimer, adviceAgeBadge, planPanelAutoExpand, tourneyLogCopy-reset ×2, diagCopy-reset ×2, diag_autorefresh, swFallbackPing, streetCard_fade + streetCard_heightRelease). Diag auto-refresh now starts on panel-open / stops on close (was unconditional interval at boot). DI-leaked `setTimeout`/`clearTimeout` primitives dropped from coordinator construction in side-panel.js — defaults in `render-coordinator.js` own them. `render-street-card.js` gains `setStreetCardTimerHost(host)` setter; fade falls back to instant content swap when no host is wired (visual harness path), keeping the module free of bare timers without breaking harness. Discipline test at `side-panel/__tests__/timer-discipline.test.js` strips comments + string literals and greps for `setTimeout(` / `setInterval(` in the two files — pins the invariant so any future bare call fails CI. Suite 1534 → 1536 passing; `node build.mjs` clean (6 entry points).

- **SR-6.2 COMPLETE — 2026-04-13:** Harness corpus extension shipped. 22 new fixtures appended to `side-panel/__tests__/fixtures.js`: Z0 (z0_bootRace), Z1 (z1_occupiedZeroSeat), Z3 (z3_villainPostflopGrid, z3_multiwaySelector, z3_noAggressorPlaceholder), Z4 (z4_rt61AutoExpandTrigger, z4_noPlanPath, z4_moreAnalysisOnly, z4_debugFlagOffNoAudit), Zx (zx_x1_connectedIdle, zx_x1_disconnectedMidHand, zx_x3_graceExpired, zx_x4_implicitClearMultiMessage, zx_x4c_reEnableTimer, zx_x5_tournamentLevelTransition, zx_x5b_zoneTransition, zx_x5c_finalLevel, zx_x5d_belowAverage, zx_x5e_criticalUrgency, zx_x5fg_farFromBubbleNoPredictions, zx_x6_observerNoVillain, zx_x7_observerWithTournament). Two justified merges: X.4 composite (implicit-clear + multi-message) and X.5f+X.5g (both absent-state ICM variants) — documented inline. RT-61 post-collapse state justified as coordinator-layer (not state-snapshot expressible). All legacy-path (flag stays false). Suite 1314 → 1534 passing (+220 via null-safety/no-undefined iteration over ALL_FIXTURES). Replay determinism (S1–S13) re-ran twice with identical hashes. Harness screenshot sweep deferred to SR-6.8 when zone containers land — before/after snapshots there carry more signal than baseline-only screenshots now.

- **SR-6.1 COMPLETE — 2026-04-13:** Foundation flag plumbing shipped. New `shared/settings.js` owns `loadSettings` / `observeSettings` / `writeSetting` (chrome.storage.local). Two keys: `settings.sidebarRebuild` + `settings.debugDiagnostics`, both default false. Coordinator gained `settings` slot in `_state` + shallow-copied into snapshot + hashed into renderKey (flag flips force re-render within 1 frame). `side-panel.js` boot: `loadSettings()` → `coordinator.set('settings', ...)` → `scheduleRender('settings_boot', IMMEDIATE)`; `observeSettings` mirrors flips the same way under reason `'settings_change'`. Gate sentinel `if (snap.settings?.sidebarRebuild) { /* empty in 6.1 */ }` placed at top of `renderAll` as attachment point for SR-6.8–6.15. New options page (`options/options.html` + `options/options.js`) with two labeled toggles; `manifest.json` gains `options_page`; `build.mjs` bundles the options entry (5→6 entry points) and copies the HTML. Tests: 1314 extension tests pass including new `shared/__tests__/settings.test.js` (7 cases: defaults, read, observe flip on both keys, ignore unrelated/non-local changes, write persist, write unknown-key rejection, RenderCoordinator snapshot+renderKey integration). Legacy path unchanged when flag false (default).

- **SR-5 COMPLETE — 2026-04-13:** Architecture audit owner-approved. Artifacts: `docs/sidebar-rebuild/05-architecture-delta.md` (56 elements graded conformant/behavioral/blocking; 11 cross-cutting findings — dual state ownership [mostly resolved], 9 direct-DOM-mutation sites, 5 unregistered timers, 4 renderKey gaps, 3 short-circuited FSMs, C7 stale-tint age split, C8 full-replace-on-push, C9 R-7.3 revoked gate still active at `render-coordinator.js:429`/`:513`, C10 no prefers-reduced-motion, C11 render-tiers.js monolith); 27/33 doctrine rules have violations cited. **4 blocking deltas:** B1 S4/02-a/b between-hands stale Z2 content (pot chip + street progress); B2 equity in row 2 not headline; B3 3.11 multiway selector missing + 4.2/4.3 not separable; B4 R-7.3 1-street gap still live. **Owner decisions:** 0.7 footer placement = bottom of sidebar; E-2 Rule V ring = new inventory row 1.11; `sidebarRebuild` flag = single flag (not per-zone); SR-6 sequencing = by-dependency. **16 SR-6.x items authored** in BACKLOG: 9 foundation (6.1 flag, 6.2 corpus, 6.3 RT-60 sweep, 6.4 renderKey, 6.5 FSMs, 6.6 freshness, 6.7 R-7.3 revocation, 6.8 zone containers, 6.9 motion) → 6 per-zone (6.10 Z0 → 6.15 Zx) → 1 cleanup (6.16). Critical path for B1/B2 fix lands in SR-6.12.

- **SR-4 COMPLETE (6/6) — 2026-04-13:** Final batch `docs/sidebar-specs/zx-overrides.md` owner-approved. 11 specs (X.1 absorbing X.2, X.3, X.4 composite a/b/c, X.5, X.5b/c/d/e/f/g, X.6, X.7) + 10 Zx batch invariants. Declared: X.5 placement = Zx-overlay-inside-active-hand-stack (between plan-panel and street-card per HTML), NOT a Z0 extension; tournament data + tournamentDetailOpen cross-hand persistence as explicit R-2.4 exception (per-tournament drill-down vs Z4's per-hand drill-down); Observer mode (X.6/X.7) is legitimate `informational` takeover of Z2 slot because no `decision-critical` precondition exists when hero has folded; X.4 authored as one composite spec per handoff invariant 5; X.5d/e/f `decision-critical` tier scoped in-zone (priority within open detail panel) NOT cross-zone preempt; Z0 chrome persists through X.3 + X.4 (user needs pipeline diagnostics); X.1 between-hands FSM with strict `(appConnected===false) AND betweenHandsOrIdle` predicate (no mid-hand mounts even if disconnected). ~12 corpus gaps flagged for SR-6; one code gap noted (X.4c re-enable timer uses direct setTimeout instead of RT-60 registerTimer contract). No new R-11 escalations. The six `docs/sidebar-specs/*.md` files are now the authoritative spec surface for SR-6 implementation PRs. SR-5 unblocked.

- **SR-4 Z4 Deep Analysis batch — 2026-04-13:** `docs/sidebar-specs/z4-deep-analysis.md` owner-approved. 3 specs (4.1 PLAN chevron, 4.2 More Analysis, 4.3 Model Audit) + 7 Z4 batch invariants. Declared: coordinator-owned collapsible state for all three rows (extends RT-61 pattern to 4.2/4.3); RT-61 auto-expand contract scoped to 4.1 only (user-collapse-wins-within-hand, `hand:new` clears timer); Z4-scoped R-1.3 reflow exception (unmount-and-reflow within Z4 permitted when entire collapsible has no data, since Z4 is below-the-fold and absent from the Z1–Z3 glance pathway); `informational`-only tier lock (no promotion to `decision-critical`, ever — promotion path is "surface in Z2/Z3 instead"); stale-advice cross-zone inheritance from Z2 (single data source, single renderKey field, single 1 Hz timer, per `z2-decision.md` batch invariant 8); `settings.debugDiagnostics` flag contract — 4.3 absent from DOM when off (not hidden); Z4 slot-collapse-to-0-px permission when all three rows unmount. 4 corpus gaps flagged for SR-6 (RT-61 auto-expand before/after, no-plan path, 4.2 one-block path, flag-off absence + flag-on no-audit). No new R-11 escalations. Progress 5/6.
- **SR-4 Z3 Street Card batch — 2026-04-13:** `docs/sidebar-specs/z3-street-card.md` owner-approved. 11 specs (3.1 action history header, 3.2 action chips, 3.3 rationale, 3.4 fold%, 3.5 hand plan, 3.6 range grid, 3.7 headline, 3.8 legend, 3.11 multiway seat selector, 3.12 no-aggressor placeholder, 3.9 waiting) + batch invariants. 3.10 omitted (duplicate of 3.9). Single-slot carve-out: 3.6 frame owner, grid canvas is 3.6 XOR 3.12, 3.8 legend always rendered (grayed with 3.12), ~152px invariant height per R-1.3. Rule V Z3-half: HU-auto → last villain BET/RAISE (recency tiebreak) → PFA → 3.12 placeholder; pill-click override persists until next BET/RAISE or `hand:new`. 3.9 declared Z3-local (inside street card frame) per S4/02 corpus — not a Zx takeover. No new R-11 escalations. 3 corpus gaps tagged for SR-6: 3.6-villain-postflop, 3.11-multiway-selector, 3.12-no-aggressor. Progress 4/6.
- **SR-4 Z2 Decision batch — 2026-04-13:** `docs/sidebar-specs/z2-decision.md` owner-approved. 10 specs (2.1 action headline, 2.2 edge callout, 2.3 equity inline [sole equity owner], 2.4 SPR-only row, 2.5 board chips, 2.6 hero hole cards, 2.7 pot chip, 2.8 street progress strip, 2.9 "Analyzing…" placeholder, 2.10 stale-advice tint+badge absorbed from row 1.8) + 9 Z2 batch invariants. **E-1 executed:** inventory row 1.8 moved from §Z1 to §Z2 as row 2.10 (R-11 execution, not a verdict re-opening). **E-3 opened:** S4/02-a (pot chip stale between hands) + S4/02-b (street strip stale between hands) flagged for SR-6 root-cause — both share hypothesis that between-hands Zx override fails to blank Z2 live-context-derived elements; spec-correct behavior declared, no code patched. Declared: 2.9 vs 2.10 disambiguation (advice presence is disambiguator); 2.10 cross-zone contract with Z4 (single data source, single renderKey, single 1 Hz timer); color-channel non-collision (action/pot/stale/pipeline-dot all independent palettes). Progress 3/6.
- **SR-4 Z1 Table Read batch — 2026-04-13:** `docs/sidebar-specs/z1-table-read.md` owner-approved. 7 specs (1.1 seat circle, 1.3 hero star, 1.4 PFA, 1.5 bet chip, 1.7 dealer button, 1.9 villain pill row, 1.10 check indicator) + Z1 batch invariants. Escalations: **E-1** — inventory row 1.8 (stale-advice tint) reassigned from Z1 to Z2 under R-11 (RT-48 renders only on action bar + plan panel; no Z1 component exists); Z2 batch will spec it. **E-2** — Rule V seat-arc selection ring flagged as a new visual surface not yet in inventory; owner to decide whether to add as row 1.11 or fold into 1.1. Owner decisions: 1.5 declared sole owner of per-seat annotation slot (1.10 renders only when 1.5 blanks); 1.1 zero-hand ring uses `—` placeholder distinct from vacant-seat dashed outline; 1.9 pills stable-sorted by seat index (not action recency) per R-1.2. Progress 2/6.
- **SR-4 Z0 Chrome batch — 2026-04-13:** `docs/sidebar-specs/z0-chrome.md` owner-approved. 7 specs (0.1 pipeline dot + absorbed refresh, 0.2 hands-captured, 0.4 app-state, 0.6 tournament log, 0.7 diagnostics link, 0.8 invariant badge, 0.9 health strip) + Z0-wide batch invariants. Owner decisions: debug-flag key `settings.debugDiagnostics` approved as new setting (gates both 0.7 and 4.3); footer placement deferred to SR-5; 0.7 slot-collapse exception approved; diagnostics co-trigger (0.7/0.8 both open same panel, first-in-wins) approved; 3 corpus TODOs accepted to address in SR-6. Progress 1/6.
- **SR-4 scaffold — 2026-04-13:** created `docs/sidebar-specs/` with README (8-field spec template + standardized drill-down vocabulary + batching table).
- **SR-3 — 2026-04-12 (CLOSED):** Group 6 verdict recorded — owner chose **by-zone batching** for SR-4 (~6 sessions: Z0 → Z1 → Z2 → Z3 → Z4 → Zx). Inventory + doctrine v2 sealed. SR-4 now NEXT.
- **SR-3 (partial) — 2026-04-12:** Corpus extended S1→S13 (8 new builders + signatures + test files + screenshots — all deterministic, full suite 1,306 tests passing). Inventory grew 42→48 rows during capture (pipeline health strip, seat check indicators, recovery banner sub-components, tournament detail panel rows, observer scouting panel, OBSERVING badge). Groups 1–5 all resolved. Doctrine amended to v2 via R-11 process: R-1.2 reframed (targeted-glance replaces ≤5-metric cap), R-1.3 rationale reinforced, new R-1.5 glance pathways added. Group 6 (SR-4 batching) is the only remaining item before SR-3 closes.
- **SR-2 — 2026-04-12:** Design Principles Doctrine APPROVED (now v2). `docs/SIDEBAR_DESIGN_PRINCIPLES.md` finalized with v1 amendments (R-7.3 stale-recomputing, R-7.1 three-tier invariants) and v2 amendments (R-1.2 reframe, R-1.3 rationale, R-1.5 glance pathways).

---

## Recently Completed

- Phase C + D — 2026-04-12: 9 items closed plus orphan-panel cleanup. 6 verified-done from prior sprint work (RT-49/50/51/52/53/55). RT-48 stale advice indicator ships with yellow-orange border + "Stale Ns" badge on action bar + plan panel; auto-cleared when fresh advice arrives. RT-61 routes planPanel auto-expand through scheduleRender. RT-66 unblocks Rule 10 (was dead because snap.pipelineEvents isn't in the snapshot) and surfaces invariant violations via a red "!" badge on the status bar with 30s auto-decay.
- Phase B — 2026-04-12: 8 items closed (RT-43/44/45/47/54/58/59/60). RT-47 verified-done (audit stale). RT-43 completion root-caused the "wrong info for hand state" symptom to an incomplete renderKey fingerprint — tournament data, last-action amount, and advice sizing+villainSeat now drive re-renders. Dead-code ReferenceError traps deleted (RT-58). Dual liveContext write paths unified (RT-59). Advice guard gained hand-number binding (RT-45). Coordinator timer registration contract shipped with 4 sites migrated (RT-60).
- Phase A — 2026-04-12: 8 items closed across R6+R7+R8. RT-46/RT-56 verified-done (audit stale). RT-63 not-a-bug (verified with test). RT-57 (tournament XSS), RT-62 (Zone 3 scary card ranks, upstream + sidebar), RT-64 (multiway pot odds), RT-65 (capture-port validateMessage), RT-67 (canonical STREET_RANK) shipped.
- R5 sprint — 2026-04-07: all 8 roundtable findings resolved (RT-35 through RT-42)
- R4 sprint — 2026-04-07: all 8 findings resolved (RT-27+ through RT-34)

---

## Pending Review

**None pending.** All 25 R6/R7/R8 roundtable findings resolved (2026-04-12). Sidebar backlog fully cleared.

**R7 Roundtable (2026-04-11)** — 6 new findings focused on sidebar self-verification + root cause analysis:
- 2 P1: RT-51 (message-level integration test harness), RT-43 scope expanded (single-owner state store), RT-45 scope expanded (hand-number binding)
- 3 P2: RT-52 (tournament timer detached DOM), RT-53 (render stale indicator), RT-54 (renderKey missing community cards)
- 2 P3: RT-55 (dead panel render functions), RT-56 (_receivedAt NaN in stale timeout)

**R6 Roundtable (2026-04-09)** — 8 findings (still in REVIEW):
- 4 P1: RT-43, RT-44, RT-45, RT-46
- 2 P2: RT-47, RT-48, RT-49
- 1 P3: RT-50

---

## Alerts

- **UI Quality: RED** — Root cause identified (R7): dual state ownership between module vars and RenderCoordinator. Not fixable by adding sync calls — requires single-owner state store (RT-43 expanded). 14 total REVIEW findings (R6+R7).
- **Test Health: YELLOW** — Sidebar temporal harness tests render layer only, bypasses message handler pipeline where real bugs live. Message-level integration harness needed (RT-51).
- **Security: GREEN** — All Phase A security items closed. RT-46 verified-done, RT-57 shipped (tournament innerHTML escaped), RT-65 shipped (capture-port validateMessage gate).
- **Product Correctness: GREEN** — RT-62 shipped card-rank display in Zone 3. RT-63 closed as not-a-bug with test evidence. RT-64 shipped multiway pot-odds fix.
- **Post-RT-43 Regression: GREEN** — All Phase B items closed. Dead-fn bare-var closures deleted (RT-58). Timer registration contract ships with 4 sites migrated (RT-60) — orphan-fire after table switch now impossible. renderKey captures tournament/advice/action content (RT-43/44/54).

---

## Project Health

- **Tests:** 5,422 passing across 185 test files (+ 1,618 extension tests)
- **Architecture:** v122 → SYSTEM_MODEL v1.7.0 — React + Vite + Tailwind, mobile-optimized 1600x720
- **Programs:** Security YELLOW, Engine Accuracy GREEN, UI Quality RED, Test Health YELLOW
- **Active backlog:** 17 NEXT (R6: 7, R7: 5, R8: 5), 0 REVIEW, 1 PAUSED, 8 closed in Phase A
- **Open failure modes:** 0 active (5 archived)
- **Last eng-engine audit:** 2026-04-12 R8 (all 25 findings resolved same-day across Phases A, B, C, D)
