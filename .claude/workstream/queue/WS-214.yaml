id: "WS-214"
title: "Seam tests: rangeEngine → exploitEngine (real-profile fixtures) + fix bucketQueryUtils unit fixtures"
type: improvement
status: backlog
claimed_by: ""
claimed_at: null
completed_at: null
priority_score: 12.0
priority_label: "P2"
category: "seam-integrity"
capability: "core"
program: "domain-correctness"
description: |
  2026-06-10 cross-stream seam audit (post commit 299a5f8): the entire
  rangeEngine → exploitEngine boundary is covered only by hand-authored
  fixtures — no test feeds real buildRangeProfile / getRangeWidthSummary /
  detectTraits output into rangeRules / subActionRules / pipRules /
  generateExploits (traitShapeSeam.test.js now covers the traits slice only).
  Author a seam-test family on the anchorMatchDrift pattern: build profiles
  from realistic hand fixtures, pipe through the real summary functions, feed
  rule modules, assert firing behavior. Include: correct
  bucketQueryUtils.test.js fixtures from 0-1 fractions to the real 0-100
  percentage contract (decisionAccumulator emits 0-100; current fixtures
  document a false contract that invites 100x-off consumer code), and a seam
  test driving real accumulateDecisions output through aggregateSegmentation
  + deriveMultipliersFromBuckets asserting the /100 path.
accept_criteria:
  - "At least one test imports BOTH a rangeEngine producer and an exploitEngine rule consumer and asserts firing behavior on real producer output"
  - "bucketQueryUtils.test.js fixtures use 0-100 units with a contract-pinning assertion"
  - "decisionAccumulator → villainDecisionModel /100 conversion exercised with real producer output"
effort: "M"
files_involved:
  - src/utils/rangeEngine/index.js
  - src/utils/exploitEngine/rangeRules.js
  - src/utils/exploitEngine/__tests__/bucketQueryUtils.test.js
  - src/utils/exploitEngine/decisionAccumulator.js
blocked_by: []
enables: ["WS-215"]
sprint_id: ""
decision_flags: []
source:
  type: manual
  created_by: user
  origin: "seam-audit-2026-06-10"
created_at: "2026-06-10"
