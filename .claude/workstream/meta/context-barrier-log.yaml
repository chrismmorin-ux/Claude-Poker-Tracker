# context-barrier audit trail.
#
# Every record carries `source:` -- "session" for real traffic, "test" for a harness.
# Scalars are JSON-encoded, which is also valid YAML 1.2, so this file parses.
# Prior history: context-barrier-log.quarantined-2026-08-20.yaml (~97% test residue,
# unparseable since event #3 of 1301). Do not merge the two.
events:
