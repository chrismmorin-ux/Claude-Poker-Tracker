# System Summary — compressed orientation for Standard Mode sessions
# Auto-regenerated by /session-end and /status
# Standard Mode reads THIS instead of scanning all system files individually

last_updated: "2026-07-27T15:50:00Z"
vital_signs_ok: true
red_vitals: []                    # Names of failing vital signs
# Counts are post-GC (2026-07-27: 22 queue items + 9 findings + 101 sprints
# archived, indexes rebuilt) — the live index dropped 225 -> 122 entries
# because archived items left the index, NOT because work was dropped.
queue_counts:
  backlog: 21
  in_progress: 3
  blocked: 1
  done_30d: 44
top_3_unclaimed:
  - { id: "WS-262", title: "Mass population data acquisition — source external hand data", score: 24, category: "domain-correctness" }
  - { id: "WS-036", title: "H-SC01 test", score: 22, category: "launch" }
  - { id: "WS-191", title: "Seat autoselector overhaul Phase 2 — root-cause each capture", score: 20, category: "domain-correctness" }
stale_programs:                   # Overdue protocol runs — these BLOCK new sprint composition
  - domain-correctness            # sweep overdue since 2026-07-22
  - launch                        # sweep overdue since 2026-07-22
active_context_count: 0           # Number of active items in context.md
critical_findings: 0              # Open findings with severity: critical (12 open total, none critical)
convergence_trend: "unknown"      # embedding | stable | declining | unused

# Session-end note (2026-07-27): the most recent session was UNQUEUED — a
# founder-reported production crash (E401 stale chunk) fixed across PR #3 and
# PR #4, both merged and deployed. No queue item was claimed or closed, so the
# counts above are unchanged by that work. See
# .claude/workstream/sessions/session-2026-07-26-pwa-stale-chunk-recovery.yaml
