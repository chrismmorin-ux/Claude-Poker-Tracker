# System Summary — compressed orientation for Standard Mode sessions
# Auto-regenerated by /session-end and /status
# Standard Mode reads THIS instead of scanning all system files individually

last_updated: "2026-08-04T04:55:00Z"
generated_by: "ses-20260804-0126-05a150c1"

vital_signs_ok: true
red_vitals: []
vitals_note: >
  Session-end verification 2026-08-04: 10,173 unit (352 files) + 3,241 component/hooks
  (207 files) = 13,414 passed / 0 failed / 20 skipped. Build clean, 24.9s. DO NOT diff
  13,414 against the 13,239 recorded 2026-07-31 — different denominators. That figure
  came from smart-test-runner.sh; this run covered 3 of the 4 vitest projects and
  slow-unit was NOT run (4 drillContent equity files, 5-15 min each). Session added 23
  tests (17 dash-span + 6 sizing). CAVEAT unchanged: two Monte-Carlo tests
  (miniRolloutEquity; one in advisorAccuracy) fail ~25% of runs independent of any code
  change — both green here. Unticketed, and worth fixing: a gate that fails randomly is
  not a gate.

queue_counts:
  backlog: 48
  in_progress: 3
  blocked: 10
  done: 81
  completed: 7
  deferred: 44
  total: 196

top_3_unclaimed:
  - { id: "WS-312", score: 29.0, category: domain-correctness, title: "A flush draw is reported as a 'thin value bet' — the classifier reads a draw's raw equity as made-hand value" }
  - { id: "WS-310", score: 29.0, category: domain-correctness, title: "Board convergence as an anchor — the terminal-hand-strength window, and what players do as it closes" }
  - { id: "WS-314", score: 28.0, category: domain-correctness, title: "Check-raise ranks first for EVERY weak hand — including 5-high with 12% equity" }

# Not the top-ranked item, but what the previous session recommended doing first.
next_recommended:
  id: "GUT Range P0 (unticketed — file before starting)"
  reason: >
    Hand-vs-range preflop equity, DECOMPOSED. preflopEquity.js:508 throws
    NotImplementedError on the range branch, and per .claude/projects/gut-range.md
    section 11 this blocks everything downstream: the ranking function, the domination
    cost, and the fixed point all need it. Decomposed (hitRate / conditionalWin per
    made-hand bucket) rather than a mean — the mean is exactly what hides KJo being
    crushed by AJ/KQ/AK while looking fine on average.
  do_first: >
    Read .claude/projects/gut-range.md (chartered 2026-07-29, never started) and the
    approved plan at /root/.claude/plans/reactive-singing-marble.md. v1 scope is
    RFI-only per DEC-030. CHEAPER ALTERNATIVE if a smaller bite is wanted: mine
    fourBet / foldTo4Bet from the HandHQ corpus via the existing
    scripts/generate-handhq-reference.mjs — the six mined stats have no 4-bet
    frequency, so WS-270's P1 is consequence-argued but not frequency-derived, and
    that one number is what the whole v2 pinned-ladder rests on.

programs_installed: 14
stale_programs:
  - id: "domain-correctness"
    note: "challenge protocol YAML-vs-event-log divergence ~32d (WS-268 reconciles). Caps program health at 2/9."
new_programs_this_session: []

active_context_count: 0
critical_findings: 0
open_findings_note: >
  prog-program-integrity flags FIND-039 (medium) as a promotion-gap — no corresponding WS
  item; promote or close. Also flags design at 19/8 work items (2.38× cap, floor 22).
  GC 2026-08-04 surfaced 4 graduation candidates for /audit review: theory-spec drift (3),
  first-principles decision modeling (6), range estimation correctness (3), game tree EV
  correctness (3).

convergence_trend: "unknown"

# --- Things a next session would otherwise rediscover the hard way -------------
standing_cautions:
  - "WORK IS ON A BRANCH, not main: claude/3bet-range-co-button-hda1mw (4 commits; d4339a5 P-1a, 1be6d9d P-1b). No PR opened."
  - "archetypeRanges.js is DRILL CONTENT, not the engine. Its own header says it is 'not consumed by the exploit/range engines' — it is imported only by PostflopDrillsView. Quoting it as engine output is how this session started, and it was wrong. The live preflop engine (preflopAdvisor) has no concept of a range at all; it prices sizings for the two cards hero holds."
  - "spr is NEVER passed on the preflop path (zero occurrences in preflopAdvisor.js), so every preflop EV silently assumes SPR_ZONES.HIGH. And getSPRZone buckets everything >=13 as DEEP, so 100bb and 200bb currently produce byte-identical ranges."
  - "Preflop blockers cancel by construction — foldThroughPerCombo calibrates against a hero-dead deck, so AKs and 32o give the same fold-through (measured delta 1.8e-4 at 3 seats). The engine cannot distinguish A5s from 65s as a bluff."
  - "DEC-034 is NOT yet written into gut-range.md and must be before any v2 pinned-subgame ladder is built. A 0%/100% node lock is the same mathematics as WS-302's zeroed cell — 'a starting belief of zero is not a prior, it is a permanent verdict.' Confidence must propagate as the MINIMUM along the path; the existing consequence gate is local."
  - "Range-advantage framework thresholds were calibrated while ranges were truncated by the dash bug (fixed in d4339a5). They may be tuned to compensate for a defect that no longer exists — review before scoring generated ranges against anything."
  - "CLAUDE.md doc drift: says '8 reducers ... IndexedDB v13'; actual is 14 reducers (12 wired into useAppState) and DB_VERSION = 27 with 25 stores. PERSISTENCE_OVERVIEW.md is ~4 months past its 60-day staleness threshold and still lists 7 stores."
  - "out/hero-ev-KILLED-3players-inadmissible.json reports c3Passes:true and is NOT admissible evidence. Its README sibling explains why. Never append it to scorecard-history.yaml."
  - "Any hero-EV figure below ~30 contributing players is noise regardless of how tight its CI looks — the interval is a cluster bootstrap over players. Measured 2026-07-31: +16.7 bb at k=3 collapsed to +1.37 at k=28."
  - "withCarriedContext / CARRIED_AXES in pokerCore/situationKey.js have NO production caller. Scaffolding with a proven contract; wiring one is FSA Phase 2's first task."
  - "docs/claims/ is still templates and April drafts. Provenance is promoted; the CLAIMS half — what a number is NOT — has no home yet."
