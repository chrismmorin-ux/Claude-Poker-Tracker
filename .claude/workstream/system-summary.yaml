# System Summary — compressed orientation for Standard Mode sessions
# Auto-regenerated by /session-end and /status
# Standard Mode reads THIS instead of scanning all system files individually

last_updated: "2026-08-06T05:15:00Z"
generated_by: "ses-20260805-1521-fc271296"

vital_signs_ok: true
red_vitals: []
vitals_note: >
  Session-end verification 2026-08-06: engine suites 3,654 passed / 0 failed / 5 skipped
  (107 files — exploitEngine 2,949 + rangeEngine + pokerCore, 376s); build clean, PWA 63
  entries. Full-suite baseline remains 2026-08-04 at 14,113/0.
  computeDepth2Plan.test.js times out 5-9 of 19 at a 5000ms limit against a 2000ms
  refinement budget ON AN IDLE MACHINE — that is WS-379, still open and unclaimed, and
  contention is NOT required to reproduce it. The two known-flaky Monte-Carlo tests
  (miniRolloutEquity; one in advisorAccuracy) remain unticketed: a gate that fails
  randomly is not a gate. cwos-verify's 17 FAILs are pre-existing and 11 are structurally
  inapplicable to an adopter repo (friction fr-021) — diff the FAIL list, do not read
  "verify is red" as a regression signal here.

# Post-GC 2026-08-06: archived items are not indexed, so `done` counts only live items.
# Backlog jumped 65 -> 125 because this session filed WS-386..425 (40 items) on top of
# the roundtable's findings — correctness debt made visible, not scope creep.
queue_counts:
  backlog: 125
  in_progress: 3
  blocked: 10
  done: 45
  completed: 7
  deferred: 44
  decomposed: 1
  dismissed: 1
  review: 1
  total: 237

top_3_unclaimed:
  - { id: "WS-402", score: 26.0, category: domain-correctness, title: "The engine raises 91.9% facing aggression where the pool raises 12.5% — and it collapses ESS to 15%" }
  - { id: "WS-389", score: 26.0, category: domain-correctness, title: "Refinement depth is wall-clock gated, so a scored run is not reproducible" }
  - { id: "WS-399", score: 26.0, category: domain-correctness, title: "The learned range channel is algebraically inert — one scalar per (position, action), divided out at the point of use" }

next_recommended:
  id: "context-shift-implementation"
  reason: >
    NOT a queue item, and deliberately so. The context rebuild was designed this session
    and handed to a FRESH one, because this session carried the maximum accumulated drag
    the shift exists to remove. A three-arm comparison with criteria and predictions fixed
    before any arm existed returned greenfield 33.5 vs 24/24 — and BOTH predictions
    failed, so by the pre-registered rule the context-drag hypothesis is NOT supported for
    design tasks in the form stated. The refined finding, labelled post-hoc and weaker for
    it: context-ladenness substituted for LOOKING. The max-drag arm reinvented a schema
    already on disk and running; the greenfield arm read the code.
  do_first: >
    Read .claude/projects/context-shift-implementation.md, then the judge's synthesis at
    docs/context-system-comparison.md. Do NOT import the prior session's narrative — the
    handoff states facts with their evidence, and that is all that should cross. Then
    engine work in order: WS-402 (over-raising is the binding constraint on every
    measurement — still 82.3% vs pool 15.2% at ESS ~30% AFTER this session's fix),
    WS-403 (scheduler starves the flop at every budget), WS-404 (per-stage ablation flags;
    no per-capability EV number is possible without it), WS-411 (wall clock). THEN the
    baseline EV run — docs/runbooks/baseline-ev-run.md, ~4 hours. It has never completed
    at >=30 clusters, and that is the only thing standing between the founder and the
    aggregate figure he asked for.

programs_installed: 15
stale_programs: []
stale_note: >
  domain-correctness blind_spot RAN 2026-08-06 as declared (6 lenses + cross-critic +
  facilitator, ~1.66M subagent tokens) and the sprint gate is clear. But note its own
  protocol question 3 — "static for 6+ months" — has NEVER been able to fire: the repo's
  first commit is 2025-12-01 and the oldest src/utils file was touched 2026-03-06, so it
  has returned an empty set on every run by construction rather than by health.
  methodology-integrity's max_open_items was raised 3 -> 20 this session; the cap was
  written before the program had run and was suppressing WS-405, a P1 the founder had
  specifically asked for.

active_context_count: 0
critical_findings: 2
open_findings_note: >
  FIND-043 (the live decision rule is three literals violating POKER_THEORY 3.4.3 and 6.3)
  and FIND-044 (the engine is not unconnected — it is fully wired to the venue the founder
  does not play) are the two CRITICALs. FIND-040..054 registered this session; integrity 0
  violations after closing 12 orphan finding refs and recovering 3 findings from the
  2026-07-30 delta run that were never filed. A problem_class validator now warns on
  invented vocabulary (cwos-reconcile.js) — but it lives in a kit-managed file and will be
  reverted by the next /kit-upgrade unless upstreamed (WS-413).

convergence_trend: "unknown"

# --- Things a next session would otherwise rediscover the hard way -------------
standing_cautions:
  - "out/hero-ev-KILLED-3players-inadmissible.json reports c3Passes:true and is NOT admissible evidence. Its README sibling explains why. Never append it to scorecard-history.yaml."
  - "Any hero-EV figure below ~30 contributing players is noise regardless of how tight its CI looks — the interval is a cluster bootstrap over players. Measured: +16.7 bb at k=3 collapsed to +1.37 at k=28."
  - "withCarriedContext / CARRIED_AXES in pokerCore/situationKey.js have NO production caller. Scaffolding with a proven contract; wiring one is FSA Phase 2's first task."
  - "docs/claims/ is still templates and April drafts. Provenance is promoted; the CLAIMS half — what a number is NOT — has no home yet."
  - "INV-VIEW-SCROLL (src/test/viewScrollContainers.test.js) gates every routable view's scroll shell. If you add a view and it fails, wrap it in <FluidView> or give the ScaledContainer canvas an overflow-y-auto region — do NOT add it to EXEMPT. EXEMPT is for surfaces where clipping is genuinely correct, and every entry needs a written reason."
  - "cwos-verify has a standing wall of 17 FAILs, 11 structurally inapplicable to an adopter repo (friction fr-021). Diff the FAIL list; do not read red as regression."
  - "The 2026-08-06 roundtable's convergence was PARTLY INHERITED — the headline was already recorded at file:line resolution in memory/project_allin_side_pots.md:19, loaded at every session start. The tell was one lens reproducing the conclusion's structure with its mechanism WRONG. Before treating cross-lens agreement as evidence, check whether the answer was already sitting in context."
  - "RC-depth-ablation's deltaBB -0.4711 is RETIRED. ~90% of it is riverPerCombo on a street where depth-2 never runs (all 45 river decisions report depthReached=1), on a build predating the omniscience fix. The non-river delta is +0.4231."
  - "Settled 2026-08-06, do not re-derive: depth-1 does NOT overfold (it under-folds by ~49 points); depth-1 does NOT lack check-raise (candidate sets are identical in both arms — WS-314 is already fixed at HEAD, close it); the represented range is NOT the dominant residual (its 27-48 point swing was measured against an un-narrowed uniform grid, i.e. the null — so it is sensitivity, not error); and the continue-probability truncation was NOT compensating for it (crossed probe returns identical deltas under both range shapes)."
  - "The 20 bb/hr scale test is a REAL figure (Marc Goone, $5/$5 Hustler; persuadeo 18.8 bb/hr at $1/$3) and still unfit as a post — total-realised vs incremental-modelled, two stake tiers up. Rake-adjusted onto a Vegas $1/$3 fee structure it is 12.3-14.6, which an independent publication-bias correction brackets at 10-16. Prefer the DROP LINE: 20-29 bb/100 in rake+jackpot+tips, pace-independent, venue-published, and the only quantity in that research the founder can measure himself in one session."
  - "No published live standard deviation exists anywhere. Every circulating live SD traces to one author's tutorial worked examples replicated across five sites. Without one, nobody can compute a confidence interval on a live winrate — including us."
  - "This program has verified CORRECTNESS and never REACHABILITY, and its own instrument is broken: nine of thirteen paths in its scope patterns match ZERO files, and src/hooks/** and src/components/** are outside scope entirely (FIND-046). Do not read a clean delta run as coverage."
  - "UNVERIFIED but load-bearing if true: git config core.hooksPath reportedly still points at the abandoned pre-relocation tree, which would mean .git/hooks is never consulted and six shipped pre-commit hooks have been silently dead since the repo moved. Claude Code's own PreToolUse gates DO fire (two blocked work on 2026-08-06), so this would be git hooks only. Check before trusting any pre-commit guarantee."
  - "Every spawned SUBAGENT registers a session and overwrites .claude/workstream/.current-session (friction fr-022). 52 session files exist; at most 2 were ever concurrently active. Locate the real session by grepping status:active, not by trusting the pointer."
