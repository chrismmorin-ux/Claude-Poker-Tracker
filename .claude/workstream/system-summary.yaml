# System Summary — compressed orientation for Standard Mode sessions
# Auto-regenerated by /session-end and /status
# Standard Mode reads THIS instead of scanning all system files individually

last_updated: "2026-08-07T04:35:00Z"
generated_by: "ses-20260806-2347-91644756"

vital_signs_ok: true
red_vitals: []
vitals_note: >
  Session-end verification 2026-08-07: 13,581 passed / 0 failed / 20 skipped, clean build
  (68 precache entries). +342 tests over the 2026-07-31 baseline of 13,239, all from this
  session's two features (bankroll capture + variance; session logging + all-in EV).
  CAVEAT — the known-flaky Monte-Carlo class now has THREE members: miniRolloutEquity, one
  in advisorAccuracy, and gameTreeEvaluator "high fold model increases bet EV vs no model"
  (measured this session: fails ~1 in 3 runs in isolation, on code no change touched). All
  three landed green on the final run. A single red on any of them is not evidence of a
  regression; re-run before investigating. Still unticketed, still worth fixing.

queue_counts:
  backlog: 47
  in_progress: 3
  blocked: 10
  done: 60
  completed: 7
  deferred: 44
  review: 1
  dismissed: 1
  decomposed: 1
  total: 174
# done-count differs from the 2026-07-31 snapshot because GC archived completed
# artifacts this session (5 sprints); archived items leave the live index.

top_3_unclaimed:
  - { id: "WS-312", score: 29.0, category: domain-correctness, title: "A flush draw is reported as a 'thin value bet' — the classifier reads a draw's raw equity as made-hand value" }
  - { id: "WS-310", score: 29.0, category: domain-correctness, title: "Board convergence as an anchor — the terminal-hand-strength window, and what players do as it closes" }
  - { id: "WS-314", score: 28.0, category: domain-correctness, title: "Check-raise ranks first for EVERY weak hand — including 5-high with 12% equity" }

# Not the top-ranked item, but what the previous session recommended doing first.
# NOTE 2026-08-07: the session below was founder-directed and did NOT touch WS-320 or
# any queue item. The recommendation stands unconsumed. The competing recommendation
# from 2026-08-07 is in `founder_direction` at the bottom of this file.
next_recommended:
  id: "WS-320"
  reason: >
    Study-ladder separability test. A MEASUREMENT that could redirect several builds — if
    the ladder separates it is the first archetype axis with earned justification, and it
    changes whether FSA treats the pool as one Field surface or several strata. Cheap
    because §11.8's method already exists and only the axes change.
  do_first: >
    Read out/hero-ev-44players-PARTIAL-650dec.json. The run was killed a third time at
    650/1200, so out/hero-ev-scaled.json does NOT exist. The partial reached 44
    contributing players — past the 30 bar — and reads edge +3.69 with CI low -5.99, i.e.
    armsWouldPass FALSE on its own arithmetic, not merely blocked on admissibility. Still
    inadmissible (INCOMPLETE_RUN); do not record it. See the session handoff for the full
    k=3/28/31/44 trajectory and the command to finish the run (~55 min).

programs_installed: 14
stale_programs:
  - id: "domain-correctness"
    note: "challenge protocol YAML-vs-event-log divergence ~32d (WS-268 reconciles). Caps program health at 2/9."
new_programs_this_session:
  - id: "data-provenance"
    tier: active
    note: "Installed 2026-07-31. Registry + chain map PROMOTED, so drift detection is live for the first time."

active_context_count: 0
critical_findings: 0
open_findings_note: >
  prog-program-integrity flags FIND-039 (medium) as a promotion-gap — no corresponding WS
  item; promote or close. Also flags design at 19/8 work items (2.38× cap, floor 22).

convergence_trend: "unknown"

# --- Things a next session would otherwise rediscover the hard way -------------
standing_cautions:
  - "out/hero-ev-KILLED-3players-inadmissible.json reports c3Passes:true and is NOT admissible evidence. Its README sibling explains why. Never append it to scorecard-history.yaml."
  - "Any hero-EV figure below ~30 contributing players is noise regardless of how tight its CI looks — the interval is a cluster bootstrap over players. Measured this session: +16.7 bb at k=3 collapsed to +1.37 at k=28."
  - "withCarriedContext / CARRIED_AXES in pokerCore/situationKey.js have NO production caller. Scaffolding with a proven contract; wiring one is FSA Phase 2's first task."
  - "docs/claims/ is still templates and April drafts. Provenance is promoted; the CLAIMS half — what a number is NOT — has no home yet."

# --- Founder direction, 2026-08-07 (supersedes queue ranking if he returns to it) ---
founder_direction:
  summary: >
    Founder is moving his bankroll record off a Google Sheet and into the app. Two features
    shipped on branch claude/bankroll-session-variance-mct79d (commits f012db1, 86d0d08),
    not merged, no PR: (1) 78-row sheet history imported + Bankroll & Variance band;
    (2) log/edit any session + all-in EV adjusted win rate.
  headline_number: >
    52 cash sessions, +$16.27/hr, 95% CI -$29…+$62 all-time — the sample does NOT establish
    a winning rate, and the UI says so. June-forward is a different regime: 26 sessions,
    +$75.56/hr, 70% CI +$32…+$119, 95% CI -$6.52…+$157.65. Nearly significant, not
    significant. ~20 more logged sessions decide it.
  do_first: >
    Nothing engineering-blocking. The highest-value next action is founder behaviour —
    log sessions through the new form. If he wants code first, the SessionsView component
    test gap (below) is the one with teeth.
  unfiled_followups:
    - severity: high
      note: >
        No SessionsView component test exists. That gap is why an icon import that does not
        exist in this lucide version (NotebookPen) reached the browser and would have blanked
        the entire view — caught only by Playwright. Every child component is tested; the view
        that composes them is not.
    - severity: medium
      note: "Shared modal-stack primitive — two `fixed inset-0 z-50` overlays cannot coexist; second time it has bitten."
    - severity: medium
      note: "Repeatable CSV import. Founder deferred it explicitly; current path is a one-time seed, so sheet rows after 5 Aug 2026 need the seed file regrown by hand."
    - severity: low
      note: "Multiway all-in equity across side pots — declined by design, not approximated."
    - severity: low
      note: "bb/100 normalisation — blocked on hand counts the imported history does not carry."

standing_cautions_added_2026_08_07:
  - "POKER_THEORY §14.3/§14.4 is load-bearing on everything in sessionStats/ and handStats/. The cluster unit is the SESSION, never the hand. Every estimator returns `clusterUnit` and `n` so a caller cannot obtain a figure without its sample size. Projected figures render with a visible `modelled` tag. Do not relax any of this for convenience."
  - "The all-in adjusted rate shows ZERO COVERAGE today and that is correct, not broken — all 78 imported sessions have handCount: 0. It must keep saying so rather than printing a number equal to the raw rate, which would read as confirmation of it."
  - "Manual 'I lost that to variance' tagging was proposed by the founder and REFUSED as an input to the win rate (DEC-030). Tags for analysis are fine; tags feeding the number are not. Do not reintroduce."
  - "`updateSessionField` only dispatches — persistence runs through a debounced auto-save in useSessionPersistence with a HARDCODED field list. Any new editable session field must be added to that list or the edit will show in the UI and vanish on reload."
  - "`parseAndEncode` (pokerCore/cardParser) returns -1, not NaN, for an unparseable card. Validate on range, never with Number.isInteger."
  - "Suits are the unicode glyphs ♠♥♦♣ from gameConstants.SUITS, not letters. A fixture using 'Ah' parses to nothing and fails confusingly."
