# System Summary — compressed orientation for Standard Mode sessions.
#
# Regenerated by cwos-reconcile.js, which runs from the SessionStart hook.
# Until 2026-08-03 the only writer was /session-end Step 5.6 — a command that
# runs ~12% of the time — so this file sat 10 days stale while the preamble
# told every Standard-Mode session to read it FIRST. See ADR-065.
#
# vital_signs_ok / red_vitals are NOT computed here: reconcile does not run
# the invariant suite. They are carried forward from the last writer that did,
# with vitals_observed_at naming when that was. Check that date before
# trusting them, and run /verify if it is old.
last_updated: "2026-08-20T21:15:11Z"
generated_by: cwos-reconcile
vital_signs_ok: true
red_vitals: []
vitals_observed_at: "2026-08-06T05:15:00Z"
verify_last_completed_at: null
reconcile_integrity_ok: true
queue_counts:
  decomposed: 1
  done: 77
  in_progress: 4
  deferred: 46
  backlog: 255
  dismissed: 5
  completed: 7
  review: 1
  blocked: 10
  active: 2
  claimed: 1
top_3_unclaimed:
  - id: WS-575
    title: "Completing depth-2 pushes hero to raise 98% of the time — the truncation was MASKING an aggression…"
    score: 95
    category: domain-correctness

  - id: WS-548
    title: Exploit card prices a 2bb open with a fold rate measured against 3-4bb raises - condition every sea…
    score: 94
    category: domain-correctness

  - id: WS-596
    title: "The estimator's bias is a function of ESS, not of the policy — r = -0.95 across 8 arms"
    score: 94
    category: methodology-integrity

stale_programs:
  - id: anti-hallucination
    tier: watch
    overdue_protocols: [delta (never run)]

  - id: change-management
    tier: active
    overdue_protocols: [sweep (never run), delta (never run), challenge (never run)]

  - id: data-provenance
    tier: active
    overdue_protocols: [delta (6d overdue), challenge (never run)]

  - id: design
    tier: active
    overdue_protocols: [delta (never run), challenge (never run)]

  - id: domain-correctness
    tier: critical
    overdue_protocols: [theory_completeness (never run), delta (2d overdue)]

  - id: engineering
    tier: watch
    overdue_protocols: [delta (never run)]

  - id: guide-authority
    tier: active
    overdue_protocols: [sweep (never run), delta (never run), challenge (never run)]

  - id: launch
    tier: active
    overdue_protocols: [delta (never run), challenge (never run)]

  - id: methodology-integrity
    tier: active
    overdue_protocols: [delta (never run), challenge (never run)]

  - id: self-compliance
    tier: critical
    overdue_protocols: [delta (6d overdue), challenge (never run), blind_spot (never run)]

  - id: strategy-of-record
    tier: active
    overdue_protocols: [delta (never run), challenge (never run)]

active_context_count: 1
critical_findings: 9