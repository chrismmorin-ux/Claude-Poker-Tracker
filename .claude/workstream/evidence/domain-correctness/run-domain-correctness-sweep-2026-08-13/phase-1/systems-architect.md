# Phase 1 — systems-architect (fresh-context dispatch)
Run: run-domain-correctness-sweep-2026-08-13 | Window: 5aa1419..HEAD | Captured verbatim from agent final output.

### SYSTEMS ARCHITECT — Domain-Correctness Drift Sweep, `5aa1419..HEAD`

#### Key Concerns (top 5)

1. **`standardOfRecord/` (49 files, ~10.7% of the window's diff) is entirely absent from `SYSTEM_MODEL.md` §1's component table and §1.2 dependency graph.** It is a real cross-cutting module — `src/utils/standardOfRecord/layerProbes.js:55` imports `brierScore`/`logLoss` from `../exploitEngine/calibrationMetrics`, creating a documented-nowhere coupling edge from a new top-level utils directory into `exploitEngine/`. SYSTEM_MODEL.md's own frontmatter (`last-verified-against-code: 2026-05-10`, `staleness-threshold-days: 30`) is 95 days stale as of 2026-08-13 despite 6 commits touching the file in this window (`git log 5aa1419..HEAD -- .claude/context/SYSTEM_MODEL.md`) — those edits added prose to §2.3/§2.4 but never advanced the verification date or added the new module to §1. This is the exact failure CLAUDE.md's "Update the System Model after any architectural shift" rule exists to prevent.

2. **The three declared complexity hotspots in SYSTEM_MODEL.md §5.2 (rated "Low priority — working, well-tested") grew 38–82% in this window and the risk rating was never revisited:**
   - `src/utils/exploitEngine/gameTreeEvaluator.js`: 1182 → 1922 lines (+63%)
   - `src/utils/exploitEngine/gameTreeDepth2.js`: 1247 → 1721 lines (+38%)
   - `src/utils/exploitEngine/foldEquityCalculator.js`: 439 → 799 lines (+82%)
   - `src/utils/exploitEngine/villainDecisionModel.js`: 521 → 801 lines (+54%)
   `gameTreeEvaluator.js` also underwent a structural rewrite mid-window (WS-334, two-phase mandatory/refinement clocks) — a change to the file's fundamental execution model, not incremental growth. The "well-tested, low priority" label in the architecture doc predates that rewrite and was never re-evaluated against it.

3. **Fan-in on shared constant/config modules is climbing toward the god-module threshold and none of it is tracked in §6.1 Hidden Coupling:** `populationPriors.js` (22 importers), `gameTreeConstants.js` (16), `villainModelData.js` (13). All three were also the most heavily rewritten files in the window (`populationPriors.js` alone: +690/−lines in the diff stat). A shape change to any one has a 13–22-file blast radius with no fan-in ceiling documented or enforced.

4. **`modelAudit.js:114` and `:278`** (`src/utils/exploitEngine/modelAudit.js`) still call `classifyStyle(pct)` and pass `style` into `buildVillainDecisionModel(...)`, even though WS-436 (POKER_THEORY.md v2.4, `exploitEngine/CLAUDE.md:120-127`) states "the engine's playerStats struct cannot even represent a style field" and `villainDecisionModel.js` confirmed to have zero references to `.style` anywhere in its source. The code is honestly self-documenting about this ("style is carried for parity but has no model channel," line 111; "style, which rides for display parity," line 291) — so it is not a live decision-path violation — but it is a dead parameter threaded through a function signature specifically to preserve shape-parity with a production call site, which is the inverse of the shipped-but-inert pattern the founder's own memory (`feedback_shipped_but_inert_capability.md`) flags as a recurring failure class in this repo. Worth a structural fix (drop the field, or assert the model explicitly ignores it) rather than a comment carrying the invariant.

5. **Two files (`gameTreeEvaluator.js`, `gameTreeDepth2.js`) now exceed 1700–1900 lines each** and are both on the hot decision path (§2.3 game tree evaluation flow). No test-file split or extraction accompanied the growth — `gameTreeDepth2.js` picked up an 822-line diff and `gameTreeEvaluator.js` a 1330-line diff in a single window without a corresponding architectural decision-log entry proposing decomposition (contrast with the 2026-05-14 `useLiveActionAdvisor` split precedent, TD-17, which *did* get an extraction when it crossed 500 LOC).

#### Hidden Risks

- **The System Model's staleness is itself unmeasured.** The doc declares its own 30-day staleness threshold and is silently past it 3x over, while having been edited 6 times in the window — meaning the edits happened but the currency-tracking mechanism (the `last-verified-against-code` field) is not gated by anything that would catch a partial update. Anyone reading SYSTEM_MODEL.md today for architectural context is reading a doc that both claims and fails to demonstrate freshness.
- **`preflopFoldQuantities.js` (new, WS-371) and `preflopFoldResolver.js` (new, WS-274)** sit adjacent, both ~600-700 lines, both about preflop fold rates — read together they are cleanly delineated (one names two easily-confused stats, the other resolves a fidelity hierarchy for one of them) and the fix was verified threaded into both consumers (`weaknessDetector.js`, `villainObservations.js` both now read `FOLD_VS_PREFLOP_RAISE` correctly per grep). Flagging only because two similarly-named, similarly-sized new modules in the same directory is exactly the shape that produces confusion for a solo dev six months out if the docblocks that currently disambiguate them ever drift.
- **`OnlineAnalysisContext.jsx`'s `value` object** (`src/contexts/OnlineAnalysisContext.jsx`) now carries `advice` from the two-phase evaluator (fast result, then a refined result up to 2000ms later) inside a `useMemo` whose deps include `advice` — every Context consumer re-renders on both the fast and the refined update. Not dangerous at today's per-hand update rate, but WS-334's two-clock model doubles the update count per decision versus the pre-refactor single-clock design, and this is exactly the kind of context-carries-a-derived-fast-changing-value pattern that becomes a cascade-re-render risk if update frequency ever rises (e.g., a future streaming/live-multi-table mode).

#### Likely Missing Elements

- **No ADR or documented boundary decision for `standardOfRecord/` vs. `decisionSystems/` (the existing shared spine for stateful modules, TD-15/C-15).** `standardOfRecord/surfaceRegistry.js` implements what looks like a registry pattern without importing `decisionSystems/registry`; there's no artifact recording whether that's a deliberate scope exclusion (standardOfRecord is a reporting/measurement layer, not a stateful IDB-backed decision system) or an unexamined gap. Given C-15 exists specifically to prevent a 5th independent reimplementation of the same pattern, this should be a one-line decision-log entry, not silence.
- **No updated fan-in ceiling / god-module check for `populationPriors.js`, `gameTreeConstants.js`, `villainModelData.js`.** SYSTEM_MODEL.md §6.1 documents several *other* hidden-coupling instances in detail (action constants, seat numbering, profile keys) but has no entry for these three despite them being the most-changed, most-imported files in the window.
- **No decision-log entry proposing decomposition of `gameTreeEvaluator.js` / `gameTreeDepth2.js`** despite both crossing the size where TD-17 previously triggered a split for a 528-line hook. The tech debt register (§11) has no TD entry tracking these two files' growth in this window.

#### Dangerous Assumptions

- **That "well-tested" (§5.2's stated mitigation for these hotspots) still holds at the current size/complexity.** Test coverage ratio at the aggregate utils layer is healthy (437 test files vs 369 source files, >1.0x), but that's a directory-wide average — it does not confirm per-file coverage density kept pace with `gameTreeEvaluator.js` nearly doubling in a single window. Not verified in this pass; flagged as an assumption rather than a measured finding.
- **That `heroRangeBuilder.js`'s and `gameTreeDepth2.js`'s direct calls to `narrowByBoard` (outside `holdingKnowledge/`) are correctly out-of-scope** for the WS-292 "all 6 production narrowing sites route through holdingKnowledge" claim. Verified in this pass: `gameTreeDepth2.js` has zero live `narrowByBoard(` calls (the only match is a docblock comment describing legacy behavior); `heroRangeBuilder.js`'s calls build *hero's own* range/represented range, a documented separate concern from villain-holding-belief narrowing. This holds up under inspection — noted as a checked assumption, not a live gap.
- **That the `SET_SETTINGS_FOCUS` / `voiceReasoningNotes` reducer additions and the `rakeResolver` Context wiring (`OnlineAnalysisContext.jsx`) are the only state-layer changes in the window** — confirmed via `git diff --stat` (4 files, 91 insertions) that this is true; state-layer churn in this window is minimal and clean.

#### Checked and found CLEAN (with evidence)

- **No circular import between `foldEquityCalculator.js` and `villainDecisionModel.js`** (flagged in decision log 2026-04-04, RT-16). Current state: one-directional only (`foldEquityCalculator.js:9` imports `queryActionDistribution`; `villainDecisionModel.js` has zero references back).
- **No `pokerCore/` → `rangeEngine/` or `pokerCore/` → `exploitEngine/` imports**; **no `rangeEngine/` → `exploitEngine/` imports**; **no `handAnalysis/` → `exploitEngine/` imports** (INV-08 held throughout the window — grep across all four directions returned empty).
- **No `exploitEngine/` file imports from `src/components/`** — the views/utils boundary held.
- **`WEAKNESS_EXPLOIT_MAP` anti-pattern did not resurface** — only references are in doc/CLAUDE.md prose recording its removal, none in executable code.
- **WS-371's fold-quantity fix (`foldTo3Bet` ambiguity) is correctly threaded into both downstream consumers** — `weaknessDetector.js` and `villainObservations.js` both read `FOLD_VS_PREFLOP_RAISE` and explicitly relabel their user-facing text ("folding to opens, not to 3-bets") rather than silently keeping the old ambiguous field.
- **`scripts/backtest/` isolation held** — every reference to `scripts/backtest` inside `src/` is a docblock/comment citation, not an executable import; the offline-harness/app-bundle boundary (SYSTEM_MODEL §2.2a) is intact.
- **Reducer default branches are unchanged from pre-window pattern** (`uiReducer.js:377`, `settingsReducer.js:399` both silently `return state`) — this is a pre-existing repo-wide pattern, not something introduced or regressed in this window.
- **Style-label removal (WS-436) holds on all live decision paths** — every non-comment reference to the removed style tables (`STYLE_PRIORS`, `STYLE_STEEPNESS_MULT`, `STYLE_RAISE_PARAMS`, `STYLE_BET_CENTER`, `STYLE_FOLD_DEFAULTS`) across `gameTreeConstants.js`, `gameTreeContext.js`, `gameTreeDepth2.js`, `gameTreeEvaluator.js`, `gameTreeSizingHelpers.js`, `preflopFoldResolver.js`, `villainDecisionModel.js`, `villainModelData.js` is a docblock/comment recording the removal — none are executable code paths (only `modelAudit.js`'s inert display-parity field, noted above).
