# Challenge 2026-08-13 — Phase 1 — SENIOR ENGINEER

Run: run-domain-correctness-challenge-2026-08-13 (protocol: challenge, engine: eng-engine)
Dispatched fresh-context, read-only, adversarial brief ("assume the engine is wrong until proven otherwise").
Verbatim agent output below.

---

Confirms: no self-test/fixture file exists for `check-required-equity-seam.mjs` — only the script itself, wired directly into `smart-test-runner.sh`. That confirms the gap noted above.

### SENIOR ENGINEER

#### Key Concerns (top 3-5)

1. **The WS-450 enforcement gate is a regex heuristic with real, demonstrable blind spots.** `scripts/check-required-equity-seam.mjs:22` — `PATTERN = /\b(\w+)\s*\/\s*\(\s*\w+(?:\.\w+)*\s*\+\s*\1\s*\)/` — requires the numerator/backreference term to be a bare identifier (`\w+`, no dots) and requires it to appear as the *second* addend in a fixed left-to-right order. Three trivial rewrites of the exact banned shape evade it entirely: (a) member access on the repeated term, e.g. `ctx.betSize / (ctx.pot + ctx.betSize)` — the backreference can't match past the dot; (b) swapped addend order, `betSize / (betSize + pot)`; (c) extraction to an intermediate variable, `const denom = pot + betSize; betSize / denom`. The commit message (`ed3dc3b6`) states this "gate[s] the pattern... so the sixth inline re-derivation fails the build instead of shipping" — a stronger guarantee than a single-line text regex can deliver for a bug that has already shipped identically five times. There is also no self-test/fixture asserting the gate *catches* a known-bad pattern (confirmed: no test file references `check-required-equity-seam`), unlike the ground-truth-harness self-verification pattern the same codebase praises elsewhere (`exploitEngine/CLAUDE.md:42-44`).

2. **The "one seam" claim in WS-451 is violated inside the same commit that made it.** `potCalculator.js` gained `rakedShowdownPot()` specifically as, per its own docblock, "the one seam for per-branch rake in EV formulas" (commit `4880ca94`). But `computeRiverBetEV` in `gameTreeDepth2.js` (the `callEV`/`raiseEV` lines touched by the same commit) still computes rake inline as `potSize - estimateRake(potSize, rakeConfig, 'river')` rather than calling the new helper. This is exactly the "four copies of one quantity is how the engine comes to answer one question differently depending on who asked" anti-pattern the same file's `CLAUDE.md` (~lines 430-437) explicitly names elsewhere — reproduced by the fix that was supposed to close a sibling instance of it.

3. **Parameter/options sprawl on the hot EV path is the structural cause of FIND-113, and remains unaddressed.** `computePerComboEV`, `computePerComboCheckEV`, `computeCallDepth2EV`, `computeBetCallDepth2EV`, `computeRaiseResponseDepth2EV`, and `computeDepth3BarrelEV` in `gameTreeDepth2.js` each take 15-20+ named options, threaded by hand through ~10 call sites in the same file. WS-432 added `workMeter` to all of them; WS-451 added `rakeConfig` to all of them. The rake bug this session fixed (FIND-113) was precisely a case where a cross-cutting concern (rake) was correctly threaded to the "leaf" river functions but not to the depth-2/3 "stage" functions for some period — nothing in this architecture prevents the same shape of omission the next time a cross-cutting parameter is added.

4. **`decisionRecord.mjs`'s `close()` silently drops unparseable lines and still emits a clean-looking summary.** `scripts/backtest/decisionRecord.mjs:305`: `try { obj = JSON.parse(ln); } catch { continue; }`, followed by a `rowCount`/`contentHash` computed only over the lines that survived (lines 298-323). This is the module whose own docblock states its entire purpose is to be the trustworthy record an hours-long run leaves behind ("If answering a new decomposition question requires re-running, the record was the wrong shape") — but a torn last line from a mid-write crash produces a record that *validates cleanly* while silently missing rows, which is a worse failure than the one the docblock defends against. This directly contradicts the repo's own `.claude/rules/error-handling.md` ("Never swallow exceptions silently... Empty catch blocks are bugs"). Already filed as FIND-118/WS-456, still `status: backlog`.

5. **Silent default-to-a-number instead of a distinguishable "no data" signal recurs at the aggregation layer, one level above where the codebase already fixed the same class of bug.** `gameTreeContext.js:311` (`let heroEquity = 0.5`) and `:332-333` (`{ value: 0, meta: null }` on zero combos) and four `totalWeight > 0 ? totalEV / totalWeight : 0` sites in `gameTreeDepth2.js` are all indistinguishable, downstream, from a genuinely-measured 50/50 or genuinely-zero EV. POKER_THEORY §3.6.1 states as binding doctrine, for the range layer, that "a model that assigns probability zero to an event that then occurs has been refuted, not merely miscalibrated" — the same failure mode is reproduced one layer up, at aggregation, and is unaddressed (FIND-120/WS-458, `status: backlog`).

#### Hidden Risks

- **`gameTreeDepth2.js:378` — `void popCallRate;`.** A per-combo equity-derived baseline is computed (`computePopBaselineFromEquity`) then explicitly discarded, directly under a comment claiming it *is* used ("Use per-combo equity logistic... for population baseline") — the code two lines below reads the flat `POPULATION_PRIORS.bet.fold` constant instead. Per this project's own stated rule ("If docs or comments contradict the code, the code wins"), the comment is simply wrong, and it is wrong in the evidence tier where per-combo precision matters most (bucket-evidence fallback, no situation rates available). Filed as FIND-119/WS-457, unresolved.
- **The workstream queue is out of sync with the branch's own git history in a way that could cause double-work or an accidental revert.** `WS-450.yaml` and `WS-451.yaml` both show `status: active`, `completed_at: null`, even though their fix commits (`ed3dc3b6`, `4880ca94`) are already present on this branch. `CLAUDE.md`'s Command Routing section instructs reading `/pulse`/`/workstream` output rather than the YAML directly — but that guidance assumes the YAML tracks reality; here, at the moment of this read, it visibly does not.
- **No mechanical enforcement of the `rakedShowdownPot` seam itself** — the same style of gate that closed WS-450 (grep-based, wired into `smart-test-runner.sh`) does not exist for "raw `estimateRake` used outside `potCalculator.js`/the seam," which is exactly the gap `computeRiverBetEV` fell into in the same commit that introduced the seam.
- **`SYSTEM_MODEL.md`'s complexity-hotspot assessment of `gameTreeDepth2.js` is stale.** §5.2 rates it "Low priority (working, well-tested)," and the frontmatter (`last-verified-against-code: 2026-05-10`) predates the entire WS-432/450/451 wave that added two more cross-cutting parameters threaded by hand through the same hotspot. `CLAUDE.md`'s own working principle ("Update the System Model after any architectural shift") has not been applied to this file in three months of load-bearing changes to it.

#### Likely Missing Elements

- An AST-based (not text-regex) check for the required-equity seam, given the documented cost of the bug (shipped identically 5 times before detection) and the demonstrated ease of defeating a text pattern with a semantically-identical rewrite.
- A negative/self-test fixture for `check-required-equity-seam.mjs` — a file containing a deliberately-bad pattern that the script must flag — mirroring the self-verification discipline this codebase already applies to its ground-truth equity harness (`exploitEngine/CLAUDE.md:42-51`).
- A shared base/contract for sidecar record writers (`decisionRecord.mjs`, `atomStore.mjs`) so a future third writer inherits the "refuse on truncation" discipline mechanically rather than by convention — `WS-456`'s own description notes the sibling `atomStore.mjs` already implements this correctly, meaning the discipline exists once and was not extracted before a second, weaker implementation shipped beside it.
- A structural (not per-function, hand-threaded) mechanism for propagating cross-cutting evaluation options (`workMeter`, `rakeConfig`, and whatever comes next) through the depth-2/3 call graph in `gameTreeDepth2.js`, so the FIND-113 failure mode (a concern correctly threaded to some call sites and silently absent from others) cannot recur by omission.

#### Dangerous Assumptions

- That the WS-450 commit message's framing ("the sixth inline re-derivation fails the build instead of shipping") is a completed guarantee rather than a partial mitigation — a future engineer trusting the commit message rather than reading the regex will believe a stronger property holds than the code delivers.
- That `requiredEquitySeam.test.js` and `rakePerBranch.test.js` — both of which validate new code against a hand-derived or self-referential reconstruction using the *same* underlying primitives (`rakedShowdownPot`, `villainRaiseSize`, `comboActionProbabilities`) — provide coverage for the *absence* of un-migrated call sites elsewhere in the ~4,700-line `exploitEngine/` surface (or `rangeEngine/`, `liveAdvisor/`). That burden is carried entirely by the regex gate, which has known blind spots (Key Concern 1).
- That manually threading a new cross-cutting option (`rakeConfig`, `workMeter`) through every call site of a ~1,700-line file with 6+ parallel EV-computation functions will continue to be done completely and correctly by hand each time. FIND-113 is direct, recent, in-repo evidence that this assumption already failed once; nothing about the architecture that produced it has changed.

Relevant file paths read during this analysis: `.claude/context/SYSTEM_MODEL.md`, `.claude/context/POKER_THEORY.md`, `src/utils/exploitEngine/CLAUDE.md`, `src/utils/exploitEngine/gameTreeDepth2.js`, `src/utils/exploitEngine/gameTreeContext.js`, `src/utils/exploitEngine/foldEquityCalculator.js`, `src/utils/potCalculator.js`, `src/utils/exploitEngine/refinementWork.js`, `scripts/check-required-equity-seam.mjs`, `scripts/backtest/decisionRecord.mjs`, `src/utils/exploitEngine/__tests__/rakePerBranch.test.js`, `src/utils/exploitEngine/__tests__/requiredEquitySeam.test.js`, `.claude/workstream/queue/WS-450.yaml` through `WS-458.yaml`.
