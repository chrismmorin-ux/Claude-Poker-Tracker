# Challenge 2026-08-13 — Phase 1 — SYSTEMS ARCHITECT

Run: run-domain-correctness-challenge-2026-08-13 (protocol: challenge, engine: eng-engine)
Dispatched fresh-context, read-only, adversarial brief ("assume the engine is wrong until proven otherwise").
Verbatim agent output below.

---

### SYSTEMS ARCHITECT

Scope covered: `.claude/context/SYSTEM_MODEL.md`, `STATE_SCHEMA.md`, `CLAUDE.md`, `POKER_THEORY.md` (partial, cross-referenced), `exploitEngine/CLAUDE.md`, `rangeEngine/CLAUDE.md`, plus direct diffs for WS-451 (`4880ca94`), WS-450 (`ed3dc3b6`), WS-436 chain (`b2c326fa` et al.), and `refinementWork.js` (WS-432). Read-only; no code executed.

#### Key Concerns (top 5)

1. **Cross-cutting engine concerns are threaded as accreting optional named parameters, not a shared config/context — and the newest one (rake) shipped without the structural gate its sibling ticket invented the same week.** `computePerComboEV`, `computePerComboCheckEV`, `computeCallDepth2EV`, `computeBetCallDepth2EV`, `computeRaiseResponseDepth2EV`, `computeDepth3BarrelEV` (`src/utils/exploitEngine/gameTreeDepth2.js`) each carry 15-20 named options (`villainModel, isOnTurn, nextRealization, bettingRates, sizingTells, maxCombos, cache, boardTexture, playerStats, spr, modelSituation, workMeter, rakeConfig, opponentModels…`). WS-451 (`4880ca94`) added `rakeConfig = null` to ~10 call sites by hand (`gameTreeDepth2.js:446,563,674,830,967,1078,1521,1645`), each defaulting silently to "no rake" via `potCalculator.js:447` (`if (!rakeConfig...) return 0`). The very same day, WS-450 (`ed3dc3b6`) shipped `scripts/check-required-equity-seam.mjs` — a grep-gate wired into `smart-test-runner.sh` that fails the build if a sixth inline re-derivation of the pot-odds formula appears — precisely because "five inline re-derivations" is the class of bug this repo has now seen repeatedly (bucket labels, position labels, style tables, index-literal ranges — all documented in `exploitEngine/CLAUDE.md`). Rake threading has the identical shape (N call sites, one correct seam, silent-safe-looking default) and got a reconstruction test (`rakePerBranch.test.js`) but no coverage gate. A future call site that forgets `rakeConfig` reproduces exactly the failure SYSTEM_MODEL §6.2 already names as a live cross-module risk: *"Missing rake = systematically optimistic EV."*

2. **`workMeter` is a shared mutable object threaded through the same call graph, with the same no-enforcement gap.** `refinementWork.js:96-103` documents it plainly: *"An object rather than a number so the estimators can charge it in place through the options plumbing... It never reads any clock: identical inputs charge identical units in an identical order, which is the whole determinism claim."* That determinism claim depends on every nested call receiving and forwarding the *same reference*. Nothing statically verifies that; a function that receives `workMeter` but omits it from one nested call silently breaks budget-capping for that subtree with no error, only a possibly-slower-than-expected refinement pass. This is the same class of coupling as (1), unaddressed by the same tooling that fixed the sibling defect.

3. **The `style` field's removal from the decision path (WS-436) is a compute-layer fix that left a display-layer fork.** `exploitEngine/CLAUDE.md:120-127` and the `POKER_THEORY.md` changelog (v2.4, `2026-08-12`) are explicit: *"The style tier is GONE... the engine's playerStats struct no longer carries a style field at all"* — because the measured evidence (10,147 paired corpus decisions) found the label *"carried ZERO villain-action information"* and the k=2 clustering study found *"21.1% Unknown fallthrough"* refuting the six-way partition. But `classifyStyle()` still runs and its output is still rendered directly: `src/components/views/StatsView.jsx:207-215`, `OnlineView/SeatGrid.jsx:34-42`, `OnlineView/SeatDetailPanel.jsx:29-37`, `AnalysisView/PlayerAnalysisPanel.jsx:99,187`, and threaded into `useLiveActionAdvisor.js:298` (`assembledAdvice.villainStyle`) and `useCitedDecisions.js:120` (`options.villainStyle` passed into `produceCitedDecision` for `surface: 'drill'`). The changelog itself flags this gap — *"display categorization is greenfielded separately (WS-447)"* — but until WS-447 lands, the founder is being shown a categorization the codebase's own domain-correctness sweep has already measured as empirically empty, with no caveat badge analogous to the `lowConfidence`/`caveats[]` pattern the codebase already uses elsewhere (FM-Drill-Layer-Synthetic-Input-Epistemics, RT-111's `drillModeEngine` wrapper).

4. **The refinement logical clock's determinism is single-machine, not cross-device, while the app's hard constraint (HC-04) is mobile.** `refinementWork.js:36-51` and `docs/standard-of-record/REFINEMENT-CLOCK-CALIBRATION.md` calibrate `REFINEMENT_UNITS_PER_MS = 319` on *"MorinComputer (G16), Windows 11 Pro, Node v24.11.1"* — a desktop dev machine — while `CLAUDE.md` states the target device is "Samsung Galaxy A22 landscape" and SYSTEM_MODEL §7.1 target is `1600×720` mobile. The module's own docstring overclaims: *"a budget of N ms buys the amount of work a calibrated reference machine does in N ms — but deterministically, on every machine."* That's true for the unit *count* (bit-identical across runs/machines) but not for the wall-clock experience: a slower device doing the same fixed unit count takes longer in real ms than the `refinementBudgetMs` label implies, which is precisely the residual the project's own memory record (`project_advice_depth_tracks_machine_load.md`) already tracks as open (FIND-109, "unmeasured wall-clock latency on the phone, not answer divergence"). Confirmed still true by static reading; worth restating because it means the two-phase advisor's phase-2 timing on-device remains unmeasured against the desktop-derived constant.

5. **Production engine internals are read directly by the offline SOR/backtest harness, insulated only by a naming convention and one bare-import CI gate, not a typed contract.** `refinementWork.js:20-23` states the module *"sits inside the engine's 37-module worker closure (WS-433) and is also read by the replication stamp through the harness loader"* and must stay `BARE-NODE-SAFE, ZERO IMPORTS` — enforced by `check-engine-bare-import.mjs`. `REFINEMENT_CLOCK_VERSION`, `MAX_STAGE_SHARE`, and `COMBO_EVAL_COST` are exported consts consumed both by the live app's `gameTreeEvaluator.js:59` and by the harness's replication-manifest stamping. This is a tight, undeclared coupling between "what the engine computes" and "what the SOR system asserts is reproducible" — a well-intentioned reshape of these exports (e.g., wrapping them in an object, renaming a key) breaks manifest stamping silently unless the harness's own tests happen to catch it, since nothing ties the two consumers together except the module's docstring and the (deliberately minimal) bare-import gate.

#### Hidden Risks

- The `rakeConfig`/`workMeter` optional-parameter pattern is now the *third* instance of the same shape of debt already catalogued in the codebase's own tech-debt register (TD-15/C-15 "4-piece stateful module pattern shipped 4× before extraction," TD-24 "gameTree caches have no public factory or versioning contract, caller-supplied cache with no provenance check"). Each new cross-cutting concern is bolted onto the ~10-site call graph by hand rather than routed through a shared evaluation-context object — the exact anti-pattern TD-24 already names for caches specifically.
- `foldEstimates = population prior + bucket counts (one pipeline)` (WS-436 commit message, `b2c326fa`) means the engine's decision-model priors and the observed-stat shrinkage are now explicitly documented as mutually exclusive pipelines that must never both feed one estimate — but this is enforced by doctrine in `exploitEngine/CLAUDE.md:113-129`, not by a type system or a runtime assertion comparable to the `narrowHolding` basis-required throw (`holdingKnowledge`). A future contributor adding a "style-adjacent" continuous feature (e.g., re-deriving something style-shaped from `playerStats.aggFreq`) has no structural barrier stopping a repeat of the exact double-counting WS-436 measured and removed.
- `villainStyle` flowing into `produceCitedDecision`'s `options` (`useCitedDecisions.js:120`) is a second, less obvious re-entry point: if WS-447's display-layer categorization changes what `style` values mean or how many categories exist, this drill-citation surface consumes the string with no schema/enum guarding it — a silent semantic drift risk into a system (drill citations / assumption baselines) that is otherwise carefully schema'd (SOR schemas, `decisionRecord`).

#### Likely Missing Elements

- A shared "engine evaluation context" object (rake config, work meter, holding basis, playerStats, board texture, etc.) that every EV function receives as one required argument rather than N optional ones — the same "extract the repeated shape" move the codebase already executed successfully for `decisionSystems/` (TD-15, SPR-078) has not yet been applied to the game-tree evaluator's parameter surface, despite `gameTreeDepth2.js` and `gameTreeEvaluator.js` being flagged in SYSTEM_MODEL §5.2 as complexity hotspots already.
- A `check-rake-seam.mjs` (or equivalent) paralleling `check-required-equity-seam.mjs`, asserting every `computePerComboEV`/`computePerComboCheckEV`/depth-2/3 call site forwards `rakeConfig` — given the precedent exists literally one commit earlier in the same file, this is a discoverable gap rather than a novel recommendation.
- An on-device (mobile Chrome / Android) calibration data point for `REFINEMENT_UNITS_PER_MS`, or at minimum a documented plan to gather one, given HC-04's mobile-first constraint and the module's explicit "deterministic on every machine" claim that the calibration itself cannot support yet.
- A caveat/flag mechanism on UI-rendered `style` values analogous to `lowConfidence`/`caveats[]`, now that the underlying categorization has been explicitly measured as non-separating by the same program that removed it from the engine.

#### Dangerous Assumptions

- That defaulting `rakeConfig = null` → no rake is a "safe" fallback. It's safe in the sense of never throwing, but it silently reproduces the exact systematically-optimistic-EV failure mode the architecture doc already names for the pre-WS-451 code, and nothing distinguishes "rake genuinely off for this game type" from "rakeConfig dropped by a refactor."
- That WS-436 fully closed the "style as a decision input" architectural concern. It closed the *engine* half; the *display* half (5+ components/hooks reading `.style`) still surfaces the same categorization the closing ticket's own measurement discredited, with the greenfield replacement (WS-447) not yet landed.
- That `REFINEMENT_CLOCK_VERSION = 'logical-v1'` delivers cross-machine determinism as documented. It delivers cross-*run*, same-machine determinism in unit-count; the ms-to-units pacing constant (319 units/ms) is a single-machine calibration artifact, and the module's own docstring overstates the guarantee ("deterministically, on every machine") in a way that could mislead a future reader of `REFINEMENT-CLOCK-CALIBRATION.md` into treating the 2000ms budget as wall-clock-portable to the S22/A22 target device.
- That the bare-import boundary (`check-engine-bare-import.mjs`) plus a docstring is sufficient insulation between the shipped engine and the offline SOR harness reading its exported constants directly. It prevents the engine module from acquiring *new* imports, but does nothing to prevent the harness from breaking silently if the engine module's *export shape* changes — the two systems are coupled by convention, not by a contract either side enforces.
