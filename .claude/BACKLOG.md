# Project Backlog

Active work items only. Completed items archived in `BACKLOG_ARCHIVE.md`.

**Last updated:** 2026-04-27 (EAL Gate 5 Phase 6 Stream D B3 — Section G shipped + visual-verified end-to-end; 1 bug fixed) | **Current version:** v123

---

## NEXT — Exploit Anchor Library (project: `docs/projects/exploit-anchor-library.project.md`)

Owner-described purpose: multi-street spot archetypes extending VillainAssumption v1.1 with perception-model primitives + GTO baseline decomposition. Dual-surface (small live badge + large study library/dashboard). Three-tier model: Tier 0 one-click hand observation capture (Phase 1), Tier 2 AI-authored anchors with owner override/retire (Phase 1), Tier 3 auto-retirement on GTO convergence (Phase 1); Tier 1 candidate promotion from clustered observations (Phase 2). S1: charter + Gates 1+2 + schema + 8 primitives + 4 seeds. S2: Gate 3 (3 JTBDs DS-57/58/59 + persona observation-capture section + red lines 8→9 + owner-interview verdicts A/A/B + 9 anti-patterns + Gate 2 re-run GREEN). S3: Gate 4 batch 1 (ACP + WRITERS.md + hand-replay-observation-capture surface). S4: Gate 4 batch 2 (anchor-library + calibration-dashboard + anchor-retirement journey). **S5: Gate 4 CLOSED — session-review-anchor-rollup surface (new route `SCREEN.SESSION_REVIEW`, auto-opens post-cashout) + consolidated P3 decisions doc (version-compat matrix + DB_VERSION dynamic-target pattern + 6-store backup/export scope + presession-filter checklist + 25-hand newcomer threshold). All 9 Gate 4 carry-forwards complete. Next: Gate 5 (Implementation) — Streams B/C/D/E/F per charter phases 5-9.**

| ID | Pri | Status | Description | Accept Criteria |
|----|-----|--------|-------------|-----------------|
| EAL-SC | P2 | COMPLETE (2026-04-24) | Schema delta + 8 perception primitives + 4 seed anchors | `docs/projects/exploit-anchor-library/schema-delta.md`, `perception-primitives.md`, `seed-anchors/01..04-*.md` |
| EAL-G1 | P2 | COMPLETE (2026-04-24) | Gate 1 — Entry (inline in charter) | Verdict YELLOW → Gate 2 triggered |
| EAL-G2 | P2 | COMPLETE (2026-04-24) | Gate 2 — Blind-Spot Roundtable (3 voices) | `docs/design/audits/2026-04-24-blindspot-exploit-anchor-library.md`. YELLOW verdict; 3 structural risks; 9 autonomy red lines; 14 Gate 4 items; 6 Gate 5 test items |
| EAL-G3-J1 | P2 | COMPLETE (2026-04-24 S2) | Author JTBD DS-57: capture-the-insight | Shipped in `docs/design/jtbd/domains/drills-and-study.md` §DS-57 |
| EAL-G3-J2 | P2 | COMPLETE (2026-04-24 S2) | Author JTBD DS-58: validate-confidence-matches-experience | Shipped in `drills-and-study.md` §DS-58 |
| EAL-G3-J3 | P2 | COMPLETE (2026-04-24 S2) | Author JTBD DS-59: retire-advice-that-stopped-working | Shipped in `drills-and-study.md` §DS-59 |
| EAL-G3-P1 | P2 | COMPLETE (2026-04-24 S2) | Amend `chris-live-player.md` with `observation_capture_active` attribute + capture-mode paragraph | Section added; red lines 8→9 (added #9 incognito observation mode); DS-57/58/59 added to Related JTBD |
| EAL-G3-O | P2 | COMPLETE (2026-04-24 S2) | Owner interview — enrollment granularity, incognito default, transparency activation | Verdicts A/A/B formalized in `gate3-owner-interview.md` + rationale + Gate 4 implications + 3 carry-forwards |
| EAL-G3-OOS | P2 | COMPLETE (2026-04-24 S2) | Document out-of-scope anti-patterns (streak / leaderboard / auto-surfacing) with autonomy rationale | Shipped at `anti-patterns.md` — 9 anti-patterns + 3 inherited; every entry cites red line violated |
| EAL-G3-RR | P2 | COMPLETE (2026-04-24 S2) | Gate 2 re-run against updated framework | Verdict GREEN at `docs/design/audits/2026-04-24-blindspot-exploit-anchor-library-rerun.md`; 17 findings Closed + 3 explicit G4/G5 carry-forwards |
| EAL-G4-ACP | P2 | COMPLETE (2026-04-24 S3) | Expand charter §Acceptance Criteria with 9 autonomy red lines | Shipped — charter §Acceptance Criteria now enumerates 9 red lines inline with Q1-A/Q3-B citations + AP-05/06/07/08/09 cross-refs + graded-work-trap refusal line |
| EAL-G4-W | P2 | COMPLETE (2026-04-24 S3) | WRITERS.md — writer registry for exploitAnchors / anchorObservations / perceptionPrimitives | Shipped at `docs/projects/exploit-anchor-library/WRITERS.md` — 13 writers across 4 stores + 7 cross-store invariants (I-WR-1..7) + CI-grep enforcement sketch |
| EAL-G4-S1 | P2 | COMPLETE (2026-04-24 S4) | `surfaces/anchor-library.md` — study-mode flat list with filters; ⓘ icon + long-press transparency per Q3 | Shipped at `docs/design/surfaces/anchor-library.md` — full filter/sort/card/panel/empty-state spec + 9 red-line compliance assertions + AP-01/AP-04/AP-05/AP-07 refusal enforcement + Phase 5 code-path plan |
| EAL-G4-S2 | P2 | COMPLETE (2026-04-24 S4) | `surfaces/calibration-dashboard.md` — per-anchor observed-vs-predicted + override/retire + model-accuracy framing | Shipped at `docs/design/surfaces/calibration-dashboard.md` — cross-project one-view-with-tabs architecture ratified (predicates + anchors + primitives); full 3-tab spec + AP-06 copy-discipline via CI-linted `calibrationCopy.js` generator + 9 red-line compliance + 8 anti-pattern refusals |
| EAL-G4-S3 | P2 | COMPLETE (2026-04-24 S3) | `surfaces/hand-replay-observation-capture.md` — ReviewPanel section G + Tier 0 modal with primary-visible Incognito toggle | Shipped at `docs/design/surfaces/hand-replay-observation-capture.md` — full Anatomy + State + 9 red-line compliance assertions + Phase 5 code-path plan; CATALOG + parent hand-replay-view updated |
| EAL-G4-S4 | P2 | COMPLETE (2026-04-24 S5) | `surfaces/session-review-anchor-rollup.md` | Shipped at `docs/design/surfaces/session-review-anchor-rollup.md` — new route `SCREEN.SESSION_REVIEW` (not SessionsView extension; rationale documented, owner-reversible); full anatomy + per-anchor row with session-scoped statistics + Δ-note conditional (>15pt divergence, n≥5) + AP-04/06/08 refusals + 9 red-line compliance + Phase 5 code-path plan |
| EAL-G4-J | P2 | COMPLETE (2026-04-24 S4) | `journeys/anchor-retirement.md` — copy-guidance against graded-work framing | Shipped at `docs/design/journeys/anchor-retirement.md` — 5-variation journey (Retire/Suppress/Reset/Auto-Tier3/Un-retire) + copy-discipline rule ladder (AP-06 via `retirementCopy.js` CI-linted generator) + cross-surface consistency (single RetirementConfirmModal shared between anchor-library + calibration-dashboard) + AP-05 no-reconsider-nudge enforcement |
| EAL-G4-V | P3 | COMPLETE (2026-04-24 S5) | Version-compat matrix for VillainAssumption ↔ ExploitAnchor | Shipped in `docs/projects/exploit-anchor-library/gate4-p3-decisions.md` §1 — compound semver `<base>-anchor-<ext>` with independent bump rules + 5-row compat table + validator rule |
| EAL-G4-DB | P3 | COMPLETE (2026-04-24 S5) | DB_VERSION coordination rule (max(current+1, target) pattern) | Shipped in `gate4-p3-decisions.md` §2 — dynamic `max(currentVersion+1, targetVersion)` pattern; multi-store migrations in same version bump supported; additive-only invariant |
| EAL-G4-EX | P3 | COMPLETE (2026-04-24 S5) | Backup/export expansion scope (6 stores: 4 anchor + villainAssumptions + rangeProfiles) | Shipped in `gate4-p3-decisions.md` §3 — 6-store expansion table + incognito observation export policy (included in owner's own export, retain incognito status on import) + single-transaction import boundary |
| EAL-G4-PS | P3 | COMPLETE (2026-04-24 S5) | Presession-filter-without-drift-data surface decision | Shipped in `gate4-p3-decisions.md` §4 — surface-by-surface checklist (library hides drift, dashboard hides nav-link, review-rollup hides Tier-3-banner, capture unaffected) + `isPresessionContext()` shared helper + Gate 5 test assertion |
| EAL-G4-NC | P3 | COMPLETE (2026-04-24 S5) | Newcomer hand-threshold for anchor-feature activation | Shipped in `gate4-p3-decisions.md` §5 — **25 hands** threshold chosen with 3-thread rationale (SLS inheritance + anchor calibration minimum + cognitive load) + `CONSTANTS.ANCHOR_LIBRARY_UNLOCK_THRESHOLD` editable + edge cases (import, deletion) + dev-mode override escape hatch |
| EAL-G4-AP | P3 | COMPLETE (2026-04-24 S2) | Anti-pattern refusal list documented | Shipped at `anti-patterns.md` — 9 + 3 inherited with red-line citations |
| EAL-Stream-E | P2 | IN PROGRESS — math layer COMPLETE (Commits 1+2+3+4 shipped 2026-04-24..25 S6+S7+S8+S9) | Stream E — Tier 1 simulator + retirement evaluator + primitive validity posterior updates | **Math layer DONE:** anchorLibrary scaffold + validator + scenario runner + 4 synthetic villains + 4 Tier-1 scenarios + retirement evaluator + primitive validity (202 tests green). **Remaining:** Commit 2.5 only — end-to-end producer wiring (replace scenario test stubs with real assumptionEngine integration; requires exploit-deviation coordination on PREDICATE_KEYS). |
| EAL-Stream-D | P2 | IN PROGRESS (B3 capture flow visually-verified end-to-end COMPLETE 2026-04-25..27 S10..S17 + visual verification) | Stream D — IDB v19 migration + Anchor Library view + Calibration Dashboard + HandReplay Tier 0 capture + Session Review rollup | **B3 capture flow shipped + visually verified (S10..S17 + visual verification):** S10-S17 layers (capture pure-utils + migrateV19 + wrappers + reducer/context/hook + AppRoot wiring + orchestrator hooks + Button/List/Modal components + AnchorObservationSection composite + ReviewPanel.jsx Section G slot) + Playwright visual verification at 1600×720 against 61-hand seeded data (button renders + modal opens with all spec elements + Incognito red-line-#9 visible-and-disabled-when-not-enrolled + chip→Save→list-row→IDB persistence + reload-hydration with "2m ago" timestamp). **Real bug caught + fixed during visual verification:** `hand.handId` from IndexedDB is a number (auto-increment PK) but EAL pure-utils require string per schema-delta §3.1 — fixed by string-coercion at the AnchorObservationSection adapter boundary; +2 regression tests added. **554 EAL tests green** (23 files; +2 from S17 visual-verification fix). The Tier 0 owner observation capture surface is operational at user level. **Remaining:** 3 study surfaces (anchor-library / calibration-dashboard / session-review-anchor-rollup) + Phase 8 LiveAdviceBar anchor badge integration. |
| EAL-Stream-C | P2 | BLOCKED by EAL-Stream-D + EAL-Stream-B-matcher | Stream C — LiveAdviceBar anchor-badge + SizingPresetsPanel embed + headless observation writer | Badge renders classifier-fired; observation writer logs; badge respects 1.5s glance |
| EAL-Stream-B-matcher | P2 | BLOCKED by EAL-Stream-D | Stream B — Anchor matcher (pure utils) + status-filter + retirement evaluator | `src/utils/anchorLibrary/` with tests; matcher short-circuits on candidate status |
| EAL-Stream-B-expand | P3 | BLOCKED by EAL-Stream-B-matcher | Stream B — Anchor library expansion to 10 anchors | 10 anchors in library, Tier 1-validated |
| EAL-Stream-F | P3 | BLOCKED by EAL-Stream-B-expand + EAL-Stream-D | Stream F — Drill card types for anchor recognition + application | 2–3 drills per anchor; plug into PresessionDrillView |
| EAL-G5-HH | P2 | BLOCKED by EAL-Stream-D | Hook-hoisting race test (cross-surface) — Calibration Dashboard + Anchor Library simultaneous mount | Test passes; single persistence-hook load; cross-surface-write visibility in 1 render tick |
| EAL-G5-CA | P2 | BLOCKED by EAL-Stream-E | Cross-anchor invalidation atomicity test — PP flag triggers atomic degradation | Test passes; `dependentAnchorCount` invariant holds |
| EAL-G5-SI | P2 | PARTIAL (idempotency shipped 2026-04-25 S11; markdown-vs-IDB content drift test deferred) | Markdown-to-IDB seed idempotency test — re-running seed doesn't duplicate primitives | **Idempotency shipped:** `migrateV19` re-open test verifies 8 primitives stay 8 (no duplicates) + put-loop overwrites with same content. **Remaining:** content-drift snapshot test comparing PERCEPTION_PRIMITIVE_SEEDS constant against perception-primitives.md content (Phase 6+ build-time generator script). |
| EAL-G5-SS | P2 | BLOCKED by EAL-Stream-B-matcher | Per-seed-anchor snapshot tests (RT-108 pattern) | 4 scenarios green; engine-drift fails CI |
| EAL-G5-T1 | P2 | BLOCKED by EAL-Stream-E | Tier 1 simulator scenarios for 4 seed anchors | |predicted − realized| ≤ 5% per scenario |
| EAL-G5-RL | P2 | BLOCKED by EAL-Stream-D + EAL-Stream-C | In-app test assertions for 9 autonomy red lines | Each red line has a passing test |
| EAL-P8-EX | P3 | BLOCKED by EAL-Stream-C | Phase 8 — Extension doctrine in `ignition-poker-tracker/CLAUDE.md`: anchor-agnostic | Doctrine rule added |

Gate 3 owner-interview questions are listed in the Gate 2 audit §"Owner interview questions". Answers shape Gate 4 `surfaces/calibration-dashboard.md` design spec.

---

## NEXT — Monetization & PMF (project: `docs/projects/monetization-and-pmf.project.md`)

Owner-described purpose: monetization-readiness program — pricing design + paywall mechanics + telemetry foundation + evaluator-journey design + UX redesign against falsifiable assumptions. Clean-slate build on codebase with zero existing monetization infrastructure. **Session 1:** charter + Gate 1 inline (YELLOW); 3 PROTO personas (Evaluator core + trial-first-session + returning-evaluator); paywall-spectrum design doc (9 × 9 × 7 × 7 × 5 × 3 dimension option space + 5 compatible bundles α/β/γ/δ/ε); market-research doc; 8 owner-interview questions Q1–Q8 parked. **Session 2:** Gate 2 blind-spot roundtable shipped (3 voices — Product/UX + Autonomy skeptic + Market lens). Verdict YELLOW; 3 structural risks (dark-pattern cancellation + Q7 legal posture + evaluator evidence gap); 10 commerce red lines enumerated; 12 anti-patterns; expanded JTBD scope 7 → 10 (added SA-76 tier-migration + SA-77 payment-method + SA-78 renewal-transparency); expanded owner-interview Q1 → Q10 (added Q9 domain-split + Q10 ignition-mode-situational). **Q1 (doctrine scope) and Q7 (legal/ToS) are the two pivotal unresolved questions.** Gate 3 required per LIFECYCLE.md; Gate 4 unblocked pending Q1+Q7 verdicts.

| ID | Pri | Status | Description | Accept Criteria |
|----|-----|--------|-------------|-----------------|
| MPMF-G1 | P1 | COMPLETE (2026-04-24 S1) | Gate 1 — Entry (inline in charter) | Verdict YELLOW → Gate 2 triggered |
| MPMF-G2 | P1 | COMPLETE (2026-04-24 S2) | Gate 2 — Blind-Spot Roundtable (3 voices) | `docs/design/audits/2026-04-24-blindspot-monetization-and-pmf.md`. YELLOW verdict; 3 structural risks; 10 commerce red lines; 12 anti-patterns; 10 Gate 3 items; 21 Gate 4 items; 9 Gate 5 items |
| MPMF-G3-J1 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD SA-71: try-before-paying | Shipped in `subscription-account.md` §SA-71 |
| MPMF-G3-J2 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD SA-72: understand-free-vs-paid | Shipped in `subscription-account.md` §SA-72 |
| MPMF-G3-J3 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD SA-73: hit-paywall-with-dignity | Shipped in `subscription-account.md` §SA-73 |
| MPMF-G3-J4 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD SA-74: cancel-without-friction | Shipped in `subscription-account.md` §SA-74 |
| MPMF-G3-J5 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD SA-75: evaluate-sidebar-separately | Shipped in `subscription-account.md` §SA-75 |
| MPMF-G3-J6 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD SA-76: switch-between-plan-tiers | Shipped in `subscription-account.md` §SA-76 (Q9 split deferred) |
| MPMF-G3-J7 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD SA-77: manage-payment-method-without-churning | Shipped in `subscription-account.md` §SA-77 (Q9 split deferred) |
| MPMF-G3-J8 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD SA-78: know-when-ill-be-billed-and-how-much | Shipped in `subscription-account.md` §SA-78 (Q9 split deferred) |
| MPMF-G3-J9 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD CC-88: honest-telemetry-transparency | Shipped in `cross-cutting.md` §CC-88 |
| MPMF-G3-J10 | P1 | COMPLETE (2026-04-24 S3a) | Author JTBD ON-88: expert-bypass-onboarding-for-evaluators | Shipped in `onboarding.md` §ON-88 (distinct from ON-84 + ON-87) |
| MPMF-G3-DS | P1 | COMPLETE (2026-04-24 S3b) | Domain-split decision (Q9) | Q9=A verdict executed — new `billing-management.md` domain; SA-76/77/78 moved; ATLAS 16 domains |
| MPMF-G3-P1 | P1 | COMPLETE (2026-04-24 S3b) | Ratify `evaluator.md` PROTO → Owner-Confirmed | Structural ratification; evidential E1–E6 pending Stream D |
| MPMF-G3-P2 | P1 | COMPLETE (2026-04-24 S3b) | Ratify `trial-first-session.md` PROTO → Owner-Confirmed | Structural ratification; evidential TFS1–TFS4 pending Stream D |
| MPMF-G3-P3 | P1 | COMPLETE (2026-04-24 S3b) | Ratify `returning-evaluator.md` PROTO → Owner-Confirmed | Structural ratification; evidential RE1–RE4 pending Stream D |
| MPMF-G3-P4 | P2 | COMPLETE (2026-04-24 S3b) | evaluator-ignition-mode situational (Q10) | Q10=B verdict resolved as "no new file — attribute in evaluator.md" |
| MPMF-G3-O | P1 | COMPLETE (2026-04-24 S3b) | Owner interview — Q1–Q10 verdicts documented | `gate3-owner-interview.md` shipped with all 10 verdicts (all Recommended) + rationale + Gate 4 implications + carry-forwards |
| MPMF-G3-OOS | P1 | COMPLETE (2026-04-24 S3b) | Document out-of-scope anti-patterns with autonomy rationale | `anti-patterns.md` stub shipped — 12 MPMF-AP-* refusals + forbidden-string patterns + permitted alternatives |
| MPMF-G3-Q7 | P1 | NEXT (blocked on external counsel) | Q7 legal-scoping session scheduled | Q7=A verdict captured; scheduling separate external-counsel session pending; Phase 1 main-app unblocked meanwhile |
| MPMF-G3-RR | P1 | COMPLETE (2026-04-24 S3b) | Gate 2 re-run against updated framework | `docs/design/audits/2026-04-24-blindspot-monetization-and-pmf-rerun.md` verdict GREEN; all 17 findings closed or named carry-forward |
| MPMF-G4-ACP | P1 | COMPLETE (2026-04-24 S4-B1) | Expand charter §Acceptance Criteria with 10 commerce red lines inline | Charter §Acceptance Criteria expanded; 10 red lines enumerated inline with per-line MPMF-AP-* refusal cross-refs + test-target pointers; mirrors EAL S3 precedent |
| MPMF-G4-AP | P1 | NEXT | anti-patterns.md expansion at Gate 4 (beyond stub) | Stub shipped at S3b; Gate 4 expands as surface specs reveal additional refusals |
| MPMF-G4-W | P1 | COMPLETE (2026-04-24 S4-B1) | WRITERS.md — writer registry for subscription store | Shipped at `docs/projects/monetization-and-pmf/WRITERS.md` — 5 writers + 5 cross-store invariants I-WR-1..5 + CI-grep enforcement sketch |
| MPMF-G4-S1 | P1 | COMPLETE (2026-04-25 S7-B4) | surfaces/pricing-page.md | Shipped — top-level routed view; 4 TierCards equal-weight; FoundingMemberSection cap-50 factual count; pricing-tentative banner; 7-FAQ accordion; sub-shape tailoring invisible-to-user; M1 doctrine footer line |
| MPMF-G4-S2 | P1 | COMPLETE (2026-04-25 S7-B4) | surfaces/paywall-modal.md | Shipped — Variations A/B/D (modal) + Variation C (inline fallback); equal-weight 2 buttons CSS-measurement test; H-SC01 defer-to-hand-end via useDeferredPaywall; H-N07 7-day cooldown via usePaywallCooldown; PaywallGate component contract |
| MPMF-G4-S3 | P1 | COMPLETE (2026-04-25 S7-B4) | surfaces/upgrade-prompt-inline.md | Shipped — inline widget across 5 host contexts (session-recap / post-hand-review / settings-billing / session-list-row / drills-list); compact + expanded variants; sub-shape-tailored copy (E-CHRIS / E-SCHOLAR / E-IGNITION); 6 suppression rules; inline-prompt anti-patterns refused (animation / auto-scroll / re-rendering elsewhere) |
| MPMF-G4-S4 | P1 | COMPLETE (2026-04-25 S8-B5) | surfaces/trial-state-indicator.md | Shipped — persistent chip in main-nav; 7 variant states; H-PLT01 + H-SC02 + H-SC01 LOAD-BEARING; tier-aware tap routing |
| MPMF-G4-S5 | P1 | COMPLETE (2026-04-25 S8-B5) | surfaces/billing-settings.md (SettingsView extension) | Shipped — SettingsView extension; 4-card layout; 4 tier-state variants; LOAD-BEARING red lines #2 + #10; entry point for J3 + J4 journeys; equal-weight Cancel button refuses both visual-emphasis + de-emphasis |
| MPMF-G4-S6 | P1 | COMPLETE (2026-04-24 S5-B2) | surfaces/telemetry-consent-panel.md | Shipped — first-launch modal + Settings mirror + 4 categories + consent-gate pattern + extension read-only + 10 red-line compliance. Surfaced MPMF-AP-13 |
| MPMF-G4-J1 | P1 | COMPLETE (2026-04-24 S5-B2) | journeys/evaluator-onboarding.md | Shipped — 5 variations (Full tour / Fast orientation + sub-shape / Skip / At-table / Returning-resume) + picker UI + copy discipline + 10 red-line compliance. Surfaced MPMF-AP-14 |
| MPMF-G4-J2 | P1 | COMPLETE (2026-04-24 S6-B3) | journeys/paywall-hit.md | Shipped — 4 variations (A history-access / B usage-threshold / C depth-of-analysis inline fallback / D session-close → next-open) + H-SC01 defer-to-hand-end mechanism + H-N07 7-day cooldown + 10 red-line compliance + Phase 5 code-path plan |
| MPMF-G4-J3 | P1 | COMPLETE (2026-04-24 S6-B3) | journeys/cancellation.md — dark-pattern-free | Shipped — 4 variations (A Standard / B Founding-member / C Card-decline grace / D Phase-2+ placeholder) + shared CancellationConfirmModal + ~20 forbidden-string patterns + CI-linted deterministic `cancellationCopy.js` plan + 10 red-line compliance. LOAD-BEARING for red line #10 |
| MPMF-G4-J4 | P1 | COMPLETE (2026-04-24 S6-B3) | journeys/plan-change.md — SA-76 upgrade/downgrade | Shipped — 3 variations (A Upgrade immediate prorated / B Downgrade effective-at-period-end no-refund / C Lateral Phase-2+ placeholder) + shared PlanSelectModal + PlanChangeConfirmModal + data-preservation assurance + "Change" not "Downgrade" copy + CI-linted `planChangeCopy.js` plan. Refuses MPMF-AP-06/08/16 |
| MPMF-G4-HT | P2 | COMPLETE (2026-04-24 S4-B1) | heuristics/ updates — H-SC01 + H-SC02 added | Shipped in `docs/design/heuristics/poker-live-table.md` as new H-SC (session-continuity) category |
| MPMF-G4-AL | P1 | COMPLETE (2026-04-24 S4-B1) | assumption-ledger.md — 12–15 falsifiable assumptions | Shipped at `docs/projects/monetization-and-pmf/assumption-ledger.md` — 15 assumptions (M1–M8 + M9–M12 + M13–M15) + kill-criteria + event-schema sketch + review cadence |
| MPMF-G4-ES | P2 | COMPLETE (2026-04-24 S4-B1) | Bundle ε (Ignition) strict-superset of Pro confirmed | Documented inline in `docs/projects/monetization-and-pmf/entitlement-architecture.md` §Bundle ε + §Feature visibility |
| MPMF-G4-EA | P1 | COMPLETE (2026-04-24 S4-B1) | Entitlement architecture decision — main-app IDB + WebSocket-to-extension | Shipped at `docs/projects/monetization-and-pmf/entitlement-architecture.md` — full architecture + EntitlementContext + featureMap + PaywallGate + extension read-only doctrine (MPMF-P8-EX) + data-flow scenarios + testing strategy + payment-processor selection |
| MPMF-G4-IM | P2 | LATER (blocked Q3=C) | Ignition-mode surface design | Q3=C defers; reopens on Q3 re-verdict (Phase 2+) + Q7 legal scoping complete |
| MPMF-G5-ER | P1 | COMPLETE (2026-04-25 S9-G5-B1) | Entitlement reducer + EntitlementContext + useEntitlement hook | Shipped — 10-action reducer + Context (5 semantic helpers) + useEntitlement + useEntitlementPersistence + featureMap (12 features × 5 tiers) + subscriptionStore + 108 tests + AppProviders wired |
| MPMF-G5-IDB | P1 | COMPLETE (2026-04-25 S9-G5-B1) | IDB migration v17 → v18 + subscription store | Shipped — DB_VERSION 17→18; migrateV18 additive, idempotent, seeded; 16 round-trip tests via fake-indexeddb (now wired in setup.js); MPMF claimed v18 first per dynamic-target rule |
| MPMF-G5-PP | P1 | BLOCKED by MPMF-G4-* + Q7 verdict | Payment processor integration (Stripe or per Q7) | Test-mode functional; webhook plumbing |
| MPMF-G5-PG | P1 | BLOCKED by MPMF-G5-ER | PaywallGate shared component | Wraps gated features; consistent copy + CTA pattern |
| MPMF-G5-BS | P1 | BLOCKED by MPMF-G4-S5 | BillingSettings extension of SettingsView | 6-action panel shipped |
| MPMF-G5-PH | P1 | BLOCKED by MPMF-G4-S6 + Q8 verdict | PostHog install + event schema v1 | Events flowing; persona-inference dashboard + frustration dashboard |
| MPMF-G5-RL | P1 | BLOCKED by MPMF-G4-* | In-app test assertions for 10 commerce red lines | Each red line has a passing test |
| MPMF-G5-CL | P1 | BLOCKED by MPMF-G4-* | CI-linted forbidden-copy-strings check | `scripts/check-commerce-copy.sh` or equivalent |
| MPMF-G5-SC | P1 | BLOCKED by MPMF-G5-PG | H-SC01 test — paywall never fires mid-hand | Test passes; hand-in-progress defers any paywall to hand-end |
| MPMF-P8-EX | P3 | BLOCKED by MPMF-G5-ER | Phase 8 — Extension entitlement-read-only doctrine in `ignition-poker-tracker/CLAUDE.md` | Doctrine rule added |

Gate 3 owner-interview questions (Q1–Q10) are listed in the Gate 2 audit §"Owner interview questions" and charter §Gate 3. Q1 (doctrine scope) + Q7 (legal posture) are the two blocking questions for Gate 4 surface design progress.

---

## NEXT — Shape Language (project: `docs/projects/poker-shape-language.project.md`)

Owner-described purpose: visual/geometric descriptor language for poker (Silhouette / Saddle / Spire / Basin / Ridgeline / Sankey + companions) plus an adaptive lesson system that seeds based on user's skill per descriptor. Study-mode first; live surfaces render only classifier-fired badges (headless seeding). Theory RT + Gates 1+2 shipped 2026-04-23; Gate 2 verdict RED on autonomy grounds (fixable). 8 non-negotiable autonomy red lines must propagate into Gate 4.

| ID | Pri | Status | Description | Accept Criteria |
|----|-----|--------|-------------|-----------------|
| SLS-RT | P2 | COMPLETE (2026-04-23) | Theory roundtable — 6 personas web-researched | `docs/projects/poker-shape-language/roundtable.md` with top-6 descriptor catalog + 4 advanced |
| SLS-G1 | P2 | COMPLETE (2026-04-23) | Gate 1 — Entry (scope + personas + JTBD + gap analysis) | Inline output: YELLOW-leaning-RED → Gate 2 triggered |
| SLS-G2 | P2 | COMPLETE (2026-04-23) | Gate 2 — Blind-Spot Roundtable (4 voices) | `docs/design/audits/2026-04-23-blindspot-shape-language-adaptive-seeding.md`. RED verdict; top-3 risks identified; 8 red lines |
| SLS-G3 | P2 | NEXT | Gate 3 — Research + persona/JTBD expansion | 6 research threads (R1-R6) resolved; 2 personas amended; 1 situational authored; 5 JTBDs added; Gate 2 re-run GREEN/YELLOW. Triage at `docs/projects/poker-shape-language/gate3-triage.md` |
| SLS-G4 | P2 | BLOCKED by SLS-G3 | Gate 4 — Design spec | 3 surfaces authored (`shape-language-study-home`, `shape-skill-map`, `lesson-runner`); enrollment journey; interaction spec with red-line tests; cross-surface skill-state contract |
| SLS-D | P2 | BLOCKED by SLS-G4 | Stream D — Skill-state foundation | IDB v17→v18 additive (`shapeMastery`, `shapeLessons` stores); reducer; context; transparency screen functional |
| SLS-B1 | P2 | BLOCKED by SLS-D | Stream B1 — Range Silhouette classifier + lesson 1 | Classifier module + tests; lesson 1 authored; HandReplay embed |
| SLS-B2 | P2 | BLOCKED by SLS-B1 | Stream B2 — Foundation descriptors (Equity Curve, Spire/Polarization, Sizing Tag) | 3 descriptors shipped end-to-end |
| SLS-B3 | P2 | BLOCKED by SLS-B2 | Stream B3 — Saddle + Basin/Sankey | 2 advanced classifiers + lessons + session-review integration |
| SLS-B4 | P3 | BLOCKED by SLS-B3 | Stream B4 — Ridgeline + Narrowing Ribbon | New `ShapeLanguageStudyView` preflop-flow surface |
| SLS-B5 | P3 | BLOCKED by SLS-B4 | Stream B5 — Advanced (Contour Tree + Basin Map + Hand Trajectory) | 3 advanced descriptors |
| SLS-F | P3 | BLOCKED by SLS-B1..B5 | Stream F — Drill-card authoring for PresessionDrillView | Each descriptor has ≥2 recognition drills |

Gate 2 surfaced 6 owner-interview questions to resolve at SLS-G3 kickoff. See `gate3-triage.md` §"Owner interview questions".

---

## NEXT — Printable Refresher (project: `docs/projects/printable-refresher.project.md`)

Owner-described purpose: high-accuracy, UI-polished, print-optimized in-app reference surface that lets owner laminate index-card-sized artifacts for at-venue off-hand study. Content: per-seat × hand preflop charts, math tables (auto-profit / geometric / pot-odds / binomial / implied), equity-bucket tables, texture × hand-type postflop (reformulated), pure-plays + exceptions codex. First explicit Reference-mode surface under Shape Language three-intent taxonomy. Gate 1 + Gate 2 + **Gate 3** SHIPPED 2026-04-24. **Verdict YELLOW at Gate 2 → GREEN at Gate 2 re-run.** All 9 owner-interview questions answered. 5 JTBDs authored (DS-60/61, CC-82/83, SE-04); 3 personas amended/created (apprentice + rounder + new `stepped-away-from-hand` situational). **Gate 4 CLOSED 2026-04-25 — all 12 carry-forwards COMPLETE (ACP + AP + CD + H + S1 + S2 + J + W + CI + SL + CSS + MIG).** Gate 5 (Implementation) IN PROGRESS — **PRF-G5-CI CLOSED 2026-04-25 (4 commits)** + **PRF-G5-MIG CLOSED 2026-04-26 (commit `f869141`)** + **PRF-G5-SL + PRF-G5-ST CLOSED 2026-04-26 (commit `a33349b`)** + **PRF-G5-WR CLOSED 2026-04-26 (commit `d8d9b48`)** + **PRF-G5-HK CLOSED 2026-04-26 (commit `2e81b16`)** + **PRF-G5-RL + PRF-G5-RI + PRF-G5-DS + PRF-G5-LG CLOSED 2026-04-26 + AppRoot wiring landed**: persistence + selector wiring + 17-red-line test scaffolds + AppProvider integration all shipped. PRF tests now total 399 green across 19 test files (full pipeline + writer-boundary + durable-suppression + lineage-footer + red-line meta-test). **PRF-G5-B CLOSED 2026-04-26** (6 of 6 cards) + **PRF-G5-UI IN PROGRESS 2026-04-27** — S18 shipped initial UI (top-level shell + MathCardTemplate + CardCatalog + CardRow); S19 shipped sub-view nav (SuppressConfirmModal + CardDetail + LineageModal) + menu wiring. Phase B Math Tables corpus + 7 of 9 PRF-G5-UI deliverables are now landed (PrintPreview + PrintConfirmationModal + 3 remaining card templates deferred to S20+). **NEXT:** continue PRF-G5-UI (S20) — PrintPreview + PrintConfirmationModal sub-views + 3 remaining card templates (Preflop navy / Equity teal / Exceptions maroon) → PRF-G5-PDF Playwright snapshots → Q5 differentiation demo at design review → conditional PRF-G5-A Phase A Preflop → PRF-G5-C Phase C Equity+Exceptions.

| ID | Pri | Status | Description | Accept Criteria |
|----|-----|--------|-------------|-----------------|
| PRF-G1 | P2 | COMPLETE (2026-04-24) | Gate 1 — Entry (inline in charter) | Verdict YELLOW-leaning-RED → Gate 2 triggered |
| PRF-G2 | P2 | COMPLETE (2026-04-24) | Gate 2 — Blind-Spot Roundtable (5 voices: Product/UX + Market + Theory-fidelity + Autonomy + Senior-engineer-print) | `docs/design/audits/2026-04-24-blindspot-printable-refresher.md`. YELLOW verdict; 3 structural risks; 8 new red lines (#10-#17); 11 anti-patterns; 5 copy rules; 6-point fidelity bar; 8 paper heuristics |
| PRF-G3-I | P2 | COMPLETE (2026-04-24) | Owner interview — 9 questions (doctrine / phasing / venue / print format / tier / IDB / personalization / newcomer / print-date) | `gate3-owner-interview.md` — all 9 answered ("accept all recommendations" 2026-04-24) |
| PRF-G3-J1 | P2 | COMPLETE (2026-04-24) | Author JTBD DS-60: carry-the-reference-offline | Shipped in `drills-and-study.md` §DS-60 |
| PRF-G3-J2 | P2 | COMPLETE (2026-04-24) | Author JTBD CC-82: trust-the-sheet (lineage-stamped reference) | Shipped in `cross-cutting.md` §CC-82 |
| PRF-G3-J3 | P2 | COMPLETE (2026-04-24) | Author JTBD CC-83: know-my-reference-is-stale (staleness surfacing) | Shipped in `cross-cutting.md` §CC-83 |
| PRF-G3-J4 | P2 | COMPLETE (2026-04-24) | Author JTBD SE-04: pre-session kinesthetic visualization | Shipped in `session-entry.md` §SE-04 |
| PRF-G3-J5 | P2 | COMPLETE (2026-04-24) | Author JTBD DS-61: export-the-personal-codex (Phase 2+) | Shipped in `drills-and-study.md` §DS-61 |
| PRF-G3-P1 | P2 | COMPLETE (2026-04-24) | NEW situational persona `stepped-away-from-hand.md` (replaces Voice 1's mid-hand paper-permitted attribute per Q3 venue policy) | 4 canonical contexts authored; cross-persona; paired with DS-60 |
| PRF-G3-P2 | P2 | COMPLETE (2026-04-24) | Amend `apprentice-student.md` — coach-curated printable pack bullets | Goal + Missing-feature bullets added |
| PRF-G3-P3 | P2 | COMPLETE (2026-04-24) | Amend `rounder.md` — carry-vetted-reference Goal | Goal bullet added |
| PRF-G3-ATLAS | P2 | COMPLETE (2026-04-24) | Update ATLAS.md with 5 new JTBDs + domain-range bumps | DS-43..61 + SE-01..04 + CC entries added |
| PRF-G3-DB | P2 | COMPLETE (2026-04-24) | STATUS.md IDB v18 coordination decision (PRF vs Shape Language) | Dynamic `max(currentVersion + 1, 18)` rule (Q6 ratified); inherits EAL Gate 4 P3 §2 precedent |
| PRF-G3-RR | P2 | COMPLETE (2026-04-24) | Gate 2 re-run against updated framework | Verdict **GREEN** at `docs/design/audits/2026-04-24-blindspot-printable-refresher-rerun.md`; 17 findings mapped (12 closed + 5 propagated to G4/G5) |
| PRF-G4-ACP | P2 | COMPLETE (2026-04-24) | Charter §Acceptance Criteria expansion (17 red lines + 11 anti-patterns + 5 copy rules + 6-point fidelity bar + source-util whitelist/blacklist) | Shipped inline in `docs/projects/printable-refresher.project.md` §Acceptance Criteria. Testable; Gate 5 test IDs mapped (PRF-G5-RL / RI / DS / LG) |
| PRF-G4-S1 | P2 | COMPLETE (2026-04-24) | `surfaces/printable-refresher.md` (5 sub-views: catalog + card-detail + lineage-modal + print-preview + print-confirmation + suppression-confirm; filter/sort + stale banner + Letter/A4 + 12/6/4/1-up selectors; newcomer-locked empty-state; `currentIntent: 'Reference'` dispatched at mount) | Shipped at `docs/design/surfaces/printable-refresher.md`. 17 red-line compliance assertions mapped to test IDs (PRF-G5-RL/RI/DS/LG/PDF). First explicit Reference-mode surface. Unblocks S2 + J + W + CI + SL + CSS + MIG |
| PRF-G4-S2 | P2 | COMPLETE (2026-04-24) | `surfaces/printable-refresher-card-templates.md` — 4 templates (Preflop / Math / Equity / Exceptions) with layout + required manifest fields + H-PM05 atomicity justification + F1-F6 fidelity compliance + known risks per template + shared Regions 1-6 anatomy + Phase 5 implementation checklist + amendment rule | Shipped at `docs/design/surfaces/printable-refresher-card-templates.md`. Region 6 card-corner physical version-stamp is H-PM07 laminate cross-reference affordance (survives lamination glare for arm's-length cross-check vs in-app staleness-diff). `TEMPLATE_BY_CLASS` map dispatches manifest.class → template component |
| PRF-G4-H | P2 | COMPLETE (2026-04-24) | `heuristics/printable-artifact.md` (H-PM01-08 consolidated) | Shipped at `docs/design/heuristics/printable-artifact.md`. Merged V1 H-PRF01-05 + V5 H-PM01-06 → 8 final heuristics. H-PM07 staleness-channel-in-app-only is first-of-its-kind paper-medium heuristic |
| PRF-G4-J | P2 | COMPLETE (2026-04-24) | `journeys/refresher-print-and-re-print.md` — 5 variations (A first-print 8-step / B stamp-date sub-flow / C engine-changes silent-transition no-UI / D in-app-diff staleness discovery / E re-print loop) + Variation F Phase 2+ coach-pack placeholder + 8 AP-PRF refusals at journey level + 17 red-line per-line compliance assertions + 7 Playwright evidence placeholders + copy-discipline ✓/✗ demonstrations per variation | Shipped at `docs/design/journeys/refresher-print-and-re-print.md`. Session-scoped dismissal + old-batch-not-deleted + amber-is-only-state are the 3 critical-design-choice callouts. Variation E re-print pre-selects stale subset only (not full prior batch) per common intent |
| PRF-G4-CD | P2 | COMPLETE (2026-04-24) | `copy-discipline.md` (CD-1..5 with ✓/✗ examples + CI-lint forbidden-string list + worked-example Q1 RED-card reformulation) | Shipped at `docs/projects/printable-refresher/copy-discipline.md`. CI-lint regex list ready for PRF-G4-CI pickup |
| PRF-G4-AP | P2 | COMPLETE (2026-04-24) | `anti-patterns.md` (AP-PRF-01..11 + 9 EAL-inherited with transitive-inheritance rule) | Shipped at `docs/projects/printable-refresher/anti-patterns.md`. Each AP cites red line + allowed-alternative; amendment requires persona-level review |
| PRF-G4-W | P2 | COMPLETE (2026-04-24) | `WRITERS.md` — 3 writers (W-URC-1 config-prefs / W-URC-2 card-visibility / W-URC-3 print-date-stamp) × 2 stores (`userRefresherConfig` + `printBatches`) + 7 cross-store invariants I-WR-1..7 + CI-grep enforcement sketch (`scripts/check-refresher-writers.sh`) + amendment rule persona-level review | Shipped at `docs/projects/printable-refresher/WRITERS.md`. I-WR-2 Reference-mode write-silence is load-bearing (test target PRF-G5-RI). I-WR-5 `printBatches` append-only (zero delete() allowed). Mirrors EAL WRITERS.md precedent |
| PRF-G4-CI | P2 | COMPLETE (2026-04-24) | Content-drift CI spec — 6 checks (contentHash-vs-recomputation RT-108 core / source-util whitelist+blacklist / CD forbidden-string grep / schemaVersion-bump discipline / markdown-vs-generated precedence / lineage-footer completeness) + manifest shape v1 + Phase 5 implementation checklist + developer-experience troubleshooting | Shipped at `docs/projects/printable-refresher/content-drift-ci.md`. **Non-negotiable sequencing: test must be implemented + green BEFORE any Gate 5 card-authoring PR merges.** Inherits RT-108 pattern from drillContent/ + extends with F4/F6 whitelist + CD regex enforcement |
| PRF-G4-SL | P3 | COMPLETE (2026-04-24) | Selector-library contract — 6 selectors (`selectAllCards` / `selectActiveCards` / `selectPinnedCards` / `selectSuppressedCards` / `selectCardsForBatchPrint` / `selectStaleCards`) + 4 state-clear-asymmetry roundtrip tests + memoization contract + renderer coupling rules | Shipped at `docs/projects/printable-refresher/selectors.md`. R-8.1 state-symmetry coverage (SR-6 precedent). Un-suppress roundtrip is canonical proof — suppressing + un-suppressing preserves per-card pin/hide state (zero data loss) |
| PRF-G4-CSS | P2 | COMPLETE (2026-04-25) | Print-CSS doctrine spec — `@page` rules + 4 layout grids (12/6/4/1-up Letter+A4) + Regions 1-6 typography scale (10pt body floor / 8pt table floor / 6pt banned / 7pt corner stamp / 14pt title / 16pt ceiling) + 6-hue deuteranopia palette per-class assignment + B&W fallback + `@media print` doctrine + cross-browser test matrix + bundle-import enforcement (forbid html2canvas / jsPDF / pdf-lib) | Shipped at `docs/projects/printable-refresher/print-css-doctrine.md`. PRF-G5-PDF Playwright snapshot test contract: 4 grid modes × 2 page sizes × 2 color modes = 16 snapshots per MVP card. Bundle-grep CI script sketched (`scripts/check-refresher-bundle.sh`) |
| PRF-G4-NC | P3 | COMPLETE (2026-04-24) | Newcomer activation threshold decision | **1 completed session + explicit opt-in.** `CONSTANTS.PRF_UNLOCK_THRESHOLD_SESSIONS = 1`. Factual empty-state copy; no nudge. |
| PRF-G4-MIG | P2 | COMPLETE (2026-04-25) | IDB migration spec — 2 new stores (`userRefresherConfig` singleton + `printBatches` UUID-keyed with `printedAt` index) + dynamic `max(currentVersion+1, 18)` target + additive-only + idempotent + default singleton seeding at migration time + 6 PRF-G5-MIG test cases (round-trip / seed / idempotent / no-v17-mutation / index / collision-resolution) + per-record schema versioning framework for future field additions + backup/export expansion (EAL precedent inherited) | Shipped at `docs/projects/printable-refresher/idb-migration.md`. Coordinates collision-free with Shape Language v18 + future MPMF claims. PRF-G5-DS suppression-durability test shared across migration + writer/reducer layers. I-WR-5 batch-preservation verified at migration boundary |
| PRF-G5-CI | P2 | COMPLETE (2026-04-25 — all 4 sessions shipped) | Content-drift CI test (RT-108 pattern) implementing the 6 checks from PRF-G4-CI spec | **PRF-G5-CI CLOSED.** All 6 checks live + dev scripts + verify-CI-catches-drift acceptance. 161/161 PRF tests green across 7 test files. Phase B math-table card authoring (PRF-G5-B) is now unblocked. **S1 (commit 6cc45ce):** `cardRegistry.js` + stub manifest + `manifestSchema.test.js` (23 tests). **S2 (commit 78bd449):** `lineage.js` (hashUtil + stableStringify + computeSourceHash + derive7FieldLineage + printFooter + isStubContentHash) + auto-profit hash recomputed `sha256:16ec2e02...239b0b` + `lineage.test.js` (33 tests) + `contentDrift.test.js` Check 1 + Check 6 (9 tests). **S3 (commit 350dad5):** `sourceUtilPolicy.js` exporting `WHITELIST_REGEXES` (3 entries: `^src/utils/pokerCore/` / `^src/constants/gameTreeConstants\.js$` / `^\.claude/context/POKER_THEORY\.md$`) + `BLACKLIST_REGEXES` (7 entries per charter: villainDecisionModel.js / villainObservations.js / villainProfileBuilder.js / assumptionEngine namespace / anchorLibrary namespace / CalibrationDashboardView / AnchorLibraryView) + `validateSourceUtils(manifest)` returning `{valid, violations[]}` enumerating blacklist-match vs whitelist-miss vs blacklist-body-reference (bodyMarkdown reference-scan against 7 blacklisted symbols catches "see villainDecisionModel.js" prose smuggling calibration coupling onto paper even when sourceUtils-array is clean). `copyDisciplinePatterns.js` mirroring `copy-discipline.md` §CI-lint canonical patterns: CD-1 imperative tone (5 patterns: "you must" + "always|never|don't|do not" + action-verb-within-50-chars), CD-2 self-evaluation (6 patterns: "grade your" / "score your" / "check your answer" / "how did you" / "test yourself" / "(was|did) your read"), CD-3 engagement (14 patterns: "mastered" / "cards remaining" / "streak" / "keep it up" / "great job" / "level up" / "unlock" / "last chance" / "limited time" / "re-?print soon" / "users like you" / "most popular" / "trending" / "your N%"), CD-4 labels-as-inputs (1 pattern: "(vs|against|versus) (fish|nit|lag|tag|station|maniac|whale).{0,80}(iso|bet|bluff|fold|raise|call|3-?bet|4-?bet|cbet|barrel|tighten|loosen)" with POKER_THEORY-citation-within-200-chars whitelist exception), CD-5 unqualified assumptions (stakes-regex `(\$[\d.]+\/\$[\d.]+\|tournament\|rake-agnostic)` AND stack-regex `(\d+bb\|\d+BB\|effective)` both required in bodyMarkdown unless `cd5_exempt: true` with non-empty `cd5_exempt_justification`). `validateCopyDiscipline(manifest, renderedFooter)` returning `{valid, violations[]}` with rule + label + excerpt-with-padding for actionable error messages. `contentDrift.test.js` extended with Check 2 + Check 3 describes (2 new tests for current 1-manifest registry, total now 11). `sourceUtilPolicy.test.js` (16 tests: regex catalog shape + 3 whitelist-passing fixtures + 4 blacklist-failing fixtures + non-whitelisted-non-blacklisted whitelist-miss + multi-violation enumeration + bodyMarkdown reference-scan + missing-array graceful default). `copyDisciplinePatterns.test.js` (34 tests: catalog shape + per-CD-rule failing fixture + per-CD-rule passing near-miss + CD-3 "your N%" rule with plain-percentage-no-trigger + CD-4 whitelist-exception with POKER_THEORY-within-200-chars passing + CD-4 fails when citation > 200 chars away via 300-char-filler + CD-5 stakes/stack discipline + cd5_exempt bypass with non-empty justification + cd5_exempt with empty justification does NOT bypass + multi-violation enumeration). 117/117 PRF tests green (was 65 at S2). Auto-profit verified mechanically CD-clean (sourceUtils empty whitelist pass-through; bodyMarkdown declares "rake-agnostic" + "100bb effective" satisfying CD-5; no CD-1 imperative tone or CD-3 engagement triggers). **S4 done (this commit):** `getSchemaVersionChange.js` exporting `describeChange({prior, current})` (pure helper deriving `{exists, prior, current, bumped, decremented, proseOnlyEdit, diff: {bodyMarkdownDiffers, generatedFieldsDiffer, sourceUtilsDiffer, anyDiff}}`) + `evaluateCheck4(change)` (5-branch decision tree per spec §Check 4: not-in-head→PASS first-commit / decremented→FAIL monotonic-violation / no-diff-same-or-bumped→PASS / diff-bumped→PASS-intentional-re-version / diff-same-version-proseOnlyEdit-bodyMarkdownOnly→PASS-with-warning / diff-same-version-proseOnlyEdit-with-generatedFields-or-sourceUtils-diff→FAIL escape-hatch-misuse / diff-same-version-no-proseOnlyEdit→FAIL bump-required) + `getSchemaVersionChange(current, options)` (calls `child_process.execSync('git show HEAD:<path>')` via dependency-injected `gitShow`; returns `{exists: false, reason: 'no-rel-path'\|'not-in-head'\|'parse-error'}` on failure). `getSchemaVersionChange.test.js` (21 tests: describeChange diff-detection across all 7 fields + bumped/decremented/proseOnlyEdit-propagation + evaluateCheck4 all 9 branches + getSchemaVersionChange with mocked-gitShow injection covering no-rel-path / not-in-head / parse-error / valid-prior-JSON paths). `contentDrift.test.js` extended (Check 1 RT-108 escape-hatch wiring — when hash mismatches and `getSchemaVersionChange` reports bumped=true, error message specifically calls out "schemaVersion bumped but contentHash was not updated" with one-liner pointer to `node scripts/refresher-compute-hash.js`; Check 4 — per manifest evaluateCheck4(getSchemaVersionChange(m, {manifestRelPath: 'src/utils/printableRefresher/manifests/<filename>'})).pass; Check 5 placeholder resolution — strict, every `{{xxx}}` in bodyMarkdown must have corresponding generatedFields key with indexed-placeholder base-key resolution `{{x[0.5]}}` → key `x`; Check 5 hardcoded-numeric warning — soft gate emits `console.warn` when bodyMarkdown contains percentages and neither generatedFields nor theoryCitation backs them; auto-profit passes Check 5 trivially since theoryCitation `POKER_THEORY.md §3.1` provides backing). `verifyCatchesDrift.test.js` (20 tests — load-bearing acceptance for closing PRF-G5-CI per spec §Phase 5 implementation checklist final item: constructs intentionally-drifted fixture manifests for each canonical drift mode and asserts validators report expected violations; covers Check 1 stub-prefix + drifted-bodyMarkdown / Check 2 blacklist-match + whitelist-miss + blacklist-body-reference / Check 3 CD-1 + CD-3 + CD-4 + CD-5-stakes / Check 4 diff-no-bump-FAIL + proseOnlyEdit-misuse-FAIL + decremented-FAIL / Check 5 placeholder-resolution-detection / Check 6 missing-citation + null-bucket-permitted + 7-line printFooter rendering + meta-validator-presence). `scripts/refresher-compute-hash.js <cardId>` (dev script — reads manifest, runs computeSourceHash, prints to stdout; stderr diagnostic shows match/mismatch). `scripts/refresher-recompute-hashes.js [--write]` (dev script — batch over all manifests; dry-run by default exit-1-on-drift; with --write updates each manifest's contentHash field in-place; preserves trailing-newline + 2-space indent). Smart-test-runner / CI wiring is automatic via vite.config.js test glob `src/utils/**/__tests__/**/*.test.js`; `npm test` (used by both `scripts/smart-test-runner.sh` and `.github/workflows/ci.yml`) auto-discovers all PRF test files including contentDrift.test.js. **161/161 PRF tests green across 7 files** (was 117 at S3 — S4 net-adds 44 tests: getSchemaVersionChange 21 + verifyCatchesDrift 20 + contentDrift +3 for Check 4 + Check 5a placeholder + Check 5b numeric warning). **PRF-G5-CI CLOSED — Phase B math-table card authoring (PRF-G5-B) and migration scaffold (PRF-G5-MIG) are unblocked.** |
| PRF-G5-A | P2 | NEXT (Phase 5 — PRF-G5-CI green ✓; remaining gate: Q5 differentiation demo at design review) | **Phase A: Preflop Refresher** (cards #1, #10, #16) — conditional on Gate 4 visual differentiation demo vs Upswing free pack | 3 GREEN preflop cards authored with fidelity bar + lineage footer |
| PRF-G5-B | P2 | COMPLETE (2026-04-26) | **Phase B: Math Tables** (6 cards: auto-profit / pot-odds / SPR-zones / geometric-sizing / implied / binomial) — clearest market gap | **6 of 6 cards COMPLETE.** S1: `prf-math-auto-profit.json` (POKER_THEORY §3.1 auto-profit-threshold). S16: pot-odds (POKER_THEORY §6.3) + SPR-zones (5-zone enumeration MICRO/LOW/MEDIUM/HIGH/DEEP) + geometric-sizing ((1+2f)^N = SPR formula). **S17 (this batch, 2026-04-26): 2 final cards shipped — closes PRF-G5-B**: (1) `prf-math-implied.json` (POKER_THEORY §1.5 Pot Odds and Implied Odds canonical derivation; required_equity = call_cost / (current_pot + call_cost + expected_future_winnings) formula + equivalent base-threshold-multiplier form; set-mining floor `~15:1 implied to break even on the set-mine alone` per POKER_THEORY §1.5 derivation; 2 worked examples — 9-out flush draw threshold drop + pocket-pair set-mining 100bb-deep with 80bb stack-off potential adding effective threshold ~6%; reverse-implied-odds note for top-pair-weak-kicker on draw-heavy boards; rake-agnostic + 100bb-effective). (2) `prf-math-binomial.json` (POKER_THEORY §6.6 Combo Counting underlying combinatorics; P(at-least-one-hit) = 1 - (47-O)/47 × (46-O)/46 flop-to-river formula + 4 worked examples — 9-out flush draw 35.0% / 8-out open-ender 31.5% / 4-out gutshot 16.5% / 15-out combo draw 54.1%; runner-runner P(both) = (out_turn/47) × (out_river/46) with backdoor flush 4.2% + backdoor straight 1-3% examples; turn-to-river simpler O/46 form; blocker-discount + reverse-implied-odds disclaimer). Both cards `sourceUtils: []` (theory-only pass-through). atomicityJustificationWordCount: 12 (implied — note "(implied odds adjustment to pot odds)" parses as 6 words by whitespace split, totalling 12) + 10 (binomial). Both contentHashes computed via `refresher-compute-hash.js`; all 6 content-drift CI checks green per-manifest. **Phase 1 MVP target reachable:** 6 Phase B + 0-3 Phase A conditional + 4 Phase C → 10-13 cards. **Test status post-S17:** 614/614 PRF-scope tests green across 19 test files (+86 over post-S16 baseline via per-manifest scaling 4→6 in lineageFooterRendering/contentDrift/redLineCompliance/manifestSchema). Per-manifest scaling validated linearly: manifestSchema 20-per-manifest × 6 = 120 (was 80) / lineageFooterRendering 9-per-manifest × 6 = 54 (was 36) / contentDrift 11-per-manifest × 6 = 66 (was 44). Zero new test functions — infrastructure scales linearly with card count |
| PRF-G5-C | P3 | BLOCKED by PRF-G5-B + per-card Voice-3-equivalent fidelity review | **Phase C: Texture-Equity + Exceptions Codex** (cards #7, #8, #11, #15 reformulated) — per-card fidelity review + bucket-definition glossary prerequisite | 4 YELLOW cards reformulated + shipped with per-card fidelity review |
| PRF-G5-RL | P2 | COMPLETE (2026-04-26) | Red-line compliance meta-test (17 red lines indexed) | `src/utils/printableRefresher/__tests__/redLineCompliance.test.js` (24 tests) — meta-test indexing coverage for all 17 red lines. Direct assertions for testable-now red lines: #1 high-accuracy (theoryCitation + isStubContentHash), #2 anti-labels-as-inputs (CD-4 catalog + per-manifest CD-4 pass), #3 situation-qualified (CD-5 stakes regex + per-manifest CD-5 pass), #4 atomicity (atomicityJustification non-empty), #5 no-engagement (CD-3 catalog ≥10 patterns + per-manifest CD-3 pass), #6 flat-access (selectAllCards never filters), #7 editor-tone (CD-1 imperative-tone + per-manifest CD-1 pass), #10 staleness-surfacing (selectStaleCards exposed), #11 ref-mode-write-silence (delegates to writerBoundary.test.js / PRF-G5-RI), #12 lineage-mandatory (per-manifest 7-field shape), #13 durable-suppression (delegates to durableSuppression.test.js / PRF-G5-DS), #14 no-mastery (subsumed by CD-3), #15 no-proactive-print (W-URC-3 rejects empty payloads — no auto-trigger possible), #16 cross-surface segregation (default config includeCodex:false + W-URC-1 rejects includeCodex:true). Subsumed: #8 (subsumed by #11+#16) + #9 (incognito N/A — refresher doesn't capture observations) + #17 (intent-switch placeholder; Phase 2+ deferred). Coverage manifest test asserts REDLINE_COUNT === 17 — fails if red-line count diverges from charter |
| PRF-G5-PDF | P2 | BLOCKED by PRF-G5-A+B (need cards to snapshot) | Playwright print-to-PDF snapshot tests (16 per MVP card: 4 grids × 2 sizes × 2 color modes) + cross-browser + B&W fallback | Tests green across Chrome/Firefox/Safari |
| PRF-G5-RI | P2 | COMPLETE (2026-04-26) | Writer-boundary test for I-WR-2 reference-mode write-silence (red line #11) | `src/utils/printableRefresher/__tests__/writerBoundary.test.js` (6 tests) — mocks ALL store wrapper modules globally (`refresherStore` + `anchorObservationsStore` + `exploitAnchorsStore` + `perceptionPrimitivesStore` + `anchorObservationDraftsStore` + `subscriptionStore`); calls each PRF writer (W-URC-1/2a/2b/3); asserts ONLY refresherStore primitives were called via `assertNoSkillStateStoreCalls()` helper iterating over 5 skill-state-store mocks. Plus meta-assertion that all 4 writers in sequence cause zero skill-state mutations. If a future writer accidentally imports a non-PRF store wrapper + calls put/delete, this test fails — writer must be re-classified per WRITERS.md amendment rule |
| PRF-G5-DS | P2 | COMPLETE (2026-04-26) | Writer-level durable-suppression test (red line #13) — canonical R-8.1 zero-data-loss proof at writer/IDB boundary | `src/utils/printableRefresher/__tests__/durableSuppression.test.js` (5 tests) — pin-3-cards → suppress-class → un-suppress-class → all-3-still-pinned canonical proof at writer/IDB boundary; hidden card visibility survives suppress+un-suppress; mixed pinned+hidden across multiple classes survive sequence; close+reopen IDB cycle preserves both cardVisibility + suppressedClasses (cross-session durability); writeConfigPreferences (W-URC-1) does NOT touch cardVisibility/suppressedClasses (I-WR-3 segregation cross-check). Mirror of refresherSelectors.test.js PRF-G5-SL-ROUNDTRIP-UNSUPPRESS at the selector boundary |
| PRF-G5-LG | P2 | COMPLETE (2026-04-26) | Lineage footer per-manifest rendering test (red line #12) | `src/utils/printableRefresher/__tests__/lineageFooterRendering.test.js` (12 tests) — per manifest in cardRegistry: 7 numbered lines + field [1] cardIdSemver + field [2] ISO8601 + field [3] sourceUtilTrail non-empty + field [4] engineAppVersion includes both engine+app + field [5] theoryCitation echo + field [6] stable-stringified assumptions JSON-shaped + field [7] null-marker or non-empty string + lineage object has exactly 7 named keys; plus 2 fallback path tests (engineAppVersion unknown markers + printFooter deterministic for fixed input). Mirror of verifyCatchesDrift.test.js Check 6 at the per-manifest registry level |
| PRF-G5-MIG | P2 | COMPLETE (2026-04-26) | IDB migration test suite — 6 cases (round-trip / seed / idempotent / no-v19-mutation / index / collision-resolution) | All 6 cases green + 2 cross-cutting tests (PRF-G5-DS suppression-durability + I-WR-5 batch-preservation). **Implementation:** `src/utils/persistence/refresherDefaults.js` (`buildDefaultRefresherConfig()` + `REFRESHER_CONFIG_SINGLETON_ID` + `REFRESHER_CONFIG_SCHEMA_VERSION`) + `database.js` `DB_VERSION` 19 → 20 + 2 new store-name exports (`USER_REFRESHER_CONFIG_STORE_NAME` + `PRINT_BATCHES_STORE_NAME`) + `migrations.js` `migrateV20(db, transaction)` adding both stores additively (existence-check before create + singleton seeded via in-flight upgrade transaction matching EAL perceptionPrimitives precedent + printBatches NOT seeded per I-WR-5 append-only) + `runMigrations` chain extended `if (oldVersion < 20) migrateV20(db, transaction)`. **Dynamic-target rule resolved statically:** spec called `max(currentVersion + 1, 18)`; EAL Phase 6 Stream D shipped v19 first; PRF computes `max(19+1, 18) = 20`. Compile-time resolution per existing project convention. **Test suite (19 tests):** DB_VERSION case 6 (v20 + ≥18 invariant); fresh-install case 1 (userRefresherConfig keypath/autoIncrement/no-indexes + printBatches keypath/autoIncrement); seed-correctness case 2 (singleton matches buildDefaultRefresherConfig + Phase 1 structural defaults Letter/12-up/auto/lineage-on/codex-OFF/staleness-OFF + printBatches starts empty + exactly 1 record in userRefresherConfig); idempotent case 3 (re-open preserves owner mutation + does not duplicate singleton + both stores still exist); no-prior-mutation case 4 (perceptionPrimitives 8 seed records intact through v20 upgrade chain + printBatches inserted record persists across re-open); printedAt-index case 5 (index present + non-unique + supports chronological cursor traversal verified with 3 batches inserted out-of-order traversed in printedAt-ASC order); PRF-G5-DS suppression-durability (suppressedClasses byte-preserved across simulated v20→v21 close+reopen); I-WR-5 batch-preservation (single-record + 5-record persistence with id-set equality verification). 19/19 migration tests green. Adjusted 2 prior tests for v20 bump: `anchorLibraryStores.test.js` DB_VERSION assertion 19 → 20 + `database.test.js` "creates all 17 object stores" → "creates all 19 object stores" with `userRefresherConfig` + `printBatches` containment assertions. Phase 5 next: persistence + selector wiring per `WRITERS.md` (W-URC-1/2/3 writers) + `selectors.md` (6 selectors) |
| PRF-G5-SL | P2 | COMPLETE (2026-04-26) | Read-side selector library — 6 selectors per `selectors.md` | `src/utils/printableRefresher/refresherSelectors.js` exports `selectAllCards` / `selectActiveCards` / `selectPinnedCards` / `selectSuppressedCards` / `selectCardsForBatchPrint(selectedIds)` / `selectStaleCards(printBatches)` + `annotateCard()` helper. Pure functions on `(cardRegistry, userRefresherConfig)` inputs returning `AnnotatedCard[]` with `visibility` + `classSuppressed` + `isStale` + `staleSinceBatch` annotations. `selectStaleCards` builds most-recent-batch-per-card index by sorting `printBatches` by `printedAt` DESC then iterating in order; cards never printed are not stale (no laminate to be stale per spec); hidden + class-suppressed cards excluded from stale set. `refresherSelectors.test.js` (39 tests): annotateCard 6 cases; selectAllCards 4; selectActiveCards 4; selectPinnedCards 2; selectSuppressedCards 3; partition test (active ∪ suppressed === all, intersection empty per PRF-G5-SL-PARTITION); selectCardsForBatchPrint 5 incl. defense-in-depth drops; selectStaleCards 8 incl. only-most-recent-batch + out-of-order batch sort; **4 state-clear-asymmetry roundtrip tests** (PRF-G5-SL-ROUNDTRIP-PIN/HIDE/SUPPRESS/UNSUPPRESS) including the canonical un-suppress zero-data-loss proof (pin 3 cards → suppress class → un-suppress class → all 3 still pinned); 2 purity guardrails (no-mutation + deterministic). 39/39 green |
| PRF-G5-ST | P2 | COMPLETE (2026-04-26) | IDB CRUD wrappers for both PRF stores | `src/utils/persistence/refresherStore.js` exports `getRefresherConfig()` (lazy-creates default if missing — defense-in-depth for corrupted-DB recovery), `putRefresherConfig(record)` (throws on null/missing-id/wrong-id; validates `record.id === 'singleton'`), `putPrintBatch(record)` (throws on null/empty-batchId/empty-printedAt), `getPrintBatch(batchId)`, `getAllPrintBatches()` (sorted by `printedAt` DESC), `getPrintBatchesForCard(cardId)` (filtered + DESC-sorted). Pure data layer; writer-ownership enforcement (W-URC-1/2/3) deferred to PRF-G5-WR writers.js next session. Mirrors EAL `anchorObservationsStore.js` pattern (input validation throws synchronously; IDB errors logged via logError then propagated). `refresherStore.test.js` (21 tests): singleton round-trip + lazy-default-on-missing-record + 4 input-validation rejects + only-1-record-after-N-puts; printBatch round-trip + null-on-missing + 4 input-validations; getAllPrintBatches empty + DESC-sorted + field-preservation-end-to-end; getPrintBatchesForCard filtered + empty-when-not-printed + empty-cardId-rejects; append-only invariant 5-record persistence + same-batchId-overwrites note (UI must not call this path; W-URC-3 generates fresh UUIDs). 21/21 green |
| PRF-G5-WR | P2 | COMPLETE (2026-04-26) | Writer registry — 3 writers (W-URC-1 config-prefs / W-URC-2 card-visibility / W-URC-3 print-date-stamp) with validators + `scripts/check-refresher-writers.sh` CI-grep enforcement | `src/utils/printableRefresher/writers.js` exports `writeConfigPreferences(patch)` (W-URC-1 — read-merge-put via refresherStore primitives; rejects cardVisibility/suppressedClasses/unknown top-level keys via I-WR-3 segregation; rejects `printPreferences.includeCodex: true` per AP-PRF-09 + red line #16 with explicit Phase-2+ deferral cite to PRF-P2-PE; rejects unknown printPreferences/notifications nested keys with valid-keys hint in error message; rejects non-string-or-null lastExportAt; preserves `id: 'singleton'` through merge), `writeCardVisibility({cardId, visibility})` (W-URC-2a — immediate; rejects empty cardId; rejects visibility outside `{default, hidden, pinned}`; `'default'` removes the entry from cardVisibility map for clean state; preserves other cards through write), `writeSuppressedClass({classId, suppress, confirmed, ownerInitiated})` (W-URC-2b — immediate; rejects empty classId + non-boolean suppress; **suppress=true requires `options.confirmed === true`** at writer boundary as defense-in-depth against direct API calls bypassing SuppressConfirmModal per red line #13 deliberate-suppression; **suppress=false requires `options.ownerInitiated === true`** at writer boundary per AP-PRF-05 refusal of programmatic un-suppress nudges; idempotent suppress no-duplicate; un-suppress not-suppressed is no-op; preserves other suppressed classes), `writePrintBatch(payload)` (W-URC-3 — generates `batchId` via `crypto.randomUUID()` with Math.random RFC4122-v4 fallback; rejects empty cardIds + empty printedAt + empty engineVersion + empty appVersion + null perCardSnapshots + non-string-or-null label; **I-WR-6 perCardSnapshots completeness**: keys MUST equal cardIds set — rejects size mismatch + extraneous keys + missing snapshots + non-string contentHash + non-string version per snapshot; warns dev-mode console on `printedAt > +1 day` future without rejecting; final post-write side effect invokes `writeConfigPreferences({lastExportAt: printedAt})` per WRITERS.md note on lastExportAt single-ownership). `writers.test.js` (55 tests across 7 describes): W-URC-1 happy-path 6 + I-WR-3 rejects 5 + AP-PRF-09 includeCodex rejects 2 + input-shape rejects 5; W-URC-2a happy-path 5 + input rejects 4; W-URC-2b happy-path 5 + confirm/ownerInitiated guards 4 + input rejects 2; W-URC-3 happy-path 5 + I-WR-6 rejects 4 + input rejects 7; writers integration multi-writer-coexist 1. **All 55/55 green.** `scripts/check-refresher-writers.sh` (executable bash CI-grep): Check 1 — I-WR-1 enumeration scans `src/` for any put()/delete() call referencing userRefresherConfig or printBatches store names + filters out ALLOWED_WRITER_FILES (writers.js / writers.test.js / refresherStore.js / refresherStore.test.js / refresherMigration.test.js / migrations.js); Check 2 — I-WR-5 append-only specifically asserts zero .delete() calls against printBatches outside ALLOWED_PRINTBATCHES_DELETE_FILES (only refresherMigration.test.js permitted; future src/__dev__/printBatchDeleter.js placeholder noted). Smoke-tested on current tree — passes ✅ |
| PRF-G5-UI | P2 | IN PROGRESS (S18+S19+S20+S21+S22+S23+S24 shipped 2026-04-27) | UI components for PrintableRefresherView (top-level shell + 4 card templates + 5 sub-views + StalenessBanner + PRF-G5-PDF Playwright baseline + Settings staleness toggle) | **S18 (initial UI batch):** SCREEN.PRINTABLE_REFRESHER constant + ViewRouter case wired. `src/components/views/PrintableRefresherView/index.jsx` (top-level shell — header with back button + 4 class filter chips with aria-pressed + showSuppressed toggle + sort dropdown 4-options + "Showing N of M" status + lightweight error toast + deferred-features footer; uses useRefresher() + useRefresherView() + useUI(); applies filters+sort locally before passing to CardCatalog; staleCardIds Set built from getStaleCards). `MathCardTemplate.jsx` (Region 1-2 + Region 4-5 layout per print-css-doctrine.md; burnt-orange #c05621 accent on title for math class; 14pt bold serif title; 10pt body paragraphs split on \n\n; 9pt greyscale mono+serif lineage footer with 7-field printFooter output; compact mode hides footer + adds fade-mask for catalog preview). `CardRow.jsx` (cardId + abbreviated title + version line + status badge with 5-state precedence stale > suppressed > hidden > pinned > current; 4 action chips Pin/Hide/Suppress/Detail with ≥44×44 tap targets per H-ML06 + aria-pressed for active states; opacity 0.6 for suppressed/hidden). `CardCatalog.jsx` (vertical list of CardRow; empty-state CD-clean copy "No cards match the current filter. Adjust filters or show suppressed cards." — no engagement copy / no imperative tone; staleCardIds accepts both Set and Array). 4 test files (48 tests): MathCardTemplate 9 + CardRow 19 + CardCatalog 8 + PrintableRefresherView 12. **S19 (sub-view + modal nav + menu wiring):** 3 new modal/sub-view components shipped + sub-view navigation wired + menu entry added. (1) `SuppressConfirmModal.jsx` — ≤2-tap flow per surface spec: title-aware copy for suppress vs un-suppress + "I understand" checkbox + Cancel + primary Confirm button (disabled until checkbox ticked + during submit); aria-modal=true + dialog labelledby title; Esc + backdrop-click + Cancel all dispatch onCancel; onConfirm async with try/catch surfacing errors inline as alert role; ≥44px tap targets; CD-clean copy (verified mechanically). (2) `LineageModal.jsx` — read-only 7-field lineage rendered as labeled `<dl>` with monospace font + grid layout; null bucketDef renders "(not applicable)"; Esc + backdrop + ✕ button all close; aria-modal=true; red-line-#12 footer disclaimer. (3) `CardDetail.jsx` sub-view — back-to-catalog button + cardId heading + stale badge if isStale + ClassDispatchedTemplate routing math→MathCardTemplate while preflop/equity/exceptions show placeholder pending S20+ templates + LineageModal CTA + 3 actions [Pin/Hide/Suppress class] with aria-pressed + state-aware labels + status footer. (4) `index.jsx` extended with selectedCardId + pendingSuppress state + handleSuppress now opens SuppressConfirmModal (replaces S18 no-op log) + handleSuppressConfirm calls `refresher.setClassSuppressed({suppress: true, confirmed: true})` for suppress and `({suppress: false, ownerInitiated: true})` for un-suppress + handleOpenDetail navigates to CardDetail via state (replaces S18 no-op log) + selectedCard memoized from `getAllCards()` so detail stays reachable even if catalog filter would hide it + conditional render: CardDetail when selectedCardId set; SuppressConfirmModal as modal overlay when pendingSuppress set; otherwise CardCatalog. (5) Menu wire-up: `CollapsibleSidebar.jsx` adds Refresher entry with `<Printer />` icon between Online and Settings; `PokerTracker.jsx` HASH_TO_SCREEN adds `'#printableRefresher'` deep-link mapping. **3 new test files + 1 extended (48 new tests):** SuppressConfirmModal 13 (suppress flow / un-suppress flow / accessibility) + LineageModal 13 (render + close handling + accessibility) + CardDetail 16 (basic render + actions + lineage modal toggle + accessibility) + PrintableRefresherView +6 (sub-view + modal navigation: Detail chip opens CardDetail / Back returns to catalog / Suppress chip opens SuppressConfirmModal / Cancel closes modal / "Showing N of M" hidden in detail view + footer-copy update). **96/96 component tests green; full PRF scope 710/710 across 26 test files.** **Deferred to S20+:** PrintPreview + PrintConfirmationModal sub-views / 3 remaining card templates (PreflopCardTemplate navy `#1e3a5f` + EquityCardTemplate teal `#0f766e` + ExceptionsCardTemplate maroon `#7f1d1d`) / StalenessBanner / Stakes/stacks/status filter chips beyond class / "Print selected batch" + "Print preview" buttons / URL routing for sub-views (currently component-state-based). **Visual verification:** TODO at owner level — components unit-tested for render correctness + interaction wiring but no Playwright snapshot or manual browser session yet (PRF-G5-PDF pending). **S20 (3 remaining card templates + dispatcher wiring):** purely additive — case branch + import only into `CardDetail.jsx` ClassDispatchedTemplate (now a `switch` statement; default branch retains S19 placeholder for unknown class). 3 new template components + 3 new test files + 1 extended (49 net new tests). (1) `PreflopCardTemplate.jsx` — navy `#1e3a5f` accent. Region 1 title + 14pt bold serif accent. Region 2 renders 13×13 hand grid (169-cell CSS grid, generated from `manifest.generatedFields.rangeGrid` 169-entry array; pairs on diagonal as `${r}${r}`, suited above as `${r1}${r2}s`, offsuit below as `${r2}${r1}o`; cell shading: full freq → solid accent fill; mixed freq → `linear-gradient(to top, accent X%, transparent)` per H-PM02 deuteranopia-friendly intensity; outline-only when out-of-range) + sizing hint (`generatedFields.defaultSizing` rendered as accent-colored bold 10pt block under grid). Region 3 derivation/exception callout (italic 9pt prose; second+ paragraphs from bodyMarkdown). Lineage footer + compact-mode mask gradient mirror MathCardTemplate. Defensive: missing/wrong-length rangeGrid omits the grid; missing sizing omits the sizing block; missing second paragraph omits Region 3. (2) `EquityCardTemplate.jsx` — teal `#0f766e` accent. Region 2 renders the equity matrix as a `<table role="table">` with `<th scope="col">` bucket headers from `generatedFields.equityMatrix.headers` + one `<tr>` per row in `equityMatrix.rows` with `<th scope="row">` texture name + cell percentages with `%` suffix and `font-variant-numeric: tabular-nums` for visual alignment. Atomicity cap enforced at render-time: rows sliced to ≤8 per H-PM05 (matches `equityMatrix` shape contract from surface spec). Region 3 bucket-definition citation (italicized 10pt prose from second+ bodyMarkdown paragraphs). (3) `ExceptionsCardTemplate.jsx` — maroon `#7f1d1d` accent. Region 2 classifies bodyMarkdown paragraphs by leading prefix ("Solver baseline:" / "Live-pool divergence:" / "Override when:") into labeled sub-sections with accent-colored 9.5pt bold labels; unprefixed paragraphs fall through as plain prose. Region 3 hoists the override paragraph (if present) into a callout with a light-red `#fef2f2` background, dashed top border, accent-colored "Override when:" label — visual emphasis on the override trigger per surface spec §Template 4 layout. Region 4-5 lineage footer additionally renders `audit id: <auditId>` line in accent color (the audit-id citation per F4 + F5 fidelity); also exposes `data-audit-id` attribute on the article for CI introspection. (4) `CardDetail.jsx` `ClassDispatchedTemplate` extended from 1-of-1 to 1-of-4 dispatch via `switch (manifest.class)` — case branches for math/preflop/equity/exceptions; default placeholder retained for unknown classes (defensive, exercised in test "renders placeholder for unknown class"). 4 imports added at top: PreflopCardTemplate / EquityCardTemplate / ExceptionsCardTemplate (MathCardTemplate already present). **3 new test files (49 net new tests):** PreflopCardTemplate 16 (title accent / 169-grid render / grid-omits-when-missing / sizing render / Region 3 callout / Region 3 omits when no second paragraph / lineage footer compact-toggle / null-manifest / data attrs / break-inside / aria-label / data-cell labels / mixed-frequency linear-gradient); EquityCardTemplate 14 (title accent / table render / column headers / row headers / cell percentages with unique values + count for duplicates / row-cap-at-8 / matrix-omits-when-missing / Region 3 bucket citation / lineage compact-toggle / null-manifest / data attrs / break-inside); ExceptionsCardTemplate 16 (title accent / subtitle / solver-baseline-prefix classifies / divergence-prefix classifies / override hoisted to Region 3 / Region 3 omits when no override / auditId in footer / data-audit-id attr / data-audit-id absent when undefined / unprefixed-prose-fallback / lineage compact-toggle hides audit-id / null-manifest / data attrs / break-inside); CardDetail.test.jsx +3 net (replaced 1 placeholder-asserts-for-preflop with 4: dispatches preflop / dispatches equity / dispatches exceptions / falls back to placeholder for unknown class). **All 145/145 view-scope tests green across 10 files (+49 from S19 baseline of 96/7); full PRF scope 714+ across 26+ test files** in isolation. **Visual verification:** Math card detail renders correctly through new switch dispatcher (regression-verified via Playwright snapshot at `prf-s20-math-card-detail-regression.png`); zero console errors at boot or navigation. New 3 templates not yet visually verified at runtime because no preflop/equity/exceptions manifests exist yet — that work is gated on PRF-G5-A (Phase A preflop authoring) and PRF-G5-C (Phase C equity+exceptions authoring). Unit-test coverage substitutes for runtime verification until those phases ship. **Deferred to S21+:** PrintPreview + PrintConfirmationModal sub-views / StalenessBanner / Stakes/stacks/status filter chips beyond class / "Print selected batch" + "Print preview" buttons / URL routing for sub-views (currently component-state-based). **S21 (print flow end-to-end):** 4 new components + 1 stylesheet + 1 constants file + 4 new test files + 1 extended (~70 net new tests). (1) `ClassDispatchedTemplate.jsx` — lifted from `CardDetail.jsx` to a shared file so PrintPreview can compose without duplicating the switch logic. CardDetail now imports it; deleted 4 redundant template imports. (2) `PrintControls.jsx` — 4 toggle groups: Page (Letter/A4) / Cards/sheet (12/6/4/1) / Color (auto/bw) / Footer (Lineage on/off + disabled "Personal codex: Phase 2+" with tooltip per AP-PRF-09). aria-pressed reflects state; ≥44×44 tap targets; writes via `refresher.patchConfig({ printPreferences })` debounced 400ms with pending-patch ref accumulation so rapid toggles collapse into a single W-URC-1 IDB write. **Longhand `borderWidth/borderStyle/borderColor`** (not `border` shorthand) — caught a real React rerender warning in Playwright. Surfaces writer-rejection inline as role=alert. (3) `PrintPreview.jsx` — sub-view rendering all active cards in `.refresher-print-page[data-cards-per-sheet]` grids inside a `.print-preview-container` WYSIWYG wrapper. Cards chunked into pages of N where N = cardsPerSheet. Cards rendered via the lifted ClassDispatchedTemplate with a memoized `runtime` prop (`{ engineVersion: 'v123', appVersion: 'v123' }` from new `src/constants/runtimeVersions.js` — replaces the `unknown-engine`/`unknown-app` fallbacks). PrintControls panel mounted above grid; "Send to print dialog →" primary teal button below. `handleSendToPrintDialog` builds the W-URC-3 batch context (cardIds + perCardSnapshots) from a single `activeCards` array enforcing I-WR-6 1:1 contract from one source. Empty-state copy + button disabled when no active cards. (4) `PrintConfirmationModal.jsx` — modal mirroring SuppressConfirmModal/LineageModal patterns (aria-modal, aria-labelledby, Esc + backdrop close, focus trap, ≥44px tap targets) but **without 2-tap checkbox gate** (printing is normal owner action). Native `<input type="date">` defaulting to today + optional batch-label input + factual reminder copy per CD-1. Confirm calls parent's `onConfirm({printedAt, label})` which awaits `recordPrintBatch(payload)` then fires `window.print()` ONLY if the IDB write resolves; re-throws on writer rejection so modal surfaces the error inline as role=alert and does NOT auto-close. (5) `src/styles/printable-refresher.css` — global stylesheet imported via `src/main.jsx` between index.css and dev seeders. `@page { size: letter; margin: 0.4in }` default; `.refresher-print-page[data-cards-per-sheet="12|6|4|1"]` CSS Grid templates (3×4 / 2×3 / 2×2 / 1×1) with row heights tuned to Letter usable area; `body[data-page-size="a4"]` variant compresses for A4; `[data-color-mode="bw"]` grayscale filter; `[data-include-lineage="off"]` hides lineage regions; `@media print { body > #root > *:not(.print-preview-host) { display: none !important } }` hides app chrome during print; `.refresher-print-page { page-break-after: always }` for clean N-page output. (6) `src/constants/runtimeVersions.js` — tiny constants file. (7) `index.jsx` MODIFIED — added `[showPrintPreview, setShowPrintPreview]` + `[pendingPrintConfirm, setPendingPrintConfirm]` state + 5 new handlers; ternary render extended `showPrintPreview ? PrintPreview : selectedCard ? CardDetail : CardCatalog`; PrintConfirmationModal as overlay; "Print preview →" button replaces footer placeholder. (8) `src/main.jsx` MODIFIED — added 1 CSS import. (9) `CardDetail.jsx` MODIFIED — import lifted dispatcher; deleted 4 template imports. (10) Tests: ClassDispatchedTemplate 7 + PrintControls 18 + PrintPreview 16 + PrintConfirmationModal 23 + PrintableRefresherView +6. **All 215/215 PRF view-scope tests green across 14 files (was 145/10 at S20 baseline; +70 tests / +4 files); full PRF scope 784/784 green across 30 test files** in isolation. CI bundle-grep gate passes. **Visual verification via Playwright:** loaded `#printableRefresher` → catalog → Print preview → 4-up grid reflow (6 cards · 2 pages, IDB-persisted across reload) → Send to print dialog → modal opens with summary + today's date + optional label + factual reminders + enabled Confirm → Esc closes back to PrintPreview. **Zero console errors throughout the full flow.** Real browser-runtime bug caught + fixed by Playwright: PrintControls' toggle buttons emitted React "Removing border borderColor" rerender warnings — fixed by switching from shorthand to longhand triple. **Deferred to S22+:** StalenessBanner / filter expansion (phases + tiers) / URL routing for sub-views / PRF-G5-PDF Playwright snapshot baseline (now unblocked by S21) / PRF-G5-A Phase A preflop card authoring / PRF-G5-C Phase C equity + exceptions card authoring. **S22 (StalenessBanner + stale-only filter):** 1 new component + 1 new test file + extended view test (~25 net new tests). (1) `StalenessBanner.jsx` — passive amber `#451a03` background with `#fde68a` text; `role="status"` + `aria-live="polite"` so the count change is announced; copy matches surface spec §Behavior #9 ("Your YYYY-MM-DD batch [optional label]: M of N cards current, K stale"); falls back to "Your latest batch" when printedAt is missing/invalid; uses singular "card" when batchCardCount === 1; ≥44px tap targets on Review + Dismiss buttons per H-ML06; CD-1 factual copy verified mechanically (no streak / consecutive / "you've printed" / "days since" prose per AP-PRF-03). (2) `index.jsx` MODIFIED — added 2 state slots `[stalenessBannerDismissed, setStalenessBannerDismissed]` + `[showOnlyStale, setShowOnlyStale]` (both session-only; no persistence per surface spec — banner returns next session). 2 new handlers: `handleReviewStale` (sets showOnlyStale=true + dismisses banner) + `handleDismissBanner`. `stalenessBannerData` memo computes `{staleCount, batchCardCount, batchPrintedAt, batchLabel}` from `getStaleCards()` + most-recent batch (`printBatches[0]`). `stalenessBannerVisible` boolean gates on: !showPrintPreview && !selectedCard && !dismissed && **AP-PRF-08 opt-in**: `refresher.config?.notifications?.staleness === true`. `filteredSorted` now applies `staleCardIds` filter when showOnlyStale=true. "Showing N of M" status appended with "· stale only" + "Show all cards" reset button when filter active. Banner mounted above the catalog status (only in catalog mode). Reordered `selectedCard` memo above `stalenessBannerVisible` to fix temporal-dead-zone error. (3) Tests: `StalenessBanner.test.jsx` (15 tests): render conditional / copy format / batch label rendered when present / latest-batch fallback / singular form / data-stale-count attr / role=status + aria-live=polite / ≥44px tap targets / Review fires onReviewStale / Dismiss fires onDismiss / defensive missing-callback / CD-1 factual copy (no streak/consecutive/days-since prose). `PrintableRefresherView.test.jsx` extended (+10 tests, also restructured shared mock to mutable `mockRefresherState` so tests can override `notifications.staleness` + `getStaleCards` per scenario): banner renders when opt-in ON + stale exists / hidden by default (AP-PRF-08 OFF) / hidden when no stale / hidden when no batches / Dismiss hides for session / Review filters catalog to stale-only with "stale only" indicator + "Show all cards" reset / Show all cards restores unfiltered / Review also dismisses banner / banner hidden in print-preview / banner hidden in card-detail. **All 240/240 PRF view-scope tests green across 15 files (was 215/14 at S21 baseline; +25 tests / +1 file)**; CI bundle-grep gate passes. **Visual verification via Playwright:** with default IDB state, banner correctly hidden (AP-PRF-08 default OFF respected). Seeded notifications.staleness=true + a stub batch with diverging hashes via `browser_evaluate` IDB manipulation; reload → banner renders with copy "Your 2026-04-24 batch \"demo-stale\": 0 of 2 cards current, 2 stale." + Review/Dismiss buttons visible; both seeded cards show "⚠ Stale" status in catalog rows; clicked Review stale cards → catalog filtered to 2 stale cards with "Showing 2 of 6 cards · stale only" + "Show all cards" button + banner dismissed. Cleanup: flag reset to false + stub batch deleted. **Zero console errors throughout.** **Doctrine choices:** (a) **AP-PRF-08 opt-in honored at the gate** — banner is structurally complete + dormant in Phase 1 because `notifications.staleness` defaults to false. Settings UI to flip the flag is future work; until then, the banner is reachable only via dev-mode IDB seeding. (b) **Session-only dismiss** — surface spec explicitly forbids persisted dismissal records ("no days-since-dismissal counter, no nag pattern" per §Behavior #9). State is a parent-level boolean reset on each session start. (c) **Review-stale also dismisses the banner** — once owner has acknowledged + chosen to act, the banner shouldn't compete for attention with the filtered catalog they're now reviewing. (d) **showOnlyStale filter is session-only** — applied in `filteredSorted` after the existing class/phase/tier filters; "Show all cards" reset button always visible when active. **Deferred to S23+:** filter expansion (phases + tiers) / URL routing for sub-views / PRF-G5-PDF Playwright snapshot baseline / PRF-G5-A Phase A preflop card authoring / PRF-G5-C Phase C equity + exceptions card authoring / Settings UI to expose `notifications.staleness` opt-in toggle (unblocks the banner in production). **S23 (PRF-G5-PDF Playwright snapshot baseline):** Visual regression infrastructure shipped + 6-snapshot starter baseline. (1) `@playwright/test ^1.59.1` added as devDep + chromium browser binary installed via `npx playwright install chromium`. (2) `playwright.config.js` — chromium project (firefox/webkit deferred to S24+ for the cross-browser pass), Galaxy A22 1600×720 viewport per CLAUDE.md mobile target, port 5193 with `--strictPort`, `webServer` auto-spawns `npm run dev`, `fullyParallel: false` + `workers: 1` (single dev server + IDB). `expect.toHaveScreenshot` defaults: `maxDiffPixels: 100`, `threshold: 0.2`, `animations: disabled`. Output dir under `tests/playwright/.test-results/` (gitignored). (3) `tests/playwright/printable-refresher.spec.js` — 6-snapshot starter baseline with documented helper pattern: `resetRefresherIDB(page)` wipes printBatches + resets config defaults via `page.evaluate()` IDB manipulation; `setPrintPrefs(page, prefs)` patches `userRefresherConfig.printPreferences` directly so PrintPreview hydrates with desired layout (faster + less flaky than driving toggle UI); `seedStaleScenario(page)` flips `notifications.staleness=true` + inserts a stub `printBatch` with diverging contentHashes; `gotoRefresher(page)` reloads + clicks sidebar Refresher button (deep-link via `page.goto('/#hash')` doesn't re-trigger React's `useEffect` on Playwright's same-origin nav so the click pattern is more reliable). 6 snapshots captured: catalog default 6 math cards (no banner, no filter); PrintPreview default 12-up Letter Color (all 6 cards on 1 page); PrintPreview 4-up Letter Color (6 cards across 2 pages); PrintPreview 1-up Letter B&W page-1 only (single card per page, grayscale filter applied); PrintConfirmationModal opened on top of PrintPreview (date-input masked to avoid baseline drift on date change); catalog with StalenessBanner visible after IDB seed. (4) `package.json` MODIFIED — 3 new npm scripts: `test:visual` (run + compare against baselines), `test:visual:update` (regenerate baselines after intentional UI changes), `test:visual:debug` (headed run for diagnosing failures). (5) `.gitignore` MODIFIED — added `tests/playwright/.test-results/` + `playwright-report/` + `test-results/` to ignore per-run artifacts; added `!tests/playwright/**/*.png` exception so snapshot baselines ARE tracked (visual regression source-of-truth). (6) `tests/playwright/printable-refresher.spec.js-snapshots/` — 6 baseline PNGs (450 KB total) committed to repo: catalog-default + catalog-staleness-banner + print-preview-12up/4up/1up + print-confirmation-modal. **All 6 baselines stable on second run (8s).** **Real bug caught + fixed by Playwright:** initial spec used `page.goto('/#printableRefresher')` for navigation, but Playwright treats hash-only changes on same-origin SPA as no-op for `useEffect` re-fires, so the HASH_TO_SCREEN handler in PokerTracker.jsx didn't route to the catalog. All 6 tests timed out waiting for "Print preview →". Fix: rewrote `gotoRefresher` to `page.reload()` + `page.click('button:has-text("Refresher")')` — mirrors how an owner navigates + always works deterministically. Documented in spec doc-comment for future session reference. **Doctrine choices:** (a) **IDB seeding via `page.evaluate`, not UI clicks** — for state-dependent snapshots (banner-visible, custom prefs), seeding directly into IDB before navigation is faster + less flaky than driving toggle UI. Pattern documented in helpers. (b) **Region-targeted screenshots, not full viewport** — `expect(page.getByRole('main')).toHaveScreenshot()` and `expect(page.locator('.print-preview-container')).toHaveScreenshot()` keep baselines tight against scroll-position drift. Full-viewport snapshots reserved for whole-page regression checks. (c) **Date-input masking** in PrintConfirmationModal snapshot — `mask: [dialog.getByLabel('Print date')]` paints a uniform overlay so today's-date drift doesn't flake the baseline daily. Pattern reusable for any time-dependent UI. (d) **Cross-browser deferred to S24+** — chromium-only baseline ships first; firefox + webkit regression coverage when there's evidence of browser-specific rendering divergence (so far there isn't). (e) **`fullyParallel: false`** — single dev server + IDB; parallel workers would race on IDB state. Trade-off: slower test runs (8s for 6 snapshots) for deterministic state. **Deferred to S24+:** cross-browser baselines (firefox + webkit per snapshot = 18 baselines total); A4 page-size variants (currently only Letter snapshotted; A4 grids should differ at 12-up/6-up); CardDetail snapshots per class (only math has manifests today); 16-permutation full grid×page×color matrix (Phase 1 starter ships 5 representative permutations); `data-include-lineage="off"` snapshot (currently only `on`); SuppressConfirmModal + LineageModal snapshots (already unit-tested but visual regression would catch CSS drift); CI integration (Playwright config has `forbidOnly: !!process.env.CI` ready; just needs a CI workflow file when GitHub Actions ships for the project). **S24 (Settings staleness toggle — unblocks AP-PRF-08 banner for production):** 1 new component + 1 new test file + 2 file edits + 2 new Playwright snapshots. (1) `src/components/views/SettingsView/RefresherSettings.jsx` — new "Printable Refresher" section in the Settings 2-column grid. Single toggle: "Show staleness banner" with Off/On buttons (Tailwind, GOLD heading per existing section pattern). aria-pressed reflects current `notifications.staleness` value; ≥44px tap targets per H-ML06 via `min-h-[44px]` Tailwind utility. Pending-flag guards rapid double-clicks. Calls `refresher.patchConfig({ notifications: { staleness: bool } })` (W-URC-1) on click; surfaces writer-rejection inline as `role="alert"` (red styling). data-testid="refresher-settings-section" for CI introspection. Factual explainer copy ("Surface a banner... Default is off.") per CD-1; no engagement / no nag. (2) `src/components/views/SettingsView/SettingsView.jsx` MODIFIED — imports + mounts `<RefresherSettings />` between `<DataAndAbout />` and `<ErrorLogPanel />` in the existing 2-col grid. No prop threading needed; component composes `useRefresher()` directly. (3) Tests: `RefresherSettings.test.jsx` (12 tests): render heading + label + explainer / Off+On buttons in labeled group / data-testid attr / aria-pressed reflects state for Off-default + On-when-true + Off-default-when-missing-field / Tailwind min-h-[44px] class on all buttons / clicking On dispatches `patchConfig({ notifications: { staleness: true } })` / clicking Off dispatches inverse / rapid double-click safely guarded by pending flag (only first call dispatches; second is no-op while in-flight) / writer rejection surfaces inline as role=alert. **All 12/12 green.** PRF view-scope unchanged at 240/15. (4) `tests/playwright/printable-refresher.spec.js` extended with 2 new tests + 1 new snapshot: "Settings — Refresher section visible (S24)" snapshots the section card via `[data-testid="refresher-settings-section"]` locator after navigation via hash deep-link (`window.location.hash = '#settings'` then `page.reload()` — Refresher view hides sidebar, so click-based nav doesn't work from there); "end-to-end: enable staleness in Settings → banner appears in Refresher (S24)" exercises the full flow: pre-seed stale batch → confirm banner hidden in Refresher (default OFF) → navigate to Settings via hash → click On → verify aria-pressed → navigate back to Refresher via hash → banner appears. **All 8/8 Playwright snapshots stable on second run (9.5s)**, including the new "settings-refresher-section.png" baseline. **Real bug caught + fixed by Playwright:** initial e2e test used sidebar `getByRole('button', { name: 'Settings' }).click()` to navigate from Refresher to Settings — failed because the Refresher view hides the CollapsibleSidebar (it's a full-screen view with its own header; sidebar is Table-view-only). Fix: use `window.location.hash = '#settings'` + `page.reload()` (set hash BEFORE reload, mirroring the gotoRefresher pattern documented in S23). Documented inline in the e2e test for future reference. **Doctrine choices:** (a) **Toggle is gated at writer level, not component level** — `notifications.staleness` value passes through `patchConfig({ notifications })` to W-URC-1 which validates the nested key. Component just calls the writer; writer enforces. Defense-in-depth without UI-level validation duplication. (b) **No debounce on settings-toggle writes** — settings toggles are owner-deliberate, single-click actions. Unlike PrintControls (which can fire 4+ toggles in rapid succession), settings rarely sees rapid changes. Pending-flag guards double-click; that's the only protection needed. (c) **Mount in 2-col grid between DataAndAbout and ErrorLogPanel** — no semantic ordering rationale; just a visually-balanced position in the existing layout. Owner can find it scrolling the Settings page. (d) **Hash-deep-link for cross-view test navigation** — when the destination view hides the sidebar (like Refresher → Settings), use `window.location.hash = '#hash'` + `page.reload()`. Pattern reusable for any cross-view e2e tests. **Deferred to S25+:** the deferred-list from S23 still applies (cross-browser, A4 grids, CardDetail per class, 16-permutation matrix, etc.); also: filter expansion (phases + tiers) / URL routing for sub-views / PRF-G5-A Phase A preflop authoring / PRF-G5-C Phase C equity + exceptions authoring. |
| PRF-G5-HK | P2 | COMPLETE (2026-04-26) | React hooks wiring — context + reducer + 2 hooks + bundle-grep CI | **Implementation:** `src/constants/refresherConstants.js` (REFRESHER_ACTIONS frozen enum 3 actions: REFRESHER_HYDRATED + REFRESHER_CONFIG_REPLACED + REFRESHER_BATCH_APPENDED + initialRefresherState [config from buildDefaultRefresherConfig + printBatches:[] + isReady:false + schemaVersion '1.0.0'] + REFRESHER_STATE_SCHEMA + REFRESHER_VIEW_LOCALSTORAGE_KEY + initialRefresherView + VALID_SORT_VALUES); `src/reducers/refresherReducer.js` (createValidatedReducer wrapper, 3 case branches: REFRESHER_HYDRATED bulk-loads config + printBatches sets isReady true with defensive fallbacks for missing/non-array payloads; REFRESHER_CONFIG_REPLACED validates `config.id === 'singleton'` + replaces config slice; REFRESHER_BATCH_APPENDED prepends batch to most-recent-first array + replaces config slice for lastExportAt update; defensive returns same-state on invalid payloads; defense-in-depth duplicate-batchId drop while still updating config); `src/hooks/useRefresherPersistence.js` (hydrate-only — Promise.all over getRefresherConfig + getAllPrintBatches; single REFRESHER_HYDRATED dispatch; isReady true after hydration including failure path with empty payload fallback per app-remains-usable convention; cancellation guard via local cancelled flag); `src/hooks/useRefresherView.js` (localStorage-backed UI state at key `pokerTracker.refresherView.v1`; defensive merge on load coerces invalid sort values + non-array filter fields back to defaults; setFilter partial-merge / setSort enum-validated / setShowSuppressed strict-equality / resetView; persists synchronously on every change since localStorage is fast enough that debouncing would be over-engineering for this use case); `src/contexts/RefresherContext.jsx` (RefresherProvider receives state + dispatch + cardRegistry; composes useRefresherPersistence; exposes 4 writer-action helpers patchConfig/setCardVisibility/setClassSuppressed/recordPrintBatch each calling appropriate W-URC-* writer + dispatching REFRESHER_CONFIG_REPLACED or REFRESHER_BATCH_APPENDED with returned record; W-URC-3 helper composes updatedConfig from existing reducer state + new lastExportAt since W-URC-3 invokes W-URC-1 internally; exposes 6 selector helpers getAllCards/getActiveCards/getPinnedCards/getSuppressedCards/getCardsForBatchPrint/getStaleCards via useCallback memoized over cardRegistry+config+printBatches; useRefresher consumer hook throws helpful error outside Provider). **CI script** `scripts/check-refresher-bundle.sh` (executable bash CI-grep per print-css-doctrine.md §Forbidden mechanisms; scans 7 PRF surface paths for forbidden import patterns — html2canvas / jspdf / pdf-lib via 6 regex variants covering both `from '...'` and `require('...')` shapes; smoke-tested ✅ on current tree). **Tests (4 files, 76 new tests):** `refresherReducer.test.js` (19 tests — initial state 2 + REFRESHER_HYDRATED 5 incl. failure-path-still-ready + REFRESHER_CONFIG_REPLACED 5 incl. wrong-id defensive + REFRESHER_BATCH_APPENDED 7 incl. duplicate-batchId-drop with config-still-updated); `useRefresherPersistence.test.js` (5 tests — parallel-reads on mount + REFRESHER_HYDRATED dispatch shape + isReady transitions + failure-path-still-ready + cancellation guard via blocked Promise + post-unmount); `useRefresherView.test.js` (28 tests — initial state 5 incl. malformed JSON fallback + non-array filter coercion + invalid sort coercion / setFilter 4 / setSort 3 / setShowSuppressed 3 / resetView 2); `RefresherContext.test.jsx` (24 tests — Provider basics 4 + outside-Provider throw 1 + patchConfig W-URC-1 path 2 incl. error propagation + setCardVisibility 1 + setClassSuppressed 1 + recordPrintBatch 1 + 6 selector helpers including getStaleCards diverging-hash detection). **All 76/76 green.** Full PRF + persistence + reducer + hooks + context scope: 352/352 green across 15 test files. **Doctrine choices:** (a) Post-write update pattern (UI handler awaits writer → dispatch with returned record) chosen over EAL's per-slice diff-write because writers.js already validates field-ownership; per-slice diff-write would bypass writer validation. (b) Reducer initial state uses non-frozen `buildDefaultRefresherConfig()` because reducers spread+replace the slice; freezing would break that path. (c) View hook persists synchronously not debounced — localStorage is fast enough; debounce would be over-engineering. (d) View hook uses strict-equality === true for showSuppressed coercion to reject truthy non-boolean values like 'yes'. (e) Provider helper recordPrintBatch composes updatedConfig client-side from existing reducer state + new lastExportAt instead of re-reading IDB after W-URC-3 — single dispatch updates both slices atomically at reducer level |
| PRF-P2-PE | P3 | LATER (Phase 2+) | Personalization opt-in (deferred) | Explicit opt-in gesture; default OFF; red line #16 compliance |
| PRF-P2-CX | P3 | LATER (Phase 2+) | Personal codex export (DS-61 implementation) | PRF-NEW-5 JTBD served; EAL anchor integration |

Gate 4 **CLOSED 2026-04-25** — all 12 carry-forwards COMPLETE (ACP + AP + CD + H + S1 + S2 + J + W + CI + SL + CSS + MIG). **Gate 5 (Implementation) NEXT-ready.** Recommended kickoff order: **(1) PRF-G5-CI** — implement `contentDrift.test.js` + `cardRegistry.js` + `lineage.js` + `stalenessDiff.js` infrastructure (non-negotiable prerequisite — must ship green BEFORE any card-authoring PR merges). **(2) PRF-G5-MIG** — IDB migration scaffolded with seed singleton + index. **(3) Persistence + selector wiring** — `useRefresherConfig` + `useRefresherView` + 6 selectors per `selectors.md`. **(4) PRF-G5-RL + PRF-G5-RI + PRF-G5-DS + PRF-G5-LG** — red-line / reducer-boundary / durable-suppression / lineage footer test scaffolds. **(5) PRF-G5-B** — Phase B Math Tables (6 cards: auto-profit / geometric / pot-odds / implied / binomial / SPR zones) — clear-starting-point card-authoring session. **(6) Q5 differentiation demo** at design review with PRF-G5-A Phase A Preflop sample card → owner decides Phase A go/no-go. **(7) PRF-G5-C** Phase C Texture-Equity + Exceptions (last; per-card Voice-3-equivalent fidelity review; equity bucket-definition glossary prerequisite). **(8) PRF-G5-PDF** Playwright cross-browser print-snapshot tests over MVP cards. See `docs/design/audits/2026-04-24-blindspot-printable-refresher-rerun.md` + `docs/projects/printable-refresher.project.md` §Acceptance Criteria + all 12 shipped Gate 4 artifacts (5 in `docs/design/` + 7 in `docs/projects/printable-refresher/`).

---

## Status Key

| Status | Meaning |
|--------|---------|
| IN_PROGRESS | Actively being worked by a session (see Claimed By) |
| NEXT | Ready to start, not yet claimed |
| LATER | Planned but not prioritized |
| BLOCKED | Waiting on a prerequisite |
| PAUSED | Started but on hold |
| REVIEW | From eng-engine audit, needs owner approval |

---

## NEXT — Ready to Start

### Range Lab — In-App Flopzilla-Class Study Surface (project: `docs/projects/range-lab.project.md`)

| ID | Pri | Status | Description | Accept Criteria |
|----|-----|--------|-------------|-----------------|
| RL-G1 | P2 | COMPLETE (2026-04-22) | Gate 1 — Entry (scope + personas + JTBD + gap analysis) | Artifact at `docs/design/audits/2026-04-22-entry-range-lab.md`. Verdict YELLOW → Gate 2 required. |
| RL-G2 | P2 | NEXT | Gate 2 — Blind-Spot Roundtable | 5 stages complete; verdict GREEN/YELLOW/RED; follow-ups list populated. Addresses 4 open owner questions from RL-G1. |
| RL-G3 | P2 | BLOCKED by RL-G2 | Gate 3 — Research (conditional on RL-G2 YELLOW/RED) | DS-52/53/54/55 authored as `Proposed` or explicitly rejected. Owner interview if Gate 2 requests. |
| RL-G4 | P2 | BLOCKED by RL-G2/RL-G3 | Gate 4 — Design | `surfaces/postflop-drills.md` updated with Range Lab section + interaction spec + mobile decision. |
| RL-P0 | P3 | COMPLETE (2026-04-22) | Phase 0 — `rangeToString()` reverse serializer in `src/utils/pokerCore/rangeMatrix.js` | Shipped. 42 new tests. `parseRangeString` extended to accept `:weight` suffix (additive). Round-trips over PREFLOP_CHARTS + 13 canonical inputs. 216/216 pokerCore + 373/373 postflopDrillContent regression green. |
| RL-P1 | P2 | BLOCKED by RL-G4 + RL-P0 | Phase 1 — Interactive range paint + turn/river in ExplorerMode | User can paint arbitrary range; `BoardPicker` extends to 5-card; painted range persists for session. |
| RL-P2 | P2 | BLOCKED by RL-P1 | Phase 2 — Subrange filters + equity histogram + range comparison | Flopzilla parity achieved. |
| RL-P3 | P3 | BLOCKED by RL-P2 | Phase 3 — AI-native differentiators (archetype overlays, tendency auto-populate, weakness annotations) | The "better than Flopzilla" claim cashed in. |
| RL-P4 | P3 | BLOCKED by RL-P3 | Phase 4 — Hand-history paste → per-street range evolution | Stretch. |
| RL-P5 | P3 | BLOCKED by RL-P3 | Phase 5 — Save custom ranges + cross-surface cross-links (LineWalkthrough / HandReplay "inspect in Range Lab") | Stretch. |

Gate 1 flagged 4 open questions for owner before Gate 2: phasing-scope-for-Gate-2, Coach persona weighting, Drills Consolidation collision, mobile hard-req vs desktop-only. See RL-G1 artifact §"Open questions for owner".

---

### Upper-Surface Reasoning Program (rubric: `docs/upper-surface/RUBRIC.md`, plan: `C:\Users\chris\.claude\plans\cryptic-moseying-sprout.md`)

Owner-described purpose: forced exhaustive multi-perspective theoretical analyses of decision nodes — exposes weak claims on the page, generates a gradeable standard, surfaces to user as pre-session drill content. Plan completed 2026-04-23 producing rubric (v1 → v2.2), 2 pilot artifacts (flop + river), 2 self-audits, 2 leading-theory comparisons, 2 drill cards, and Gate 1 entry for pre-session drill UX.

| ID | Pri | Status | Description | Accept Criteria |
|----|-----|--------|-------------|-----------------|
| US-1 | P2 | NEXT | **Corpus scaling** — author additional upper-surface artifacts (target ≥15 total: 3+ lines × ~5 nodes each) to make pre-session drill UX meaningful | 15+ artifacts in `docs/upper-surface/reasoning-artifacts/`. v2.2-rubric-compliant. Each with companion audit + comparison + drill card. |
| US-2 | P3 | LATER | Pilot refits to v2.2 (full pass) | Flop + river artifacts re-rated to v2.2 with full §13 D13 reflexive checks applied (current refits are minimal — §3 + §11 row-tag for flop, §5 + §11 row-tag for river). All v2/v2.1-era F-findings tracked in audits closed. |
| US-3 | P3 | BLOCKED by US-1 | Pre-Session Drill Gate 2 — Blind-Spot Roundtable | Per `docs/design/audits/2026-04-23-entry-pre-session-drill.md` recommendation, defer until corpus reaches ≥15 artifacts. 5-stage roundtable; verdict GREEN/YELLOW/RED; addresses 6 open owner questions from Gate 1. |
| US-4 | P3 | BLOCKED by US-3 | Pre-Session Drill Gate 3 — Research (conditional on US-3 verdict) | Author DS-56/57/58/59 as `Proposed` in `drills-and-study.md`; possibly owner-interview on session-prep workflow. |
| US-5 | P3 | BLOCKED by US-3/US-4 | Pre-Session Drill Gate 4 — Design | `surfaces/postflop-drills.md` updated with Pre-Session mode addition + interaction spec + mobile decision. |
| US-6 | P3 | LATER | LSW soft-flag tightening | Stage 4 surfaced 2 soft flags (flop: equity-vs-subset distinction in LSW external-validation; river: stake-caveat for "25-35% bluffs"). Owner approved no re-audits; documentation-only fix. Add one-line scope-caveats to LSW external-validation logs at next LSW-program touch. **2026-04-23 update:** LSW audits A5-A8 (shipped 2026-04-23) now use `A-with-nuance` / `A-with-stake-caveat` categorization inline, codifying the discipline forward. Retroactive tightening of A1 + the pilot upper-surface artifacts still pending. Two soft-flag targets (flop equity-vs-subset; river 25-35% bluffs stake-caveat) remain un-applied. |

Plan completion summary: 13 docs in `docs/upper-surface/` + 1 in `docs/design/audits/` + 2 §9 entries appended to `POKER_THEORY.md` (§9.4, §9.5). Drill UX RED-blocked on corpus growth (US-1) — design gates execute when corpus is meaningful.

---

### Played-Hand Review Protocol (project: `docs/projects/played-hand-review-protocol.project.md`)

Owner-described purpose: close the depth gap between the theoretical programs (Upper-Surface reasoning artifacts, LSW line-study) and the shallow played-hand review surface by making `HandReplayView` a **consumer** of the theoretical library. Flag a played hand → resolve each decision to its canonical upper-surface / LSW analog → render linked ledger + counterfactual tree + drill card in a modal overlay. **Narrow / bridge scope** — consumer-only in v1. Phase 1 (Gates 1+2+3) + Phase 2 (SPOT-KEY spike) CLOSED 2026-04-23. **PAUSED** behind LSW + exploit-deviation per owner Option C directive.

| ID | Pri | Status | Description | Accept Criteria |
|----|-----|--------|-------------|-----------------|
| HRP-G1 | P2 | COMPLETE (2026-04-23) | Gate 1 — Entry | Artifact at `docs/design/audits/2026-04-23-entry-played-hand-review-protocol.md`. Verdict YELLOW (3 JTBD gaps in existing SR domain; 0 persona gaps). |
| HRP-G2 | P2 | COMPLETE (2026-04-23) | Gate 2 — Blind-Spot Roundtable | Artifact at `docs/design/audits/2026-04-23-blindspot-played-hand-review-protocol.md`. Six custom voices × 5 stages; verdict YELLOW; 7 new JTBDs; 7 Stage-C rules; 8 Stage-E rules; material design shift (ledger = modal overlay, not inline). |
| HRP-G3 | P2 | COMPLETE (2026-04-23) | Gate 3 — JTBD authoring | SR-28..34 authored in `docs/design/jtbd/domains/session-review.md` (4 Active: SR-28 deep review, SR-29 analog resolution, SR-30 counterfactual tree, SR-31 flag queue; 3 Proposed: SR-32, SR-33, SR-34). ATLAS.md updated. No new domain, no new personas. |
| HRP-SPIKE | P2 | COMPLETE (2026-04-23) | SPOT-KEY feasibility spike | Artifact at `docs/projects/played-hand-review-protocol/spot-key-spike.md`. GREEN verdict. ~70% of dimensions free from existing helpers (texture + position vocabularies match upper-surface encoding verbatim); ~30 LoC shallow build (pot-type + shorthand); ~100 LoC hard build (node classifier). Typical coverage ~60–70% confident-or-partial. |
| HRP-G4-SPEC | P2 | PAUSED | Gate 4 — Surface specs | Update `surfaces/hand-replay-view.md` (ledger-link + modal overlay + match confidence + L-key shortcut), `surfaces/analysis-view.md` (HandBrowser flag filter + flag indicator + last-reviewed column), `surfaces/showdown-view.md` (HE-17 producer entry). Author new `surfaces/hand-review-modal.md`. Schema spec for `hand.flags[]` + `hand.reviewState`. Effort: M. |
| HRP-E-RESOLVER | P2 | BLOCKED by HRP-G4-SPEC | Stream E — `spotResolver/` engine module | Per spike's proposed architecture: 7 files in `src/utils/spotResolver/` (extractor, pot-type inference, board shorthand, node classifier, corpus index, match scorer, golden tests). ~200 LoC + ~150 LoC test corpus. No imports from exploitEngine/ or rangeEngine/. Effort: M-L. |
| HRP-E-SCHEMA | P2 | BLOCKED by HRP-G4-SPEC | Stream E — IDB schema migration | `hand.flags[]` + `hand.reviewState` additive fields. Backwards-compatible load path for legacy hands. IDB version bump. Effort: S. |
| HRP-E-TREE-EXPOSE | P2 | NEXT (independent) | Stream E — Expose depth-2/3 counterfactual tree | `gameTreeEvaluator.js` output is currently discarded at `useHandReplayAnalysis` boundary. Thread through so replay analysis carries the already-computed tree. Feeds SR-30. Can ship independently of other HRP work. Effort: S. |
| HRP-U-BROWSER | P2 | BLOCKED by HRP-G4-SPEC + HRP-E-SCHEMA | Stream U — HandBrowser flag filter + indicator + last-reviewed column | HE-17 consumer side. Feature-flagged rollout. Effort: S-M. |
| HRP-U-MODAL | P2 | BLOCKED by HRP-G4-SPEC + HRP-E-RESOLVER + HRP-E-TREE-EXPOSE | Stream U — hand-review-modal | Tabs (summary / claims ledger / counterfactual tree / drill card / full artifact). Progressive disclosure. Match confidence. Audit-state propagation from linked artifact. Effort: L. |
| HRP-U-SHOWDOWN | P2 | BLOCKED by HRP-G4-SPEC + HRP-E-SCHEMA | Stream U — ShowdownView HE-17 producer entry | Flag-for-review gesture at record commit time. Effort: S. |
| HRP-U-POLISH | P2 | BLOCKED by HRP-U-MODAL | Stream U — First-open tutorial + inline glossary + L keyboard shortcut | Gate 2 Stage-E rules. Touch-target + dead-zone rules. Effort: S-M. |
| HRP-COORD-MH12 | P2 | LATER | Coordinate with exploit-deviation on `AssumptionCard` shared component | Shared infra opportunity per Gate 2 Stage D. Whichever project reaches the ledger-row render first authors the component; the other consumes. Effort: S. |

Handoff: `.claude/handoffs/hrp-gates-1-2-3-spike.md`. Two verification items deferred to HRP-G4-SPEC entry: (1) HE-17 implementation status — is `hand.flags` field present today or atlas-listed only? (2) Offline precache — are upper-surface `.md` files + LSW `lines.js` in PWA precache manifest?

---

### Design Program Compliance (project: `.claude/projects/design-compliance.md`, roadmap: `docs/design/ROADMAP.md`)

| ID | Pri | Status | Description | Accept Criteria |
|----|-----|--------|-------------|-----------------|
| DCOMP-W0 | P1 | COMPLETE | Wave 0 — Tier A baseline for 11 remaining surfaces | ✓ 12 surface artifacts authored (2 prior + 6 W0-S1 + 6 W0-S2 + 3 player-selection Session 2). ✓ CATALOG all ● for top-level views. Shipped 2026-04-21 in 2 sessions. |
| DCOMP-W1 | P1 | COMPLETE (2026-04-22) | Wave 1 — Core table surfaces (table/showdown/sessions) audit + P1 fix | **Wave shipped + verified.** 17 findings across S1–S9 (one per session) + S10 visual verification via Playwright MCP. 10 of 12 TableView items verified end-to-end with real-browser interaction (destructive actions, layout wraps, touch-targets, inputMode); F4 AGING (time-gated) + F12 range-reopen (interaction-chain) deferred to owner device walk with code+test coverage standing. ShowdownView + SessionsView + regression spot-checks validated via 6257/6258 test suite + shipped code (111/111 ShowdownView + 595/595 SessionsView + 238/238 reducer subset). S10 artifact at `docs/design/audits/2026-04-22-dcomp-w1-s10-verification.md`. **Unblocks DCOMP-W2 (review surfaces).** |
| DCOMP-W2 | P2 | AUDIT-COMPLETE (2026-04-22) | Wave 2 — Review surfaces (hand-replay/analysis/stats) audit + fix | **All 3 W2 audits shipped 2026-04-22 in one session.** A1 hand-replay-view YELLOW (12 findings: 0 P0, 3 P1, 5 P2, 4 P3) + A2 analysis-view RED (12 findings: **1 P0 — HandReviewPanel.jsx:43 native `confirm()` on hand-delete**, 4 P1, 4 P2, 3 P3) + A3 stats-view YELLOW (9 findings: 0 P0, 3 P1, 3 P2, 3 P3). **Cumulative 33 items queued + 1 P0 remaining.** Cross-audit finding: `handSignificance` module orphan — both HandReplayView + HandBrowser would consume it. Est. 1-2 sessions for W2 P0+P1 fix batch. |
| DCOMP-W4 | P2 | P0-COMPLETE | Wave 4 — Specialized (players/tournament/online/settings) | **All 4 audits shipped 2026-04-22 + 11 of 13 P0s fixed 2026-04-22 in Batches A (0715034) + B (38b334b).** Verdicts: A1 players RED (8) + A2 tournament RED (13) + A3 online RED (13) + A4 settings YELLOW (12) = 46 findings. **12 destructive-action anti-patterns fixed** (Players Clear All Seats + Per-seat Clear + Delete Player, Tournament Advance Level + End Tournament + Eliminate, Online nav-entry-add + alert→toast + ExtensionPanel canonical, Settings Reset + Sim Clear) using Wave-1 toast+undo patterns. **2 P0s deferred**: A3-F4 version-mismatch diagnostic + A3-F5 Continue-Anyway degradation — both require extension handshake protocol changes. **24 P1/P2/P3 polish items queued.** W5 sidebar framework integration now unblocked. |
| DCOMP-W4-A1-F1 | P0 | COMPLETE (Batch A, commit 0715034) | **PlayersView Clear All Seats → toast+undo** | Replace `confirm()` in `handleClearAllSeats` with optimistic clear + 12s toast+undo. Capture previous `seatPlayers` object; on undo, dispatch restore. Reuse TV-F1 pattern. | — |
| DCOMP-W4-A1-F2 | P0 | COMPLETE (Batch B, commit 38b334b) | **PlayersView Delete Player → deferred-delete toast+undo** | Replace blocking modal with "This action cannot be undone" copy with SessionsView-F1 pattern: optimistic visual removal + deferred IDB commit + 12s undo that restores player record + tendency aggregates + seat assignment side effects. M-effort (undo semantics for delete-with-side-effects non-trivial). | — |
| DCOMP-W4-A1-F3 | P0 | COMPLETE (Batch A, commit 0715034) | **PlayersView per-seat Clear → toast+undo** | 24×16 text-link directly mutates state on tap. Add 12s toast+undo using F1's undo-token pattern. | — |
| DCOMP-W4-A1-F5 | P1 | NEXT | **PlayerRow row-actions (Range/Edit/Delete) touch-target refactor** | All three 20–35×20px. Options: (a) icon-only with tooltip + ≥44px hit area, (b) row-level overflow `⋮` menu. 57 buttons visible at once → approach must stay scannable. | — |
| DCOMP-W4-A1-F6 | P1 | COMPLETE (Batch A, commit 0715034) | **SeatAssignmentGrid per-seat Clear ≥44×44** | 24×16 → ≥44×44 via padding. Ship with F3's toast+undo. | — |
| DCOMP-W4-A1-F7 | P2 | PAUSED (Table-Build absorbs 2026-04-26) | **Clear All Seats button height ≥44px** | 102×28 → 102×44 via padding. **Paused** at Table-Build Gate 4 ratification: button lives in the seat-assignment grid that TableBuildView absorbs (`docs/design/surfaces/table-build.md`). Cosmetic P2 on a code path scheduled for replacement is not worth shipping. Reactivate only if Table-Build slips. | — |
| DCOMP-W4-A1-F8 | P2 | NEXT | **PlayerFilters persist (localStorage) + optional collapse** | 8 selects + search + sort = 29% viewport chrome. Phase 1 (S): persist to localStorage per SV-F7 pattern. Phase 2 (M): collapse behind sheet/drawer. **Survives Table-Build Gate 4** — design also informs Table-Build CandidateColumn filter UX. | — |
| DCOMP-W4-A1-F13 | P0 | COMPLETE (2026-04-26) | **Seed players with avatarFeatures** | Dev-ergonomics — all 6 archetypes in `src/__dev__/seedTestData.js` now carry varied `avatarFeatures` (skin / hair + color / beard + color / eyes + color / glasses / hat) tuned per persona for maximum visual differentiation. Each archetype is visually distinct across every category. Verified: syntax-clean; `playersStorage.test.js` 34/34 pass. **Phase 0 prerequisite for Table-Build dev verification — UNBLOCKED.** | — |
| DCOMP-W4-A2-F1 | P0 | COMPLETE (Batch B, commit 38b334b) | **TournamentView End Tournament → deferred-reset toast+undo** | Currently irreversible per artifact line 108. 12s toast + Undo that restores tournamentState + reverts session field writes. M-effort. Reuses SV-F1 deferred-delete pattern. | — |
| DCOMP-W4-A2-F2 | P0 | COMPLETE (Batch B, commit 38b334b) | **ChipStackPanel eliminate → toast+undo** | `recordElimination` commits silently. Surface artifact describes an Undo button that doesn't exist in code. Add `ELIMINATION_UNDO_TOKEN` to reducer capturing `chipStacks[seat]`, `playersRemaining` before decrement, ICM snapshot. 12s toast. | — |
| DCOMP-W4-A2-F3 | P0 | COMPLETE (Batch A, commit 0715034) | **BlindTimerBar Advance Level → two-phase guard** | One-tap skips the current blind level with no confirmation. Two-phase: tap shows preview toast ("Skip to level N+1? Blinds X/Y") + 10s Confirm/Cancel; confirm executes with 12s undo. | — |
| DCOMP-W4-A2-F4 | P1 | NEXT | **updateStack typo guard + inputMode=decimal** | Numeric input commits on blur, no sanity threshold. Add order-of-magnitude detection prompt + toast+undo on commit. `inputMode="decimal"` while touching. | — |
| DCOMP-W4-A2-F5 | P1 | NEXT | **setPlayersRemaining confirm-before with delta** | Unvalidated external field-count. Confirm-before with delta display ("Set 47 → 45? Change: −2"); reject zero/negative. | — |
| DCOMP-W4-A2-F8 | P1 | NEXT | **heroStack fallback → explicit no-hero-seat state** | `Object.values(chipStacks)[0] \|\| 0` can silently pick wrong seat when mySeat null. Render explicit "No hero seat selected — set via TableView" state; don't auto-pick. | — |
| DCOMP-W4-A2-F6 | P2 | NEXT | **Empty-state back button ≥44×44** | Current `px-4 py-2` = ~40×~36, under ≥44 minimum. | — |
| DCOMP-W4-A2-F7 | P2 | NEXT | **End-form inputs inputMode** | Finish Position: `inputMode="numeric"`; Payout: `inputMode="decimal"`. Matches H-ML08 shipped in Wave 1. | — |
| DCOMP-W4-A2-F9 | P2 | NEXT | **Satellite-mode honesty banner** | Current ICM advice is calibrated for standard MTT structure and is actively wrong in satellite mode. Amber banner when `tournamentFormat === 'satellite'`: "Satellite detected — advice may be incorrect". Does not need correct satellite math (deferred L-effort). | — |
| DCOMP-W4-A2-F10 | P2 | NEXT | **contracts/tournament-to-table.md** | Document the 4 fields TableView reads from TournamentContext (`isTournament`, `currentBlinds`, `icmPressure`, `mRatioGuidance`), freshness expectations, TableView fallback. | — |
| DCOMP-W4-A2-F11 | P3 | NEXT | **Verify + document MH-07 context-coupling claim** | Surface artifact claims MH-07 (short-stack push/fold w/ICM) served via TableView consuming icmPressure. Verify in LiveAdviceBar; ties back to 2026-04-21 TableView C3 push/fold verdict mismatch. Either document or escalate. | — |
| DCOMP-W4-A2-F12 | P3 | NEXT | **Tournament persona patches** | Author `circuit-grinder-between-hands.md`; clarify Online MTT Shark use-case on TournamentView; decide home-tournament scope (Ringmaster). | — |
| DCOMP-W4-A2-F13 | P3 | NEXT | **Tournament JTBD atlas patches** | Author TS-43 (blind-out threshold) + TS-44 (rebuy decision at late-reg boundary). | — |
| DCOMP-W4-A3-F1 | P0 | COMPLETE (Batch A, commit 0715034) | **Add "Online" nav entry** | OnlineView currently only reachable via auto-route on first extension payload. Verified via Playwright: no "Online" button in the 7-entry main-app nav. Add it; route to SCREEN.ONLINE. | — |
| DCOMP-W4-A3-F2 | P0 | COMPLETE (Batch A, commit 0715034) | **Resolve OnlineView/ExtensionPanel routing canonical** | Surface artifact lists ExtensionPanel as OnlineView sub-component; `PokerTracker.jsx:105` routes SCREEN.EXTENSION to ExtensionPanel directly as a separate screen. Same artifact-code drift class as TournamentView-A2. Pick canonical: (a) keep both as separate screens + give ExtensionPanel its own surface artifact, or (b) remove SCREEN.EXTENSION + fold into OnlineView. | — |
| DCOMP-W4-A3-F3 | P0 | COMPLETE (Batch A, commit 0715034) | **Import-failure `alert()` → error toast** | `OnlineView.jsx:43` — native alert on import failure. Replace with `showError()` from ToastContext + retry affordance. | — |
| DCOMP-W4-A3-F4 | P0 | NEXT | **Version-mismatch reload: diagnostic + confirm** | `OnlineView.jsx:79` `window.location.reload()` silently. Before reload: render diagnostic (versions, contract delta), require confirm, flag sessionStorage for post-reload verify. M-effort (requires extension handshake carrying version + contract fingerprint). | — |
| DCOMP-W4-A3-F5 | P1 | NEXT | **Continue Anyway graceful degradation** | Dismissing version-mismatch banner silences warning without degrading. Add persistent amber pip on Extension indicator + suppress advice relying on mismatched fields + log for later review. | — |
| DCOMP-W4-A3-F6 | P1 | NEXT | **Import-from-File loading state** | `handleFileImport` has no loading/progress/disabled state during async work. Disable button + spinner + "Importing N hands…" + success toast. | — |
| DCOMP-W4-A3-F8 | P1 | NEXT | **OnlineView inline-styles → Tailwind migration** | 199 lines of `style={{...}}` inline objects throughout OnlineView.jsx + 10+ sub-components. Migrate to Tailwind + tokens from `designTokens.js`. Bundles F7 (touch targets ≥44×44) and F9 (Import-button size consistency empty vs populated state). M-L effort. | — |
| DCOMP-W4-A3-F10 | P2 | NEXT | **contracts/sidebar-to-online-view.md** | Document sidebar ↔ OnlineView handoff (or explicit no-handoff). When to use which surface; does seat-selection feed back; post-hand review entry point. | — |
| DCOMP-W4-A3-F11 | P2 | NEXT | **Verify Online session classification** | SV-F7 shipped All/Live/Online filter pills in Wave 1. Verify extension-imported sessions are classified as Online; if not, add `source: 'extension'` to Session record on import. | — |
| DCOMP-W4-A3-F12 | P3 | NEXT | **Online persona + JTBD patches** | `multi-tabler-switching.md`; Online MTT Shark scope clarification; Hybrid cross-format handoff; MH-01 claim reconciliation; new JTBDs MH-20/SR-45 (sidebar-advice-history) + MT-XX (extension capture verification). | — |
| DCOMP-W4-A3-F13 | P3 | NEXT | **MT-62 offline-fallback verify** | Verify analysis pipeline degrades gracefully when extension disconnects mid-session. Document behavior. | — |
| DCOMP-W4-A4-F1 | P1 | COMPLETE (Batch A, commit 0715034) | **Reset to Defaults → toast+undo** | Two-click-confirm doesn't restore pre-reset settings. Capture snapshot, write defaults, 12s toast+Undo. Reuse TV-F1 token shape. | — |
| DCOMP-W4-A4-F2 | P1 | COMPLETE (Batch A, commit 0715034) | **Hand Simulator Clear → toast+undo** | Clear button is NOT dev-gated — visible in prod. Silent destructive commit. Add toast+undo (reuse F1 token). | — |
| DCOMP-W4-A4-F3 | P1 | NEXT | **SettingsView touch targets ≥44×44** | Back to Table 40h, Dark/Light 36h, Small/Medium/Large 36h, Add 41h, Reset to Defaults 36h — all under minimum. Mechanical Tailwind class swaps. | — |
| DCOMP-W4-A4-F4 | P1 | NEXT | **Settings inputs inputMode=decimal/numeric** | Custom Game Type Buy-in (text, no inputMode) + Hand Simulator count (type=number, no inputMode). Add `inputMode="decimal"` and `"numeric"` respectively (H-ML08 pattern). | — |
| DCOMP-W4-A4-F5 | P2 | NEXT | **Backup Frequency disabled-row compression** | 3 disabled buttons + label takes full panel width for a not-yet feature. Compress to single info line "Automatic backups — coming soon" until F-P18 / backup infra lands. | — |
| DCOMP-W4-A4-F6 | P2 | NEXT | **Theme toggle visual-disabled state** | Dark/Light buttons visually active-styled (blue active, gray inactive) but panel label reads "(coming soon)". Add opacity-0.5 + cursor-not-allowed. | — |
| DCOMP-W4-A4-F7 | P2 | NEXT | **Add-button color consistency** | VenuesManager Add = purple (rgb(147, 51, 234)); GameTypesManager Add = teal. Both primary Add actions on sibling panels. Pick one canonical (blue matches Sign In primary). | — |
| DCOMP-W4-A4-F8 | P2 | NEXT | **errorReportingEnabled clarity + plumbing audit** | Checkbox "Enable error reporting (helps improve the app)" — unclear if anything is actually transmitted externally. Audit plumbing; either remove the toggle if vaporware, rename if local-only, or add privacy note. | — |
| DCOMP-W4-A4-F9 | P2 | NEXT | **AccountSection dismissible for local-first users** | Sign In pitch re-renders on every Settings visit for guest users. Add "I'll stay local" dismissal with localStorage persistence; compress to footer link after dismiss. | — |
| DCOMP-W4-A4-F10 | P3 | NEXT | **ComingSoonNotice shared component** | 3 separate "(coming soon)" treatments in view (Theme disabled buttons, Backup Frequency disabled buttons, Error Reporting active checkbox). Consolidate via `<ComingSoonNotice feature="X" />`. | — |
| DCOMP-W4-A4-F12 | P3 | NEXT | **DataAndAbout dev-tools collapsible** | Dev-build DataAndAbout panel is tallest at 400+ lines with sim + seed/clear buttons. Collapse dev-tools into a separate expandable sub-panel. | — |
| DCOMP-W3 | P3 | NEXT | Wave 3 — Drills (preflop/postflop) audit + WIP tab decision | Audit + owner decision on WIP tabs' fate. Est. 3–4 sessions. Gate 2 for drills. |
| DCOMP-W5 | P3 | BLOCKED by W4 | Wave 5 — Sidebar framework integration (5 zones) | Surface artifacts for Z0–Z4; heuristic cross-mapping. Est. 3–4 sessions. |
| DCOMP-H1 | P2 | NEXT (parallel) | Hygiene H1 — Triage initial 20-item discovery gap list | Each discovery moved to QUEUED / REJECTED / ARCHIVED. Est. 1 session. |
| DCOMP-H2 | P3 | SCHEDULED 2026-07-21 | Hygiene H2 — First quarterly PROTO persona validation pass | ≥5 of 15 personas validated or explicitly kept PROTO. Est. 1 session. |

Sequencing: W0 first (preconditions), then W1 → W2 → W4 → W3 → W5 recommended. H1 runs parallel with W0 or W1. Re-sequence allowed per owner priority.

Minimum viable compliance state: W0 + W1 + H1 = ~10 sessions. Stops at all-surfaces-catalogued + core surfaces through Tier C + discoveries triaged. Everything beyond is quality improvement, not compliance gate.

---

## REVIEW / COMPLETE — Drills Consolidation Roundtable (2026-04-20, follow-up 2026-04-21)

6-expert audit on `docs/projects/drills-consolidation.project.md`. Owner directed on 2026-04-21 to fix items that improve the code without revamping the layout — six items shipped (RT-95, RT-96, RT-100, RT-101, RT-104, RT-105). **2026-04-22: owner explicitly rejected the consolidation** — the 6 layout-dependent / consolidation-prep items (RT-93, RT-94, RT-97, RT-98, RT-99, RT-103) are now REJECTED rather than HELD. They can be re-opened if/when Scholar-persona goals change or StudyView is reactivated, but neither should be assumed; current drill-views pattern is the supported architecture. The `drills-consolidation.project.md` charter is also superseded by this rejection.

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| RT-92 | P1 | COMPLETE | ~~Block drills-consolidation until Line Study closes~~ — naturally satisfied 2026-04-20 when Line Study Phases 1–6 all shipped. | ✓ LS closed; handoff archived | Shipped 2026-04-21 |
| RT-93 | P1 | REJECTED (2026-04-22) | ~~Fix design-doc defects before execution~~ — design-doc fixes are only meaningful if the doc drives execution. Owner rejected consolidation; doc is superseded. | N/A | — |
| RT-94 | P1 | REJECTED (2026-04-22) | ~~Create `src/components/_shared/drillInternals/` barrel~~ — primary justification was consolidation prep. Touches 15+ files with real breakage risk and zero current-state bug payoff. Four drill-views pattern is the supported shape. | N/A | — |
| RT-95 | P1 | COMPLETE | INV-08 violation: lessons.test.js imports from views/ | Shipped 2026-04-21. New `src/utils/drillContent/calculatorRegistry.js` exports `CALCULATOR_NAMES` + `isKnownCalculator`; lessons.test.js imports from registry; `LessonCalculators.jsx` imports `CALCULATOR_NAMES` and runs a dev-only parity check against its `CALCULATORS` object. | ✓ grep shows no `from.*views.*Calculators` in `src/utils/`; ✓ lessons.test.js + preflopDrillsStorage.test.js green (20/20); ✓ dev-mode drift warning if calculator surfaces diverge | Shipped 2026-04-21 |
| RT-96 | P2 | COMPLETE | Prototype pollution in aggregateFrameworkAccuracy | Shipped 2026-04-21. Both `aggregateFrameworkAccuracy` (preflop) and `aggregatePostflopFrameworkAccuracy` now use `Object.create(null)` + `typeof id !== 'string'` guard. Tampered IDB records with non-string framework IDs are silently dropped rather than mutating `Object.prototype`. | ✓ Tests green (20/20 in preflopDrillsStorage.test.js); ✓ both aggregates hardened identically | Shipped 2026-04-21 |
| RT-97 | P2 | REJECTED (2026-04-22) | ~~Chrome budget audit at 1600×720~~ — only relevant for StudyView. Current four-view pattern already documented in DRILL_VIEWS.md §1 (≤180 px budget, ~120 px actual) which is sufficient without StudyView. | N/A | — |
| RT-98 | P2 | REJECTED (2026-04-22) | ~~Street-filter persistence + default spec~~ — layout-specific to StudyView. No action required on the four-view pattern. | N/A | — |
| RT-99 | P2 | REJECTED (2026-04-22) | ~~Dev-preview route gating for StudyView scaffold~~ — StudyView is not being built. No scaffold, no gate needed. | N/A | — |
| RT-100 | P2 | COMPLETE | Normalize aggregate return shapes (preflop vs postflop) | Shipped 2026-04-21. Shape difference (`avgDelta`/`deltaSamples` preflop-only) now documented inline in `postflopDrillsStorage.js` and in `.claude/context/DRILL_VIEWS.md` §4 with explicit caller-handling guidance for future unified consumers. | ✓ Both aggregates documented with identical / differ-list contract; ✓ caller guidance for merged consumers explicit | Shipped 2026-04-21 |
| RT-101 | P2 | COMPLETE | drillType history key mismatch (recipe vs line) | Shipped 2026-04-21. Convention documented in `.claude/context/DRILL_VIEWS.md` §3: table of `drillType` → store → identifier field, with guidance for iterators (branch on type or fallback `matchupKey \|\| scenarioKey`). | ✓ Table covers all 6 drillType × store combinations; ✓ rule for adding a new drillType explicit | Shipped 2026-04-21 |
| RT-102 | P3 | COMPLETE | SCREEN enum default fallback + dev observability | Shipped 2026-04-21. `PokerTracker.jsx` default case now emits `console.warn('[ViewRouter] unknown currentView=…')` in DEV before falling back to Stats. Makes stranded-screen state observable rather than silent. Two-step deprecation protocol (pre-delete sweep of prevScreen refs) and uiConstants.js comment pattern deferred to the next SCREEN removal. | ✓ Default case exists (was already present); ✓ dev warning on unknown values | Shipped 2026-04-21 (partial) |
| RT-103 | P3 | REJECTED (2026-04-22) | ~~ESLint rule: utils/ must not import from views/~~ — project has no ESLint configuration and no plan to introduce one; ruleset-of-one is not worth standing up the entire lint framework. INV-08 is enforced via test-time discipline + INV-08.a exception for drill-content layer. | N/A | — |
| RT-104 | P3 | COMPLETE | Hoisted-persistence-hook pattern doc | Shipped 2026-04-21. Documented in `.claude/context/DRILL_VIEWS.md` §2 with concrete wrong/right JSX examples and generalization to other IDB-loading hooks (`usePlayerTendencies`, `useTournamentPersistence`). | ✓ Doc exists with wrong-vs-right example; ✓ generalization rule stated | Shipped 2026-04-21 |
| RT-105 | P3 | COMPLETE | Height-contract documentation | Shipped 2026-04-21. Documented in `.claude/context/DRILL_VIEWS.md` §1 with ASCII diagram of the height chain, ownership list, and ≤180 px chrome budget rule for the 1600×720 target. | ✓ Doc enumerates contract; ✓ chrome budget rule explicit | Shipped 2026-04-21 |

**Net outcome of 2026-04-21 follow-up session:**
- 6 items shipped (RT-95, RT-96, RT-100, RT-101, RT-102, RT-104, RT-105) — hardening + documentation, zero functional change
- 1 item naturally satisfied (RT-92)
- ~~6 items held pending owner decision on consolidation (RT-93, RT-94, RT-97, RT-98, RT-99, RT-103)~~
- New file: `src/utils/drillContent/calculatorRegistry.js`
- New file: `.claude/context/DRILL_VIEWS.md` (consolidated reference for four drill-view patterns)
- Tests: 20/20 affected tests green; one pre-existing precision-audit failure unrelated to this work

**2026-04-22 owner decision: REJECTED.** Owner explicitly rejected the drills-consolidation / StudyView path. The 6 previously-HELD items (RT-93, RT-94, RT-97, RT-98, RT-99, RT-103) moved to REJECTED. The four drill-views pattern (LessonsMode, EstimateMode, FrameworkMode, LineMode) is the supported architecture. `docs/projects/drills-consolidation.project.md` is superseded — the charter can be archived. Roundtable section is now fully closed.

---

## REVIEW — Line Study Bucket-Level Teaching Roundtable (2026-04-21)

6-expert audit on the proposal to upgrade Line Study from range-level to bucket-level teaching (hero hand class × archetype-conditioned villain range, e.g., BTN vs BB 3BP JT6 donk → top-set thin-value-raise vs fish, raise-fold vs pro).

**Owner decisions (2026-04-21 — all Option A approved):**
- **Q1 Teaching contract:** HAND-LEVEL. Every decision node names a specific combo or small bucket the student is playing; bucket-EV is the natural extension. RT-106 required.
- **Q2 Synthetic-range epistemics:** SHIP WITH CAVEAT. Bucket-EV tables carry a "synthetic range" badge so the student understands the precision floor.
- **Q3 Stack-depth gating:** ADD `validEffStackRange` to schema, stubbed to `[40, 100]` on all v1 lines. Validator hides/warns when user's declared effStack is out of range.
- **Q4 Multiway scope:** DEFERRED TO POST-V1. HU lines get bucket-EV first; MW lines keep range-level teaching with a "bucket teaching coming soon" badge.
- **Q5 Scope frame:** VERTICAL SLICE. RT-106..RT-114 primitives + bucket-EV on ONE node (JT6 turn from the canonical example) + ONE archetype (fish). Proves end-to-end, then widen via rolling backlog SPI-ordered as engine improvements land.

**Approval status (2026-04-21 — all 13 items resolved):**
- **RT-109 COMPLETE** (documentation-only, applied in Phase 5 governance updates: SYSTEM_MODEL.md v1.9.3, INVARIANTS.md INV-08.a, coupling entry C-14)
- **ALL REMAINING 12 items APPROVED → NEXT**: RT-106, RT-107, RT-108, RT-110, RT-111, RT-112, RT-113, RT-114, RT-115, RT-116, RT-117, RT-118.

**Severity breakdown:** 2 P0 (schema gates), 6 P1 (primitives + invariants), 4 P2 (engineering polish), 1 P3 (defense-in-depth).

**Sequencing:** Recommended WSJF ship order: **RT-117 → RT-108 → RT-106 → RT-115 → RT-107 → RT-110 → RT-112 → RT-113 → RT-114 → RT-111 → RT-118 → RT-116**. Critical gate: **RT-106 (hero-holding schema) + RT-107 (archetype-aware correctness) + RT-108 (diff CI) must all land before any bucket-EV rendering work** (RT-111, RT-115, RT-118) begins, or authored content drifts from engine truth silently. RT-117 is independent — ship anytime. RT-109 already COMPLETE.

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| RT-106 | P0 | COMPLETE (2026-04-21) | Add hero-holding dimension to `lineSchema.js` | Shipped 2026-04-21. `lineSchema.js` schema v2: `SCHEMA_VERSION` export + new `validateHeroHolding` covering `{ combos?: string[], bucketCandidates?: string[] }`. Unicode-suit combo regex (`A♠K♥` format to match existing `parseCard` convention); duplicate-card rejected; empty-object rejected; neither-field rejected. 11 new schema tests (44 total, all green). Exemplar on `btn-vs-bb-3bp-ip-wet-t96` flop_root node (`J♥T♠` + bucket spread). New `HeroHoldingRow` component in `LineNodeRenderer.jsx` renders amber "Your hand" block with combo chips + bucket pills when present. Node-level (not line-level) so one node can span multiple buckets. Visual verification pending — component is pure props-mapping, no state/effects; will be exercised by RT-110/115. | ✓ 200/200 pre-existing tests still pass (215/215 total incl. new schema tests); ✓ existing 8 lines validate unchanged with heroHolding absent; ✓ exemplar combo `J♥T♠` present in schema-validated node; ✓ render block conditionally shown | Shipped 2026-04-21 |
| RT-107 | P0 | COMPLETE (2026-04-21) | Archetype-aware branch correctness on `lineSchema.js` | Shipped 2026-04-21. `Decision.branches[]` now accept optional `correctByArchetype?: { [archetypeId]: boolean }`. Validator: object-shape + boolean-value checks + decision-level aggregation ("every archetype referenced by any branch must have ≥1 branch with correctByArchetype[archetype]=true"). Fallback: when `correctByArchetype` absent, flat `correct: boolean` remains authoritative. Archetype IDs are non-empty strings (no enum pin — RT-112 land-point). 8 new schema tests (52 total, was 44). UI toggle integration deferred to the session that ships the archetype toggle; renderer still reads `branch.correct`. | ✓ 8 new tests green; ✓ existing 8 lines validate unchanged (no correctByArchetype yet); ✓ decision with `{fish:true,pro:false}` + `{fish:false,pro:true}` passes; ✓ decision where fish is declared-but-never-correct fails with clear archetype-named error | Shipped 2026-04-21 |
| RT-108 | P1 | COMPLETE (2026-04-21) | Engine-vs-authored diff CI for drill content | Shipped 2026-04-21. New test at `src/utils/postflopDrillContent/__tests__/engineAuthoredDrift.test.js` + baseline snapshot at `__snapshots__/engine-authored-drift.json`. Snapshots per decision node: villain-narrowed-range combo count, capped flags, segmenter bucket percentages (nuts/strong/marginal/draw/air); plus deterministic fold-curve probe grid + sentinel engine constants (POP_CALLING_RATES, POP_BETTING_RATES, BUCKET_EQUITY_ANCHORS). Regenerate baseline with `UPDATE_DRILL_DRIFT_SNAPSHOT=true`. **Verified bidirectionally:** perturbing `POP_CALLING_RATES.marginal` 0.55 → 0.60 failed test with exact line-pointer diff; revert restored green. ~30 nodes covered across 8 lines. | ✓ new test file + baseline committed; ✓ perturbation of any engine constant fails test with clean JSON diff; ✓ revert restores green; ✓ no production code touched | Shipped 2026-04-21 |
| RT-109 | P1 | COMPLETE (2026-04-21) | Resolve INV-08 silent violation; document drill-layer deps in SYSTEM_MODEL §1.2 | `handTypeBreakdown.js:32` imports `classifyComboFull` from `exploitEngine/postflopNarrower`; `rangeVsBoard.js:17` imports `computeAdvantage` from `exploitEngine/rangeSegmenter`. Current-state INV-08 violation independent of this proposal. Drill-content layer not in §1.2 dep graph. Choose pragmatic path: formally permit `postflopDrillContent/ → exploitEngine/` in §1.2 + §6 + new INV-08.a exception. WSJF 7 / 2 = 3.5. **Accept:** `SYSTEM_MODEL.md` §1.2 lists `postflopDrillContent/` with explicit upstream deps; §6 has coupling entry C-14; `INVARIANTS.md` INV-08.a permits this exception; no source code changes. | — |
| RT-110 | P1 | COMPLETE (2026-04-21) | Bucket-to-combo-set adapter utility | Shipped 2026-04-21. New module `src/utils/postflopDrillContent/bucketTaxonomy.js`: `BUCKET_TAXONOMY` frozen table maps 28 poker-natural bucket IDs (tptk, flushDraw, set, overpair, air, …) to `rangeSegmenter` HAND_TYPES. Public API: `listKnownBuckets()`, `handTypesForBucket(id)`, `isKnownBucket(id)`, `enumerateBucketCombos({bucketId, board, range})` returns `{bucketId, segmenterHandTypes, combos, sampleSize, totalWeight}` or `null` for unknown bucket. Structural test asserts every taxonomy entry references a valid segmenter HAND_TYPE. Helper `flopRanksDescending()` exposed for future topSet/middleSet refinement (deferred). 22 new tests (252 postflop total, was 222). Board-relative refinements explicitly out of scope for v1 — `set` aggregates top/middle/bottom sets for now. | ✓ given `('overpair', 'K♠7♥2♦', AA)` → 6 combos; ✓ given `('set', 'K♠7♥2♦', KK)` → 3 combos (K♠-containing combos removed); ✓ TT on T♥9♥6♠ → 3 set combos; ✓ unknown bucket → null; ✓ taxonomy entries are frozen and every segmenter handType reference is valid | Shipped 2026-04-21 |
| RT-111 | P1 | COMPLETE (2026-04-21) | Drill-mode engine wrapper with bailout + sampleSize + caveats | Shipped 2026-04-21 (v1). New module `src/utils/postflopDrillContent/drillModeEngine.js` exposes `evaluateDrillNode({bucketId, archetype, heroRange, villainRange, board, pot, actions?, depth2Eval?})` → `Promise<{bucketId, archetype, sampleSize, evs, ranking, bailedOut, caveats}>`. Integrates RT-110 bucket taxonomy + RT-112 archetype reweighter + RT-115 MIN_COMBO_SAMPLE. Composes fold rate from archetype-weighted bucket composition × POP_CALLING_RATES (archetype-responsive!). EV computation uses `calcValueBetEV` (foldEquityCalculator) with per-bucket hero-equity priors in `HERO_BUCKET_TYPICAL_EQUITY` table. Caveats always include `synthetic-range` (per Q2) + `v1-simplified-ev` (removed when depth2Eval is passed). Adds `empty-bucket` (sampleSize=0), `low-sample-bucket` (<3), `time-budget-bailout` (via depth2Eval.wasBailout()). `depth2Eval` parameter is the forward-compatible injection point for full game-tree integration — when it lands, v1-simplified-ev is auto-removed and bailedOut is populated. 27 new tests (305 postflop total, was 278) covering input validation, output shape, caveats across all branches, archetype responsiveness. **NOT YET:** depth-2 game tree call wiring (deferred — see module doc "Depth-2 integration forward requirement"), EV cache with engineVersion stamp (NEV-12 forward requirement also deferred). Wrapper shape is locked so downstream bucket-EV UI can build against it now. | ✓ wrapper returns full documented shape; ✓ low-sample bucket surfaces 'low-sample-bucket' caveat; ✓ depth2Eval.wasBailout()=true surfaces bailedOut=true + 'time-budget-bailout' caveat; ✓ fish archetype produces different bet EVs than reg (fold rate shift via archetype-weighted bucket mix); ✓ synthetic-range caveat always present; ✓ build clean | Shipped 2026-04-21 |
| RT-112 | P1 | COMPLETE (2026-04-21) | Archetype as range-delta (not decision-input) | Shipped 2026-04-21. New module `src/utils/postflopDrillContent/archetypeRangeBuilder.js` — frozen `ARCHETYPE_BUCKET_MULTIPLIERS` table for fish/reg/pro × nuts/strong/marginal/draw/air. Fish up-weights strong+marginal, down-weights air; pro opposite; reg baseline. API: `listKnownArchetypes()`, `isKnownArchetype(id)`, `buildArchetypeWeightedRange({archetype, baseRange, board})` returns `{combos, totalWeight, multipliers}`, `aggregateBucketWeights(weighted)`. Archetype is a **table key**, never a decision branch. Home module is `postflopDrillContent/` not `drillContent/` — "archetype" in exploitEngine refers to flop-archetype (different concept, naming collision noted). 16 new tests (268 postflop total, was 252) including linter that greps `postflopDrillContent/` + `drillContent/` for `archetype ===` / `archetype ==` patterns outside archetypeRangeBuilder.js — zero matches. | ✓ linter test passes (zero archetype-label comparisons in decision modules); ✓ fish's strong+marginal weight > reg's on K72r (measured); ✓ pro's draw+air weight > reg's on T96ss (measured); ✓ reg reweighting preserves total weight (identity) | Shipped 2026-04-21 |
| RT-113 | P1 | COMPLETE (2026-04-21) | Calculator dispatcher — wire `ComputeSection` to real calculators | Shipped 2026-04-21. `LineNodeRenderer.jsx` `ComputeSection` no longer a placeholder — dispatches via `CALCULATORS[section.calculator]` (imported from `PreflopDrillsView/LessonCalculators.jsx`). Unknown-calculator case renders visible rose-bordered error with the offending name. 5 calculators available today (`flopPair`, `flushOuts`, `straightRuns`, `potOdds`, `runouts`) via existing RT-95 registry. Exemplar `compute` section added to JT6 `flop_root` decision — `potOdds` calculator with authored intro walking the student through 6.8bb/(27.3+6.8) breakeven against BB's donk. Cross-view import noted for RT-94 follow-up (move `CALCULATORS` into `_shared/drillInternals/`). Build clean (11.10s); 268/268 postflop tests green. The schema-level "name registry stores names only" approach didn't need extending — dispatch lives at the render site. | ✓ compute section on live line renders an interactive `<PotOddsCalculator />` (not placeholder); ✓ unknown calculator renders visible error with the name; ✓ build succeeds | Shipped 2026-04-21 |
| RT-114 | P1 | COMPLETE (2026-04-21) | Attempt schema migration: `archetypeId` + `bucketId` fields | Shipped 2026-04-21. IDB is schemaless at the record level so no DB version bump needed — added `archetypeId?: string` + `bucketId?: string` as additive-optional fields documented in the `postflopDrillsStorage.js` record-shape JSDoc. New module `src/utils/postflopDrillContent/drillAnalytics.js` exposes taxonomy-aware aggregators: `aggregateByArchetype`, `aggregateByBucket`, `aggregateByBucketArchetype` (2D cross-tab). Each returns `{ ...byX, droppedUnknown: N }`; unknown-ID records (legacy, tampered, mistyped) are dropped with a dev-mode `console.warn`. All aggregators use `Object.create(null)` — prototype-pollution-safe (RT-96 pattern). 10 new tests (278 postflop total, was 268). Persistence layer stays taxonomy-agnostic; analytics module is the single binder between IDB records and the bucket/archetype taxonomies. | ✓ record with `archetypeId: 'fish', bucketId: 'set'` appears in aggregate stats split by those dimensions; ✓ tampered record with `archetypeId: '__proto__'` / `bucketId: 'bogus'` dropped silently + dev warning emitted + no prototype pollution; ✓ legacy records (neither field) also counted in `droppedUnknown` | Shipped 2026-04-21 |
| RT-115 | P2 | COMPLETE (2026-04-21) | Minimum-combo guard + sampleSize flag on bucket aggregators | Shipped 2026-04-21. `handTypeBreakdown.js` exports `MIN_COMBO_SAMPLE = 3`; every `perType[ht]` and `byGroup[g]` entry now carries `sampleSize` (= combo count) + `lowConfidence: count > 0 && count < MIN_COMBO_SAMPLE`. Absent buckets (count=0) are NOT flagged (nothing to caveat). `HandTypeBreakdown.jsx` renders subtle "◦" caveat marker with tooltip next to low-sample sub-types. 7 new tests (222 postflop-drill total, was 215). Invariant test asserts iff-relationship across all 22 hand-types × every authored line's default archetype range. | ✓ low-sample bucket (e.g., QQ on Q♠Q♥2♦ → 1 quads combo) flagged `lowConfidence: true`; ✓ ≥3-combo bucket unflagged; ✓ absent bucket unflagged; ✓ UI caveat marker renders when flagged | Shipped 2026-04-21 |
| RT-116 | P2 | COMPLETE (2026-04-21) | Worker batching protocol for bucket tables | Shipped 2026-04-21. `equityWorker.js` handles `{ batch: true, requests: [...] }` message shape returning `{ id, results: [{result\|error}] }`; per-request failures are inline so one bad combo doesn't fail the batch. `EquityWorkerContext` gains `computeEquityBatch(requests)` → `Promise<Array<{result?, error?}>>`; transfers all request Float64Array buffers zero-copy in a single `postMessage`. Fallback path (worker unavailable) returns the same batch shape via Promise.all so callers don't branch on worker health. 5 new tests in `EquityWorkerContext.test.jsx` (12 total, was 7) covering signature, empty input, non-array input, order preservation, and per-item-failure-without-batch-fail. Build clean. Live `Performance.mark` verification deferred to RT-111 which will be the first caller. | ✓ batch endpoint in worker + context; ✓ single postMessage for N requests (verified structurally); ✓ per-item errors returned inline; ✓ order preserved; ✓ main-thread fallback matches batch shape | Shipped 2026-04-21 |
| RT-117 | P2 | COMPLETE (2026-04-21) | Code-split `exploitEngine` in `vite.config.js` | Shipped 2026-04-21. Converted `manualChunks` from object to function form; matches `/src/utils/exploitEngine/` paths into `exploitEngine` chunk, preserves firebase split. **Delta:** main chunk 662.60 → 412.87 kB raw (-249.7 kB / -37.7%); 208.61 → 126.67 kB gzip (-81.9 kB / -39.3%). New `exploitEngine-*.js` chunk: 277.22 kB raw / 91.53 kB gzip (loads only when a consumer view mounts). 500 kB chunk-size warning eliminated. Total dist bytes +27 kB raw (standard chunk-duplication tradeoff; first-paint cost drops by full main-chunk delta). | ✓ separate `exploitEngine-*.js` chunk in dist; ✓ main chunk size drops measurably (~250 kB raw / ~82 kB gzip); ✓ build succeeds | Shipped 2026-04-21 |
| RT-118 | P3 | COMPLETE (2026-04-21) | Defensive invariants on new bucket/archetype aggregators | Shipped 2026-04-21. Substantially pre-satisfied while shipping RT-110/112/114/115 — every new aggregator in the session uses `Object.create(null)` + string-type guard, and every new aggregator test file includes a `Object.getPrototypeOf(x) === null` assertion (see bucketTaxonomy.test.js, archetypeRangeBuilder.test.js, drillAnalytics.test.js, handTypeBreakdown.test.js). This commit codifies the pattern as NEV-12 in `.claude/context/INVARIANTS.md` so future aggregators can't silently regress — enforcement is via the per-aggregator prototype-free tests plus the NEV-12 policy row's grep guidance. **EV-cache version-stamp deferred**: no EV cache exists yet — RT-111's `drillModeEngine` wrapper is where such a cache, if introduced, must land with an `engineVersion` stamp + invalidate-on-mismatch read path. Noted as a forward requirement on RT-111. | ✓ NEV-12 added to INVARIANTS.md; ✓ every aggregator shipped this session has a prototype-free test; ✓ EV-cache stamping forward requirement noted on RT-111 | Shipped 2026-04-21 |

**Owner decisions required before approving any RT-106 through RT-118:**
1. **Teaching contract** — hand-level ("what's correct for my holding") vs range-level ("what's correct for my range")? Owner's JT6 example implies hand-level; current Line Study was built range-level. Different products. Confirm.
2. **Synthetic-range epistemics** — accept that fish/pro archetype ranges on boards with no observed hands are `POPULATION_PRIORS` + style multipliers, not ground-truth? Ship with caveat or gate behind minimum-observation threshold?
3. **Stack-depth gating** — declare the effStack range over which authored bucket EVs are valid (e.g., 40–100 bb) and add `validEffStackRange` to schema?
4. **Multiway scope** — MW bucket-EV deferred to post-v1 (requires per-villain bucket ranges that don't exist). Confirm.
5. **Scope frame** — ship 1-line × 1-node × 1-archetype × 1-bucket vertical slice with RT-106 through RT-114 primitives in place, widen via rolling backlog. Confirm this is the intended frame vs. a broader generic framework.

**Convergent finding across all 6 experts:** the proposal has 4 load-bearing preconditions that were not in the owner's original scope:
1. Hero hole cards per walkthrough step (RT-106, schema)
2. Calculator dispatcher wired to `ComputeSection` (RT-113, plumbing)
3. Bucket taxonomy with mapping to `rangeSegmenter` labels (RT-110, new primitive)
4. Attempt-record schema with bucket + archetype dimensions (RT-114, IDB migration)

None individually blocks the feature. Together they ARE the MVP.

---

## IN_PROGRESS — Line Study (LS)

Owner-approved 2026-04-20. New Postflop Drills tab: branching street-by-street hand walkthroughs with Study Priority Index and multiway coverage. Charter: `docs/projects/line-study.project.md`. Handoff: `.claude/handoffs/line-study.md`.

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LS-1 | P1 | COMPLETE | **Data layer + first line** — `lineSchema.js` with schema + validator + DAG walker (`walkLine`, `lineStats`), `lines.js` with first authored HU line (BTN vs BB SRP dry Q72r, 11 nodes: 6 decision + 5 terminal, full flop/turn/river branching). 48 tests all green. Shipped 2026-04-20. | ✓ Schema rejects 20+ malformed variants; ✓ first line has 0 unreachable nodes; ✓ 153/153 `postflopDrillContent` tests green | Shipped |
| LS-2 | P1 | COMPLETE | **Line Mode UI scaffold** — Line tab on PostflopDrillsView + 7 new UI files (LineMode, LinePicker, LineWalkthrough, LineStateTracker, LineNodeRenderer, LineBranchBreadcrumb, section dispatcher w/ prose/why/adjust/mismatch accents). Persistence wired (drillType='line'). Full flop → turn → river → terminal walk verified at 1600×720. Shipped 2026-04-20. | ✓ Flop node renders with state tracker; ✓ decision reveals all branch rationales; ✓ advance threads correctly; ✓ terminal shows end-of-line panel; ✓ IDB query confirms 3 attempts captured | Shipped |
| LS-3 | P1 | COMPLETE | **Full branching + second line** — Breadcrumb retry-from-node (clickable past pills truncate path + clear reveal). Second line authored: BTN vs BB 3BP Wet T♥9♥6♠ villain donks, 12 nodes (5 decision + 7 terminal), 15 branches. Explicit coverage: "missed bluff → facing river aggression" (`river_checkback`) + "overbet → check-raise" (`river_brick_v_checkraises`). Shipped 2026-04-20. | ✓ 157/157 tests green; ✓ both paths walk clean in browser; ✓ retry-from-breadcrumb truncates correctly | Shipped |
| LS-4 | P1 | COMPLETE | **Study Priority Index** — `studyPriorityIndex.js` module (population constants + reach-probability DAG walker + SPI formula + `explainSPI` + `lineMatchesFilters`). SPIBadge + SPITooltip components. LinePicker: SPI/Title/Nodes sort toggle, HU/MW filter, pot-type chips, board-texture chips, empty state. Shipped 2026-04-20. | ✓ 176/176 tests green; ✓ SPI badges render in picker (80 / 139); ✓ tooltip shows 3-factor breakdown + dominant node; ✓ filter narrows correctly | Shipped |
| LS-5 | P1 | COMPLETE | **Multiway data + frameworks** — `multiwayFrameworks.js` with 7 frameworks merged into main registry. First MW line: BTN vs SB + BB 3-way SRP J♠8♥5♦ (10 nodes, 4 decisions, 12 branches). SPI `multiwayBoost()` factor. LineStateTracker pluralizes "Villains". MW filter narrows picker; MW frameworks auto-classify on MW scenarios. Shipped 2026-04-20. | ✓ 180/180 tests green; ✓ MW line walks clean; ✓ 5 framework chips render on MW flop node (4 MW + 1 base); ✓ MW filter narrows picker to just the MW line | Shipped |
| LS-6 | P1 | COMPLETE | **MW curriculum + line matrix fill** — 6 MW lessons (mw-bluff-death, mw-nut-necessity, mw-srp-3way, mw-squeeze, mw-cbet-shifts, mw-overcalling). 5 new lines: CO vs BB SRP OOP paired, SB vs BTN 3BP OOP wet, UTG vs BTN 4BP deep, CO vs BTN+BB 3-way SRP OOP, UTG facing squeeze. Final: 8 lines (5 HU + 3 MW), 85+ nodes. Shipped 2026-04-20. | ✓ 200/200 tests green; ✓ every MW framework on ≥1 line node; ✓ 6 MW lessons published; ✓ picker shows 8 lines sorted by SPI | Shipped |

**Line Study PROJECT COMPLETE 2026-04-20.** All 6 phases shipped in one day: schema + UI + branching + SPI + multiway + curriculum. Final deliverables: 3 core modules (`lineSchema`, `lines`, `studyPriorityIndex`) + `multiwayFrameworks` + 7 UI components (`LineMode`, `LinePicker`, `LineWalkthrough`, `LineStateTracker`, `LineNodeRenderer`, `LineBranchBreadcrumb`, `SPIBadge`, `SPITooltip`), 8 authored lines (5 HU + 3 MW) totaling 85+ nodes, 6 MW lessons, 14 frameworks (7 base + 7 MW). 200/200 postflop drill tests green. Unblocks RT-92 (drills-consolidation gating item).

---

## IN_PROGRESS — Line Study Slice Widening (LSW)

Owner-approved 2026-04-22. Follow-on from RT-106..118 bucket-teaching roundtable: widens the single-node bucket-EV vertical slice (JT6 donk, `btn-vs-bb-3bp-ip-wet-t96`) to meaningful coverage across the 8 authored lines, paired with a per-line poker-expert accuracy audit run first to catch content errors before widening lands on them. Charter: `docs/projects/line-study-slice-widening.project.md`. Audit template: `docs/design/audits/line-audits/_TEMPLATE.md`. Handoff: (not yet started — next session creates `.claude/handoffs/lsw-a1-*.md`).

**Structure:** Stream A (8 audits, each with mandatory per-decision-node web-research validation) → Stream F (content-wrong findings) + **Stream G (engine-wrong findings — new 2026-04-22)** + **Stream H (surface quality / UI rendering findings — new 2026-04-22)** → Stream B (heroHolding widening, 3 tiers) → Stream D (depth-2 engine, EV cache). Stream C (correctByArchetype) threads into B commits. Stream E polish rolls inline.

**Audit standard (2026-04-22):** every decision node requires external web-research validation — POKER_THEORY.md is our prior, not evidence. Queries categorize A (no disagreement), B (our content wrong → Stream F), C (our engine wrong → Stream G), D (intentional divergence — document in POKER_THEORY.md §11). Zero-query audits fail review.

**Surface audit standard (added 2026-04-22):** content audits (Stream A) cover `lines.js` schema; they do NOT cover UI rendering. Combined poker-expert + product-UX live-surface walk at 1600×720 (Playwright) is now the minimum for a per-line audit's rendering layer. Every audit artifact carries an explicit **Verification** box listing which nodes / viewports / archetypes were walked, what was NOT verified, and the method. Code-reading alone is insufficient. Rendering findings route to Stream H.

**Current state baseline:** 1 of 25 decision nodes has `heroHolding`; 1 of 25 decision nodes has `correctByArchetype`; `v1-simplified-ev` caveat present everywhere.

### Stream A — Per-line expert audits (8 sessions, runs first)

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LSW-A1 | P1 | COMPLETE (DRAFT — awaits owner review) | **Audit `btn-vs-bb-3bp-ip-wet-t96` (2026-04-22)** — 12 nodes (5 decision) walked through seven dimensions with ≥2 web-research queries per decision node (11 queries total: 5 A, 4 B, 2 D). **Verdict: YELLOW.** 10 findings: 3 P1 content errors + 3 P2 structural + 4 P3 polish. Key findings: (1) line's premise "BB has nut advantage on T96ss" is REVERSED per GTO Wizard — BTN has nut advantage on non-broadway middling boards; the donk is a real live-pool tendency, not a principled play. (2) **`river_checkback` pot-odds math error: claims "25% equity" for a 75% pot bet; correct is 30%.** Internal inconsistency: either the sizing is 50% or the math is wrong. (3) Pot-accounting cascade on `river_brick_v_checkraises` + 2 child terminals. Routing: LSW-F1 opened (all 10 content fixes); LSW-G1 + LSW-G2 deferred (2 engine candidates blocked on solver access); 1 POKER_THEORY.md §11 entry queued for intentional-divergence framing. Bucket-teaching queue: 2 high-leverage widening targets (`river_brick_v_calls` fish-thin-value flip + `river_checkback` bluff-catch flip) — both blocked on F1 shipping first. | ✓ Audit file at `docs/design/audits/line-audits/btn-vs-bb-3bp-ip-wet-t96.md`; ✓ bucket-teaching queue populated (2 HIGH leverage, 1 PARTIAL, 1 SKIP); ✓ external-validation log per decision node; ✓ Stream F1 opened (P1); ✓ Stream G candidates parked deferred; ✓ Category-D divergence queued for POKER_THEORY.md §11 | Claude (main) 2026-04-22 |
| LSW-A2 | P1 | COMPLETE (DRAFT — awaits owner review) | **Audit `btn-vs-bb-srp-ip-dry-q72r` (2026-04-22)** — 14 nodes (6 decision + 8 terminal) walked through seven dimensions with ≥1 web-research query per decision node (10 queries total: 7 A, 2 B, 1 D). **Verdict: GREEN (light).** 6 findings: 1 P2 + 5 P3. All category-B/D, zero category-C. Key findings: (1) BB 3bet range "KK+/AA pre" is outdated vs solver (modern BB 3bets TT+/AJs+/AQo+) — acceptable as live-pool framing with one-sentence acknowledgment (Cat D). (2) Pot-odds math on `turn_checked_back` says ~30% when actual is 27.3% (same failure class as A1's `river_checkback` P1 but lower severity here — not threshold-sensitive). (3) "99 blocks 99 itself" copy nonsense on `river_after_turn_checkback`. (4) `capped_range_check` framework citation stretched on `flop_root`. (5) `terminal_bluff_catch_win` + `terminal_river_raise_spew` pot 16.0 doesn't reconcile. (6) `river_after_flop_checkback` uses 50% sizing where 33% is thin-value canon (owner preference). Routing: LSW-F2 opened (6 items, all S-effort P2/P3, bundleable); zero Stream G tickets; 1 POKER_THEORY.md §9.2 entry queued for D1 (BB live-pool flat range). Bucket-teaching queue: 3 HIGH-leverage river widening targets (`river_after_turn_checkback` = canonical bluff-catch flip with strongest external validation of any node audited so far, `river_after_barrel` = thin-value sizing flip, `turn_checked_back` = call-vs-probe flip). **Line is ready for LSW-B2 widening with or without F2 shipping first** (no P0/P1 blockers). | ✓ Audit file at `docs/design/audits/line-audits/btn-vs-bb-srp-ip-dry-q72r.md`; ✓ bucket-teaching queue populated (3 HIGH, 2 PARTIAL, 1 NONE); ✓ external-validation log per decision node; ✓ Stream F2 opened (P2/P3); ✓ zero Stream G candidates; ✓ Category-D divergence queued for POKER_THEORY.md §9.2 | Claude (main) 2026-04-22 |
| LSW-A3 | P1 | COMPLETE (DRAFT — awaits owner review) | **Audit `co-vs-bb-srp-oop-paired-k77` (2026-04-22)** — 8 nodes (3 decision + 5 terminal; actual count, backlog "3/2" was off-by-one) walked through seven dimensions with 3 external web-research queries across 3 decision nodes (2 A, 1 B). **Verdict: YELLOW.** 4 findings: 1 P1 + 3 P3. Key finding: **position mismatch** — hero declared as CO but CO is IP vs BB postflop; line's action flow (`villainAction: null` on every decision + prompt "Hero acts OOP" + `'oop'` tag) is consistent with OOP-hero framing. Fix is setup-only, not strategy rewrite: **Option A** (recommended) rename hero CO → SB (SB is OOP vs BB — matches authored action flow); **Option B** keep CO + flip action flow to IP (villainAction → `{kind:'check'}`, prompt → "Hero acts IP"). The `correct` flags on all 3 decisions are unaffected — cbet 33% on K77 paired is canonical ([solver 60.2% dominant choice](https://www.getcoach.poker/articles/heads-up-poker-a-strategic-guide-to-winning-one-on-one/)); turn bet-for-value is right; river 33% thin-value is textbook. Other findings: turn 50% sizing upper-end of paired-board canon (33% more standard — P3 owner preference); two shared terminals with pot-drift across parent paths (P3 structural, same pattern as A1/A2). Zero category-C engine findings. Zero POKER_THEORY.md §9 entries (no live-pool-vs-solver divergence). Bucket-teaching queue: 1 HIGH (river_after_barrel thin-value flip), 2 PARTIAL. **Line is BLOCKED on LSW-F3 shipping before B1/B2 widening** — stricter gate than A2 because position mismatch would propagate wrong archetype mapping into `correctByArchetype` authoring. | ✓ Audit file at `docs/design/audits/line-audits/co-vs-bb-srp-oop-paired-k77.md`; ✓ bucket-teaching queue populated (1 HIGH, 2 PARTIAL); ✓ external-validation log per decision node; ✓ Stream F3 opened (P1 blocker + 3 P3 polish); ✓ zero Stream G candidates; ✓ zero POKER_THEORY.md entries needed | Claude (main) 2026-04-22 |
| LSW-A4 | P1 | COMPLETE (DRAFT — awaits owner review) | **Audit `sb-vs-btn-3bp-oop-wet-t98` (2026-04-22)** — 8 nodes (3 decision + 5 terminal; actual count) walked through seven dimensions with 3 external web-research queries across 3 decision nodes (1 A, 2 B). **Verdict: YELLOW.** 6 findings: 2 P1 + 2 P2 + 2 P3. Key findings: (1) **Schema declaration mismatch** — `setup.hero.action: 'fourBet'` contradicts pot 21 + copy "SB flatted BTN 3bet" + tag `'3bp'`. Author's own comment "treat as 4bp for schema" is wrong. Fix: `hero.action` 'fourBet' → 'call' (3BP resolve). (2) **"AA is a bluff-catcher" framing is pedagogically wrong** — AA has ~60% equity vs BTN 3bet range on T98ss; it's strong-but-vulnerable, not a bluff-catcher. Solver mixes check (dominant) and small-polar-bet (10% frequency per GTO Wizard). Needs rationale rewrite. (3) Range/nut advantage frameworks inverted on T98ss (BTN has both, not hero). (4) "JJ coin-flip with hero range" factually wrong (AA vs JJ = 81/19). (5) SB flat of BTN 3bet is near-solver-impossible; authored "rare but OK" acknowledges but understates the divergence — route Category D to POKER_THEORY.md §9.3. (6) Terminal pot drift on shared terminal. Zero category-C engine findings. **Line is BLOCKED on LSW-F4 shipping before B1/B2 widening AND blocked on G4-TD-3** (archetypeRanges for BTN_vs_SB 3bet-caller) before v2 migration. Bucket-teaching queue: 2 HIGH (flop_root bet/check by archetype, river_after_turn_call scary-runout overpair fold), 1 PARTIAL. | ✓ Audit file at `docs/design/audits/line-audits/sb-vs-btn-3bp-oop-wet-t98.md`; ✓ bucket-teaching queue populated (2 HIGH, 1 PARTIAL); ✓ external-validation log per decision node; ✓ Stream F4 opened (2 P1 blockers + 4 P2/P3); ✓ zero Stream G candidates; ✓ 1 POKER_THEORY.md §9.3 entry queued (D2 SB-flat divergence) | Claude (main) 2026-04-22 |
| LSW-A5 | P2 | NEXT | **Audit `co-vs-btn-bb-srp-mw-oop`** — 3 nodes (2 decision). MW SRP OOP — tests MW framework coverage from LS Phase 5. | ✓ Audit file + verdict + queue table + external-validation log per decision node (zero-query audits fail review) + F batch if content-wrong + G batch if engine-wrong | — |
| LSW-A6 | P2 | NEXT | **Audit `btn-vs-bb-sb-srp-mw-j85`** — 10 nodes (4 decision). MW SRP IP — biggest MW node count. | ✓ Audit file + verdict + queue table + external-validation log per decision node (zero-query audits fail review) + F batch if content-wrong + G batch if engine-wrong | — |
| LSW-A7 | P2 | NEXT | **Audit `utg-vs-btn-squeeze-mp-caller`** — 2 nodes (1 decision). Preflop-heavy MW squeeze; narrow but pedagogically unique. | ✓ Audit file + verdict + queue table + external-validation log per decision node (zero-query audits fail review) + F batch if content-wrong + G batch if engine-wrong | — |
| LSW-A8 | P2 | NEXT | **Audit `utg-vs-btn-4bp-deep`** — 3 nodes (1 decision). Rarest spot; audit last. | ✓ Audit file + verdict + queue table + external-validation log per decision node (zero-query audits fail review) + F batch if content-wrong + G batch if engine-wrong | — |

### Stream B — `heroHolding` widening (5 sessions, per-line gated by Stream A)

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LSW-B1 | P1 | BLOCKED by LSW-A2..A8 (HU lines) + LSW-G6 (MW lines) | **Flop decision roots** — author `heroHolding` on 7 remaining flop roots. Hero combo + bucketCandidates pulled from each line's audit queue. Single commit per session (HU batch + MW batch once G6 lands). **Unblocked so far:** LSW-A1 complete (2026-04-22 content audit), LSW-H1 complete (2026-04-22 surface mechanical fixes), LSW-H3 complete (2026-04-22 visual sweep + per-line feasibility checklist). **Per-line feasibility from LSW-H3 sweep (surface audit appendix):** (a) Q72r `['A♠Q♣']` / `['topPair']`, (b) K77 `['A♥K♦']` / `['topPair']`, (c) T98 `['A♦A♣']` / `['overpair']` (highest-SPI target), (d) AK2 `['A♥K♦']` / `['twoPair']`, (e) J85mw `['A♦J♣']` / `['topPair']` **blocked on LSW-G6**, (f) Q53mw `['A♠Q♣']` / `['topPair']` **blocked on LSW-G6**, (g) UTG-squeeze pre_root **skip — preflop-only, outside B1 scope**. **Still blocked by:** A2..A8 per-line content audits (may revise recommended `combos` / `bucketCandidates`) before authoring on any HU line; LSW-G6 MW bucket-EV engine before authoring on MW lines. | ✓ 4 HU flop roots (Q72r, K77, T98, AK2) authored with validated `heroHolding`; ✓ 2 MW flop roots (J85, Q53) authored post-G6; ✓ UTG-squeeze pre_root excluded in acceptance; ✓ `lineSchema.validateLine` passes for all 8 lines; ✓ `engineAuthoredDrift.test.js` green (re-baseline if snapshot shifts); ✓ 1600×720 visual verification of picker + reveal panel on each | — |
| LSW-B2 | P2 | BLOCKED by LSW-B1 + per-line A | **Turn decision nodes where bucket shifts** — nodes where turn card peels/completes/pairs and the student's bucket class changes. Node list from Stream A dimension-6 output (est. ~8 nodes). | ✓ Every flagged turn decision has `heroHolding`; ✓ tests green; ✓ visual verification | — |
| LSW-B3 | P3 | BLOCKED by LSW-B2 | **River decision nodes** — narrow-specificity bucket teaching (nutFlush vs secondFlush vs weakFlush, etc.). Node list from Stream A (est. ~10 nodes). | ✓ Every flagged river decision has `heroHolding`; ✓ tests green; ✓ visual verification | — |

### Stream C — `correctByArchetype` widening

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LSW-C | P1 | THREADED into LSW-B1..B3 | **Archetype correctness widening** — each B tier commit ALSO authors `correctByArchetype` on decision branches where the audit declared a fish/reg/pro split. Not a separate session. | ✓ Every decision with audit-declared archetype split has `correctByArchetype`; ✓ `resolveBranchCorrect` fallback continues to hold for decisions without; ✓ walkthrough toolbar toggles flip badge coherently | — |

### Stream D — Engine accuracy follow-on (3 sessions, parked until A+B1+B2 land)

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LSW-D1 | P1 | BLOCKED by LSW-B2 | **Depth-2 injection** — wire `depth2Eval` → `evaluateGameTree` depth-2 rollout in `drillModeEngine.js`. Replace `HERO_BUCKET_TYPICAL_EQUITY` coarse table with real per-combo equity. Plumb `bailedOut` + `time-budget-bailout` caveat from depth-2. Remove `v1-simplified-ev` caveat when depth-2 was used. Re-baseline `engineAuthoredDrift.test.js` (sentinel constants may shift). | ✓ `v1-simplified-ev` caveat no longer appears on live panel (except when depth-2 bailed out); ✓ drill-mode EV within 5% of `evaluateGameTree` depth-2 output on a validation probe grid; ✓ tests green | — |
| LSW-D2 | P2 | BLOCKED by LSW-D1 | **EV cache + engineVersion stamp (NEV-12 forward requirement from RT-118)** — in-memory cache keyed by `(bucketId, archetype, boardHash, pot, engineVersion)`; invalidate on version mismatch. | ✓ Cache hit rate >80% on full LinePicker → every-decision walk (one-time measurement); ✓ `engineVersion` bump invalidates cache (unit-tested); ✓ NEV-12 invariant passes | — |

### Stream E — Polish (rolling, no separate tickets)

Threaded into any B or D session: caveat copy pass, empty-bucket UX wording, low-sample UX crowding check, 1600×720 toolbar stress test after B1 ships.

### Stream F — Content fixes from audit category-B findings (opened per audit as needed)

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LSW-F1 | P1 | COMPLETE (2026-04-22) | **JT6 line content fixes from LSW-A1 audit** — shipped single commit: A1 (flop_root `why` rewrite, frames BB donk as live-pool exploit not nut-advantage-driven), A2 (raise-vs-fish sub-archetype nuance), A3 (TPTK-ish → top-pair-strong-kicker), A5 (river_checkback pot-odds 25% → 30% for 75% bet — math rewritten in why-section), A6 (node rename `river_brick_v_checkraises` → `turn_brick_v_checkraises`, 2 string changes), A7 (pot-accounting reconciled: node pot 108→184, child terminals 108→184 and 200→258; prompt "90bb total"→"112bb total"; why-section math: "47bb to win 155bb"→"75bb to call into 184bb pot, ~29% equity"), A8 (new terminal `terminal_river_overfold_bluffcatch` for river-fold path; river_checkback Fold branch rerouted), A9 (`terminal_flop_raise_folds_weak` pot 50→38.5 reflecting BB-calls-raise state with comment; NOT removed per plan — schema requires pot), A10 (`terminal_river_overbet_spew` pot kept at 78 for most-common reach path with comment documenting the multi-path ambiguity). **A4 (framework citation thin) deferred** per audit's own guidance ("do NOT pad frameworks"). D1 (live-pool donk divergence) added as POKER_THEORY.md §9.1. engineAuthoredDrift snapshot regenerated for the node-key rename. | ✓ All P1 shipped; ✓ P2/P3 shipped except A4 explicitly deferred; ✓ lineSchema validates all lines; ✓ postflop-drill 311/311 green; ✓ full suite 6257/6258 (1 pre-existing precisionAudit flake unrelated); ✓ build clean; ✓ LSW-A1 audit sign-off closed with linked commit (commit hash TBD at commit time) | Claude (main) 2026-04-22 |
| LSW-F4 | P1 | COMPLETE (2026-04-22) | **T98 line content fixes from LSW-A4 audit** — single commit shipped: A1 (hero.action 'fourBet' → 'call'; dropped author's inline "treat as 4bp" note; line correctly declared as 3BP SB-flat-3bet defense). A2 (AA framing rewrite — replaced "bluff-catcher" with "strong-but-vulnerable, ~60% equity vs range, ahead of range but vulnerable to runout cards"; removed factual error "JJ coin-flip with hero range"). A3 (frameworks `range_advantage` + `nut_advantage` dropped as inverted — BTN has both on T98ss not hero; replaced with `board_tilt`). A4 (POKER_THEORY.md §9.3 SB-flat-3bet divergence entry appended). A5 (solver-mix acknowledgment added to Check rationale — ~10% small-polar-bet mix with strong overpairs; also added live-pool pathway label to flop_root prose section). A6 (terminal_raise_wet_aa pot 21.0 preserved with comment acknowledging multi-parent pot drift). Full suite 6388/6389 green (1 pre-existing precisionAudit flake in unrelated directory unchanged). `npm run build` clean (11.15s, PWA 1630 KiB). | ✓ All 6 shipped; ✓ LSW-A4 audit sign-off closed | Claude (main) 2026-04-22 |
| LSW-F3 | P1 | COMPLETE (2026-04-22) | **K77 line content fixes from LSW-A3 audit** — single commit shipped: A1 chose Option A (rename hero CO → SB). Edits: `setup.hero.position` CO→SB, `villains[0].vs` CO→SB, line id `co-vs-bb-...` → `sb-vs-bb-...`, constant `LINE_CO_VS_BB_SRP_OOP_PAIRED` → `LINE_SB_VS_BB_SRP_OOP_PAIRED`, title + summary rewrites, `villainRangeContext.baseRangeId` `co_vs_bb_srp_bb_flat` → `sb_vs_bb_srp_bb_flat`. `villainRanges.js` alias key + tuple `vs: 'CO'` → `vs: 'SB'`. `villainRanges.test.js` 3 test assertions updated. Audit file renamed via `mv`. A2 chose canon (50% → 33% turn sizing); pot cascade: river_after_barrel 18.1 → 15.1; 4 terminal pots in 18.1 → 15.1. A3 (terminal pot reconciliation) landed as inline comments on shared terminals documenting multi-parent drift. A4 (villainAction ambiguity) zero edits per Option A. `engineAuthoredDrift.test.js` snapshot regenerated (3 lineId entries). Full suite 6388/6389 green (1 pre-existing precisionAudit flake unrelated). Build clean (11.15s, PWA 1630 KiB). Forward-looking docs updated: line-study.project.md, line-study-slice-widening.project.md (3 references), btn-vs-bb-3bp-ip-wet-t96-surface.md (2 references). | ✓ All 4 shipped; ✓ LSW-A3 audit sign-off closed | Claude (main) 2026-04-22 |
| LSW-F2 | P2 | COMPLETE (2026-04-22) | **Q72r line content fixes from LSW-A2 audit** — single commit shipped: A1 (flop_root `why` rewrite — live-pool acknowledgment "live cash flats QQ-TT much more often than solver, which 3bets QQ+/JJ/TT+"; tightens nut-advantage claim from "both favor BTN" to "modest, not strong"). A2 (flop_root frameworks — dropped `capped_range_check` and replaced with `nut_advantage` which the `why` section actually discusses). A3 (turn_checked_back pot-odds "~30%" → "~27%" — correct math for 60% pot bet is 27.3%). A4 (river_after_turn_checkback "99 blocks 99 itself" replaced with plain un-blocker note pointing decision to population bluff frequency). A5 (river_after_flop_checkback 50% sizing preserved per owner-pragmatic choice; rationale sentence added explaining why 50% here vs 33% canon on the sibling `river_after_barrel` branch — villain's probe-turn-called-then-checked range is weaker but LESS condensed). A6 (terminal_bluff_catch_win + terminal_river_raise_spew pot 16.0 → 22.7; math: turn pot 9.1 + 6.825 villain bet + 6.825 hero call = 22.75 rounded). POKER_THEORY.md §9.2 appended (BB live-pool flat-range divergence). Zero schema changes, zero node renames. Full suite 6388/6389 green (1 pre-existing precisionAudit flake unrelated). Build clean (11.15s, PWA 1630 KiB). | ✓ All 6 shipped; ✓ POKER_THEORY.md §9.2 appended; ✓ LSW-A2 audit sign-off closed | Claude (main) 2026-04-22 |
| LSW-F* | varies | LAZY | **Future content-fix batches per audit** — one F entry opened per audit that found category-B P0/P1 issues (our authored content disagrees with external truth). Single-commit batch applying the audit's findings to `lines.js` (and `frameworks.js` / `multiwayFrameworks.js` if framework citation changes). | ✓ Audit's category-B P0/P1 findings show all items shipped or explicitly waived with reasoning | — |

### Stream G — Engine / system upgrades from audit category-C findings (new 2026-04-22, opened per finding)

Audit-driven engine changes: when external validation reveals a number our *engine* produces is wrong, it routes here. This is the feedback loop the owner requested 2026-04-22 — "any deviations from our predictions should inform changes or upgrades to our system."

Typical surfaces: `POP_CALLING_RATES`, `HERO_BUCKET_TYPICAL_EQUITY`, `ARCHETYPE_BUCKET_MULTIPLIERS`, fold-curve parameters, bucket taxonomy refinements, hand-classification edge cases.

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LSW-G1 | P2 | DEFERRED (blocked on solver access) | **Candidate: `HERO_BUCKET_TYPICAL_EQUITY.topPair` overstates on wet boards.** LSW-A1 surfaced: authored "~40%+ equity" claim for JT on T96ss vs BB's 3bet range is consistent with combinatorial estimation (37–44%) but far below our current `HERO_BUCKET_TYPICAL_EQUITY.topPair = 0.68` constant. This suggests the table needs board-texture conditioning, not just bucket-level values. **Blocked on:** access to GTO Wizard subscription or a direct PioSOLVER run to verify the exact equity distribution on T96ss in 3BP. | ✓ Solver-verified equity value; ✓ new board-conditioned table or multiplier lands; ✓ tests updated; ✓ LSW-A1 audit category-C row resolved | — |
| LSW-G2 | P3 | DEFERRED (blocked on population data) | **Candidate: `ARCHETYPE_BUCKET_MULTIPLIERS.fish` may not shift bucket composition enough to reflect live-pool fish donk ranges.** LSW-A1 audit suggests fish-archetype donk ranges on wet middling boards contain more `middlePair` + `gutshot` + random-draw combos than our current multipliers reproduce. **Blocked on:** population-anchored fish-range dataset (live cash tracker data or published fish-pool study). | ✓ Revised multipliers backed by population data; ✓ archetypeRangeBuilder tests updated; ✓ LSW-A1 audit category-C row resolved | — |
| LSW-G3 | P1 | COMPLETE (2026-04-22) | **Backdoor flush-draw + backdoor straight-draw taxonomy + priors** (surface audit S4). Shipped. G3a (audit): `exploitEngine/postflopNarrower.js` `detectDraws` already computed `hasBackdoorFlush` / `hasBackdoorStraight` flags — classifier was discarding them by falling through to `air`. G3b (extension): `rangeSegmenter.js` gained 3 new HAND_TYPES (`airBackdoorCombo`, `airBackdoorFlush`, `airBackdoorStraight`) + display labels + group membership + classifier emission (air-only, so made-hand shapes and direct draws still win strongest-first — per-combo TP+BDFD bump is LSW-H2's job). `bucketTaxonomy.js` gained 3 new BUCKET_TAXONOMY entries (`backdoorFlushDraw → [airBackdoorFlush, airBackdoorCombo]`, `backdoorStraightDraw → [airBackdoorStraight, airBackdoorCombo]`, `backdoorCombo → [airBackdoorCombo]`). `drillModeEngine.js` HERO_BUCKET_TYPICAL_EQUITY gained priors (airBackdoorFlush: 0.20, airBackdoorStraight: 0.15, airBackdoorCombo: 0.25; aggregate bucket entries match). `handTypeBreakdown.js` `pctAir` broadened to include the 3 new air-backdoor types, preserving `pctAir + pctWeakDraws + pctStrongDraws + pctAnyPairPlus ≈ 1`; added `pctAirStrict` for callers that want pre-G3 strict semantics. Whiff framework / scenario classifications unchanged (backdoor still counts as "whiff" for direct-equity purposes — they fold to c-bets). **NOT changed:** `flop_root.heroHolding.bucketCandidates` stays at `['topPair']` — J♥T♠ classifies as `topPairGood` (strongest-wins), so `airBackdoor*` buckets wouldn't populate. Honest widening to surface JT6's TP+BDFD+BDSD is LSW-H2's hero-combo EV row, not a bucket. **Verification:** observed at JT6 flop_root that J♥T♠ classifies `topPairGood` with `hasBackdoorFlush=true` / `hasBackdoorStraight=true` detectable via `detectDraws` (pre-existing); G3 adds three classifier branches + taxonomy entries + priors. Not visually re-walked (taxonomy-only change with no content update — visual would be identical to post-H1 walk). | ✓ `rangeSegmenter.test.js` +6 new cases (BDFD-only / BDSD-only / combo / topPair-with-backdoor keeps topPairGood / pure-air on dry board / HAND_TYPES exports); ✓ `bucketTaxonomy.test.js` +5 new cases (3 bucket IDs known, 3 maps match, 1 population check, 1 superset check); ✓ handTypeBreakdown sum invariant preserved; ✓ scenarioValidator whiff-rate classifications unchanged; ✓ full suite green; ✓ no line-content update required — future-BDFD-only hero holdings can reference the new buckets honestly | Claude (main) 2026-04-22 |
| LSW-G4 | P1 | COMPLETE (roundtable 2026-04-22; Path 2 selected; spec drafted) | **Paradigm roundtable — villain-bucket-first BucketEVPanel redesign** (surface audit S5). Gate-2 Blind-Spot Roundtable shipped at `docs/design/audits/2026-04-22-blindspot-lsw-g4-villain-first-panel.md` (verdict YELLOW, 3 paths presented). **Owner selected Path 2 (full restructure)** 2026-04-22 with forward-compatibility directive: "design for the final state so we don't redesign when populating more slices — variation in hero hands, first action being hero's, varied villain first actions." **Gate-4 spec drafted at `docs/design/surfaces/bucket-ev-panel-v2.md`** covering 8 axes of variation (villain first action / who-acts-first / hero view type / villain count / street depth with range narrowing / range source / archetype scope / action granularity), 8 primitives (VillainRangeDecomposition / WeightedTotalTable / HeroViewBlock / ActionRecommendationStrip / StreetNarrowingContext / ConfidenceDisclosure / PerVillainDecomposition / MixedStrategyDisclosure), 11 canonical variants (V1-V11) with bluff-catch (V5) + thin-value (V6) first-class per owner directive, data contract for `ComputeBucketEVsInput`/`Output`, schema extensions for `lines.js` (`heroView`, `villainRangeContext`, `narrowingRule`), persona × JTBD coverage table, v1 ship scope (V1-V6 + primitives P1-P6), extension seams for axes 6-8 + MW + range-level + mixed-strategy, 5-commit migration path (primitive extraction → engine v2 contract → JT6 canary → LSW-B1 line migration → v1 panel deletion), 6 open Gate-4 questions, 5 risks. Spec currently under 3 parallel specialist reviews (systems-architect / product-ux-engineer / senior-engineer). Opens downstream tickets on sign-off: LSW-G4-IMPL-{1..5} commits, LSW-J1 Gate-3 JTBD additions, LSW-J2 first-principles-learner situational persona. | ✓ Roundtable closed; ✓ owner path recorded + dated; ✓ Gate-4 spec file exists with 8 axes + 8 primitives + 11 variants + data contract + v1 scope + migration path; ✓ 3 specialist reviews completed; ✓ review findings synthesized into revised spec or tracked as Gate-4 heuristic-audit backlog | Claude (main) 2026-04-22 |
| LSW-G4-IMPL | P1 | COMPLETE (all 6 commits shipped 2026-04-22) | **Implementation of `bucket-ev-panel-v2` per spec.** 6-commit migration path. **Commit 1 COMPLETE** (primitive extraction): sub-components extracted to `panels/`. **Commit 2 COMPLETE** (engine v2): `computeBucketEVsV2` + `computeDecomposedActionEVs` + `computeValueBeatRatio` + `GROUP_CALL_RATES` parallel exports; v1 untouched. **Commit 2.5 COMPLETE** (schema v3 + villainRanges alias): new `villainRanges.js` alias-layer over `archetypeRanges.js` (seeded with JT6 + Q72r + K77 aliases; T98/AK2/MW deferred); `lineSchema.js` bumped 2→3 with new validators for `heroView` / `villainRangeContext` / `narrowingSpec` + migration guard rejecting dual-authored `heroHolding + heroView`; enum exports `HERO_VIEW_KINDS` / `DECISION_KINDS` / `DECISION_STRATEGIES` / `NARROWING_SPEC_KINDS`. Tests: new `villainRanges.test.js` 13/13 + `lineSchema.v3.test.js` 26/26; v2 `lineSchema.test.js` SCHEMA_VERSION test bumped 2→3; regression 441/441 across full drillContent + PostflopDrillsView; `npm run build` clean. **Commit 4 COMPLETE** (LSW-B1 partial): Q72r + K77 flop_root migrated `heroHolding → heroView` + `villainRangeContext.baseRangeId` + `decisionKind: 'standard'`. Q72r: `['A♠Q♣']` / `['topPairGood']` / `btn_vs_bb_srp_bb_flat`. K77: `['A♥K♦']` / `['topPairGood']` / `co_vs_bb_srp_bb_flat`. Visual verify (`docs/design/audits/evidence/lsw-g4-impl-commit4-{q72r,k77}-v2.png`): Q72r shows AQ DOMINATING 5 regions (backdoor SD 36.3%, middle pair 20.9%, backdoor combo 13.7%, air 10.1%, TP strong 5.8%); K77 shows AKo DOMINATING underpair 72.6% + twoPair 17% and CRUSHED by trips 10% + premium 0.4%. Tests 455/455, build clean. T98 + AK2 deferred pending G4-TD-3 archetypeRanges authoring. MW lines deferred on LSW-G6. **Commit 3 COMPLETE** (JT6 canary): 11 new files shipping `BucketEVPanelV2.jsx` composition root reading `VARIANT_RECIPES` + 7 new primitives (P1 VillainRangeDecomposition expanded-by-default with 3 modes, P2 WeightedTotalTable with responsive layout, P3 HeroViewBlock with EV-prop-excluded interface, P4 ActionRecommendationStrip, P5 StreetNarrowingContext, P6 ConfidenceDisclosure, P6b GlossaryBlock) + shared BucketLabel + useResponsiveBreakpoint hook. BUCKET_DEFINITIONS + BUCKET_DISPLAY_NAMES added to bucketTaxonomy.js. DOMINATION_GROUPS definitions added to drillModeEngine.js. JT6 flop_root migrated `heroHolding → heroView`; LineNodeRenderer dual-paths on `heroView` presence. 14 new variantRecipes tests + 441→455 full regression. Visual verify 1600×720: P1 villain-first expanded showing 5 decomposition rows (Overcards Ax 45.2%/76%eq · Overpair CRUSHED 28.8%/19%eq · Gutshot FAVORED 20.5%/73%eq · Non-nut FD 2.7%/72%eq · Set CRUSHED 1.4%/8%eq) with archetype-invariance annotation + ⓘ tap targets; scrolled view shows P2/P4/P3/P6/P6b stack. **Known v1-ship limitations:** call-kind EV modeling stubbed (raise picked as best when authored answer is Call — v1.1 scope); authoredReason override path deferred. **Remaining commits:** Commit 4 (LSW-B1 line migration — Q72r + K77 + T98 + AK2; T98/AK2 blocked on G4-TD-3 archetypeRanges.js expansion) → Commit 5 (v1 mechanical deletion). Deferred: MW (V9, blocked on LSW-G6), range-level V10, preflop V11 (wrong surface), mixed strategy (P8 stub). | ✓ All 6 commits land (2.5/6 done); ✓ JT6 + 4 HU flop roots render via v2 panel; ✓ v1 panel code deleted from codebase; ✓ all tests green; ✓ 1600×720 + 900×400 visual verification at each commit; ✓ Gate-4 F01/F02/F03/F04 fixes present in Commit 3 | Claude (main) 2026-04-22 |
| LSW-J1 | P1 | COMPLETE (2026-04-22) | **Gate-3 JTBD additions.** Added DS-48 (villain range as decision driver), DS-49 (weighted-total EV decomposition), DS-50 (line-study walkthrough — promoted from implicit), DS-51 (range-shape recognition — promoted from implicit) to `docs/design/jtbd/domains/drills-and-study.md`. Also updated `docs/design/surfaces/postflop-drills.md` JTBD list to reference the now-explicit DS-50/51. | ✓ 4 new JTBDs in atlas with standard shape; ✓ postflop-drills surface artifact updated with explicit references; ✓ all 4 marked Active | Claude (main) 2026-04-22 |
| LSW-J2 | P2 | COMPLETE (2026-04-22) | **Gate-3 persona work.** New situational persona at `docs/design/personas/situational/first-principles-learner.md` capturing the range-first-cognition habit-formation window — the primary paradigm-sensitive persona for the `bucket-ev-panel-v2` restructure. Cross-referenced from `apprentice-student.md` + `surfaces/postflop-drills.md`. | ✓ Persona file created and referenced; ✓ apprentice-student.md updated with sub-persona link; ✓ postflop-drills surface artifact references new persona | Claude (main) 2026-04-22 |
| LSW-G4-HEUR | P1 | COMPLETE (2026-04-22) | **Gate-4 heuristic audit of v2 spec.** Walked spec through Nielsen-10 + Poker-Live-Table (applicable subset) + Mobile-Landscape. Verdict YELLOW. 14 findings (4 P1 pre-Commit-1 blockers resolved in-spec: F01 glossary as first-class P6b primitive with inline tap-for-definition, F02 responsive layout at <1200/<900/<640 viewports, F03 errorState discriminant added to output contract, F04 unified "Show all N" disclosure pattern across P1 row-overflow + P2 column-overflow; 8 P2 findings tracked; 3 P3 deferred). Audit at `docs/design/audits/2026-04-22-heuristic-bucket-ev-panel-v2.md`. | ✓ Audit file exists; ✓ 4 P1 findings resolved in `bucket-ev-panel-v2.md` with evidence; ✓ remaining findings tracked in audit + spec; ✓ Commit 1 unblocked | Claude (main) 2026-04-22 |
| LSW-G5 | P1 | COMPLETE (2026-04-22) | **Domination map below pinned-combo row** (surface audit S6). Shipped. New `computeDominationMap` pure async helper in `drillModeEngine.js` segments villain range by a 12-entry `DOMINATION_GROUPS` taxonomy (overpair deliberately split from topPair for pedagogy), computes per-group equity via handVsRange (MC trials=250 per group, parallel), classifies via `classifyDomination` into crushed/dominated/neutral/favored/dominating based on equity thresholds. Attached to `pinnedCombo.dominationMap` on the `computeBucketEVs` output. New `DominationMapDisclosure` component — collapsed by default, expands to show one row per group with color-coded relation badge + weight% + equity%. Uses base villain range (archetype-invariant for hero's showdown equity; archetype-weighted refinement deferred). **1600×720 Playwright walk on Reg + Fish + Pro confirms:** JT6 flop_root shows 4 groups — FAVORED Overcards 45.2%/79% · DOMINATED Overpair 28.8%/22% · FAVORED Direct Draws 24.7%/62% · CRUSHED Set/Trips 1.4%/8% (vs Reg). Per-group equities shift with archetype via MC variance; weightPct distribution is identical across archetypes (base range). Evidence `evidence/btn-vs-bb-3bp-ip-wet-t96-flop_root-post-g5-{11..14}-*.png`. **Verification limits:** observed on flop_root only; other pinned-combo nodes elsewhere not yet verified (H3 sweep will flag). | ✓ 6 new cases in drillModeEngine.test.js (classifyDomination pure thresholds + NaN fallback); ✓ 5 new cases in BucketEVPanel.test.jsx (dominationMap population / row shape / heaviest-first sort / overpair crushed-or-dominated for JT6 / absent when no pinned combo); ✓ full G5 test files green (65/65); ✓ 4 screenshots archived; ✓ surface audit appendix updated | Claude (main) 2026-04-22 |
| LSW-G* | varies | LAZY | **Future engine change tickets from audit findings** — one G entry per category-C audit finding. Cites the originating audit + external source + specific engine change + test delta. Every G ticket re-baselines `engineAuthoredDrift.test.js` and may loop back to re-run the audit node it surfaced from. | ✓ Engine change shipped + tests green + drift snapshot re-baselined + originating audit's category-C row marked resolved | — |

### Stream H — Surface quality (UI rendering findings from surface audits, new 2026-04-22)

Audit-driven UI rendering changes. Scope distinct from Stream F (content / `lines.js` schema) and Stream G (engine / numeric truth). Findings enter Stream H when a combined poker-expert + product-UX live-surface walk (1600×720, Playwright) surfaces a rendering defect that content + engine fixes cannot address.

Opened from [`docs/design/audits/line-audits/btn-vs-bb-3bp-ip-wet-t96-surface.md`](../../docs/design/audits/line-audits/btn-vs-bb-3bp-ip-wet-t96-surface.md).

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LSW-H1 | P0 | COMPLETE (2026-04-22) | **Mechanical safe fixes: per-hero feasibility gate + PotOddsCalculator seeding + `air`-rejection validator** (surface audit S1 + S3 + S7). Shipped in a single commit. Changes: `lines.js:701` (flop_root bucketCandidates `['topPair', 'flushDraw', 'openEnder', 'overpair', 'air']` → `['topPair']` interim until LSW-G3 adds BDFD/BDSD; inline comment explains rationale); flop_root compute section now carries explicit `seed: { pot: 27.3, bet: 6.8 }`; `BucketEVPanel.jsx` exports `isRowApplicable` + collapses inapplicable buckets (error, `empty-bucket` caveat, or `sampleSize === 0`) behind a new `InapplicableDisclosure` component when `heroHolding.combos.length >= 1`; non-pinned nodes render every row inline as before; `lineSchema.js` rejects `air` in `bucketCandidates` when combos non-empty + accepts optional `compute.seed: { pot, bet }` with non-negative finite number validation; `LessonCalculators.jsx` `PotOddsCalculator` accepts optional `initialPot` / `initialBet` props with `toFiniteNonNeg` / `toFinitePositive` guards falling back to 100/50 generics; `LineNodeRenderer.jsx` `ComputeSection` threads `node` + computes `calculatorPropsForCompute(section, node)` — `section.seed` takes precedence, else `{ pot: node.pot, bet: node.pot * (node.villainAction?.size ?? 0.5) }`. **Post-H1 1600×720 walk on Reg + Fish + Pro (Playwright) confirms:** only `topPair` row renders (no phantom rows), calculator opens with 27.3/6.8 showing 16.6% break-even, archetype-toggle still shifts EV across Reg/Fish/Pro (+20.30 / +20.23 / +20.32bb). Evidence saved to `docs/design/audits/line-audits/evidence/btn-vs-bb-3bp-ip-wet-t96-flop_root-post-h1-{04..07}-*.png`. Surface audit's Batch-1-closure appendix updated with findings status + screenshot references. **Not touched:** S2/S4/S5/S6 remain OPEN per plan; LSW-H3 visual sweep still required before LSW-B1 can widen. | ✓ `lineSchema.test.js` +7 new cases (air rejection + `compute.seed` validation); ✓ `BucketEVPanel.test.jsx` +8 new cases (`isRowApplicable` predicate + content-pin regression guards); ✓ `LessonCalculators.test.jsx` new file (+10 cases for seeding + fallback + computed output); ✓ smart-test-runner 6286/6287 passing (1 pre-existing precisionAudit flake unrelated); ✓ audit's Batch-1-closure appendix updated with evidence + per-finding status; ✓ 1600×720 walk confirms only topPair row + calculator 27.3/6.8/16.6% + archetype toggling intact | Claude (main) 2026-04-22 |
| LSW-H2 | P0 | COMPLETE (2026-04-22) | **Hero-combo-specific EV row above bucket table** (surface audit S2). Shipped. New `computePinnedComboEV` pure helper in `drillModeEngine.js` computes per-combo equity via `handVsRange` (MC, trials=800) against villain's base range + applies per-action EV using the same archetype-weighted `foldPct`. New `parseComboString` export in `BucketEVPanel.jsx` parses `heroHolding.combos[0]` (e.g., "J♥T♠") → `{ card1, card2 }` card integers. `computeBucketEVs` now runs the pinned path in parallel with bucket evaluations and returns `result.pinnedCombo`. New `PinnedComboRow` component renders above the bucket table with distinct amber typography showing combo / equity / best-action+EV / runner-up. `BucketEVTable` accepts `demoted` prop; when pinnedCombo is present the bucket table: (1) gets a small-caps divider label ("RANGE-LEVEL VIEW · OTHER COMBOS IN YOUR RANGE THAT FALL IN THESE BUCKETS"), (2) header cell shifts from "Your hand class" to "Bucket (other combos in your range)", (3) 80% opacity + softer border. Non-pinned nodes unchanged. **Verification** (1600×720 Playwright, all 3 archetypes): Reg → equity 56.1% / bet 150% +17.99bb (topPair bucket avg +20.30bb); Fish → 57.7% / +17.95bb (+20.23bb); Pro → 59.6% / +19.47bb (+20.32bb). Per-combo EV consistently ~2bb lower than topPair bucket average across archetypes — honest surface of the fact that J♥T♠ is below-average for BTN's flat-3bet TP range (dominated by AT/KT/QT). Archetype sensitivity visible in both equity (tighter-range → higher equity for hero) and bet EV (Fish lowest fold equity → smaller bet 75% EV delta). Evidence `evidence/btn-vs-bb-3bp-ip-wet-t96-flop_root-post-h2-{08..10}-*.png`. **NOT changed:** `evaluateDrillNode` signature — pinnedCombo is a separate helper; bucket rows still use coarse `HERO_BUCKET_TYPICAL_EQUITY` priors with `v1-simplified-ev` caveat; depth-2 replacement is LSW-D1. | ✓ BucketEVPanel.test.jsx +8 cases (parseComboString shape / pinned-combo block returns expected structure / equity within plausible range / per-combo EV differs from bucket EV / graceful null on malformed input); ✓ full surface test file 27 tests green; ✓ 1600×720 walk on Reg/Fish/Pro confirms per-combo row + demoted bucket table + archetype responsiveness; ✓ 3 screenshots archived; ✓ surface audit appendix updated with H2 closure | Claude (main) 2026-04-22 |
| LSW-H3 | P1 | COMPLETE (2026-04-22) | **Visual verification sweep of remaining nodes + lines (blocker for LSW-B1 widening)** (surface audit closure support). Shipped. 1600×720 Playwright walk of 11 target nodes: 4 remaining decision nodes on `btn-vs-bb-3bp-ip-wet-t96` (`turn_after_call`, `river_brick_v_calls`, `river_checkback`, `turn_brick_v_checkraises`) + 7 other-line flop roots (Q72r, K77, T98, AK2, J85mw, Q53mw, UTG-squeeze pre_root). All nodes render cleanly post-H1/H2/G3/G5/G5.1/G5.2; no S1/S3/S7 surface defects fire (only JT6 `flop_root` has `heroHolding` today, so BucketEVPanel doesn't render on the other 10 nodes). **F1 pot-accounting corrections verified live** on `river_checkback` (WHY section reads `25.5 / 85 → ~30%` equity) and `turn_brick_v_checkraises` (pot 184bb, math `74.8 / 258.4 → ~29%`). **Per-line feasibility checklist produced** for LSW-B1 consumption: (a) Q72r `A♠Q♣` / `['topPair']`, (b) K77 `A♥K♦` / `['topPair']` or `['topPairGood']`, (c) T98 `A♦A♣` / `['overpair']` (highest-SPI B1 target), (d) AK2 `A♥K♦` / `['twoPair']`, (e) J85mw `A♦J♣` / `['topPair']` **blocked on new LSW-G6 (MW bucket-EV engine)**, (f) Q53mw `A♠Q♣` / `['topPair']` **blocked on LSW-G6**, (g) UTG-squeeze pre_root **skip — preflop decision, no bucket semantics**. **3 new findings surfaced:** H3-F1 P2 — `BucketEVPanel.computeBucketEVs` has no MW protocol (routes to new LSW-G6); H3-F2 P3 — preflop-as-flop schema workaround at `lines.js:2158` undocumented in POKER_THEORY.md §9 (routes to next POKER_THEORY.md touch); H3-F3 P3 — `topPair` vs `topPairGood`/`topPairWeak` taxonomy drift between H1 interim and G5.1 split (routes to optional consistency pass). **Evidence:** `evidence/lsw-h3-{00..11}-*.png` (12 screenshots). Surface-audit appendix updated with filled-in sweep section + per-line checklist + change-log entry. **LSW-B1 now unblocked on 4 of 7 remaining flop roots.** | ✓ all 4 remaining decision nodes walked; ✓ 7 other-line flop roots walked; ✓ per-node screenshot + appendix paragraph; ✓ per-line feasibility checklist produced + ready for LSW-B1 acceptance criteria reference; ✓ 3 new findings surfaced + routed (1 P2 → LSW-G6, 2 P3 → doc passes); no code changes; no tests required (non-functional sweep) | Claude (main) 2026-04-22 |
| LSW-G6 | P1 | BLOCKED (opened by LSW-H3) | **Multiway bucket-EV engine for `BucketEVPanel`** (H3-F1 finding). Today `computeBucketEVs` calls `handVsRange(villainRange, ...)` against a single villain range; the MW lines (`btn-vs-bb-sb-srp-mw-j85`, `co-vs-btn-bb-srp-mw-oop`) have two villains. Rendering bucket EV on those lines with a HU-calibrated computation would produce misleading numbers and is worse than not rendering at all. **Scope:** extend `drillModeEngine.computeBucketEV` + pinned-combo + domination paths to accept `villains: [{range, position}]`; compute per-villain equity; compute cascading fold probability across the villain sequence (parallels `foldEquityCalc`'s MW model). Domination map should segment against the combined range or render one disclosure per villain (design call during impl). **Blocker for:** LSW-B1 widening on J85 + Q53 MW flop roots. Does not block B1 on the 4 HU flop roots. | ✓ MW lines render pinned-combo row + demoted bucket table + domination map with numerically valid per-villain EVs; ✓ `computeBucketEVs.test.jsx` new cases covering 2-villain inputs; ✓ 1600×720 Playwright walk on J85 and Q53 MW flop roots with heroHolding authored confirms sensible EVs across archetypes; ✓ engine drift snapshot re-baselined if needed | — |
| LSW-H* | varies | LAZY | **Future surface-quality tickets from audit findings** — one H entry per rendering defect a combined expert+UX visual audit surfaces. Every H ticket has a Verification line per the surface-audit standard. | ✓ UI change shipped + tests green + visual verification at 1600×720 documented + originating audit's row marked resolved | — |

### Stream P — Hand Plan Layer (sub-charter, opened 2026-04-27)

Owner-approved 2026-04-27. Adds a **forward-looking, hand-specific decision plan** dimension on top of `heroHolding` widening — closes the GTOwizard-style "what's my plan with these hole cards" gap that Stream B alone doesn't address. Sub-charter: [`docs/projects/line-study-slice-widening/stream-p-hand-plan.md`](../../docs/projects/line-study-slice-widening/stream-p-hand-plan.md).

**Foundational sub-streams (P1/P2/P4/P5) ACTIVE now**, slot between LSW phases 4 and 5 (4–5 sessions). **Content sub-streams (P3/P6/P7) DEFERRED to LSW-v2** — re-prioritized post-closeout based on v1 student-usage telemetry. Resolved decisions (Q1–Q4) recorded in sub-charter's Decisions log.

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LSW-P1 | P1 | COMPLETE (2026-04-27) | **Plan rule chip taxonomy + spec doc.** Shipped `src/utils/postflopDrillContent/planRules.js` with all 12 owner-approved chips (mdf-defense / range-protection / checking-range-construction / call-with-a-plan / call-fold-to-turn-barrel / raise-fold / thin-value-with-foldout / blocker-driven-raise / capped-uncapped-exploitation / polarized-linear-response / equity-realization-defense / board-improvement-plan). Each chip carries `{ id, label, shortBody, fullBody, citations }`; citations anchored to POKER_THEORY.md §1.4/§1.5/§3.3/§3.5/§3.6/§3.7/§4.1/§4.2/§5.6/§5.7/§5.8/§6.1/§6.2/§6.4/§6.6/§7.1. PLAN_RULE_CHIP_ORDER leads with math foundations (MDF + range protection) → construction → planning → range-vs-range. Helpers: `isKnownRuleChip`, `listKnownRuleChips`, `getRuleChip`. Spec doc at `docs/design/surfaces/hand-plan-layer.md` (under existing `surfaces/` convention rather than new `specs/` dir) covers I-HP-1/2/3 doctrine + LSW-P2 schema contract + LSW-P5 default-visibility rule + chip-tap contract + two-source plan-model table + v1 ship scope + 3 deferred open questions. Tests: `planRules.test.js` 17/17 green covering ≥10-chip count, shape invariants, kebab-case ID format, citation anchor format, freeze invariants, owner-approved-name presence, lookup helpers, named-export consistency. Full postflopDrillContent suite **390/390** green (+17 from baseline 373). **Unblocks LSW-P2.** | ✓ Taxonomy file with 12 chips; ✓ spec doc shipped; ✓ taxonomy invariants test green; ✓ chip IDs stable kebab-case strings | Claude (main) 2026-04-27 |
| LSW-P2 | P1 | COMPLETE (2026-04-27) | **Schema additions + validator.** Shipped: SCHEMA_VERSION 3 → 4 with version-history note; `PLAN_TEXT_MAX_CHARS=320` exported constant; pure-import of `isKnownRuleChip` from `planRules.js`; new validators `validateComboPlanEntry` + `validateComboPlans` with `{ allowedBuckets, allowedCombos }` options; wired into `validateNode` after `decisionStrategy`. Validator rules: (a) `comboPlans` must be object with ≥1 entry; (b) bucket keys must be in `heroView.bucketCandidates`; (c) `comboPlans` rejected when `heroView` absent or `bucketCandidates` empty; (d) `planText` non-empty trimmed ≤ 320 chars; (e) `ruleChips` non-empty array, every ID resolves via `isKnownRuleChip`; (f) override combo keys match COMBO_REGEX + duplicate-card guard + (when `heroView.combos` present) membership check; (g) nested overrides rejected; (h) cross-layer engine-bucket-combo-set membership deferred to engine per `villainRangeContext.baseRangeId` precedent. Purely additive — legacy content remains valid. Tests: new `lineSchema.comboPlans.test.js` 25/25 green covering happy path (4 cases), bucket-key validation (5 cases), rule chip validation (4 cases), planText validation (4 cases incl. ≤320 boundary), override key validation (7 cases). Existing assertions bumped: `lineSchema.test.js` SCHEMA_VERSION 3 → 4 (history note extended); `lineSchema.v3.test.js` SCHEMA_VERSION 3 → 4 (v3 fields remain valid). **Full postflopDrillContent suite 415/415 green (+25 from P1 baseline 390). Authored-content validation: `lines.test.js` 44/44 + `engineAuthoredDrift.test.js` 3/3 green — all 8 production-authored lines validate clean against v4. Full-project run surfaces 14 unrelated failures (EAL v21 migration tests + pre-existing precisionAudit flake + doc-drift `schema-validation.test.js`); none touch lineSchema/planRules.** **Unblocks LSW-P4 + LSW-P5.** | ✓ Schema accepts well-formed `comboPlans`; ✓ rejects unknown chip IDs + orphaned bucket keys + orphaned combo overrides + nested overrides + bad shapes; ✓ 25 new test cases (vs ~6 acceptance target); ✓ existing tests green; ✓ authored lines validate clean | Claude (main) 2026-04-27 |
| LSW-P4 | P1 | COMPLETE (2026-04-27) | **Engine-derived plan panel.** Shipped `src/utils/postflopDrillContent/planDerivation.js` (new file, sibling of `drillModeEngine.js`) with two exports: (a) `derivePlanFromBucketEVs(bucketEVs, options)` — pure synchronous transformer mapping a `computeBucketEVsV2` output to the EnginePlan shape (per-action EV + best-action label/reason + caveats + errorState + nextStreetPlan stub); (b) `computeEnginePlan(input, heroComboString)` — async convenience wrapper that runs `computeBucketEVsV2` + transforms in one call, never throws (graceful degradation per I-HP-2). EnginePlan shape: `{ heroCombo, perAction[], bestActionLabel, bestActionReason, decisionKind, caveats[], nextStreetPlan, errorState }`. **Per-action shape**: `{ actionLabel, actionKind, betFraction, ev, evLow, evHigh, isBest, unsupported }`. **Doctrine choices**: (1) `nextStreetPlan` stays null in v1 — P4 is structurally a stub for the forward-look field that LSW-D1 will populate once `evaluateGameTree.recommendations[].handPlan` is wired through to the drill engine (D1 already builds `buildResponseGuidance` with `ifCall`/`ifRaise`/`ifVillainBets`/`nextStreet` branches; P4 just exposes the surface for it). (2) `caveats` is sliced (independent array) so P5 mutations don't pollute engine output. (3) Empty templatedReason normalizes to null (avoids rendering blank "reason" lines). (4) Defensive defaults on every field (NaN EV → 0, missing actionLabel → '', missing CI → null) so partial engine output never crashes the UI. (5) Multiway routing surfaces the `MW engine is LSW-G6` errorState into the plan layer for P5 to render an "MW deferred" empty state. Tests: new `planDerivation.test.js` 33/33 green covering bad-input branches (5 cases), errorState propagation (2 cases), happy-path shape (12 cases incl. perAction count, best-action mark, bestActionLabel/Reason propagation, caveat propagation, independent caveats array, nextStreetPlan null in v1, heroCombo passthrough, defaults, decisionKind forwarding, unsupported flag, EV CI bounds, errorState=null), defensive defaults (6 cases), and end-to-end integration via `computeEnginePlan` on a real K72r Q♣Q♦ fixture (8 cases incl. real archetype range, MW errorState, decisionKind forwarding). **Full postflopDrillContent suite 448/448 green (+33 from P2 baseline 415).** **Unblocks LSW-P5** — P5 now has both authored (P2-validated `comboPlans`) and engine-derived (P4-derived) plan sources to render. | ✓ Plan derivation function shipped; ✓ derivation tests cover non-null depth-2 output paths + bailedOut graceful degradation + multiway/null/error inputs; ✓ inherits `v1-simplified-ev` caveat correctly; ✓ tests green (33 new + 415 prior); visual smoke test deferred to P5 (no UI in P4) | Claude (main) 2026-04-27 |
| LSW-P5 | P1 | NEXT (P2 closed 2026-04-27; parallel with P4) | **UI integration in `LineNodeRenderer.jsx` + `BucketEVPanel.jsx`.** Plan section under each branch with: (a) authored plan render (when `comboPlans[bucketId]` exists); (b) engine-derived plan render (when P4 returns non-null); (c) conditional default visibility per Q2=C — authored present → hide engine, no authored → show engine; (d) `sessionStorage`-persisted "Show solver plan" toggle; (e) clickable rule chips opening shared `<RuleChipModal>` per Q3=B. 1600×720 visual verification mandatory. | ✓ `<RuleChipModal>` shipped (~80–120 LOC reusable); ✓ conditional render + toggle hook (~15 LOC); ✓ component tests cover authored-only / engine-only / both-with-toggle paths; ✓ 1600×720 Playwright walk on JT6 flop_root + 1 unauthored node confirms layout + chip-tap; ✓ no regression on existing tests | — |
| LSW-P3 | P2 | DEFERRED — LSW-v2 | **Authored plans on canonical flop roots** (8 lines × ~3–6 buckets ≈ 60–80 plan strings + ~10–15 per-combo overrides). Authored to the bucket-teaching queue from each line's Stream A audit. Per-line gated on audit close. **Reason for deferral:** v1 student-usage data should drive prioritization — author where the plan layer earns its teaching weight, not blindly across every audited bucket. Re-open after LSW v1 closeout. | ✓ Every flop decision root on the 8 audited lines has `comboPlans` covering its `bucketCandidates`; ✓ overrides authored where audit dim-8 flagged combo-level divergence; ✓ tests green | — |
| LSW-P6 | P2 | DEFERRED — LSW-v2 | **Audit dimension 8 — plan accuracy** added to `docs/design/audits/line-audits/_TEMPLATE.md`. Per-line walks compare authored plan vs engine-derived plan; disagreements route to Stream G (engine wrong) or Stream F (content wrong) per the existing category-A/B/C/D rule. **Reason for deferral:** only meaningful once authored content (P3) exists. Re-open with P3. | ✓ Template updated with dimension 8 specification; ✓ all 8 line audits walked through dim-8 with closure verdicts; ✓ disagreements routed to F/G batches | — |
| LSW-P7 | P3 | DEFERRED — LSW-v2 | **Authored plans on turn bucket-shift nodes** (~8 nodes flagged dim-6-YES + dim-8-YES). Per-line gated on Stream B2 turn-node coverage. **Reason for deferral:** depends on P3 closure + Stream B2 turn-node coverage. Re-open with P3. | ✓ Every flagged turn decision has `comboPlans` declared; ✓ tests green | — |

### Stream LSP-G — Chip Glossary as Study Surface (placeholder, opened 2026-04-27)

Follow-on ambition captured during Stream P Q3 deliberation. Promotes rule chips from in-line tap-to-explain (Stream P scope) to a full glossary navigation surface with cross-links, embedded `compute` calculators, and entry points from HandReplay + TableView education tooltips + drill walkthroughs.

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| LSP-G | P3 | PARKED — opened 2026-04-27 | **Chip Glossary as Study Surface.** When chips become a knowledge-graph entry point, they should surface beyond line study — HandReplay tooltips, TableView education, drill walkthroughs. Scope (Stream P=B for v1; this ticket captures C-ambition): glossary modal with cross-links to other plans citing the same chip, walk-to-node action, embedded `compute` calculators (MDF, pot-odds, equity-realization) reusing the existing `compute: { calculator }` pattern from JT6 flop_root. Estimate ~3 sessions if pursued as its own program. **Blocker:** LSW Stream P P5 closure (need clickable chips live first) + post-v1 student-usage data identifying which chips would earn cross-surface placement. | ✓ Glossary modal with cross-link index + chip cross-references; ✓ embedded `compute` calculators on math-bearing chips; ✓ surface integration in ≥1 non-line-study venue (HandReplay or TableView); ✓ tests green | — |

---

## IN_PROGRESS — Player Entry Overhaul (PEO)

Owner-approved 2026-04-16. 4-session program to rebuild live-table player selection UX for mobile-landscape casino use. Plan: `C:\Users\chris\.claude\plans\fluttering-booping-puddle.md`. Charter: `.claude/projects/player-entry-overhaul.md`. Sequenced as S1 data-layer → S2 editor → S3 picker + wire entries → S4 cutover + cleanup. No feature flags per CLAUDE.md — staging handles rollout.

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| PEO-1 | P1 | COMPLETE | **Data Layer + Avatar System** — shipped 2026-04-16 (dark merge). IDB v13→v14 (additive `playerDrafts` store keyed by userId), `avatarFeatures` sub-object + `nameSource` on Player (nullable), `linkPlayerToPriorSeatHands` pure fn with session-scoped walk and idempotence, `batchUpdateSeatPlayers` atomic, reducer `RETROACTIVELY_LINK_PLAYER` + undo, `statsRecompute`, custom SVG feature avatars (6 categories) + `AvatarRenderer` + `AvatarMonogram` + `PlayerAvatar`, 3 hooks (`usePlayerDraft`, `useRetroactiveLinking`, `useScreenFocusManagement`), invariants I-PEO-1..4. 5,422 → 5,644 tests. | ✓ Tests green; ✓ v14 additive-only verified; ✓ retro-link → stats recompute → undo round trip tested; ✓ STATE_SCHEMA/INVARIANTS/CHANGELOG updated | Shipped |
| PEO-2 | P1 | COMPLETE | **Fullscreen PlayerEditorView** — shipped 2026-04-16 (dark merge). `SCREEN.PLAYER_EDITOR` route, non-blocking form with swatch-based `AvatarFeatureBuilder` (6 categories + live preview + auto-hide color rows when style=none), `NameSection` with non-blocking duplicate warning, collapsible `PhysicalSection` + `ImageUploadSection`, `DraftResumeBanner`, `BackToTableBar` with draft flush. `usePlayerEditor` hook wires draft + autoName (user → seat+feature → timestamp) + atomic save. Post-save: auto-seat-assign + retro-link with undo toast. `playerAutoName.js` pure utility. 5,644 → 5,699 tests. | ✓ Tests green; ✓ editor reachable via SET_SCREEN dispatch; ✓ draft resume verified; ✓ autoName fallback chain covered; ✓ schema-drift version tests pass | Shipped |
| PEO-3 | P1 | COMPLETE | **Fullscreen PlayerPickerView + wire entries** — shipped 2026-04-16. `SCREEN.PLAYER_PICKER` route with autofocused name search (prefix match on name + nickname, case-insensitive), inline expandable feature chips (Skin/Hair/Beard/Glasses/Hat, AND semantics), recognition-first `ResultCard` (avatar-left, bolded prefix, faded non-match chips, gold left-border accent), sticky `CreateFromQueryCTA` seeds editor. Batch mode + seat-progress ribbon (D9). `SeatContextMenu` gains "🔍 Find Player…" + reroutes "Create New" to editor. On pick: assign + retro-link + undo toast. `usePlayerPicker` hook + `scorePlayerMatch` primitive. **Feature live.** 5,699 → 5,760 tests. | ✓ Right-click seat → picker works; ✓ name+feature filters narrow results; ✓ pick existing fires retro-link + undo-toast; ✓ batch mode advances; ✓ create-CTA seeds editor | Shipped |
| PEO-4 | P1 | COMPLETE | **Cutover, cleanup, docs** — shipped 2026-04-16. Migrated PlayersView create+edit buttons to `openPlayerEditor`. Deleted `src/components/ui/PlayerForm/` directory (7 files incl. tests). Removed `pendingSeatForPlayerAssignment` from reducer + schema + UIContext + TableView. PlayersView tests rewritten against new architecture. Full post-mortem written. 5,760 → 5,623 tests (net drop from deleted legacy PlayerForm tests; 0 failing). | ✓ `grep PlayerForm src/` only comments remain; ✓ `grep pendingSeatForPlayerAssignment src/` only historical comment remains; ✓ suite green; ✓ docs updated | Shipped |

**Player Entry Overhaul CLOSED 2026-04-16.** All 4 sessions shipped in one day. Post-mortem at `.claude/projects/player-entry-overhaul.md`. Plan retained at `C:\Users\chris\.claude\plans\fluttering-booping-puddle.md`. Per-session handoffs at `.claude/handoffs/peo-{1,2,3,4}-*.md`.

---

## NEXT — Sidebar Recurrence Program 2026-04-15 (owner-approved)

Owner-reported: S1–S5 symptoms persist despite SR-0→SR-8 closure. 6-expert roundtable diagnosed 24 findings. Owner approved 20 items 2026-04-15 prioritising long-term infrastructural trust; 4 items (RT-84, RT-86, RT-87, RT-91) held as LATER. Status on each row below reflects approval.

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| RT-68 | P0 | COMPLETE | SW reanimation must emit fresh live_context before cached advice | `service-worker.js:52–104` `pushFullStateToSidePanel`: send live_context first, advice second; drop cached advice if no live_context. Owner verifies: after 5-min idle + Chrome re-wake mid-hand, sidebar never shows advice from prior hand | — |
| RT-69 | P0 | COMPLETE | `_pendingAdvice` must clear on hand:new boundary | `render-coordinator.js` — add pending clear to `handNew` dispatch path. Owner verifies: new hand never shows stuck advice from prior hand | — |
| RT-70 | P0 | COMPLETE | Invariant checker runs pre-dispatch, not post-render | `render-coordinator.js:440–456` — evaluate invariants on candidate snapshot; on violation gate or fallback to last-good. Closes M4 as real gate. Owner verifies: S2 (preflop advice on flop) never paints even for one frame | — |
| RT-71 | P0 | COMPLETE | renderKey missing freshness + violation fields | `render-coordinator.js:339–384` — add `_freshness` digest + `lastViolationAt` + `_pendingAdvice` presence. Owner verifies: stale tint and "!" badge appear within 1s of threshold cross without requiring other state change | — |
| RT-72 | P0 | COMPLETE | `renderBetweenHands` reads raw state instead of FSM panel output | `side-panel.js:1222–1257` — use `snap.panels.betweenHands` as sole slot authority; stop writing `showEl/hideEl` on street-card slot. Owner verifies: between-hands banner never pops over active hand (S4) | — |
| RT-73 | P0 | COMPLETE | `refreshHandStats` main path missing tableId guard | `side-panel.js:308–312, 377–390` — port retry-path `tableId === currentTableId` guard to primary path; reset `_refreshInFlight` on `clearForTableSwitch`. Owner verifies: rapid table switch never shows prior table's stats | — |
| RT-74 | P1 | NEXT | Replay corpus sequence extension (6 minimum sequences) | Extend SR-1 with sub-coalesce interleave playback + transition-trace hashing. Cover: reconnect-mid-advice, SW-reanimate, handNew-within-80ms, two-advice-before-context, donk→cbet, table-switch-during-fade. Owner verifies: `npm run replay` gains new sequence suite that catches at least one seeded race | — |
| RT-75 | P1 | NEXT | `betweenHandsFsm` debounce + modeAExpired symmetry | `fsms/between-hands.fsm.js:27` — debounce `inactive→active`; add `modeAExpired→active` return on repeated `betweenHandsOrIdle=true`. Owner verifies: transient malformed payload doesn't flash banner | — |
| RT-76 | P1 | COMPLETE | Doctrine R-5.3: FSM-output exclusivity | Amend `SIDEBAR_DESIGN_PRINCIPLES.md`: slot with registered FSM reads only `snap.panels.<id>`. Lint via spec-meta extension. Owner verifies: rule documented, test rejects raw-state reads in named renderers | — |
| RT-77 | P1 | COMPLETE | Doctrine R-7.4: pre-dispatch invariant gate | Amend doctrine to require pre-dispatch invariant evaluation; pair with RT-70 code change. Owner verifies: rule documented, R-7.2 reframed as observability-only | — |
| RT-78 | P1 | NEXT | Freeze incoming live_context at SW boundary | `service-worker.js` — `Object.freeze` after validation before forwarding. Owner verifies: mutation attempts throw in tests | — |
| RT-79 | P1 | NEXT | Microtask-deferred timer registration leak | `side-panel.js:527, 1084` — register timers synchronously; close RT-60 coverage gap. Owner verifies: registry contains `staleContext` + `adviceAgeBadge` immediately after IIFE | — |
| RT-80 | P1 | NEXT | SW→side-panel port missing `validateMessage` | `service-worker.js:267–283` — add schema gate parity with capture + app-bridge ports. Owner verifies: malformed side-panel message is rejected in test | — |
| RT-81 | P1 | COMPLETE | `lastAct.amount.toFixed(0)` crashes on non-number | `render-orchestrator.js:1197` — coerce/validate before `.toFixed`; M1 generalisation. Owner verifies: non-numeric amount doesn't blank the sidebar (S1 variant) | — |
| RT-82 | P2 | NEXT | Per-zone innerHTML diff + `tourneyTimer` guard | `tourneyTimer` rebuilds innerHTML 60×/min; `renderSeatArc`/`renderPipelineHealth`/`renderPidSummary`/`updateAppStatus` write unconditionally. Add `lastHTML === html` fast-path per zone. Owner verifies: tournament bar no longer flickers | — |
| RT-83 | P2 | NEXT | `staleContext` dual-writer oscillation | 10 s interval sets, `handleLiveContextPush:468` clears on every push. Consolidate to single owner; regression test | — |
| RT-84 | P2 | LATER | Content + SW throttle coordination on reconnect | Independent 200 ms windows reset on reconnect → 400 ms worst case. Deferred — mitigated in practice once RT-68 ships | — |
| RT-85 | P2 | COMPLETE | Spec-meta `+200` slack + R-2.3 count-only baseline | `spec-meta-discipline.test.js:183` + `dom-mutation-discipline.test.js` — slack and count-lock permit silent drift. Tighten to zero + owner-whitelist. **Required for doctrine trust.** | — |
| RT-86 | P2 | LATER | CAPTURED_HANDS migration bypasses `validateHandForRelay` | One-time migration path; defer | — |
| RT-87 | P2 | LATER | `QuotaExceededError` silent swallow on session storage | Rare in practice; defer until observed | — |
| RT-88 | P3 | NEXT | Z3 stale-advice zone-wide amber band | Current per-row `(stale)` suffix invisible at 1.5 s glance. Promote to Z3 band per R-1.5. Owner verifies live: stale state obvious without reading row labels | — |
| RT-89 | P3 | NEXT | `animationend` fallback safety timer | `render-street-card.js:186` — GPU-skip fallback (coordinator-scheduled 300 ms) prevents class leak on Android thermal throttle | — |
| RT-90 | P3 | NEXT | DEALING shimmer animates prior-hand content | `render-street-card.js:97–103` — blank Z3 body during DEALING, not just shimmer frame. Owner verifies: during DEALING no prior-hand advice visible | — |
| RT-91 | P3 | LATER | Multiway postflop corpus gap (3.6/3.11/3.12) | Deferred until sequence-mode harness (RT-74) lands — will author as sequences not snapshots | — |

**Program shape — "Sidebar Trust Program" (SRT-1 … SRT-4), owner-approved 2026-04-15:**

- **SRT-1 COMPLETE (e3fe152):** RT-68/69/71/72/73/81. 6 P0 root-cause fixes. Suite 1842 → 1895.
- **SRT-2 COMPLETE (739df10):** RT-70/76/77/85. Doctrine v3 (R-5.6 + R-7.2 amendment), pre-dispatch invariant gate, baseline slack tightened, system-model I-INV-PRE + I-FSM-EXCL, failure file SW_REANIMATION_REPLAY.md. Suite 1895 → 1897.
- **SRT-3 (Sequence-mode replay harness, ~1 session):** RT-74 alone. Extends SR-1 framework with sub-coalesce interleave playback + transition-trace hashing. Minimum 6 seeded sequences. **This is the single most important item for long-term trust — it gives every subsequent fix mechanical regression evidence.**
- **SRT-4 (Hardening + UX, ~1 session):** RT-75 → RT-78 → RT-79 → RT-80 → RT-82 → RT-83 → RT-88 → RT-89 → RT-90. Cheap, mostly independent.

Post-mortem at `.claude/failures/SIDEBAR_REBUILD_PROGRAM.md` gets an amendment noting SRT-1..SRT-4 are prerequisite to genuine S1–S5 closure — appended at SRT-2 close.

| ID | Pri | Status | Description | Accept Criteria | Claimed By |
|----|-----|--------|-------------|-----------------|------------|
| SR-0 | P0 | COMPLETE | **Sidebar Rebuild Program — Stage 0 Forensics.** Owner-approved 2026-04-12. Symptom register sealed. | `.claude/projects/sidebar-rebuild/00-forensics.md` — sealed | — |
| SR-1 | P0 | COMPLETE | **Stage 1 — Replay framework.** DONE 2026-04-12. All 5 signatures (S1–S5) fire deterministically. Framework: recorder, replayer, 5 corpus files, determinism script, `npm run replay`. | Replayer reproduces ≥3 symptoms deterministically | — |
| SR-2 | P0 | COMPLETE | **Stage 2 — Design Principles Doctrine.** APPROVED 2026-04-12 at `docs/SIDEBAR_DESIGN_PRINCIPLES.md`. Doctrine at **v2** after 2026-04-12 UX-model reframe amendment: R-1.2 reframed (glance model replaces ≤5-metric cap), R-1.3 rationale reinforced, R-1.5 glance pathways added (see §11 amendment log). v1 amendments: R-7.3 (stale-recomputing label) and R-7.1 (three-tier invariant classification). | Doctrine approved by owner | — |
| SR-3 | P0 | COMPLETE | **Stage 3 — Panel Inventory & Purpose Audit.** DONE 2026-04-12. All 6 groups resolved; 48-row inventory at `docs/SIDEBAR_PANEL_INVENTORY.md`; doctrine at v2; Group 6 verdict: by-zone batching for SR-4. | `docs/SIDEBAR_PANEL_INVENTORY.md` owner-approved | — |
| SR-4 | P0 | COMPLETE | **Stage 4 — Per-element design specs** (by-zone batching, 6 sessions: Z0 → Z1 → Z2 → Z3 → Z4 → Zx). All 6 batches owner-approved 2026-04-13. Zx overrides batch at `docs/sidebar-specs/zx-overrides.md` (11 specs — X.1, X.3, X.4 composite, X.5+X.5b–g, X.6, X.7 — + 10 Zx batch invariants; X.5 placement declared Zx-overlay-inside-active-hand-stack NOT Z0 extension; tournament data cross-hand persistence as explicit R-2.4 exception; Observer mode tier-release for Z2 slot; X.4 authored as one composite spec; X.5d/e/f `decision-critical` scoped in-zone not cross-zone preempt; ~12 corpus gaps flagged for SR-6 + one code gap noted for X.4c re-enable timer; no new R-11 escalations). 6/6 complete; the six spec files are the authoritative spec surface for SR-6. Z0 chrome batch owner-approved 2026-04-13 at `docs/sidebar-specs/z0-chrome.md` (7 specs + batch invariants; debug-flag key `settings.debugDiagnostics` approved; 0.7 footer placement deferred to SR-5; slot-collapse exception and diagnostics co-trigger approved). Z1 table read batch owner-approved 2026-04-13 at `docs/sidebar-specs/z1-table-read.md` (7 specs + Z1 batch invariants; 1.8 escalated out of Z1 via R-11 — moved to Z2 as 2.10 in Z2 batch). Z2 decision batch owner-approved 2026-04-13 at `docs/sidebar-specs/z2-decision.md` (10 specs — 2.1–2.9 + absorbed 2.10 — + 9 batch invariants; E-1 executed by moving inventory row 1.8 → 2.10; E-3 flags S4/02-a pot chip + S4/02-b street strip regressions for SR-6; 2.3 sole equity owner; 2.9 vs 2.10 disambiguation rule; cross-zone contract with Z4 for 2.10). Z3 street card batch owner-approved 2026-04-13 at `docs/sidebar-specs/z3-street-card.md` (11 specs — 3.1–3.9 + 3.11–3.12 — + batch invariants; 3.10 omitted as duplicate of 3.9; single-slot carve-out: 3.6 frame owner, grid canvas 3.6 XOR 3.12, 3.8 legend always rendered, ~152px invariant height per R-1.3; Rule V Z3-half: HU-auto → last villain BET/RAISE → PFA → 3.12 placeholder, pill-click override persists until next BET/RAISE or hand:new; 3.9 declared Z3-local per S4/02 corpus; no new R-11 escalations; 3 corpus gaps tagged for SR-6: 3.6-villain-postflop, 3.11-multiway-selector, 3.12-no-aggressor). Z4 deep analysis batch owner-approved 2026-04-13 at `docs/sidebar-specs/z4-deep-analysis.md` (3 specs — 4.1, 4.2, 4.3 — + 7 Z4 batch invariants; coordinator-owned collapsible state for all three rows extends RT-61 pattern; RT-61 auto-expand scoped to 4.1 only; Z4-scoped R-1.3 reflow exception justified by below-the-fold placement; `informational`-only tier lock with promotion path "surface in Z2/Z3 instead"; stale-advice cross-zone inheritance from Z2 via shared 1 Hz timer; `settings.debugDiagnostics` gates 4.3 absence-from-DOM; 4 corpus gaps flagged for SR-6; no new R-11 escalations). Progress: **5/6 batches authored**. Next: Zx overrides / edge states. | Every kept element has approved spec in `docs/sidebar-specs/` | — |
| SR-5 | P0 | COMPLETE | **Stage 5 — Render architecture audit + rebuild backlog.** Owner-approved 2026-04-13. Artifacts: `docs/sidebar-rebuild/05-architecture-delta.md` (56 elements audited; 4 blocking deltas B1–B4; 11 cross-cutting findings; 27/33 doctrine rules with violations); 16 SR-6.x items (below) in dependency order. Owner decisions: 0.7 footer = bottom of sidebar; E-2 = new inventory row 1.11 (seat-arc selection ring); single `sidebarRebuild` flag; by-dependency sequencing. | `05-architecture-delta.md` + ordered backlog items | — |
| SR-6 | P0 | NEXT | **Stage 6 — Incremental rebuild behind `sidebarRebuild` flag** (16 PRs, dependency-ordered). Unblocked 2026-04-13 on SR-5 approval. First item: SR-6.1 (flag plumbing). Each PR passes 4-gate check. | All Stage 5 backlog items shipped behind flag | Blocked on SR-5 approval |
| SR-6.1 | P0 | COMPLETE | **Foundation — flag plumbing.** Shipped 2026-04-13. `settings.sidebarRebuild` + `settings.debugDiagnostics` in `chrome.storage.local` (defaults false). New `shared/settings.js` owns read/write/observe; coordinator holds `settings` slot in `_state` + snapshot + renderKey; `side-panel.js` loads at boot and wires `observeSettings` → `scheduleRender('settings_change')`. Gate sentinel `if (snap.settings?.sidebarRebuild)` present in `renderAll` as attachment point for SR-6.8–6.15. New options page (`options/options.html` + `options.js`) with two toggles, wired into manifest + build.mjs (6 entry points). 1314 extension tests pass (+ new `shared/__tests__/settings.test.js`). Legacy path unchanged when flag false. | Flag toggled in dev, no-op when false; debug-flag key reads from storage | Shipped |
| SR-6.2 | P0 | COMPLETE | **Harness corpus extension.** Shipped 2026-04-13. 22 new fixtures in `side-panel/__tests__/fixtures.js` across Z0 (1), Z1 (1), Z3 (3), Z4 (4), Zx (13). Naming convention `<zone>_<specRow>_<variant>`; two justified merges documented inline (X.4 implicit-clear+multi-message; X.5f+X.5g absent-state variants). All fixtures state-only, legacy path (flag=false). Suite 1314 → 1534 passing (+220 from null-safety/no-undefined iteration over ALL_FIXTURES). Replay determinism: 13 S-series hashes identical across two runs. Harness screenshots deferred — fixtures exercise render-layer via null-safety tests; screenshot sweep scheduled into SR-6.8 when zone containers land. | 19+ new corpus entries + replay determinism passes | Shipped |
| SR-6.3 | P0 | COMPLETE | **RT-60 sweep — done 2026-04-14.** All bare timers in `side-panel.js` + `render-street-card.js` now flow through `coordinator.scheduleTimer`. Coordinator gained `scheduleTimer`/`hasTimer`/`activeTimerCount` + `_setInterval` dep. The scope went beyond the handoff's 5 listed sites to every bare call in both modules (owner chose long-term-fix reading): recovery-btn re-enable, tableGrace, handStats retry, staleContext, tourneyTimer, adviceAgeBadge, planPanelAutoExpand, tourneyLogCopy-reset, diagCopy-reset, diag_autorefresh, swFallbackPing, streetCard_fade, streetCard_heightRelease. Diagnostics auto-refresh now starts on panel-open, stops on close (not boot-permanent interval). DI-leaked timer primitives dropped from coordinator construction in side-panel.js (defaults cover them). `render-street-card.js` gains `setStreetCardTimerHost(host)` setter; fade falls back to instant swap when no host (visual harness). Discipline test `side-panel/__tests__/timer-discipline.test.js` pins the invariant (strips comments/strings, greps executable code). Suite 1534 → 1536. | ✓ Zero bare timers; ✓ discipline test green; ✓ full suite passes; ✓ build clean | Shipped |
| SR-6.4 | P0 | COMPLETE | **renderKey completion — done 2026-04-14.** Four fields added to `buildRenderKey`: `deepExpanderOpen`, `planPanelOpen`, `tournamentCollapsed` (1 bit each) + `lastGoodExploits` presence bit + `appConnected` bit (exploitPushCount handles push increments; these cover clears + appConnected transitions). 5 regression tests in state-drift block. Suite 1536 → 1541. R-5.4 satisfied. Unblocks SR-6.5, 6.6, 6.8. | ✓ renderKey test asserts all 4 fields cause re-render | Shipped |
| SR-6.5 | P0 | COMPLETE | **FSM authoring — shipped 2026-04-14.** 5 declarative panel FSMs in `side-panel/fsms/` (recovery-banner, seat-popover, deep-expander, between-hands, street-card) using `fsm.js` helper (`defineFsm` → `{transition}`; unknown event = no-op). Coordinator gained `panels` slot + `seatPopoverDetail` slot + full dispatch API (`registerFsm`, `dispatch`, `getPanelState`, `dispatchTableSwitch`, `dispatchHandNew`, `onHandNew`) — every transition schedules IMMEDIATE render with reason `fsm:<id>:<event>`. Snapshot + renderKey include panel states (sorted `id:state` pairs) + seatPopoverDetail.seat. `clearForTableSwitch` fans `tableSwitch`; PREFLOP/DEALING boundary fans `handNew`. 9 DOM-mutation sites rewired in `side-panel.js`: (1–3) port callbacks set connState+cause, `renderConnectionStatus` writes status-dot/text; (4–5) `renderRecoveryBanner` + dispatch wiring replaces showRecoveryBanner/hideRecoveryBanner + button click mutations; (6) tournament detail idempotent classList sync; (7) deep-expander click dispatches + renderDeepExpander idempotent sync; (8) seat popover extracted to `buildSeatPopoverHtml` + `renderSeatPopover` reads FSM state + seatPopoverDetail; (9) stale-tint toggle stays in render path. `render-street-card.js` dispatches `streetChange`/`fadeTimerFire`/`heightReleaseFire` via timer-host shim — FSM is observable spec; timer-host still drives fade. 49 new tests (`fsm.test.js` + `coordinator-dispatch.test.js`). Suite 1560 → 1609. Build clean. betweenHands FSM registered but not yet dispatched from app code — wiring lands in SR-6.12. | ✓ 5 FSMs shipped; ✓ dispatch API in coordinator; ✓ 9 sites rewired; ✓ +49 tests; ✓ build clean | Shipped |
| SR-6.6 | P0 | COMPLETE | **Freshness records + R-4.3 partial-payload retention.** DONE 2026-04-14. Freshness sidecar `{timestamp, source}` at coordinator boundary (per-field currentLiveContext + per-seat appSeatData); field-level merge in `handleLiveContext` mid-hand + new `mergeAppSeatData()` for push_exploits. Hand-boundary still full-replaces. Per-rendered-value envelope unwrap deferred to zone PRs. 1609 → 1618 tests. | S1 regression test passes: partial push never nulls a prior field | — |
| SR-6.7 | P0 | COMPLETE | **R-7.3 tolerance revoked + stale-recomputing label — shipped 2026-04-14.** `handleAdvice` + `handleLiveContext` pending-promote branches tightened from `(adviceRank - liveRank) <= 1` to `adviceRank === liveRank` (exact match only). Ahead-of-context advice held in `_pendingAdvice` until live context catches up. `side-panel.js` renderAll computes `isStreetMismatch` (advice.currentStreet ≠ liveCtx.currentStreet); when true, OR'd into `isAdviceStale` AND passed as reason to `updateStaleAdviceBadge` which renders `"Stale — recomputing"` (1 Hz timer path also aware). lastGoodAdvice is NOT blanked — stale tint + label surface the recomputation. 4 new cases in `SR-6.7 — exact-street match for advice acceptance` describe block in `render-coordinator.test.js` (S2: flop advice held on preflop, preflop advice lingers but signals street mismatch on flop, exact match accepted, mid-hand reconnect rejects tolerance-gap). Updated `message-integration.test.js` Scenario 11 legacy "accepted with gap=1" case to new "held under revoked tolerance" expectation. Suite 1618 → 1622 (+4 net). Build clean. **Foundation band fully sealed.** | ✓ S2 regression: advice panel never shows preflop advice while flop is live; ✓ stale-recomputing label surfaces without blanking | Shipped |
| SR-6.8 | P0 | COMPLETE | **Zone containers + CSS heights — shipped 2026-04-14.** `side-panel.html` gains `#hud-content-v2` with 6 empty zone containers (`.zone-z0/z1/z2/z3/z4/zx`) + per-zone `min-height` in the inline style block (72/204/150/300/24/0 px). `renderAll` toggles between legacy `#hud-content` and the v2 shell inside the SR-6.1 gate sentinel. New `__tests__/zone-shell.test.js` pins (a) all 6 zone IDs/classes exist, (b) each declares min-height, (c) CSS lint forbids `display:none` on any `.zone-*` selector. Suite 1541 → 1555. Build clean. Unblocks SR-6.9; SR-6.10/6.12/6.14 lose SR-6.8 dep. | ✓ Zone DOM containers present; ✓ CSS lint forbids display:none inside R-1.3 slots | Shipped |
| SR-6.9 | P0 | COMPLETE | **Motion budget + `prefers-reduced-motion` — shipped 2026-04-14.** `.street-card.fade-in` 0.15s → 0.2s (R-6.1 floor), `.deep-body` max-height 0.35s → 0.3s (R-6.2 ceiling). JS fade timer in `render-street-card.js` bumped 150 → 200 to stay in sync with CSS. New `@media (prefers-reduced-motion: reduce)` block at end of inline style zeroes `animation-duration` + `transition-duration` globally (`!important`) — R-6.3. New `side-panel/__tests__/motion-budget.test.js` (5 cases) pins CSS durations, media block presence, and JS/CSS timer sync. Suite 1555 → 1560. Build clean. | ✓ Reduced-motion media query honored; ✓ all transition durations within spec bands; ✓ JS fade timer matches CSS | Shipped |
| SR-6.10 | P1 | COMPLETE | **Z0 PR — shipped 2026-04-14.** 0.7 `diag-show` + `diag-panel` gated by `settings.debugDiagnostics`: new `#diag-footer` wrapper defaults to `display:none`; `renderDiagnosticsGate(snap)` called each render toggles display by flag + closes + clears `diag_autorefresh` if flag flips off mid-session; click handler guarded with belt-and-braces flag check. 0.2 unknown placeholder: coordinator `lastHandCount` initial `null` (distinct from known-zero); `updateStatusBar` renders `—` (U+2014) when null, numeric otherwise. 0.7 footer placement already at body end (pre-existing). New `__tests__/z0-chrome.test.js` (8 cases) pins coordinator default, HTML wrapper + default style, renderer source-level assertions, click-handler guard, renderAll wiring. Suite 1622 → 1630. Build clean (6 entry points). | ✓ Z0 batch invariants checklist passes; ✓ 0.7 absent in DOM when flag off | Shipped |
| SR-6.11 | P1 | COMPLETE | **Z1 PR — shipped 2026-04-14.** 1.1 logarithmic ring: new pure `quantizeSampleBand(handsCount)` in render-orchestrator.js → `{empty, tiny (<20), small (<100), medium (<500), large}`. Seat circle gains `.ring-band-{tiny/small/medium/large}` class emitted by `buildSeatArcHTML`; CSS renders concentric outer box-shadow with escalating stroke width. R-4.2 `—` glyph overlay for occupied-zero seats via new `.seat-sample-unknown` span (distinct from tiny band so low-sample ≠ no-sample in glance). 1.11 Rule V selection ring: new coordinator slot `rangeSelectedSeat` (snapshot + renderKey), cleared on table switch + new-hand boundary (R-2.4 hand-scoped); seat circle gets `.rule-v-selected` class + CSS dashed outline (visually distinct from `.pinned` pin glyph and `.focused` gold glow — R-5.1 three channels, three modifiers). Rule V pill-click contract made explicit: villain-tab click now writes `rangeSelectedSeat` only (old code wrote `pinnedVillainSeat`, conflating scouting pin with range-grid selection); `computeFocusedVillain` gets a new top-priority branch `rangeSelectedSeat > pinnedVillainSeat > advice.villainSeat > pfAggressor > HU` so existing range-grid UX survives. New `__tests__/z1-table-read.test.js` (23 cases): band thresholds, seat arc band class + `—` placeholder, Rule V priority in computeFocusedVillain, coordinator lifecycle (default null, snapshot, renderKey flip, clearForTableSwitch, hand-boundary reset), villain-tab source contract, CSS pins for all 4 band rules + rule-v-selected + placeholder. Suite 1639 → 1662 (+23). Build clean. | ✓ Ring + placeholder + selection ring all harness-visible; ✓ Rule V event documented in code comment | Shipped |
| SR-6.12 | P1 | COMPLETE | **Z2 PR — shipped 2026-04-14. Fixes B1 + B2.** B2: `_buildStandardActionBar` restructured — equity `{N}% equity` + EV edge now inline on `ab-row1` (headline); `ab-row2` is SPR-only per §2.4 (shows `SPR: N.N` with R-4.2 `SPR: —` placeholder when unknown). Mixed-unit bug defense: `heroEquity ≤ 1` treated as fraction and ×100-scaled. B1: between-hands pot chip + street pill suppressed in `buildUnifiedHeaderHTML` by an explicit `handLive` guard (`live.state !== 'COMPLETE' && !== 'IDLE'`); same guard added to `renderAll` street-progress branch so the dot-strip blanks between hands (previously rendered from `lastGoodAdvice.currentStreet` fallback, producing S4/02-a + -b all-filled-dots regression). §2.10 consolidation: new `computeAdviceStaleness(advice, ctx, now)` pure helper is the single source of truth — both the main render path and the 1 Hz `adviceAgeBadge` timer now call through it; duplicated `Date.now() - _receivedAt` + `advice.currentStreet !== ctx.currentStreet` arithmetic deleted from both call sites. New `__tests__/z2-decision.test.js` (15 cases): B2 equity in headline + row-2 SPR-only + R-4.2 placeholders + mixed-unit defense; B1 pot/street blank on COMPLETE/IDLE, render on FLOP; §2.10 source-level pins (helper exists, render calls helper without inline arithmetic, 1 Hz timer calls helper, four reason branches present). Suite 1662 → 1677 (+15). Build clean (6 entry points). Z2 batch invariants 1–9 satisfied. | ✓ S4/02 replay: pot chip + street dots blank between hands; ✓ equity inline in S3/01 replay | Shipped |
| SR-6.13 | P1 | COMPLETE | **Z3 PR — shipped 2026-04-15.** 3.12 no-aggressor placeholder (`renderNoAggressorPlaceholder`) renders inside `<div class="range-slot">` when postflop + empty `villainRanges` (Rule V Q1-c), with R-5.1 always-on legend. `renderVillainRangeSection` wraps all three variants (hero preflop GTO, villain postflop grid, 3.12 placeholder) in the same `.range-slot` frame — R-1.3 no-reflow invariant. Effective street now sourced from `liveContext.currentStreet` first (was `advice.currentStreet`), fixing the postflop-with-null-advice case that previously rendered hero GTO. 3.11 multiway pill strip (existing `.villain-tab[data-range-seat]`) + its click→`rangeSelectedSeat` writer (SR-6.11) were already in place; verified. New BET/RAISE clear in `handleLiveContextPush`: scans the new action tail for `bet`/`raise` and clears `rangeSelectedSeat` so Rule V re-asserts on next aggressor (CHECK/CALL/FOLD preserves override). `hand:new` clear already in `coordinator.handleLiveContext` PREFLOP/DEALING boundary (SR-6.11). 3.10 orphan: already absent from `render-street-card.js`; new test pins this. CSS: `.range-slot { min-height: 152px }` + `.range-slot-placeholder { min-height: 152px }` + `.rg-legend--muted` for grayed legend in 3.12. 17 new tests in `__tests__/z3-range-slot.test.js`; 3 preexisting `render-street-card.test.js` postflop-null-advice cases updated to expect 3.12 placeholder (replaces `"Waiting for flop\|turn\|river"` fallback). Suite 1677 → 1694 (+17). `node build.mjs` clean (6 entry points). **Not in this PR (deferred):** moving Z3 content into `#zone-z3` container (SR-6.8 shell) — following SR-6.12 pattern, container migration is SR-6.16 cleanup; strict Rule V `isFocused` (current `.villain-tab.active` also highlights `advice.villainSeat` even when `rangeSelectedSeat` differs — pragmatic legacy behavior); harness visual verification not run this session. | ✅ Multiway replay corpus fixtures exercise selector; ✅ override persists through CHECK/CALL/FOLD, clears on BET/RAISE + hand:new (tested) | Shipped |
| SR-6.14 | P1 | COMPLETE | **Z4 PR — shipped 2026-04-15.** Split `deepExpanderOpen` → `moreAnalysisOpen` + `modelAuditOpen` on coordinator (defaults, snapshot, renderKey, table-switch, hand:new). Added `lastAutoExpandAdviceAt` + `userToggledPlanPanelInHand` as RT-61 discriminators. FSM `deep-expander.fsm.js` renamed → `more-analysis.fsm.js`; new `model-audit.fsm.js` parallel definition; both registered on coordinator. `buildDeepExpanderHTML` split into `buildMoreAnalysisHTML` (6 sections, showButton reflects content presence) + `buildModelAuditHTML` (audit section only). Legacy auto-open-on-postflop removed from 4.2 per batch invariant 2. New `renderMoreAnalysis` + `renderModelAudit` in side-panel.js; Model Audit DOM scaffold is **removed entirely** from the document when `settings.debugDiagnostics !== true` (re-inserted via `insertAdjacentHTML` when flag flips on). RT-61 predicate rewritten in renderPlanPanel: arms the 8 s timer only when `advice._receivedAt` differs from the tracker AND panel is closed AND handPlan present AND user hasn't toggled the panel this hand (pre-fix the timer re-armed every render and never actually fired). User click sets `userToggledPlanPanelInHand = true` (sticky until hand:new). Cross-zone stale-tint inheritance (Z4 batch invariant 5): `.stale` now also toggles on `#more-analysis-content` + `#model-audit-content` sharing the existing `computeAdviceStaleness` source of truth. `.deep-expander-content.stale` CSS rule added. Harness updated to drive both new collapsibles. 19 new tests in `z4-deep-analysis.test.js` + builder split tests updated in `render-orchestrator.test.js` + FSM tests updated in `fsm.test.js` / `coordinator-dispatch.test.js`. Suite 1694 → 1765 (+71). Build clean. **Deferred to SR-6.16:** `render-tiers.js` monolith deletion, container migration into `#zone-z4`, `.deep-expander-*` CSS class rename, harness visual verification. | ✓ Flag-off: 4.3 absent from DOM (tests + renderModelAudit source-level pin); ✓ 4.2 and 4.3 independently toggleable via distinct coordinator keys + FSMs | Shipped |
| SR-6.15 | P1 | COMPLETE | **Zx PR — shipped 2026-04-14.** X.1 betweenHands FSM wired into app code: `handleLiveContextPush` dispatches `liveContextArrived` with `betweenHandsOrIdle = state === 'IDLE' \|\| state === 'COMPLETE'` on every live-context push (mid-hand states never mount the banner — X.4 owns disconnected-during-hand); `handleAdvicePush` dispatches `adviceArrived` on any accepted advice (both immediate and buffered `_pendingAdvice` paths), which cancels the banner the moment hero has live advice. X.4c re-enable timer verified on `coordinator.scheduleTimer('recoveryBtn_reEnable', …)` (RT-60 compliance, pre-existing from SR-6.3). New `__tests__/zx-overrides.test.js` (9 cases): source-level wiring pins + end-to-end FSM transitions through the coordinator (inactive/active/tableSwitch/adviceArrived). Suite 1630 → 1639. Build clean. **Deferred inside scope:** Observer-mode tier verification (X.6/X.7) and X.5 placement verification are both currently satisfied by pre-existing rendering behavior (covered by corpus frames S11/S10 and the zone shell); making those assertions explicit harness-level tests belongs to the Z2/Z3 PRs when those zones have containers. | ✓ X.1 replay: no mid-hand banner; ✓ FSM transitions pinned end-to-end | Shipped |
| SR-6.16 | P2 | COMPLETE | **Cleanup — shipped 2026-04-15.** Scoped to safe items only. **DOM container migration (Z3/Z4) deferred to SR-6.17** after discovering that `#hud-content-v2` hides itself when `!hasTableHands` — moving any zone DOM into the shell breaks both flag-off (legacy) and flag-on paths until the shell visibility model is redesigned. Shipped: (1) `.deep-expander-*` → `.collapsible-*` CSS rename across `side-panel.html` + `side-panel.js` (9 class refs + 2 comment refs updated). (2) Strict Rule V on `.villain-tab` in `render-street-card.js:754` — `.active` now tracks `focusedVillain` only; advice villain that isn't focused gets new `.advice-match` class with subtle gold-dim border (no full-fill lie about which range is displayed); new CSS rule + 2 test cases in `render-street-card.test.js`. (3) New `__tests__/zx-observer-mode.test.js` (4 cases) pins X.6/X.7 tier release — `classifyBetweenHandsMode` returns OBSERVING when hero folded, `buildBetweenHandsHTML('OBSERVING')` renders the scouting panel, `buildUnifiedHeaderHTML` emits the Observing badge. BACKLOG had "X.4" — user-confirmed typo for X.6. (4) Orphan-state audit: `tourneyLogVisible`, `cachedDiagnosticData`, `activePidFilter`, `diagVisible` audited — IIFE-scope is correct by design (UI preferences, not render-driving state); each annotated with a justification comment, no coordinator migration needed. Suite 1770 → 1771 (+7 new, −0). Build clean (6 entry points). **`render-tiers.js` retained** — its section builders are still the sole implementation for 6 More-Analysis + 1 Model-Audit section; explicit keep. | ✓ All 1771 tests green; ✓ build clean; ✓ SR-6.17 filed to unblock SR-7 | Shipped |
| SR-6.17 | P0 | COMPLETE | **Shell visibility redesign + all-zone DOM migration — shipped 2026-04-15.** Applied Option (b): `#hud-content-v2` wrapper deleted; zones `z1`/`z2`/`z3`/`zx`/`z4` nested inside `#hud-content` (single shell, still `hasTableHands`-gated); zone-`z0` promoted to body-top chrome wrapper containing status-bar + pipeline-health + pid-summary (always visible). Content migration: z1 ← seat-arc; z2 ← action-bar/context-strip/cards-strip/plan-panel; z3 ← tournament-bar/detail + street-progress + street-card; zx ← between-hands + app-launch-prompt; z4 ← more-analysis + model-audit collapsibles. `renderAll` shell toggle collapsed from 21-line branch to 10-line single gate; `rebuildOn` sentinel + `hudV2` lookup deleted. `#hud-content-v2 { display:flex }` CSS rule removed; all 6 min-heights preserved. `zone-shell.test.js` rewritten (drops v2 assertion, adds z0-outside-hud-content + per-zone content placement pins). Legacy render pipeline unchanged — renderers write into same IDs. Suite 1771 → 1795 (+24). Build clean (6 entry points). Replay framework + harness still reference `#hud-content` which persists as sole shell. **SR-7 unblocked.** | Flag-on sidebar identical to flag-off; Z0 chrome always visible; no regressions | Shipped |
| SR-7 | P0 | COMPLETE | **Stage 7 — Cutover.** Shipped 2026-04-15 across three commits: 7a flag delete (`8d026fe`), 7b post-mortem (`72c5ef8`), 7c doc hygiene (this commit). Audit recommended deleting the `sidebarRebuild` flag rather than flipping default since it never gated any branch (Finding 0 in `.claude/projects/sidebar-rebuild/07-pre-cutover-audit.md`). | Post-mortem at `.claude/failures/SIDEBAR_REBUILD_PROGRAM.md` | Shipped |
| SR-8.1 | P2 | COMPLETE | **C-2 + C-3 polish — shipped 2026-04-15** (in `830fd4f` SR-6 squash). 4 CSS/code annotations disambiguating residual classes from deleted inventory rows, plus 3 new corpus fixtures (`z0_pipelineAllGreen`, `z2_longStaleAdvice`, `z4_debugFlagOnWithAudit`) + 2 existing-fixture annotations. | Suite 1795 → 1828 | Shipped |
| SR-8.2 | P2 | COMPLETE | **R-2.3 DOM-mutation discipline — shipped 2026-04-15** (`551e354`). New `dom-mutation-discipline.test.js` baseline-locks 23 mutation sites across 3 files (side-panel.js / render-street-card.js / render-tiers.js). New mutations fail CI; cleanups require baseline decrement. | Pattern mirrors `timer-discipline.test.js` (SR-6.3 precedent) | Shipped |
| SR-8.3 | P2 | COMPLETE | **R-7.2 cross-panel invariant coverage — shipped 2026-04-15** (`551e354`). Verified existing two-layer architecture satisfies the doctrine rule. New `cross-panel-invariant-coverage.test.js` pins both layers: render gate (`computeAdviceStaleness` + 4-panel `.stale` toggle) + observability (`_rule3_adviceStreetMatch` violations). No code change needed. | Documents existing implementation | Shipped |
| SR-8.4 | P3 | COMPLETE | **R-3.3 tier preemption lint — shipped 2026-04-15.** Doctrine extended: `docs/sidebar-specs/README.md` template now requires a `spec-meta` fenced block (tier/owner/slot) before §1 on every spec. All 50 specs across Z0/Z1/Z2/Z3/Z4/Zx back-annotated. New `spec-meta-discipline.test.js` asserts (a) every spec has a spec-meta block, (b) tier is one of the 4 R-3.1 values, (c) owner + slot present. Suite 1837 → 1842 (+5). | spec-meta tier lint passes | Shipped |
| SR-8.5 | P3 | COMPLETE | **R-5.2 module-boundary lint — shipped 2026-04-15.** Same `spec-meta-discipline.test.js` parses owner module + slot DOM IDs, baseline-locks cross-module `getElementById`/`#id` references from non-owner panel modules. Baseline = 0 cross-refs at current coarse ownership granularity (most slots owned by `side-panel.js:renderAll`); tightens as FSM ownership decomposes. | spec-meta owner lint passes | Shipped |

**Sidebar Rebuild Program CLOSED 2026-04-15.** Post-mortem at `.claude/failures/SIDEBAR_REBUILD_PROGRAM.md`. Master plan `C:\Users\chris\.claude\plans\composed-fluttering-snowflake.md` and full per-stage handoffs at `.claude/handoffs/sr-*.md` retained for historical reference.

---

## PAUSED

| ID | Pri | Status | Description | Notes |
|----|-----|--------|-------------|-------|
| 6 | P3 | PAUSED | Firebase Cloud Sync | Auth at Phase 4/6. Resume after core analytics stable. |

---

## Recently Completed Phases

**STP-1 — Sidebar Trust Program phase 2 (CLOSED 2026-04-16).** Five-stage bundle shipped after production surfaced a 213-violation badge that the SRT-1/SRT-2 audit had missed. Stage 1: rolling-30s violation counter replaces lifetime accumulator (`render-coordinator.js`, `side-panel.js` badge tooltip, `render-coordinator.test.js`). Stage 2: `validateLiveContext` topology checks (disjoint-set + hero-in-set + seat-range) in `shared/wire-schemas.js`; R11/R12 invariants in `state-invariants.js`. Stage 3: `STATE_FIELD_SCOPES.md` registry + `state-clear-symmetry.test.js` + 11 field clears added to `clearForTableSwitch`. Stage 4: 4 audit primitives — `lifecycle-soak.test.js`, `rendered-text-contract.test.js`, `diagnostics-dump.test.js`, `payload-fuzz.test.js`. Stage 5: doctrine R-7.3 / R-7.4 / R-8.1 / R-10.1 in `SIDEBAR_DESIGN_PRINCIPLES.md`; `.claude/failures/STATE_CLEAR_ASYMMETRY.md`; `SYSTEM_MODEL.md` §4.1 + §4.2 extended; BACKLOG + STATUS updated. Suite 1,897 → 1,974 (+77). Build clean. Plan: `C:\Users\chris\.claude\plans\cryptic-booping-yao.md`.

**Phase A (2026-04-12)** — RT-46, RT-56, RT-57, RT-62, RT-63, RT-64, RT-65, RT-67.
**Phase B (2026-04-12)** — RT-43, RT-44, RT-45, RT-47, RT-54, RT-58, RT-59, RT-60.
**Phase C + D (2026-04-12)** — RT-48, RT-49, RT-50, RT-51, RT-52, RT-53, RT-55, RT-61, RT-66, plus orphan renderExploitPanel/renderWeaknessPanel cleanup.

Total: 25 roundtable findings resolved in one session. See `BACKLOG_ARCHIVE.md`.
